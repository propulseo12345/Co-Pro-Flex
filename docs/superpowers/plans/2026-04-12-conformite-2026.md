# Module Conformité 2026 — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Créer le module Conformité 2026 avec trois sous-pages : PPT (kanban 10 ans), DPE Collectif (fiche par copropriété), et Factur-X (table factures + génération XML mock).

**Architecture:** Nouveau module de navigation de niveau 1 (`/conformite`) avec pattern gestionnaire→détail pour PPT et DPE. Les hooks encapsulent les données mockées (pas de Supabase en v1). Chaque sous-module est isolé dans son propre dossier de composants.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript 5, CSS Modules, Lucide React, clsx

---

## Fichiers à créer / modifier

| Fichier | Action | Rôle |
|---------|--------|------|
| `src/types/models/conformite.ts` | Créer | Types PPT, DPE, FacturX |
| `src/types/index.ts` | Modifier | Exporter les nouveaux types |
| `src/lib/config/navigation.ts` | Modifier | Ajouter module Conformité 2026 |
| `src/app/(dashboard)/conformite/layout.tsx` | Créer | Sous-nav PPT / DPE / Factur-X |
| `src/app/(dashboard)/conformite/ppt/page.tsx` | Créer | Vue gestionnaire PPT |
| `src/app/(dashboard)/conformite/ppt/[coproprieteId]/page.tsx` | Créer | Kanban + timeline par copro |
| `src/app/(dashboard)/conformite/dpe/page.tsx` | Créer | Vue gestionnaire DPE |
| `src/app/(dashboard)/conformite/dpe/[coproprieteId]/page.tsx` | Créer | Fiche DPE par copropriété |
| `src/app/(dashboard)/conformite/facturx/page.tsx` | Créer | Table Factur-X |
| `src/app/(dashboard)/maintenance/ppt/page.tsx` | Modifier | Redirect vers /conformite/ppt |
| `src/components/features/conformite/ppt/PPTGestionnaireGrid.tsx` | Créer | Grille copros niveau 1 |
| `src/components/features/conformite/ppt/PPTGestionnaireGrid.module.css` | Créer | Styles |
| `src/components/features/conformite/ppt/PPTKanban.tsx` | Créer | Kanban 5 colonnes |
| `src/components/features/conformite/ppt/PPTKanban.module.css` | Créer | Styles |
| `src/components/features/conformite/ppt/PPTYearSelector.tsx` | Créer | Pills 2026-2035 |
| `src/components/features/conformite/ppt/PPTYearSelector.module.css` | Créer | Styles |
| `src/components/features/conformite/ppt/PPTCardDetail.tsx` | Créer | Modal timeline |
| `src/components/features/conformite/ppt/PPTCardDetail.module.css` | Créer | Styles |
| `src/components/features/conformite/dpe/DPEGestionnaireTable.tsx` | Créer | Table copros niveau 1 |
| `src/components/features/conformite/dpe/DPEGestionnaireTable.module.css` | Créer | Styles |
| `src/components/features/conformite/dpe/DPEFicheDetail.tsx` | Créer | Fiche 2 colonnes |
| `src/components/features/conformite/dpe/DPEFicheDetail.module.css` | Créer | Styles |
| `src/components/features/conformite/dpe/DPEEnergyScale.tsx` | Créer | Échelle A-G |
| `src/components/features/conformite/dpe/DPEEnergyScale.module.css` | Créer | Styles |
| `src/components/features/conformite/dpe/DPEHistorique.tsx` | Créer | Tableau historique |
| `src/components/features/conformite/facturx/FacturXTable.tsx` | Créer | Table + action génération |
| `src/components/features/conformite/facturx/FacturXTable.module.css` | Créer | Styles |
| `src/hooks/usePPT.ts` | Créer | Hook données PPT |
| `src/hooks/useDPE.ts` | Créer | Hook données DPE |
| `src/hooks/useFacturX.ts` | Créer | Hook données Factur-X |

---

## Tâche 1 — Types & données mockées

**Fichiers :**
- Créer : `src/types/models/conformite.ts`
- Modifier : `src/types/index.ts`

- [ ] **Étape 1.1 — Créer les types**

Créer `src/types/models/conformite.ts` :

```typescript
import type { TravauxPrevisionnelStatut, TypeTravauxPrevisionnel } from '@/types/enums';

// ─── PPT ────────────────────────────────────────────────────────────────────

export interface ITravauxPPT {
  id: string;
  titre: string;
  type: TypeTravauxPrevisionnel | string;
  datePrevisionnelle: string;
  dateVote?: string;
  dateRealisation?: string;
  montantEstime: number;
  montantVote?: number;
  montantReel?: number;
  statut: TravauxPrevisionnelStatut | string;
  priorite: 'FAIBLE' | 'NORMALE' | 'HAUTE' | 'CRITIQUE';
  description?: string;
  etapes: IEtapeTravaux[];
}

export interface IEtapeTravaux {
  id: string;
  label: string;
  date?: string;
  statut: 'FAIT' | 'EN_COURS' | 'A_VENIR';
  montant?: number;
  commentaire?: string;
}

export interface IPPTCopropriete {
  coproprieteId: string;
  nom: string;
  nbLots: number;
  travaux: ITravauxPPT[];
  derniereMAJ: string;
}

// ─── DPE ────────────────────────────────────────────────────────────────────

export type ClasseDPE = 'A' | 'B' | 'C' | 'D' | 'E' | 'F' | 'G';
export type StatutDPE = 'VALIDE' | 'EXPIRE_BIENTOT' | 'EXPIRE' | 'MANQUANT';

export interface IDPEHistorique {
  id: string;
  dateDiagnostic: string;
  classeEnergie: ClasseDPE;
  diagnostiqueur: string;
}

export interface IDPE {
  id: string;
  coproprieteId: string;
  coproprieteNom: string;
  nbLots: number;
  classeEnergie: ClasseDPE;
  classeGES: ClasseDPE;
  dateDiagnostic: string;
  dateExpiration: string;
  diagnostiqueur: string;
  numeroADEME: string;
  consoEnergie: number;
  emissionsGES: number;
  statut: StatutDPE;
  travauxRecommandes: ITravauxPPT[];
  historique: IDPEHistorique[];
}

// ─── FACTUR-X ────────────────────────────────────────────────────────────────

export type StatutFacturX = 'GENERE' | 'EN_ATTENTE' | 'NON_APPLICABLE';

export interface IFactureFacturX {
  id: string;
  numero: string;
  copropriete: string;
  fournisseur: string;
  montantTTC: number;
  date: string;
  statutPaiement: 'PAYEE' | 'EN_ATTENTE' | 'EN_RETARD';
  statutFacturX: StatutFacturX;
  dateGeneration?: string;
  profil: 'MINIMUM' | 'BASIC_WL' | 'EN16931';
}
```

- [ ] **Étape 1.2 — Exporter depuis index.ts**

Ouvrir `src/types/index.ts` et ajouter après les exports existants :

```typescript
export type {
  ITravauxPPT,
  IEtapeTravaux,
  IPPTCopropriete,
  ClasseDPE,
  StatutDPE,
  IDPEHistorique,
  IDPE,
  StatutFacturX,
  IFactureFacturX,
} from './models/conformite';
```

- [ ] **Étape 1.3 — Créer les données mockées PPT**

Créer `src/components/features/conformite/ppt/mock-data.ts` :

```typescript
import type { IPPTCopropriete } from '@/types';
import { TravauxPrevisionnelStatut } from '@/types/enums';

export const MOCK_PPT_COPROPRIETES: IPPTCopropriete[] = [
  {
    coproprieteId: 'copro-1',
    nom: 'Résidence Les Pins',
    nbLots: 32,
    derniereMAJ: '2026-03-15',
    travaux: [
      {
        id: 'trav-1',
        titre: 'Ravalement de façade',
        type: 'FACADE',
        datePrevisionnelle: '2027-06-01',
        montantEstime: 85000,
        montantVote: 87500,
        statut: TravauxPrevisionnelStatut.VOTE,
        priorite: 'HAUTE',
        description: 'Ravalement complet avec isolation par l\'extérieur',
        etapes: [
          { id: 'e1', label: 'Devis', date: '2026-09-15', statut: 'FAIT', montant: 87500 },
          { id: 'e2', label: 'Vote en AG', date: '2026-11-20', statut: 'FAIT' },
          { id: 'e3', label: 'Commande', date: '2027-02-01', statut: 'EN_COURS' },
          { id: 'e4', label: 'Intervention', statut: 'A_VENIR' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
      {
        id: 'trav-2',
        titre: 'Remplacement chaudière collective',
        type: 'CHAUFFAGE',
        datePrevisionnelle: '2026-10-01',
        montantEstime: 42000,
        statut: TravauxPrevisionnelStatut.EN_COURS,
        priorite: 'CRITIQUE',
        etapes: [
          { id: 'e1', label: 'Devis', date: '2026-01-10', statut: 'FAIT', montant: 41800 },
          { id: 'e2', label: 'Vote en AG', date: '2026-02-14', statut: 'FAIT' },
          { id: 'e3', label: 'Commande', date: '2026-03-01', statut: 'FAIT' },
          { id: 'e4', label: 'Intervention', date: '2026-10-01', statut: 'EN_COURS' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
      {
        id: 'trav-3',
        titre: 'Étanchéité toiture terrasse',
        type: 'ETANCHEITE',
        datePrevisionnelle: '2028-04-01',
        montantEstime: 28000,
        statut: TravauxPrevisionnelStatut.PREVU,
        priorite: 'NORMALE',
        etapes: [
          { id: 'e1', label: 'Devis', statut: 'A_VENIR' },
          { id: 'e2', label: 'Vote en AG', statut: 'A_VENIR' },
          { id: 'e3', label: 'Commande', statut: 'A_VENIR' },
          { id: 'e4', label: 'Intervention', statut: 'A_VENIR' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
      {
        id: 'trav-4',
        titre: 'Mise aux normes ascenseur',
        type: 'ASCENSEUR',
        datePrevisionnelle: '2029-01-01',
        montantEstime: 35000,
        statut: TravauxPrevisionnelStatut.A_L_ETUDE,
        priorite: 'NORMALE',
        etapes: [
          { id: 'e1', label: 'Devis', statut: 'A_VENIR' },
          { id: 'e2', label: 'Vote en AG', statut: 'A_VENIR' },
          { id: 'e3', label: 'Commande', statut: 'A_VENIR' },
          { id: 'e4', label: 'Intervention', statut: 'A_VENIR' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
      {
        id: 'trav-5',
        titre: 'Réfection parkings souterrains',
        type: 'AUTRE',
        datePrevisionnelle: '2025-08-01',
        dateRealisation: '2025-09-15',
        montantEstime: 18000,
        montantReel: 17400,
        statut: TravauxPrevisionnelStatut.TERMINE,
        priorite: 'NORMALE',
        etapes: [
          { id: 'e1', label: 'Devis', date: '2025-02-01', statut: 'FAIT', montant: 17400 },
          { id: 'e2', label: 'Vote en AG', date: '2025-03-10', statut: 'FAIT' },
          { id: 'e3', label: 'Commande', date: '2025-04-01', statut: 'FAIT' },
          { id: 'e4', label: 'Intervention', date: '2025-08-01', statut: 'FAIT' },
          { id: 'e5', label: 'Réception', date: '2025-09-15', statut: 'FAIT' },
        ],
      },
    ],
  },
  {
    coproprieteId: 'copro-2',
    nom: 'Immeuble Voltaire',
    nbLots: 18,
    derniereMAJ: '2026-01-20',
    travaux: [
      {
        id: 'trav-6',
        titre: 'Peinture cage d\'escalier',
        type: 'AUTRE',
        datePrevisionnelle: '2026-07-01',
        montantEstime: 12000,
        statut: TravauxPrevisionnelStatut.VOTE,
        priorite: 'FAIBLE',
        etapes: [
          { id: 'e1', label: 'Devis', date: '2026-02-01', statut: 'FAIT', montant: 11800 },
          { id: 'e2', label: 'Vote en AG', date: '2026-03-15', statut: 'FAIT' },
          { id: 'e3', label: 'Commande', statut: 'A_VENIR' },
          { id: 'e4', label: 'Intervention', statut: 'A_VENIR' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
      {
        id: 'trav-7',
        titre: 'Remplacement menuiseries',
        type: 'FACADE',
        datePrevisionnelle: '2030-01-01',
        montantEstime: 65000,
        statut: TravauxPrevisionnelStatut.A_L_ETUDE,
        priorite: 'NORMALE',
        etapes: [
          { id: 'e1', label: 'Devis', statut: 'A_VENIR' },
          { id: 'e2', label: 'Vote en AG', statut: 'A_VENIR' },
          { id: 'e3', label: 'Commande', statut: 'A_VENIR' },
          { id: 'e4', label: 'Intervention', statut: 'A_VENIR' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
    ],
  },
  {
    coproprieteId: 'copro-3',
    nom: 'Les Jardins du Lac',
    nbLots: 65,
    derniereMAJ: '2026-04-01',
    travaux: [
      {
        id: 'trav-8',
        titre: 'Rénovation espaces verts',
        type: 'ESPACES_VERTS',
        datePrevisionnelle: '2026-05-01',
        montantEstime: 22000,
        statut: TravauxPrevisionnelStatut.EN_COURS,
        priorite: 'NORMALE',
        etapes: [
          { id: 'e1', label: 'Devis', date: '2026-01-15', statut: 'FAIT', montant: 21500 },
          { id: 'e2', label: 'Vote en AG', date: '2026-02-20', statut: 'FAIT' },
          { id: 'e3', label: 'Commande', date: '2026-03-10', statut: 'FAIT' },
          { id: 'e4', label: 'Intervention', date: '2026-05-01', statut: 'EN_COURS' },
          { id: 'e5', label: 'Réception', statut: 'A_VENIR' },
        ],
      },
    ],
  },
  {
    coproprieteId: 'copro-4',
    nom: 'Résidence Berlioz',
    nbLots: 12,
    derniereMAJ: '2026-02-10',
    travaux: [],
  },
];
```

- [ ] **Étape 1.4 — Créer les données mockées DPE**

Créer `src/components/features/conformite/dpe/mock-data.ts` :

```typescript
import type { IDPE } from '@/types';

export const MOCK_DPE_LIST: IDPE[] = [
  {
    id: 'dpe-1',
    coproprieteId: 'copro-1',
    coproprieteNom: 'Résidence Les Pins',
    nbLots: 32,
    classeEnergie: 'D',
    classeGES: 'D',
    dateDiagnostic: '2023-06-15',
    dateExpiration: '2033-06-15',
    diagnostiqueur: 'Cabinet Thermique Conseil',
    numeroADEME: '2306150001',
    consoEnergie: 215,
    emissionsGES: 48,
    statut: 'VALIDE',
    travauxRecommandes: [],
    historique: [
      { id: 'h1', dateDiagnostic: '2018-04-10', classeEnergie: 'E', diagnostiqueur: 'DiagExpert PACA' },
    ],
  },
  {
    id: 'dpe-2',
    coproprieteId: 'copro-2',
    coproprieteNom: 'Immeuble Voltaire',
    nbLots: 18,
    classeEnergie: 'F',
    classeGES: 'F',
    dateDiagnostic: '2020-09-20',
    dateExpiration: '2026-09-20',
    diagnostiqueur: 'EcoThermie SAS',
    numeroADEME: '2009200042',
    consoEnergie: 375,
    emissionsGES: 84,
    statut: 'EXPIRE_BIENTOT',
    travauxRecommandes: [],
    historique: [
      { id: 'h2', dateDiagnostic: '2015-11-03', classeEnergie: 'F', diagnostiqueur: 'EcoThermie SAS' },
    ],
  },
  {
    id: 'dpe-3',
    coproprieteId: 'copro-3',
    coproprieteNom: 'Les Jardins du Lac',
    nbLots: 65,
    classeEnergie: 'C',
    classeGES: 'C',
    dateDiagnostic: '2024-03-01',
    dateExpiration: '2034-03-01',
    diagnostiqueur: 'Diagnotec Île-de-France',
    numeroADEME: '2403010088',
    consoEnergie: 145,
    emissionsGES: 32,
    statut: 'VALIDE',
    travauxRecommandes: [],
    historique: [],
  },
  {
    id: 'dpe-4',
    coproprieteId: 'copro-4',
    coproprieteNom: 'Résidence Berlioz',
    nbLots: 12,
    classeEnergie: 'G',
    classeGES: 'G',
    dateDiagnostic: '2019-01-10',
    dateExpiration: '2025-01-10',
    diagnostiqueur: 'Cabinet Immo Diag',
    numeroADEME: '1901100005',
    consoEnergie: 510,
    emissionsGES: 115,
    statut: 'EXPIRE',
    travauxRecommandes: [],
    historique: [
      { id: 'h3', dateDiagnostic: '2012-07-22', classeEnergie: 'G', diagnostiqueur: 'Cabinet Immo Diag' },
    ],
  },
];
```

- [ ] **Étape 1.5 — Créer les données mockées Factur-X**

Créer `src/components/features/conformite/facturx/mock-data.ts` :

```typescript
import type { IFactureFacturX } from '@/types';

export const MOCK_FACTURES_FACTURX: IFactureFacturX[] = [
  {
    id: 'fac-001',
    numero: 'FAC-2026-001',
    copropriete: 'Résidence Les Pins',
    fournisseur: 'Chauffage Pro SARL',
    montantTTC: 4200,
    date: '2026-04-01',
    statutPaiement: 'PAYEE',
    statutFacturX: 'GENERE',
    dateGeneration: '2026-04-02',
    profil: 'EN16931',
  },
  {
    id: 'fac-002',
    numero: 'FAC-2026-002',
    copropriete: 'Immeuble Voltaire',
    fournisseur: 'EcoThermie SAS',
    montantTTC: 1850,
    date: '2026-04-05',
    statutPaiement: 'EN_ATTENTE',
    statutFacturX: 'EN_ATTENTE',
    profil: 'EN16931',
  },
  {
    id: 'fac-003',
    numero: 'FAC-2026-003',
    copropriete: 'Les Jardins du Lac',
    fournisseur: 'VerdurePaysage',
    montantTTC: 3600,
    date: '2026-04-08',
    statutPaiement: 'EN_ATTENTE',
    statutFacturX: 'EN_ATTENTE',
    profil: 'EN16931',
  },
  {
    id: 'fac-004',
    numero: 'FAC-2025-198',
    copropriete: 'Résidence Berlioz',
    fournisseur: 'Électricité Batiment',
    montantTTC: 920,
    date: '2025-12-15',
    statutPaiement: 'PAYEE',
    statutFacturX: 'NON_APPLICABLE',
    profil: 'MINIMUM',
  },
  {
    id: 'fac-005',
    numero: 'FAC-2026-004',
    copropriete: 'Résidence Les Pins',
    fournisseur: 'Ascenseurs Rhône-Alpes',
    montantTTC: 12400,
    date: '2026-04-10',
    statutPaiement: 'EN_RETARD',
    statutFacturX: 'EN_ATTENTE',
    profil: 'EN16931',
  },
];
```

- [ ] **Étape 1.6 — Commit**

```bash
git add src/types/models/conformite.ts src/types/index.ts \
  src/components/features/conformite/ppt/mock-data.ts \
  src/components/features/conformite/dpe/mock-data.ts \
  src/components/features/conformite/facturx/mock-data.ts
git commit -m "feat(conformite): types et données mockées PPT, DPE, Factur-X"
```

---

## Tâche 2 — Navigation & routing

**Fichiers :**
- Modifier : `src/lib/config/navigation.ts`
- Créer : `src/app/(dashboard)/conformite/layout.tsx`
- Modifier : `src/app/(dashboard)/maintenance/ppt/page.tsx`

- [ ] **Étape 2.1 — Ajouter l'import des icônes dans navigation.ts**

Dans `src/lib/config/navigation.ts`, modifier la ligne d'import Lucide pour ajouter `ShieldCheck`, `ClipboardList`, `Zap`, `FileCode` :

```typescript
import {
  LayoutDashboard, Users, Building2, DollarSign, Wrench,
  FileText, MessageSquare, Scale, Calendar,
  Calculator, Receipt, ArrowLeftRight, FolderOpen, Mail,
  AlertTriangle, BookOpen, ClipboardList, BarChart3,
  ShieldCheck, Zap, FileCode,
  type LucideIcon
} from 'lucide-react';
```

- [ ] **Étape 2.2 — Ajouter le module dans MODULES**

Dans le tableau `MODULES`, ajouter après le module `maintenance` (après la fermeture de son objet `}`) :

```typescript
  {
    id: 'conformite',
    label: 'Conformité 2026',
    icon: ShieldCheck,
    href: '/conformite/ppt',
    subPages: [
      { label: 'PPT', href: '/conformite/ppt', icon: ClipboardList },
      { label: 'DPE Collectif', href: '/conformite/dpe', icon: Zap },
      { label: 'Factur-X', href: '/conformite/facturx', icon: FileCode },
    ],
  },
```

- [ ] **Étape 2.3 — Créer le layout Conformité**

Créer `src/app/(dashboard)/conformite/layout.tsx` :

```typescript
export default function ConformiteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

_(Le sous-nav est rendu dans chaque page via FinanceTopBar — pas besoin d'un layout complexe.)_

- [ ] **Étape 2.4 — Redirect maintenance/ppt**

Remplacer le contenu de `src/app/(dashboard)/maintenance/ppt/page.tsx` :

```typescript
import { redirect } from 'next/navigation';

export default function PPTRedirectPage() {
  redirect('/conformite/ppt');
}
```

- [ ] **Étape 2.5 — Commit**

```bash
git add src/lib/config/navigation.ts \
  src/app/(dashboard)/conformite/layout.tsx \
  src/app/(dashboard)/maintenance/ppt/page.tsx
git commit -m "feat(conformite): navigation + redirect maintenance/ppt"
```

---

## Tâche 3 — Hook usePPT

**Fichiers :**
- Créer : `src/hooks/usePPT.ts`

- [ ] **Étape 3.1 — Créer le hook**

Créer `src/hooks/usePPT.ts` :

```typescript
'use client';

import { useState, useMemo, useCallback } from 'react';
import type { IPPTCopropriete, ITravauxPPT } from '@/types';
import { MOCK_PPT_COPROPRIETES } from '@/components/features/conformite/ppt/mock-data';
import { TravauxPrevisionnelStatut } from '@/types/enums';

const YEARS = [2026, 2027, 2028, 2029, 2030, 2031, 2032, 2033, 2034, 2035];

export type PPTFilter = 'TOUTES' | 'A_COMPLETER' | 'EN_RETARD' | 'A_JOUR';

function getStatutGlobal(copro: IPPTCopropriete): PPTFilter {
  if (copro.travaux.length === 0) return 'A_COMPLETER';
  const hasEnRetard = copro.travaux.some(t =>
    t.statut === TravauxPrevisionnelStatut.EN_COURS &&
    new Date(t.datePrevisionnelle) < new Date()
  );
  if (hasEnRetard) return 'EN_RETARD';
  return 'A_JOUR';
}

interface UsePPTOptions {
  coproprieteId?: string;
}

export function usePPT({ coproprieteId }: UsePPTOptions = {}) {
  const [selectedYear, setSelectedYear] = useState<number | null>(null);
  const [filter, setFilter] = useState<PPTFilter>('TOUTES');
  const [selectedTravail, setSelectedTravail] = useState<ITravauxPPT | null>(null);

  // Vue gestionnaire : toutes les copros
  const coproprietes = useMemo(() => {
    if (filter === 'TOUTES') return MOCK_PPT_COPROPRIETES;
    return MOCK_PPT_COPROPRIETES.filter(c => getStatutGlobal(c) === filter);
  }, [filter]);

  // Vue détail : copro sélectionnée
  const selectedCopro = useMemo(
    () => coproprieteId ? MOCK_PPT_COPROPRIETES.find(c => c.coproprieteId === coproprieteId) ?? null : null,
    [coproprieteId]
  );

  // Travaux filtrés par année
  const travaux = useMemo(() => {
    if (!selectedCopro) return [];
    if (!selectedYear) return selectedCopro.travaux;
    return selectedCopro.travaux.filter(t => {
      const year = new Date(t.datePrevisionnelle).getFullYear();
      return year === selectedYear;
    });
  }, [selectedCopro, selectedYear]);

  // Groupement par statut pour le kanban
  const travauxByStatut = useMemo(() => {
    const map: Record<string, ITravauxPPT[]> = {
      A_L_ETUDE: [],
      PREVU: [],
      VOTE: [],
      EN_COURS: [],
      TERMINE: [],
    };
    travaux.forEach(t => {
      const key = t.statut as string;
      if (key in map) map[key].push(t);
    });
    return map;
  }, [travaux]);

  const openTravailDetail = useCallback((travail: ITravauxPPT) => {
    setSelectedTravail(travail);
  }, []);

  const closeTravailDetail = useCallback(() => {
    setSelectedTravail(null);
  }, []);

  return {
    // Gestionnaire
    coproprietes,
    filter,
    setFilter,
    // Détail copro
    selectedCopro,
    travaux,
    travauxByStatut,
    selectedYear,
    setYear: setSelectedYear,
    years: YEARS,
    // Modal timeline
    selectedTravail,
    openTravailDetail,
    closeTravailDetail,
    isLoading: false,
  };
}
```

- [ ] **Étape 3.2 — Commit**

```bash
git add src/hooks/usePPT.ts
git commit -m "feat(conformite): hook usePPT avec kanban et filtres"
```

---

## Tâche 4 — Module PPT — composants

**Fichiers :**
- Créer : `src/components/features/conformite/ppt/PPTGestionnaireGrid.tsx` + `.module.css`
- Créer : `src/components/features/conformite/ppt/PPTYearSelector.tsx` + `.module.css`
- Créer : `src/components/features/conformite/ppt/PPTKanban.tsx` + `.module.css`
- Créer : `src/components/features/conformite/ppt/PPTCardDetail.tsx` + `.module.css`

- [ ] **Étape 4.1 — PPTGestionnaireGrid**

Créer `src/components/features/conformite/ppt/PPTGestionnaireGrid.tsx` :

```typescript
'use client';

import { useRouter } from 'next/navigation';
import { ArrowRight, Building2, AlertTriangle, CheckCircle, Clock } from 'lucide-react';
import clsx from 'clsx';
import type { IPPTCopropriete } from '@/types';
import { TravauxPrevisionnelStatut } from '@/types/enums';
import styles from './PPTGestionnaireGrid.module.css';

interface PPTGestionnaireGridProps {
  coproprietes: IPPTCopropriete[];
}

function getProgressPercent(travaux: IPPTCopropriete['travaux']): number {
  if (travaux.length === 0) return 0;
  const termines = travaux.filter(t => t.statut === TravauxPrevisionnelStatut.TERMINE).length;
  return Math.round((termines / travaux.length) * 100);
}

function getStatutLabel(copro: IPPTCopropriete): { label: string; variant: 'success' | 'warning' | 'danger' | 'neutral' } {
  if (copro.travaux.length === 0) return { label: 'À compléter', variant: 'neutral' };
  const hasEnRetard = copro.travaux.some(
    t => t.statut === TravauxPrevisionnelStatut.EN_COURS && new Date(t.datePrevisionnelle) < new Date()
  );
  if (hasEnRetard) return { label: 'En retard', variant: 'danger' };
  return { label: 'À jour', variant: 'success' };
}

export function PPTGestionnaireGrid({ coproprietes }: PPTGestionnaireGridProps) {
  const router = useRouter();

  if (coproprietes.length === 0) {
    return (
      <div className={styles.empty}>
        <Building2 size={40} />
        <p>Aucune copropriété ne correspond aux filtres sélectionnés.</p>
      </div>
    );
  }

  return (
    <div className={styles.grid}>
      {coproprietes.map(copro => {
        const { label, variant } = getStatutLabel(copro);
        const progress = getProgressPercent(copro.travaux);
        const byStatut = {
          aEtude: copro.travaux.filter(t => t.statut === TravauxPrevisionnelStatut.A_L_ETUDE).length,
          prevu: copro.travaux.filter(t => t.statut === TravauxPrevisionnelStatut.PREVU).length,
          vote: copro.travaux.filter(t => t.statut === TravauxPrevisionnelStatut.VOTE).length,
          enCours: copro.travaux.filter(t => t.statut === TravauxPrevisionnelStatut.EN_COURS).length,
          termine: copro.travaux.filter(t => t.statut === TravauxPrevisionnelStatut.TERMINE).length,
        };

        return (
          <div key={copro.coproprieteId} className={styles.card}>
            <div className={styles.cardHeader}>
              <div>
                <div className={styles.cardTitle}>{copro.nom}</div>
                <div className={styles.cardMeta}>{copro.nbLots} lots · {copro.travaux.length} travaux planifiés</div>
              </div>
              <span className={clsx(styles.badge, styles[`badge${variant.charAt(0).toUpperCase() + variant.slice(1)}`])}>
                {variant === 'success' && <CheckCircle size={11} />}
                {variant === 'danger' && <AlertTriangle size={11} />}
                {variant === 'neutral' && <Clock size={11} />}
                {label}
              </span>
            </div>

            <div className={styles.statRow}>
              <span className={styles.stat}><span className={styles.statDot} style={{ background: '#64748b' }} />{byStatut.aEtude} à l'étude</span>
              <span className={styles.stat}><span className={styles.statDot} style={{ background: '#f59e0b' }} />{byStatut.prevu} prévus</span>
              <span className={styles.stat}><span className={styles.statDot} style={{ background: '#60a5fa' }} />{byStatut.vote} votés</span>
              <span className={styles.stat}><span className={styles.statDot} style={{ background: '#3b82f6' }} />{byStatut.enCours} en cours</span>
              <span className={styles.stat}><span className={styles.statDot} style={{ background: '#22c55e' }} />{byStatut.termine} terminés</span>
            </div>

            <div className={styles.progressWrap}>
              <div className={styles.progressBar}>
                <div className={styles.progressFill} style={{ width: `${progress}%` }} />
              </div>
              <span className={styles.progressLabel}>{progress}% terminé</span>
            </div>

            <button
              className={styles.ctaBtn}
              onClick={() => router.push(`/conformite/ppt/${copro.coproprieteId}`)}
            >
              Voir le PPT <ArrowRight size={14} />
            </button>
          </div>
        );
      })}
    </div>
  );
}
```

Créer `src/components/features/conformite/ppt/PPTGestionnaireGrid.module.css` :

```css
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 16px;
}

.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 60px 24px;
  color: var(--text-tertiary);
  background: #1a1d2e;
  border-radius: 12px;
  border: 1px solid rgba(148,163,184,0.08);
}

.card {
  background: #1a1d2e;
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 12px;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 14px;
  transition: border-color 0.15s;
}

.card:hover {
  border-color: rgba(59,130,246,0.25);
}

.cardHeader {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12px;
}

.cardTitle {
  font-size: 15px;
  font-weight: 700;
  color: #e2e8f0;
}

.cardMeta {
  font-size: 12px;
  color: #94a3b8;
  margin-top: 3px;
}

.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 10px;
  border-radius: 8px;
  font-size: 11px;
  font-weight: 500;
  white-space: nowrap;
  flex-shrink: 0;
}

.badgeSuccess { background: rgba(34,197,94,0.1); color: #4ade80; }
.badgeDanger { background: rgba(239,68,68,0.1); color: #f87171; }
.badgeWarning { background: rgba(245,158,11,0.1); color: #fbbf24; }
.badgeNeutral { background: rgba(148,163,184,0.1); color: #94a3b8; }

.statRow {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.stat {
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 11px;
  color: #94a3b8;
}

.statDot {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  flex-shrink: 0;
}

.progressWrap {
  display: flex;
  align-items: center;
  gap: 10px;
}

.progressBar {
  flex: 1;
  height: 5px;
  background: rgba(148,163,184,0.1);
  border-radius: 99px;
  overflow: hidden;
}

.progressFill {
  height: 100%;
  background: #3b82f6;
  border-radius: 99px;
  transition: width 0.3s;
}

.progressLabel {
  font-size: 11px;
  color: #94a3b8;
  white-space: nowrap;
}

.ctaBtn {
  display: flex;
  align-items: center;
  gap: 6px;
  align-self: flex-end;
  background: rgba(59,130,246,0.1);
  color: #60a5fa;
  border: none;
  border-radius: 8px;
  padding: 7px 14px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: background 0.15s;
}

.ctaBtn:hover {
  background: rgba(59,130,246,0.18);
}
```

- [ ] **Étape 4.2 — PPTYearSelector**

Créer `src/components/features/conformite/ppt/PPTYearSelector.tsx` :

```typescript
'use client';

import clsx from 'clsx';
import styles from './PPTYearSelector.module.css';

interface PPTYearSelectorProps {
  years: number[];
  selectedYear: number | null;
  onSelect: (year: number | null) => void;
}

export function PPTYearSelector({ years, selectedYear, onSelect }: PPTYearSelectorProps) {
  return (
    <div className={styles.container}>
      <button
        className={clsx(styles.pill, selectedYear === null && styles.pillActive)}
        onClick={() => onSelect(null)}
      >
        Tous
      </button>
      {years.map(year => (
        <button
          key={year}
          className={clsx(styles.pill, selectedYear === year && styles.pillActive)}
          onClick={() => onSelect(year)}
        >
          {year}
        </button>
      ))}
    </div>
  );
}
```

Créer `src/components/features/conformite/ppt/PPTYearSelector.module.css` :

```css
.container {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
}

.pill {
  padding: 5px 14px;
  border-radius: 99px;
  border: 1px solid rgba(148,163,184,0.12);
  background: transparent;
  color: #94a3b8;
  font-size: 12px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
}

.pill:hover {
  background: rgba(148,163,184,0.06);
  color: #e2e8f0;
}

.pillActive {
  background: #3b82f6;
  border-color: #3b82f6;
  color: #fff;
}
```

- [ ] **Étape 4.3 — PPTKanban**

Créer `src/components/features/conformite/ppt/PPTKanban.tsx` :

```typescript
'use client';

import { Euro } from 'lucide-react';
import type { ITravauxPPT } from '@/types';
import styles from './PPTKanban.module.css';

const COLONNES = [
  { statut: 'A_L_ETUDE', label: 'À l\'étude', color: '#64748b' },
  { statut: 'PREVU',     label: 'Prévu',       color: '#f59e0b' },
  { statut: 'VOTE',      label: 'Voté en AG',  color: '#60a5fa' },
  { statut: 'EN_COURS',  label: 'En cours',    color: '#3b82f6' },
  { statut: 'TERMINE',   label: 'Terminé',     color: '#22c55e' },
];

const PRIORITE_COLORS: Record<string, string> = {
  FAIBLE: '#64748b', NORMALE: '#94a3b8', HAUTE: '#f59e0b', CRITIQUE: '#ef4444',
};

interface PPTKanbanProps {
  travauxByStatut: Record<string, ITravauxPPT[]>;
  onCardClick: (travail: ITravauxPPT) => void;
}

function formatEur(n: number): string {
  return new Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR', maximumFractionDigits: 0 }).format(n);
}

export function PPTKanban({ travauxByStatut, onCardClick }: PPTKanbanProps) {
  return (
    <div className={styles.board}>
      {COLONNES.map(col => {
        const travaux = travauxByStatut[col.statut] ?? [];
        const total = travaux.reduce((s, t) => s + t.montantEstime, 0);

        return (
          <div key={col.statut} className={styles.column}>
            <div className={styles.colHeader}>
              <div className={styles.colTitle}>
                <span className={styles.colDot} style={{ background: col.color }} />
                {col.label}
                <span className={styles.colCount}>{travaux.length}</span>
              </div>
              {total > 0 && <span className={styles.colTotal}>{formatEur(total)}</span>}
            </div>

            <div className={styles.cards}>
              {travaux.length === 0 && (
                <div className={styles.emptyCol}>Aucun travail</div>
              )}
              {travaux.map(t => (
                <button
                  key={t.id}
                  className={styles.card}
                  onClick={() => onCardClick(t)}
                >
                  <div className={styles.cardTop}>
                    <span className={styles.cardTitle}>{t.titre}</span>
                    <span className={styles.cardPrio} style={{ color: PRIORITE_COLORS[t.priorite] ?? '#94a3b8' }}>
                      {t.priorite}
                    </span>
                  </div>
                  <div className={styles.cardType}>{t.type}</div>
                  <div className={styles.cardFooter}>
                    <span className={styles.cardDate}>
                      {new Date(t.datePrevisionnelle).toLocaleDateString('fr-FR', { month: 'short', year: 'numeric' })}
                    </span>
                    <span className={styles.cardMontant}>
                      <Euro size={11} />{formatEur(t.montantEstime)}
                    </span>
                  </div>
                </button>
              ))}
            </div>
          </div>
        );
      })}
    </div>
  );
}
```

Créer `src/components/features/conformite/ppt/PPTKanban.module.css` :

```css
.board {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12px;
  align-items: start;
}

@media (max-width: 1200px) {
  .board { grid-template-columns: repeat(3, 1fr); }
}

.column {
  background: #1a1d2e;
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 12px;
  padding: 14px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.colHeader {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.colTitle {
  display: flex;
  align-items: center;
  gap: 7px;
  font-size: 11px;
  font-weight: 600;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.colDot {
  width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0;
}

.colCount {
  margin-left: auto;
  background: rgba(148,163,184,0.1);
  color: #94a3b8;
  border-radius: 99px;
  padding: 1px 7px;
  font-size: 11px;
}

.colTotal {
  font-size: 12px;
  font-weight: 600;
  color: #e2e8f0;
  font-family: 'SF Mono', monospace;
  font-variant-numeric: tabular-nums;
}

.cards { display: flex; flex-direction: column; gap: 8px; }

.emptyCol {
  font-size: 12px;
  color: #475569;
  text-align: center;
  padding: 16px 0;
}

.card {
  background: #131620;
  border: 1px solid rgba(148,163,184,0.06);
  border-radius: 8px;
  padding: 12px;
  cursor: pointer;
  text-align: left;
  width: 100%;
  transition: border-color 0.15s, transform 0.1s;
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.card:hover {
  border-color: rgba(59,130,246,0.3);
  transform: translateY(-1px);
}

.cardTop {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 8px;
}

.cardTitle {
  font-size: 13px;
  font-weight: 600;
  color: #e2e8f0;
  line-height: 1.3;
}

.cardPrio {
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.4px;
  white-space: nowrap;
  flex-shrink: 0;
}

.cardType {
  font-size: 11px;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.4px;
}

.cardFooter {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 2px;
}

.cardDate { font-size: 11px; color: #94a3b8; }

.cardMontant {
  display: flex;
  align-items: center;
  gap: 3px;
  font-size: 12px;
  font-weight: 500;
  color: #e2e8f0;
  font-family: 'SF Mono', monospace;
  font-variant-numeric: tabular-nums;
}
```

- [ ] **Étape 4.4 — PPTCardDetail (modal timeline)**

Créer `src/components/features/conformite/ppt/PPTCardDetail.tsx` :

```typescript
'use client';

import { X, CheckCircle, Clock, Circle } from 'lucide-react';
import type { ITravauxPPT, IEtapeTravaux } from '@/types';
import styles from './PPTCardDetail.module.css';

interface PPTCardDetailProps {
  travail: ITravauxPPT;
  onClose: () => void;
}

function EtapeIcon({ statut }: { statut: IEtapeTravaux['statut'] }) {
  if (statut === 'FAIT') return <CheckCircle size={16} style={{ color: '#22c55e' }} />;
  if (statut === 'EN_COURS') return <Clock size={16} style={{ color: '#3b82f6' }} />;
  return <Circle size={16} style={{ color: '#475569' }} />;
}

function formatEur(n: number): string {
  return new Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR', maximumFractionDigits: 0 }).format(n);
}

export function PPTCardDetail({ travail, onClose }: PPTCardDetailProps) {
  return (
    <div className={styles.overlay} onClick={onClose}>
      <div className={styles.modal} onClick={e => e.stopPropagation()}>
        <div className={styles.header}>
          <div>
            <div className={styles.title}>{travail.titre}</div>
            <div className={styles.meta}>
              {travail.type} · Estimation : {formatEur(travail.montantEstime)}
            </div>
          </div>
          <button className={styles.closeBtn} onClick={onClose}><X size={18} /></button>
        </div>

        {travail.description && (
          <p className={styles.desc}>{travail.description}</p>
        )}

        <div className={styles.timelineLabel}>Étapes</div>
        <div className={styles.timeline}>
          {travail.etapes.map((etape, i) => (
            <div key={etape.id} className={styles.etape}>
              <div className={styles.etapeLeft}>
                <EtapeIcon statut={etape.statut} />
                {i < travail.etapes.length - 1 && (
                  <div className={styles.connector} style={{ background: etape.statut === 'FAIT' ? '#22c55e' : '#1e293b' }} />
                )}
              </div>
              <div className={styles.etapeContent}>
                <div className={styles.etapeLabel}>{etape.label}</div>
                {etape.date && (
                  <div className={styles.etapeDate}>
                    {new Date(etape.date).toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}
                  </div>
                )}
                {etape.montant && (
                  <div className={styles.etapeMontant}>{formatEur(etape.montant)}</div>
                )}
                {etape.commentaire && (
                  <div className={styles.etapeComment}>{etape.commentaire}</div>
                )}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

Créer `src/components/features/conformite/ppt/PPTCardDetail.module.css` :

```css
.overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  padding: 24px;
}

.modal {
  background: #1a1d2e;
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 12px;
  width: 100%;
  max-width: 520px;
  max-height: 80vh;
  overflow-y: auto;
  padding: 24px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.4);
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 16px;
  margin-bottom: 16px;
}

.title {
  font-size: 18px;
  font-weight: 700;
  color: #e2e8f0;
}

.meta {
  font-size: 13px;
  color: #94a3b8;
  margin-top: 4px;
}

.closeBtn {
  background: rgba(148,163,184,0.06);
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 8px;
  color: #94a3b8;
  cursor: pointer;
  padding: 6px;
  flex-shrink: 0;
  transition: background 0.15s;
}

.closeBtn:hover { background: rgba(148,163,184,0.12); }

.desc {
  font-size: 13px;
  color: #94a3b8;
  margin-bottom: 16px;
  line-height: 1.6;
}

.timelineLabel {
  font-size: 11px;
  font-weight: 600;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 16px;
}

.timeline { display: flex; flex-direction: column; }

.etape {
  display: flex;
  gap: 14px;
}

.etapeLeft {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
}

.connector {
  width: 2px;
  flex: 1;
  min-height: 20px;
  margin: 4px 0;
}

.etapeContent {
  padding-bottom: 20px;
  flex: 1;
}

.etapeLabel {
  font-size: 14px;
  font-weight: 600;
  color: #e2e8f0;
}

.etapeDate {
  font-size: 12px;
  color: #94a3b8;
  margin-top: 3px;
}

.etapeMontant {
  font-size: 13px;
  font-weight: 500;
  color: #22c55e;
  font-family: 'SF Mono', monospace;
  margin-top: 4px;
}

.etapeComment {
  font-size: 12px;
  color: #64748b;
  margin-top: 4px;
}
```

- [ ] **Étape 4.5 — Commit composants PPT**

```bash
git add src/components/features/conformite/ppt/
git commit -m "feat(conformite/ppt): composants PPTGestionnaireGrid, PPTYearSelector, PPTKanban, PPTCardDetail"
```

---

## Tâche 5 — Module PPT — pages

**Fichiers :**
- Créer : `src/app/(dashboard)/conformite/ppt/page.tsx`
- Créer : `src/app/(dashboard)/conformite/ppt/[coproprieteId]/page.tsx`

- [ ] **Étape 5.1 — Page gestionnaire PPT**

Créer `src/app/(dashboard)/conformite/ppt/page.tsx` :

```typescript
'use client';

import { FinanceTopBar } from '@/components/layout/FinanceTopBar';
import { PPTGestionnaireGrid } from '@/components/features/conformite/ppt/PPTGestionnaireGrid';
import { usePPT, type PPTFilter } from '@/hooks/usePPT';
import styles from './ppt.module.css';

const FILTERS: { value: PPTFilter; label: string }[] = [
  { value: 'TOUTES', label: 'Toutes' },
  { value: 'A_JOUR', label: 'À jour' },
  { value: 'EN_RETARD', label: 'En retard' },
  { value: 'A_COMPLETER', label: 'À compléter' },
];

export default function PPTGestionnairePage() {
  const { coproprietes, filter, setFilter } = usePPT();

  return (
    <div className="container">
      <FinanceTopBar
        title="Plan Pluriannuel de Travaux"
        subtitle="Suivi des PPT sur l'ensemble de votre portefeuille"
        actions={
          <div className={styles.filters}>
            {FILTERS.map(f => (
              <button
                key={f.value}
                className={filter === f.value ? styles.filterActive : styles.filter}
                onClick={() => setFilter(f.value)}
              >
                {f.label}
              </button>
            ))}
          </div>
        }
      />
      <PPTGestionnaireGrid coproprietes={coproprietes} />
    </div>
  );
}
```

Créer `src/app/(dashboard)/conformite/ppt/ppt.module.css` :

```css
.filters {
  display: flex;
  align-items: center;
  gap: 4px;
  background: #131620;
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 8px;
  padding: 3px;
}

.filter {
  padding: 5px 14px;
  border-radius: 6px;
  border: none;
  background: transparent;
  color: #94a3b8;
  font-size: 13px;
  cursor: pointer;
  transition: all 0.15s;
}

.filter:hover { background: rgba(148,163,184,0.06); color: #e2e8f0; }

.filterActive {
  padding: 5px 14px;
  border-radius: 6px;
  border: none;
  background: #1a1d2e;
  color: #e2e8f0;
  font-size: 13px;
  cursor: pointer;
  font-weight: 500;
}
```

- [ ] **Étape 5.2 — Page détail PPT par copropriété**

Créer `src/app/(dashboard)/conformite/ppt/[coproprieteId]/page.tsx` :

```typescript
'use client';

import { useParams, useRouter } from 'next/navigation';
import { ArrowLeft } from 'lucide-react';
import { FinanceTopBar } from '@/components/layout/FinanceTopBar';
import { PPTYearSelector } from '@/components/features/conformite/ppt/PPTYearSelector';
import { PPTKanban } from '@/components/features/conformite/ppt/PPTKanban';
import { PPTCardDetail } from '@/components/features/conformite/ppt/PPTCardDetail';
import { usePPT } from '@/hooks/usePPT';
import styles from './ppt-detail.module.css';

export default function PPTDetailPage() {
  const { coproprieteId } = useParams<{ coproprieteId: string }>();
  const router = useRouter();
  const {
    selectedCopro,
    travauxByStatut,
    selectedYear,
    setYear,
    years,
    selectedTravail,
    openTravailDetail,
    closeTravailDetail,
  } = usePPT({ coproprieteId });

  if (!selectedCopro) {
    return (
      <div className="container">
        <div className={styles.notFound}>Copropriété introuvable.</div>
      </div>
    );
  }

  return (
    <div className="container">
      <FinanceTopBar
        title={`PPT — ${selectedCopro.nom}`}
        subtitle={`${selectedCopro.nbLots} lots · ${selectedCopro.travaux.length} travaux planifiés`}
        actions={
          <button className={styles.backBtn} onClick={() => router.push('/conformite/ppt')}>
            <ArrowLeft size={14} /> Retour à la liste
          </button>
        }
      />

      <div className={styles.yearRow}>
        <span className={styles.yearLabel}>Filtrer par année :</span>
        <PPTYearSelector years={years} selectedYear={selectedYear} onSelect={setYear} />
      </div>

      <PPTKanban travauxByStatut={travauxByStatut} onCardClick={openTravailDetail} />

      {selectedTravail && (
        <PPTCardDetail travail={selectedTravail} onClose={closeTravailDetail} />
      )}
    </div>
  );
}
```

Créer `src/app/(dashboard)/conformite/ppt/[coproprieteId]/ppt-detail.module.css` :

```css
.notFound {
  padding: 60px 24px;
  text-align: center;
  color: var(--text-tertiary);
  font-size: 14px;
}

.backBtn {
  display: flex;
  align-items: center;
  gap: 6px;
  background: rgba(148,163,184,0.06);
  border: 1px solid rgba(148,163,184,0.08);
  color: #94a3b8;
  border-radius: 8px;
  padding: 7px 14px;
  font-size: 13px;
  cursor: pointer;
  transition: all 0.15s;
}

.backBtn:hover { background: rgba(148,163,184,0.1); color: #e2e8f0; }

.yearRow {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 20px;
}

.yearLabel {
  font-size: 13px;
  color: #94a3b8;
  white-space: nowrap;
}
```

- [ ] **Étape 5.3 — Commit pages PPT**

```bash
git add src/app/(dashboard)/conformite/ppt/
git commit -m "feat(conformite/ppt): pages gestionnaire et détail par copropriété"
```

- [ ] **Étape 5.4 — Vérification navigateur**

Démarrer le serveur si nécessaire (`npm run dev`).  
Ouvrir http://localhost:3000/conformite/ppt.  
Vérifier : grille des 4 copropriétés visible, filtres fonctionnels.  
Cliquer sur "Voir le PPT" → kanban s'affiche.  
Sélectionner une année → cards filtrées.  
Cliquer sur une card → modal timeline s'ouvre et se ferme.

---

## Tâche 6 — Hook useDPE

**Fichiers :**
- Créer : `src/hooks/useDPE.ts`

- [ ] **Étape 6.1 — Créer le hook**

Créer `src/hooks/useDPE.ts` :

```typescript
'use client';

import { useMemo } from 'react';
import type { IDPE } from '@/types';
import { MOCK_DPE_LIST } from '@/components/features/conformite/dpe/mock-data';

interface UseDPEOptions {
  coproprieteId?: string;
}

export function useDPE({ coproprieteId }: UseDPEOptions = {}) {
  // Vue gestionnaire
  const coproprietes = useMemo(() => MOCK_DPE_LIST, []);

  // Vue détail
  const selectedDPE = useMemo(
    () => coproprieteId ? MOCK_DPE_LIST.find(d => d.coproprieteId === coproprieteId) ?? null : null,
    [coproprieteId]
  );

  return {
    coproprietes,
    selectedDPE,
    isLoading: false,
  };
}
```

- [ ] **Étape 6.2 — Commit**

```bash
git add src/hooks/useDPE.ts
git commit -m "feat(conformite): hook useDPE"
```

---

## Tâche 7 — Module DPE — composants

**Fichiers :**
- Créer : `DPEGestionnaireTable.tsx` + `.module.css`
- Créer : `DPEEnergyScale.tsx` + `.module.css`
- Créer : `DPEHistorique.tsx`
- Créer : `DPEFicheDetail.tsx` + `.module.css`

- [ ] **Étape 7.1 — DPEEnergyScale**

Créer `src/components/features/conformite/dpe/DPEEnergyScale.tsx` :

```typescript
'use client';

import clsx from 'clsx';
import type { ClasseDPE } from '@/types';
import styles from './DPEEnergyScale.module.css';

const CLASSES: { classe: ClasseDPE; label: string; range: string; color: string }[] = [
  { classe: 'A', label: 'A', range: '< 70', color: '#15803d' },
  { classe: 'B', label: 'B', range: '70–110', color: '#16a34a' },
  { classe: 'C', label: 'C', range: '110–180', color: '#65a30d' },
  { classe: 'D', label: 'D', range: '180–250', color: '#ca8a04' },
  { classe: 'E', label: 'E', range: '250–330', color: '#ea580c' },
  { classe: 'F', label: 'F', range: '330–420', color: '#dc2626' },
  { classe: 'G', label: 'G', range: '> 420', color: '#991b1b' },
];

interface DPEEnergyScaleProps {
  classeActive: ClasseDPE;
}

export function DPEEnergyScale({ classeActive }: DPEEnergyScaleProps) {
  return (
    <div className={styles.scale}>
      {CLASSES.map((c, i) => {
        const isActive = c.classe === classeActive;
        const width = 40 + i * 12;
        return (
          <div key={c.classe} className={clsx(styles.row, isActive && styles.rowActive)}>
            <div className={styles.bar} style={{ width, background: c.color }}>
              {c.label}
            </div>
            <span className={styles.range}>{c.range} kWh/m²/an</span>
            {isActive && <span className={styles.arrow}>◄ votre bien</span>}
          </div>
        );
      })}
    </div>
  );
}
```

Créer `src/components/features/conformite/dpe/DPEEnergyScale.module.css` :

```css
.scale { display: flex; flex-direction: column; gap: 4px; }

.row {
  display: flex;
  align-items: center;
  gap: 10px;
  opacity: 0.5;
  transition: opacity 0.15s;
}

.rowActive { opacity: 1; }

.bar {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 26px;
  border-radius: 4px;
  color: #fff;
  font-size: 13px;
  font-weight: 700;
  flex-shrink: 0;
  min-width: 40px;
}

.range {
  font-size: 11px;
  color: #94a3b8;
}

.arrow {
  font-size: 11px;
  font-weight: 600;
  color: #e2e8f0;
  margin-left: 4px;
}
```

- [ ] **Étape 7.2 — DPEHistorique**

Créer `src/components/features/conformite/dpe/DPEHistorique.tsx` :

```typescript
'use client';

import type { IDPEHistorique } from '@/types';
import styles from './DPEHistorique.module.css';

interface DPEHistoriqueProps {
  historique: IDPEHistorique[];
}

export function DPEHistorique({ historique }: DPEHistoriqueProps) {
  if (historique.length === 0) {
    return <p className={styles.empty}>Aucun DPE antérieur enregistré.</p>;
  }

  return (
    <table className={styles.table}>
      <thead>
        <tr>
          <th>Date</th>
          <th>Classe</th>
          <th>Diagnostiqueur</th>
        </tr>
      </thead>
      <tbody>
        {historique.map(h => (
          <tr key={h.id}>
            <td>{new Date(h.dateDiagnostic).toLocaleDateString('fr-FR')}</td>
            <td><span className={styles.classe}>{h.classeEnergie}</span></td>
            <td>{h.diagnostiqueur}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

Créer `src/components/features/conformite/dpe/DPEHistorique.module.css` :

```css
.empty { font-size: 13px; color: #64748b; }

.table { width: 100%; border-collapse: collapse; }

.table th {
  font-size: 10px;
  font-weight: 600;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  padding: 8px 12px;
  text-align: left;
  background: rgba(148,163,184,0.04);
  border-bottom: 1px solid rgba(148,163,184,0.04);
}

.table td {
  font-size: 13px;
  color: #e2e8f0;
  padding: 10px 12px;
  border-bottom: 1px solid rgba(148,163,184,0.04);
}

.classe {
  display: inline-block;
  background: rgba(59,130,246,0.1);
  color: #60a5fa;
  border-radius: 6px;
  padding: 2px 8px;
  font-weight: 700;
  font-size: 13px;
}
```

- [ ] **Étape 7.3 — DPEGestionnaireTable**

Créer `src/components/features/conformite/dpe/DPEGestionnaireTable.tsx` :

```typescript
'use client';

import { useRouter } from 'next/navigation';
import { AlertTriangle, CheckCircle, Clock, XCircle, Eye } from 'lucide-react';
import clsx from 'clsx';
import type { IDPE, StatutDPE } from '@/types';
import styles from './DPEGestionnaireTable.module.css';

const STATUT_CONFIG: Record<StatutDPE, { label: string; icon: React.ReactNode; variant: string }> = {
  VALIDE:        { label: 'Valide',           icon: <CheckCircle size={12} />,  variant: 'success' },
  EXPIRE_BIENTOT:{ label: 'Expire bientôt',   icon: <AlertTriangle size={12} />,variant: 'warning' },
  EXPIRE:        { label: 'Expiré',           icon: <XCircle size={12} />,      variant: 'danger'  },
  MANQUANT:      { label: 'Manquant',         icon: <Clock size={12} />,        variant: 'neutral' },
};

const CLASSE_COLORS: Record<string, string> = {
  A: '#15803d', B: '#16a34a', C: '#65a30d', D: '#ca8a04', E: '#ea580c', F: '#dc2626', G: '#991b1b',
};

interface DPEGestionnaireTableProps {
  dpeList: IDPE[];
}

export function DPEGestionnaireTable({ dpeList }: DPEGestionnaireTableProps) {
  const router = useRouter();

  return (
    <div className={styles.wrap}>
      <table className={styles.table}>
        <thead>
          <tr>
            <th>Copropriété</th>
            <th>Lots</th>
            <th>Classe DPE</th>
            <th>Date diagnostic</th>
            <th>Expiration</th>
            <th>Statut</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {dpeList.map(dpe => {
            const cfg = STATUT_CONFIG[dpe.statut];
            return (
              <tr key={dpe.id} className={styles.row}>
                <td className={styles.nomCell}>{dpe.coproprieteNom}</td>
                <td className={styles.numCell}>{dpe.nbLots}</td>
                <td>
                  <span className={styles.classeBadge} style={{ background: CLASSE_COLORS[dpe.classeEnergie] + '22', color: CLASSE_COLORS[dpe.classeEnergie] }}>
                    {dpe.classeEnergie}
                  </span>
                </td>
                <td className={styles.dateCell}>
                  {new Date(dpe.dateDiagnostic).toLocaleDateString('fr-FR')}
                </td>
                <td className={styles.dateCell}>
                  {new Date(dpe.dateExpiration).toLocaleDateString('fr-FR')}
                </td>
                <td>
                  <span className={clsx(styles.badge, styles[`badge${cfg.variant.charAt(0).toUpperCase() + cfg.variant.slice(1)}`])}>
                    {cfg.icon}{cfg.label}
                  </span>
                </td>
                <td>
                  <button
                    className={styles.viewBtn}
                    onClick={() => router.push(`/conformite/dpe/${dpe.coproprieteId}`)}
                  >
                    <Eye size={13} /> Voir la fiche
                  </button>
                </td>
              </tr>
            );
          })}
        </tbody>
      </table>
    </div>
  );
}
```

Créer `src/components/features/conformite/dpe/DPEGestionnaireTable.module.css` :

```css
.wrap { overflow-x: auto; }

.table { width: 100%; border-collapse: collapse; }

.table th {
  font-size: 10px; font-weight: 600; color: #64748b;
  text-transform: uppercase; letter-spacing: 0.5px;
  padding: 10px 16px; text-align: left;
  background: rgba(148,163,184,0.04);
  border-bottom: 1px solid rgba(148,163,184,0.04);
}

.row { border-bottom: 1px solid rgba(148,163,184,0.04); transition: background 0.1s; }
.row:hover { background: rgba(148,163,184,0.03); }
.row td { padding: 12px 16px; font-size: 13px; color: #e2e8f0; vertical-align: middle; }

.nomCell { font-weight: 600; }
.numCell { color: #94a3b8; }
.dateCell { font-family: 'SF Mono', monospace; font-variant-numeric: tabular-nums; font-size: 12px; color: #94a3b8; }

.classeBadge {
  display: inline-block; padding: 3px 10px; border-radius: 6px;
  font-size: 13px; font-weight: 700;
}

.badge {
  display: inline-flex; align-items: center; gap: 4px;
  padding: 3px 10px; border-radius: 8px; font-size: 11px; font-weight: 500;
}
.badgeSuccess { background: rgba(34,197,94,0.1); color: #4ade80; }
.badgeDanger  { background: rgba(239,68,68,0.1); color: #f87171; }
.badgeWarning { background: rgba(245,158,11,0.1); color: #fbbf24; }
.badgeNeutral { background: rgba(148,163,184,0.1); color: #94a3b8; }

.viewBtn {
  display: inline-flex; align-items: center; gap: 5px;
  background: rgba(59,130,246,0.1); color: #60a5fa;
  border: none; border-radius: 6px; padding: 5px 12px;
  font-size: 12px; font-weight: 500; cursor: pointer; transition: background 0.15s;
}
.viewBtn:hover { background: rgba(59,130,246,0.18); }
```

- [ ] **Étape 7.4 — DPEFicheDetail**

Créer `src/components/features/conformite/dpe/DPEFicheDetail.tsx` :

```typescript
'use client';

import { Download, RotateCcw, AlertTriangle, Info } from 'lucide-react';
import type { IDPE } from '@/types';
import { DPEEnergyScale } from './DPEEnergyScale';
import { DPEHistorique } from './DPEHistorique';
import styles from './DPEFicheDetail.module.css';

interface DPEFicheDetailProps {
  dpe: IDPE;
}

const STATUT_ALERT: Record<string, { icon: React.ReactNode; msg: string; variant: string }> = {
  VALIDE:         { icon: <Info size={15} />,          msg: 'DPE en cours de validité.',                         variant: 'info'    },
  EXPIRE_BIENTOT: { icon: <AlertTriangle size={15} />, msg: 'Le DPE expire dans moins de 6 mois. Planifiez le renouvellement.', variant: 'warning' },
  EXPIRE:         { icon: <AlertTriangle size={15} />, msg: 'DPE expiré — un nouveau diagnostic est obligatoire.',              variant: 'danger'  },
  MANQUANT:       { icon: <AlertTriangle size={15} />, msg: 'Aucun DPE enregistré pour cette copropriété.',      variant: 'neutral' },
};

export function DPEFicheDetail({ dpe }: DPEFicheDetailProps) {
  const alert = STATUT_ALERT[dpe.statut];

  return (
    <div className={styles.layout}>
      {/* Colonne gauche */}
      <div className={styles.left}>
        <div className={styles.card}>
          <div className={styles.cardTitle}>Classe énergétique</div>
          <DPEEnergyScale classeActive={dpe.classeEnergie} />
        </div>

        <div className={styles.card}>
          <div className={styles.cardTitle}>Informations DPE</div>
          <dl className={styles.fields}>
            <dt>Date diagnostic</dt><dd>{new Date(dpe.dateDiagnostic).toLocaleDateString('fr-FR')}</dd>
            <dt>Date expiration</dt><dd>{new Date(dpe.dateExpiration).toLocaleDateString('fr-FR')}</dd>
            <dt>Diagnostiqueur</dt><dd>{dpe.diagnostiqueur}</dd>
            <dt>N° ADEME</dt><dd className={styles.mono}>{dpe.numeroADEME}</dd>
            <dt>Conso. énergie</dt><dd className={styles.mono}>{dpe.consoEnergie} kWh/m²/an</dd>
            <dt>Émissions GES</dt><dd className={styles.mono}>{dpe.emissionsGES} kgCO₂/m²/an</dd>
          </dl>
          <div className={styles.actions}>
            <button className={styles.btnPrimary}><Download size={14} /> Télécharger PDF</button>
            <button className={styles.btnGhost}><RotateCcw size={14} /> Planifier renouvellement</button>
          </div>
        </div>
      </div>

      {/* Colonne droite */}
      <div className={styles.right}>
        <div className={styles[`alert${alert.variant.charAt(0).toUpperCase() + alert.variant.slice(1)}`]}>
          {alert.icon} {alert.msg}
        </div>

        <div className={styles.card}>
          <div className={styles.cardTitle}>Travaux recommandés suite au DPE</div>
          {dpe.travauxRecommandes.length === 0 ? (
            <p className={styles.empty}>Aucun travail recommandé enregistré.</p>
          ) : (
            dpe.travauxRecommandes.map(t => (
              <div key={t.id} className={styles.travailRow}>
                <span>{t.titre}</span>
                <span className={styles.mono}>{new Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR', maximumFractionDigits: 0 }).format(t.montantEstime)}</span>
              </div>
            ))
          )}
        </div>

        <div className={styles.card}>
          <div className={styles.cardTitle}>Historique des DPE</div>
          <DPEHistorique historique={dpe.historique} />
        </div>
      </div>
    </div>
  );
}
```

Créer `src/components/features/conformite/dpe/DPEFicheDetail.module.css` :

```css
.layout {
  display: grid;
  grid-template-columns: 380px 1fr;
  gap: 20px;
  align-items: start;
}

@media (max-width: 900px) {
  .layout { grid-template-columns: 1fr; }
}

.left, .right { display: flex; flex-direction: column; gap: 16px; }

.card {
  background: #1a1d2e;
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 12px;
  padding: 20px;
}

.cardTitle {
  font-size: 13px;
  font-weight: 700;
  color: #e2e8f0;
  margin-bottom: 16px;
  text-transform: uppercase;
  letter-spacing: 0.4px;
}

.fields { display: grid; grid-template-columns: 1fr 1fr; gap: 6px 16px; margin: 0; }
.fields dt { font-size: 11px; color: #64748b; font-weight: 500; }
.fields dd { font-size: 13px; color: #e2e8f0; margin: 0; }
.mono { font-family: 'SF Mono', monospace; font-variant-numeric: tabular-nums; }

.actions { display: flex; gap: 10px; margin-top: 20px; flex-wrap: wrap; }

.btnPrimary {
  display: inline-flex; align-items: center; gap: 6px;
  background: #3b82f6; color: #fff; border: none;
  border-radius: 8px; padding: 8px 16px; font-size: 13px; font-weight: 500; cursor: pointer;
  transition: background 0.15s;
}
.btnPrimary:hover { background: #2563eb; }

.btnGhost {
  display: inline-flex; align-items: center; gap: 6px;
  background: rgba(148,163,184,0.06); color: #94a3b8;
  border: 1px solid rgba(148,163,184,0.08);
  border-radius: 8px; padding: 8px 16px; font-size: 13px; cursor: pointer;
  transition: all 0.15s;
}
.btnGhost:hover { background: rgba(148,163,184,0.1); color: #e2e8f0; }

.alertInfo    { display: flex; align-items: center; gap: 10px; padding: 12px 16px; border-radius: 8px; font-size: 13px; background: rgba(59,130,246,0.1); border: 1px solid rgba(59,130,246,0.3); color: #60a5fa; }
.alertWarning { display: flex; align-items: center; gap: 10px; padding: 12px 16px; border-radius: 8px; font-size: 13px; background: rgba(245,158,11,0.1); border: 1px solid rgba(245,158,11,0.3); color: #fbbf24; }
.alertDanger  { display: flex; align-items: center; gap: 10px; padding: 12px 16px; border-radius: 8px; font-size: 13px; background: rgba(239,68,68,0.1); border: 1px solid rgba(239,68,68,0.3); color: #f87171; }
.alertNeutral { display: flex; align-items: center; gap: 10px; padding: 12px 16px; border-radius: 8px; font-size: 13px; background: rgba(148,163,184,0.1); border: 1px solid rgba(148,163,184,0.3); color: #94a3b8; }

.travailRow {
  display: flex; justify-content: space-between; align-items: center;
  padding: 8px 0; border-bottom: 1px solid rgba(148,163,184,0.04);
  font-size: 13px; color: #e2e8f0;
}
.travailRow:last-child { border-bottom: none; }

.empty { font-size: 13px; color: #64748b; }
```

- [ ] **Étape 7.5 — Commit composants DPE**

```bash
git add src/components/features/conformite/dpe/
git commit -m "feat(conformite/dpe): composants DPEGestionnaireTable, DPEEnergyScale, DPEHistorique, DPEFicheDetail"
```

---

## Tâche 8 — Module DPE — pages

**Fichiers :**
- Créer : `src/app/(dashboard)/conformite/dpe/page.tsx`
- Créer : `src/app/(dashboard)/conformite/dpe/[coproprieteId]/page.tsx`

- [ ] **Étape 8.1 — Page gestionnaire DPE**

Créer `src/app/(dashboard)/conformite/dpe/page.tsx` :

```typescript
'use client';

import { FinanceTopBar } from '@/components/layout/FinanceTopBar';
import { DPEGestionnaireTable } from '@/components/features/conformite/dpe/DPEGestionnaireTable';
import { useDPE } from '@/hooks/useDPE';

export default function DPEGestionnairePage() {
  const { coproprietes } = useDPE();

  return (
    <div className="container">
      <FinanceTopBar
        title="DPE Collectif"
        subtitle="Suivi des Diagnostics de Performance Énergétique — obligation légale depuis le 01/01/2026"
      />
      <DPEGestionnaireTable dpeList={coproprietes} />
    </div>
  );
}
```

- [ ] **Étape 8.2 — Page détail DPE par copropriété**

Créer `src/app/(dashboard)/conformite/dpe/[coproprieteId]/page.tsx` :

```typescript
'use client';

import { useParams, useRouter } from 'next/navigation';
import { ArrowLeft } from 'lucide-react';
import { FinanceTopBar } from '@/components/layout/FinanceTopBar';
import { DPEFicheDetail } from '@/components/features/conformite/dpe/DPEFicheDetail';
import { useDPE } from '@/hooks/useDPE';
import styles from './dpe-detail.module.css';

export default function DPEDetailPage() {
  const { coproprieteId } = useParams<{ coproprieteId: string }>();
  const router = useRouter();
  const { selectedDPE } = useDPE({ coproprieteId });

  if (!selectedDPE) {
    return (
      <div className="container">
        <p className={styles.notFound}>DPE introuvable pour cette copropriété.</p>
      </div>
    );
  }

  return (
    <div className="container">
      <FinanceTopBar
        title={`DPE — ${selectedDPE.coproprieteNom}`}
        subtitle={`${selectedDPE.nbLots} lots · Classe ${selectedDPE.classeEnergie} · ${selectedDPE.consoEnergie} kWh/m²/an`}
        actions={
          <button className={styles.backBtn} onClick={() => router.push('/conformite/dpe')}>
            <ArrowLeft size={14} /> Retour à la liste
          </button>
        }
      />
      <DPEFicheDetail dpe={selectedDPE} />
    </div>
  );
}
```

Créer `src/app/(dashboard)/conformite/dpe/[coproprieteId]/dpe-detail.module.css` :

```css
.notFound { padding: 60px 24px; text-align: center; color: var(--text-tertiary); font-size: 14px; }

.backBtn {
  display: flex; align-items: center; gap: 6px;
  background: rgba(148,163,184,0.06); border: 1px solid rgba(148,163,184,0.08);
  color: #94a3b8; border-radius: 8px; padding: 7px 14px; font-size: 13px;
  cursor: pointer; transition: all 0.15s;
}
.backBtn:hover { background: rgba(148,163,184,0.1); color: #e2e8f0; }
```

- [ ] **Étape 8.3 — Commit pages DPE**

```bash
git add src/app/(dashboard)/conformite/dpe/
git commit -m "feat(conformite/dpe): pages gestionnaire et fiche par copropriété"
```

- [ ] **Étape 8.4 — Vérification navigateur DPE**

Ouvrir http://localhost:3000/conformite/dpe.  
Vérifier : table des 4 copropriétés avec badges colorés.  
Cliquer "Voir la fiche" → fiche complète avec échelle A-G, champs, historique.  
Vérifier que le bouton retour ramène à la liste.

---

## Tâche 9 — Hook useFacturX & module Factur-X

**Fichiers :**
- Créer : `src/hooks/useFacturX.ts`
- Créer : `src/components/features/conformite/facturx/FacturXTable.tsx` + `.module.css`
- Créer : `src/app/(dashboard)/conformite/facturx/page.tsx`

- [ ] **Étape 9.1 — Créer le hook useFacturX**

Créer `src/hooks/useFacturX.ts` :

```typescript
'use client';

import { useState, useMemo, useCallback } from 'react';
import type { IFactureFacturX, StatutFacturX } from '@/types';
import { MOCK_FACTURES_FACTURX } from '@/components/features/conformite/facturx/mock-data';

export type FacturXFilter = 'TOUTES' | StatutFacturX;

export function useFacturX() {
  const [factures, setFactures] = useState<IFactureFacturX[]>(MOCK_FACTURES_FACTURX);
  const [filter, setFilter] = useState<FacturXFilter>('TOUTES');
  const [generating, setGenerating] = useState<string | null>(null);

  const filteredFactures = useMemo(() => {
    if (filter === 'TOUTES') return factures;
    return factures.filter(f => f.statutFacturX === filter);
  }, [factures, filter]);

  const genererFacturX = useCallback(async (factureId: string) => {
    setGenerating(factureId);
    await new Promise(resolve => setTimeout(resolve, 1500));
    setFactures(prev => prev.map(f =>
      f.id === factureId
        ? { ...f, statutFacturX: 'GENERE', dateGeneration: new Date().toISOString().split('T')[0] }
        : f
    ));
    setGenerating(null);
  }, []);

  return {
    factures: filteredFactures,
    filter,
    setFilter,
    genererFacturX,
    generating,
    isLoading: false,
  };
}
```

- [ ] **Étape 9.2 — FacturXTable**

Créer `src/components/features/conformite/facturx/FacturXTable.tsx` :

```typescript
'use client';

import { FileCode, Loader, Download } from 'lucide-react';
import clsx from 'clsx';
import type { IFactureFacturX, StatutFacturX } from '@/types';
import styles from './FacturXTable.module.css';

const STATUT_FACTURX_CONFIG: Record<StatutFacturX, { label: string; variant: string }> = {
  GENERE:         { label: 'Factur-X ✓', variant: 'success' },
  EN_ATTENTE:     { label: 'À générer',  variant: 'warning'  },
  NON_APPLICABLE: { label: 'N/A',        variant: 'neutral'  },
};

const STATUT_PAIEMENT_CONFIG = {
  PAYEE:     { label: 'Payée',     variant: 'success' },
  EN_ATTENTE:{ label: 'En attente',variant: 'neutral' },
  EN_RETARD: { label: 'En retard', variant: 'danger'  },
};

interface FacturXTableProps {
  factures: IFactureFacturX[];
  generating: string | null;
  onGenerer: (id: string) => void;
}

function formatEur(n: number): string {
  return new Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR', maximumFractionDigits: 2 }).format(n);
}

export function FacturXTable({ factures, generating, onGenerer }: FacturXTableProps) {
  return (
    <div className={styles.wrap}>
      <table className={styles.table}>
        <thead>
          <tr>
            <th>Facture N°</th>
            <th>Copropriété</th>
            <th>Fournisseur</th>
            <th>Montant TTC</th>
            <th>Date</th>
            <th>Paiement</th>
            <th>Factur-X</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {factures.map(f => {
            const fxCfg = STATUT_FACTURX_CONFIG[f.statutFacturX];
            const payeCfg = STATUT_PAIEMENT_CONFIG[f.statutPaiement];
            const isGenerating = generating === f.id;

            return (
              <tr key={f.id} className={styles.row}>
                <td className={styles.numCell}>{f.numero}</td>
                <td>{f.copropriete}</td>
                <td className={styles.fournisseurCell}>{f.fournisseur}</td>
                <td className={styles.montantCell}>{formatEur(f.montantTTC)}</td>
                <td className={styles.dateCell}>
                  {new Date(f.date).toLocaleDateString('fr-FR')}
                </td>
                <td>
                  <span className={clsx(styles.badge, styles[`badge${payeCfg.variant.charAt(0).toUpperCase() + payeCfg.variant.slice(1)}`])}>
                    {payeCfg.label}
                  </span>
                </td>
                <td>
                  <span className={clsx(styles.badge, styles[`badge${fxCfg.variant.charAt(0).toUpperCase() + fxCfg.variant.slice(1)}`])}>
                    {f.statutFacturX === 'GENERE' && <FileCode size={11} />}
                    {fxCfg.label}
                  </span>
                </td>
                <td>
                  {f.statutFacturX === 'EN_ATTENTE' && (
                    <button
                      className={styles.genBtn}
                      onClick={() => onGenerer(f.id)}
                      disabled={isGenerating}
                    >
                      {isGenerating ? <Loader size={12} className={styles.spin} /> : <FileCode size={12} />}
                      {isGenerating ? 'Génération…' : 'Générer'}
                    </button>
                  )}
                  {f.statutFacturX === 'GENERE' && (
                    <button className={styles.dlBtn}>
                      <Download size={12} /> Télécharger
                    </button>
                  )}
                </td>
              </tr>
            );
          })}
        </tbody>
      </table>
    </div>
  );
}
```

Créer `src/components/features/conformite/facturx/FacturXTable.module.css` :

```css
.wrap { overflow-x: auto; }

.table { width: 100%; border-collapse: collapse; }

.table th {
  font-size: 10px; font-weight: 600; color: #64748b;
  text-transform: uppercase; letter-spacing: 0.5px;
  padding: 10px 16px; text-align: left;
  background: rgba(148,163,184,0.04);
  border-bottom: 1px solid rgba(148,163,184,0.04);
}

.row { border-bottom: 1px solid rgba(148,163,184,0.04); transition: background 0.1s; }
.row:hover { background: rgba(148,163,184,0.03); }
.row td { padding: 12px 16px; font-size: 13px; color: #e2e8f0; vertical-align: middle; }

.numCell { font-family: 'SF Mono', monospace; font-size: 12px; color: #94a3b8; }
.fournisseurCell { color: #94a3b8; }
.montantCell { font-family: 'SF Mono', monospace; font-variant-numeric: tabular-nums; font-weight: 600; }
.dateCell { font-family: 'SF Mono', monospace; font-size: 12px; color: #94a3b8; }

.badge {
  display: inline-flex; align-items: center; gap: 4px;
  padding: 3px 10px; border-radius: 8px; font-size: 11px; font-weight: 500;
}
.badgeSuccess { background: rgba(34,197,94,0.1); color: #4ade80; }
.badgeDanger  { background: rgba(239,68,68,0.1); color: #f87171; }
.badgeWarning { background: rgba(245,158,11,0.1); color: #fbbf24; }
.badgeNeutral { background: rgba(148,163,184,0.1); color: #94a3b8; }

.genBtn {
  display: inline-flex; align-items: center; gap: 5px;
  background: rgba(59,130,246,0.1); color: #60a5fa;
  border: none; border-radius: 6px; padding: 5px 12px;
  font-size: 12px; font-weight: 500; cursor: pointer; transition: background 0.15s;
}
.genBtn:hover:not(:disabled) { background: rgba(59,130,246,0.18); }
.genBtn:disabled { opacity: 0.6; cursor: not-allowed; }

.dlBtn {
  display: inline-flex; align-items: center; gap: 5px;
  background: rgba(34,197,94,0.08); color: #4ade80;
  border: none; border-radius: 6px; padding: 5px 12px;
  font-size: 12px; cursor: pointer; transition: background 0.15s;
}
.dlBtn:hover { background: rgba(34,197,94,0.15); }

@keyframes spin { to { transform: rotate(360deg); } }
.spin { animation: spin 0.8s linear infinite; }
```

- [ ] **Étape 9.3 — Page Factur-X**

Créer `src/app/(dashboard)/conformite/facturx/page.tsx` :

```typescript
'use client';

import { FinanceTopBar } from '@/components/layout/FinanceTopBar';
import { FacturXTable } from '@/components/features/conformite/facturx/FacturXTable';
import { useFacturX, type FacturXFilter } from '@/hooks/useFacturX';
import styles from './facturx.module.css';

const FILTERS: { value: FacturXFilter; label: string }[] = [
  { value: 'TOUTES', label: 'Toutes' },
  { value: 'EN_ATTENTE', label: 'À générer' },
  { value: 'GENERE', label: 'Générées' },
  { value: 'NON_APPLICABLE', label: 'N/A' },
];

export default function FacturXPage() {
  const { factures, filter, setFilter, genererFacturX, generating } = useFacturX();

  return (
    <div className="container">
      <FinanceTopBar
        title="Factur-X"
        subtitle="Génération des factures hybrides PDF+XML — obligatoire dès septembre 2026 (Profil EN 16931)"
        actions={
          <div className={styles.filters}>
            {FILTERS.map(f => (
              <button
                key={f.value}
                className={filter === f.value ? styles.filterActive : styles.filter}
                onClick={() => setFilter(f.value)}
              >
                {f.label}
              </button>
            ))}
          </div>
        }
      />
      <FacturXTable factures={factures} generating={generating} onGenerer={genererFacturX} />
    </div>
  );
}
```

Créer `src/app/(dashboard)/conformite/facturx/facturx.module.css` :

```css
.filters {
  display: flex; align-items: center; gap: 4px;
  background: #131620; border: 1px solid rgba(148,163,184,0.08); border-radius: 8px; padding: 3px;
}
.filter {
  padding: 5px 14px; border-radius: 6px; border: none;
  background: transparent; color: #94a3b8; font-size: 13px; cursor: pointer; transition: all 0.15s;
}
.filter:hover { background: rgba(148,163,184,0.06); color: #e2e8f0; }
.filterActive {
  padding: 5px 14px; border-radius: 6px; border: none;
  background: #1a1d2e; color: #e2e8f0; font-size: 13px; cursor: pointer; font-weight: 500;
}
```

- [ ] **Étape 9.4 — Commit module Factur-X**

```bash
git add src/hooks/useFacturX.ts \
  src/components/features/conformite/facturx/ \
  src/app/(dashboard)/conformite/facturx/
git commit -m "feat(conformite/facturx): table factures avec génération XML mock"
```

- [ ] **Étape 9.5 — Vérification navigateur Factur-X**

Ouvrir http://localhost:3000/conformite/facturx.  
Vérifier : table avec 5 factures, badges colorés par statut.  
Cliquer "Générer" sur une facture EN_ATTENTE → spinner 1.5s → badge passe en vert "Factur-X ✓".  
Vérifier que le filtre "À générer" affiche uniquement les factures EN_ATTENTE.

---

## Auto-review du plan

**Couverture spec :**
- ✅ Types PPT, DPE, FacturX définis (Tâche 1)
- ✅ Navigation Conformité 2026 (Tâche 2)
- ✅ Redirect maintenance/ppt (Tâche 2)
- ✅ Hook usePPT avec kanban + filtre par année (Tâche 3)
- ✅ PPTGestionnaireGrid — grille copros niveau 1 (Tâche 4)
- ✅ PPTYearSelector — pills 2026-2035 (Tâche 4)
- ✅ PPTKanban — 5 colonnes (Tâche 4)
- ✅ PPTCardDetail — modal timeline (Tâche 4)
- ✅ Pages PPT gestionnaire + détail (Tâche 5)
- ✅ Hook useDPE (Tâche 6)
- ✅ DPEGestionnaireTable (Tâche 7)
- ✅ DPEEnergyScale A-G (Tâche 7)
- ✅ DPEHistorique (Tâche 7)
- ✅ DPEFicheDetail 2 colonnes + alert (Tâche 7)
- ✅ Pages DPE gestionnaire + fiche (Tâche 8)
- ✅ useFacturX + FacturXTable + génération mock (Tâche 9)

**Types cohérents :** `ITravauxPPT.etapes: IEtapeTravaux[]` défini en Tâche 1, utilisé en Tâche 4 et 5. `IDPE.coproprieteNom` défini en Tâche 1, utilisé en DPEGestionnaireTable et DPEDetailPage. `IFactureFacturX.statutPaiement` défini en Tâche 1, utilisé en FacturXTable.

**Pas de placeholders.** Tout le code est complet.
