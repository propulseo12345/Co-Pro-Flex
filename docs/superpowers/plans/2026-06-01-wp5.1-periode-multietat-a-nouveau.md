# WP5.1 — Période comptable multi-état + report à-nouveau — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Clôturer un exercice N et ouvrir N+1 avec une écriture de reprise des soldes (à-nouveau) idempotente et provisoire, figée automatiquement à l'approbation des comptes en AG.

**Architecture:** Une RPC `open_next_period` (re)génère une transaction `source_type='opening_balance'` dans N+1 : report à l'identique des comptes de bilan (classes 1/4/5 hors comptes de report) et virement du résultat (classes 6/7) en 120. L'immutabilité du grand livre reçoit une exemption ciblée (cette reprise reste mutable tant que N n'est pas `approved`). L'approbation passe par l'orchestrateur AG existant (`activate_ag_decisions` → `approve_period` → stub `regularize_period`).

**Tech Stack:** Supabase/Postgres (project `iyfesbjnkpynmwlsmxnp`), PL/pgSQL, migrations SQL idempotentes (`CREATE OR REPLACE`, `DROP ... IF EXISTS`), tests = assertions SQL via Supabase MCP `execute_sql` sur la copro « Le Clos Saint-Michel ».

---

## Décisions validées (forks métier — 2026-06-01)

1. **Précondition d'approbation** : `approve_period` n'accepte que `status='closed'`. Le gestionnaire **clôture N (`close_period`) AVANT l'AG** ; le hook AG appelle seulement `approve_period`.
2. **Gel au rejet** : une période `rejected` **reste régénérable** → prédicat d'exemption = `ap.status <> 'approved'`.
3. **Comptes de report** : harmoniser les libellés du 120 **ET** créer le 110 manquant sur les copros qui n'en ont pas (libellés décret 2005-240).
4. **Unicité de la reprise** : **index UNIQUE partiel en base** `(copro_id, source_id) WHERE source_type='opening_balance'`.

## Constantes vérifiées en base (boucle d'or — read-only)

- Copro Le Clos : `22222222-aaaa-bbbb-cccc-222222222222`.
- Exercice 2026 (N) : `bda8aa71-bba8-4613-9300-214e3d356fd0`, `2026-01-01..2026-12-31`, `status='open'`. Exercice 2025 : `b62c5114-0e6c-42e0-8807-1165ffd69cfb`, `closed`. **Aucune** période 2027 n'existe.
- Comptes Le Clos : 450-1 `37402bed-d9d4-43a0-8742-3ed16be37507` ; 401 `059b0d03-d838-479d-ab9d-3e84f48cdefe` ; 512 `c97e645a-4ef9-4151-b140-f2c47c8599d4` ; 120 `59a574c6-9450-4400-b83b-965b33ea0bab` ; 110 `0064bb95-8924-47c9-9d79-754dd1cb42fe` (les deux comptes de report préexistent → Task 1 neutre pour Le Clos).
- Soldes de clôture 2026 (balance équilibrée Σ=26 799,12 €) : 450-1 par lot → 001/`46d03b3b-855e-45fb-abd9-066636ee55fe`=1350,40 · 101/`6c96e73f-7882-4121-9181-7052cc28cf74`=1503,94 · 102/`a4c05cbb-efa4-496a-8a79-5e9210aa7e7e`=1748,07 · 201/`60293b00-0b46-4e88-9bcb-116b786d7124`=1535,46 · 202/`6ac900ec-9199-4c2d-9cc4-32500f1bbbac`=1795,29 · 301/`a1419ea3-306e-438f-96f3-9a35ed6c5e5e`=748,04 (Σ=8681,20 D) ; 401=480,00 C ; 512=3818,96 D ; classe 6 (615=480,00 + 616=2500,00)=2980,00 ; classe 7 (701)=15000,16. **Résultat = +12 020,16 € (excédent) → CRÉDIT 120**. Reprise 2027 : D 8681,20 + 3818,96 = C 480,00 + 12 020,16 = **12 500,16** (équilibrée par construction).

## Notes transverses (ordre & dépendances)

Ordre d'application **strict** (timestamps croissants) :

1. `20260601090000_wp5_1_normalize_report_accounts.sql` — comptes 110/120 (prérequis pour que `open_next_period` trouve toujours le 120). **Neutre pour Le Clos** (120 et 110 préexistent : `59a574c6…` / `0064bb95…`) mais **prérequis réel** pour une copro sans compte de report.
2. `20260601091000_wp5_1_source_type_opening_balance.sql` — ajoute `opening_balance` au CHECK (utilisé par la reprise en Task 6).
3. `20260601092000_wp5_1_opening_balance_immutability_exemption.sql` — exemption d'immutabilité (doit précéder toute régénération).
4. `20260601093000_wp5_1_opening_balance_unique_index.sql` — index d'unicité de la reprise.
5. `20260601094000_wp5_1_period_functions.sql` — fiabilise `close_period` + crée `approve_period` + stub `regularize_period` (avant le hook AG en Task 7).
6. `20260601095000_wp5_1_open_next_period.sql` — RPC à-nouveau (dépend de 1, 2, 3, 4).
7. `20260601096000_wp5_1_ag_approval_hook.sql` — branche `prepare_ag_decisions` + `activate_ag_decisions` (dépend de 5).

**Garde-fou général** : avant CHAQUE `apply_migration` ou création de données de test, **demander l'accord explicite de l'utilisateur** (règle projet CLAUDE.md). Les requêtes d'assertion (SELECT) sont en lecture seule et ne nécessitent pas d'accord.

**Source de vérité pour les fonctions réécrites** (close_period, prepare/activate_ag_decisions) : `pg_get_functiondef` **live**, PAS les fichiers migration (les versions DB diffèrent des préfixes de fichiers). On ne modifie que les branches/lignes désignées.

---

## Task 1 : Normaliser les comptes de report 110/120

**Files:**
- Create: `supabase/migrations/20260601090000_wp5_1_normalize_report_accounts.sql`

But : uniformiser le libellé du compte 120 (4 copros, 2 noms divergents aujourd'hui) et créer le compte 110 sur les copros qui n'en ont pas. Aucun doublon intra-copro n'existe (contrainte `UNIQUE(copro_id, code)`), donc **aucun re-aiguillage d'écriture** n'est requis. La table `accounts` n'a **aucun trigger d'immutabilité** → `UPDATE`/`INSERT` libres.

- [ ] **Step 1 : Écrire la requête d'assertion (échoue avant)**

Run (lecture) :
```sql
-- Attendu APRÈS migration : toutes les copros ayant un 120 ont aussi un 110,
-- et le libellé du 120 est unique.
SELECT
  (SELECT count(DISTINCT name) FROM accounts WHERE code = '120') AS noms_120,
  (SELECT count(*) FROM accounts WHERE code = '120') AS nb_120,
  (SELECT count(*) FROM accounts WHERE code = '110') AS nb_110;
```
Attendu AVANT : `noms_120 = 2`, `nb_120 = 4`, `nb_110 = 2` (incohérent).

- [ ] **Step 2 : Lancer la requête pour constater l'état incohérent**

Run via `mcp__claude_ai_Supabase__execute_sql` (project_id `iyfesbjnkpynmwlsmxnp`). Constater `noms_120 = 2` et `nb_110 = 2`.

- [ ] **Step 3 : Écrire la migration (SQL complet)**

```sql
-- ============================================================================
-- WP5.1 — Normalisation des comptes de report (110/120)
-- ============================================================================
-- Aucun doublon intra-copro possible (UNIQUE(copro_id, code)). On (1) uniformise
-- le libellé du compte 120 selon le décret 2005-240, (2) crée le compte 110
-- manquant sur les copros qui ont un 120 mais pas de 110, afin que
-- open_next_period (WP5.1) trouve toujours ses comptes de report.
-- Idempotent : ré-exécutable sans effet de bord.
-- ============================================================================

-- 1) Libellé canonique du 120 (opérations courantes)
UPDATE public.accounts
SET name = 'Solde en attente sur opérations courantes', updated_at = now()
WHERE code = '120'
  AND name <> 'Solde en attente sur opérations courantes';

-- 2) Libellé canonique du 110 (travaux & opérations exceptionnelles)
UPDATE public.accounts
SET name = 'Solde en attente sur travaux et opérations exceptionnelles', updated_at = now()
WHERE code = '110'
  AND name <> 'Solde en attente sur travaux et opérations exceptionnelles';

-- 3) Créer le 110 manquant pour toute copro disposant d'un 120 mais pas d'un 110.
--    account_type = 'equity' (capitaux propres / report à nouveau), comme le 120.
INSERT INTO public.accounts (copro_id, code, name, account_type, is_system, is_active)
SELECT a120.copro_id, '110',
       'Solde en attente sur travaux et opérations exceptionnelles',
       'equity', false, true
FROM public.accounts a120
WHERE a120.code = '120'
  AND NOT EXISTS (
    SELECT 1 FROM public.accounts a110
    WHERE a110.copro_id = a120.copro_id AND a110.code = '110'
  );
```

- [ ] **Step 4 : Demander l'accord puis appliquer, puis relancer l'assertion (PASS)**

Demander l'accord utilisateur → `mcp__claude_ai_Supabase__apply_migration` (name `wp5_1_normalize_report_accounts`). Relancer la requête du Step 1.
Attendu APRÈS : `noms_120 = 1`, `nb_110 = nb_120`.

- [ ] **Step 5 : Commit**

```bash
git add supabase/migrations/20260601090000_wp5_1_normalize_report_accounts.sql
git commit -m "chore: normalise comptes de report 110/120 (WP5.1)"
```

---

## Task 2 : Autoriser le source_type `opening_balance`

**Files:**
- Create: `supabase/migrations/20260601091000_wp5_1_source_type_opening_balance.sql`

Le CHECK `ledger_transactions_source_type_check` autorise 12 valeurs (def live = celle de `20260531180000_wp4_validate_expense_to_ledger.sql`, qui inclut `budget_expense`). On **repart de la liste live à 12 valeurs** et on ajoute `opening_balance` (13ᵉ). `opening` (déjà utilisé par 3 écritures de seed) reste distinct et **non concerné** par l'exemption.

- [ ] **Step 1 : Vérifier la def live du CHECK (référence à recopier)**

Run (lecture) :
```sql
SELECT pg_get_constraintdef(oid)
FROM pg_constraint
WHERE conrelid = 'public.ledger_transactions'::regclass
  AND conname = 'ledger_transactions_source_type_check';
```
Vérifier que la liste contient bien `budget_expense` (12 valeurs) et **pas** `opening_balance`.

- [ ] **Step 2 : Écrire la requête d'assertion (échoue avant)**

```sql
SELECT pg_get_constraintdef(oid) LIKE '%opening_balance%' AS ok
FROM pg_constraint
WHERE conrelid = 'public.ledger_transactions'::regclass
  AND conname = 'ledger_transactions_source_type_check';
```
Attendu AVANT : `ok = false`.

- [ ] **Step 3 : Écrire la migration (SQL complet)**

```sql
-- ============================================================================
-- WP5.1 — Ajoute 'opening_balance' aux source_type autorisés du grand livre
-- ============================================================================
-- 'opening_balance' = écriture de reprise des soldes (à-nouveau) régénérable
-- produite par open_next_period. Distinct de 'opening' (soldes d'ouverture
-- initiaux / seed, immuables). On reprend la liste live À L'IDENTIQUE (12
-- valeurs, incluant budget_expense) + opening_balance.
-- ============================================================================
ALTER TABLE public.ledger_transactions
  DROP CONSTRAINT IF EXISTS ledger_transactions_source_type_check;

ALTER TABLE public.ledger_transactions
  ADD CONSTRAINT ledger_transactions_source_type_check
  CHECK (
    source_type IS NULL OR source_type = ANY (ARRAY[
      'budget', 'budget_expense', 'call_for_funds', 'payment',
      'supplier_invoice', 'supplier_payment', 'bank_movement',
      'transfer', 'od', 'opening', 'closing', 'manual', 'opening_balance'
    ])
  );
```

- [ ] **Step 4 : Demander l'accord, appliquer, relancer l'assertion (PASS)**

Accord utilisateur → `apply_migration` (name `wp5_1_source_type_opening_balance`). Relancer Step 2 → `ok = true`.

- [ ] **Step 5 : Commit**

```bash
git add supabase/migrations/20260601091000_wp5_1_source_type_opening_balance.sql
git commit -m "feat: autorise source_type opening_balance dans le grand livre (WP5.1)"
```

---

## Task 3 : Exemption d'immutabilité ciblée pour la reprise

**Files:**
- Create: `supabase/migrations/20260601092000_wp5_1_opening_balance_immutability_exemption.sql`

On réécrit 4 fonctions de trigger pour **lever l'immutabilité uniquement** quand le tx est une reprise (`source_type='opening_balance'`) ET que la période source (`source_id`) n'est pas `approved`. Dès que N passe `approved`, l'exemption disparaît (gel passif).

**Prédicat d'exemption — 3 sources de données distinctes selon le trigger :**
- `trg_ledger_tx_immutable` (UPDATE tx) et `trg_ledger_tx_no_delete_posted` (DELETE tx) : lisent **`OLD.source_type` / `OLD.source_id`** (colonnes du tx).
- `trg_ledger_entry_immutable` (UPDATE/DELETE entry) : lit le tx parent via **`OLD.tx_id`**.
- `trg_ledger_entry_no_insert_posted` (INSERT entry) : lit le tx parent via **`NEW.tx_id`**.

Forme stricte : `EXISTS(SELECT 1 FROM accounting_periods ap WHERE ap.id = <source_id> AND ap.status <> 'approved')` (refuse l'exemption si la période source est introuvable → pas d'exemption « orpheline »). Le **retour `posted → draft`** de `trg_ledger_tx_immutable` **reste interdit** même pour la reprise (la régénération fait DELETE-then-recreate, jamais de dé-post).

- [ ] **Step 1 : Capturer les defs live (référence)**

Run (lecture) :
```sql
SELECT p.proname, pg_get_functiondef(p.oid)
FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'public'
  AND p.proname IN ('trg_ledger_tx_immutable','trg_ledger_tx_no_delete_posted',
                    'trg_ledger_entry_immutable','trg_ledger_entry_no_insert_posted');
```
Confirmer que les corps testent uniquement `status='posted'` (aucune lecture de `source_type`).

- [ ] **Step 2 : Écrire l'assertion (échoue avant)**

```sql
-- Test fonctionnel : une reprise postée sur une période non-approuvée doit être
-- supprimable. On le vérifiera de bout en bout via open_next_period (Task 8 / Test 5).
-- Assertion statique ici : les corps mentionnent 'opening_balance' après migration.
SELECT bool_and(pg_get_functiondef(p.oid) LIKE '%opening_balance%') AS ok
FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'public'
  AND p.proname IN ('trg_ledger_tx_immutable','trg_ledger_tx_no_delete_posted',
                    'trg_ledger_entry_immutable','trg_ledger_entry_no_insert_posted');
```
Attendu AVANT : `ok = false`.

- [ ] **Step 3 : Écrire la migration (SQL complet)**

```sql
-- ============================================================================
-- WP5.1 — Exemption d'immutabilité ciblée pour la reprise 'opening_balance'
-- ============================================================================
-- Lève l'immutabilité UNIQUEMENT pour un tx source_type='opening_balance' dont
-- la période source (source_id) n'est PAS 'approved'. Gel automatique dès que N
-- passe 'approved'. Le retour posted->draft reste interdit (non requis par la
-- régénération DELETE-then-recreate). Idempotent (CREATE OR REPLACE).
-- ============================================================================

-- A) UPDATE sur transaction postée
CREATE OR REPLACE FUNCTION public.trg_ledger_tx_immutable()
RETURNS TRIGGER LANGUAGE plpgsql SET search_path TO 'public' AS $$
BEGIN
  IF OLD.status = 'posted'
     AND NOT (
       OLD.source_type = 'opening_balance'
       AND EXISTS (SELECT 1 FROM accounting_periods ap
                   WHERE ap.id = OLD.source_id AND ap.status <> 'approved')
     ) THEN
    RAISE EXCEPTION 'Cannot modify posted transaction (id=%)', OLD.id
      USING ERRCODE = 'restrict_violation';
  END IF;

  -- Retour posted -> draft : TOUJOURS interdit (même pour la reprise).
  IF NEW.status = 'draft' AND OLD.status = 'posted' THEN
    RAISE EXCEPTION 'Cannot revert posted transaction to draft (id=%)', OLD.id
      USING ERRCODE = 'restrict_violation';
  END IF;

  RETURN NEW;
END;
$$;

-- B) DELETE sur transaction postée
CREATE OR REPLACE FUNCTION public.trg_ledger_tx_no_delete_posted()
RETURNS TRIGGER LANGUAGE plpgsql SET search_path TO 'public' AS $$
BEGIN
  IF OLD.status = 'posted'
     AND NOT (
       OLD.source_type = 'opening_balance'
       AND EXISTS (SELECT 1 FROM accounting_periods ap
                   WHERE ap.id = OLD.source_id AND ap.status <> 'approved')
     ) THEN
    RAISE EXCEPTION 'Cannot delete posted transaction (id=%)', OLD.id
      USING ERRCODE = 'restrict_violation';
  END IF;
  RETURN OLD;
END;
$$;

-- C) UPDATE / DELETE sur une entry dont le tx parent est posté
CREATE OR REPLACE FUNCTION public.trg_ledger_entry_immutable()
RETURNS TRIGGER LANGUAGE plpgsql SET search_path TO 'public' AS $$
DECLARE
  v_tx_status      TEXT;
  v_tx_source_type TEXT;
  v_tx_source_id   UUID;
BEGIN
  SELECT status, source_type, source_id
    INTO v_tx_status, v_tx_source_type, v_tx_source_id
  FROM ledger_transactions
  WHERE id = OLD.tx_id;

  IF v_tx_status = 'posted'
     AND NOT (
       v_tx_source_type = 'opening_balance'
       AND EXISTS (SELECT 1 FROM accounting_periods ap
                   WHERE ap.id = v_tx_source_id AND ap.status <> 'approved')
     ) THEN
    IF TG_OP = 'UPDATE' THEN
      RAISE EXCEPTION 'Cannot modify entry on posted transaction (entry_id=%, tx_id=%)', OLD.id, OLD.tx_id
        USING ERRCODE = 'restrict_violation';
    ELSIF TG_OP = 'DELETE' THEN
      RAISE EXCEPTION 'Cannot delete entry from posted transaction (entry_id=%, tx_id=%)', OLD.id, OLD.tx_id
        USING ERRCODE = 'restrict_violation';
    END IF;
  END IF;

  IF TG_OP = 'DELETE' THEN
    RETURN OLD;
  END IF;
  RETURN NEW;
END;
$$;

-- D) INSERT d'une entry sur un tx posté (source = NEW.tx_id)
CREATE OR REPLACE FUNCTION public.trg_ledger_entry_no_insert_posted()
RETURNS TRIGGER LANGUAGE plpgsql SET search_path TO 'public' AS $$
DECLARE
  v_tx_status      TEXT;
  v_tx_source_type TEXT;
  v_tx_source_id   UUID;
BEGIN
  SELECT status, source_type, source_id
    INTO v_tx_status, v_tx_source_type, v_tx_source_id
  FROM ledger_transactions
  WHERE id = NEW.tx_id;

  IF v_tx_status = 'posted'
     AND NOT (
       v_tx_source_type = 'opening_balance'
       AND EXISTS (SELECT 1 FROM accounting_periods ap
                   WHERE ap.id = v_tx_source_id AND ap.status <> 'approved')
     ) THEN
    RAISE EXCEPTION 'Cannot add entry to posted transaction (tx_id=%)', NEW.tx_id
      USING ERRCODE = 'restrict_violation';
  END IF;

  RETURN NEW;
END;
$$;
```

- [ ] **Step 4 : Demander l'accord, appliquer, relancer l'assertion (PASS)**

Accord utilisateur → `apply_migration` (name `wp5_1_opening_balance_immutability_exemption`). Relancer Step 2 → `ok = true`. (Vérification dynamique complète en Task 8 / Test 5 et Test 6.)

- [ ] **Step 5 : Commit**

```bash
git add supabase/migrations/20260601092000_wp5_1_opening_balance_immutability_exemption.sql
git commit -m "feat: exemption d'immutabilite ciblee pour la reprise opening_balance (WP5.1)"
```

---

## Task 4 : Index d'unicité de la reprise

**Files:**
- Create: `supabase/migrations/20260601093000_wp5_1_opening_balance_unique_index.sql`

Garantit **au niveau base** au plus une reprise active par exercice source (spec §7), même si une reprise était créée hors `open_next_period`.

- [ ] **Step 1 : Assertion (échoue avant)**

```sql
SELECT EXISTS (
  SELECT 1 FROM pg_indexes
  WHERE schemaname='public' AND indexname='uq_ledger_tx_opening_balance'
) AS ok;
```
Attendu AVANT : `ok = false`.

- [ ] **Step 2 : Écrire la migration (SQL complet)**

```sql
-- ============================================================================
-- WP5.1 — Unicité de la reprise à-nouveau par exercice source
-- ============================================================================
-- Au plus UNE transaction opening_balance par (copro, période source).
-- Index partiel : ne contraint que les reprises, n'impacte pas les autres tx.
-- ============================================================================
CREATE UNIQUE INDEX IF NOT EXISTS uq_ledger_tx_opening_balance
  ON public.ledger_transactions (copro_id, source_id)
  WHERE source_type = 'opening_balance';
```

- [ ] **Step 3 : Demander l'accord, appliquer, relancer l'assertion (PASS)**

Accord utilisateur → `apply_migration` (name `wp5_1_opening_balance_unique_index`). Relancer Step 1 → `ok = true`.

- [ ] **Step 4 : Commit**

```bash
git add supabase/migrations/20260601093000_wp5_1_opening_balance_unique_index.sql
git commit -m "feat: index unique partiel sur la reprise opening_balance (WP5.1)"
```

---

## Task 5 : Fiabiliser close_period + créer approve_period + stub regularize_period

**Files:**
- Create: `supabase/migrations/20260601094000_wp5_1_period_functions.sql`

`close_period` reçoit le `SET search_path TO 'public'` manquant (cohérence avec `lock_period`). `approve_period(p_period_id)` passe une période **`closed` → `approved`** (décision 1). `regularize_period(p_copro_id, p_period_id)` est un **stub no-op** (point d'accroche WP5.3).

- [ ] **Step 1 : Assertion (échoue avant)**

```sql
SELECT
  (SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
    WHERE n.nspname='public' AND p.proname='approve_period') AS has_approve,
  (SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
    WHERE n.nspname='public' AND p.proname='regularize_period') AS has_regularize;
```
Attendu AVANT : `has_approve = 0`, `has_regularize = 0`.

- [ ] **Step 2 : Écrire la migration (SQL complet)**

```sql
-- ============================================================================
-- WP5.1 — Fonctions de période : close_period (hardening) + approve_period + stub
-- ============================================================================

-- 1) close_period : ajoute SET search_path (manquant), comportement inchangé.
CREATE OR REPLACE FUNCTION public.close_period(p_period_id uuid)
RETURNS boolean
LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public'
AS $$
BEGIN
  UPDATE accounting_periods
  SET status = 'closed', closed_at = now(), closed_by = auth.uid()
  WHERE id = p_period_id AND status IN ('open','locked');
  RETURN FOUND;
END;
$$;

-- 2) approve_period : N (closed) -> approved. Fige automatiquement la reprise N+1
--    (l'exemption d'immutabilité ne s'applique plus quand N = 'approved').
CREATE OR REPLACE FUNCTION public.approve_period(p_period_id uuid)
RETURNS jsonb
LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public'
AS $$
DECLARE v_updated int;
BEGIN
  UPDATE accounting_periods
  SET status = 'approved', approved_at = now(), approved_by = auth.uid()
  WHERE id = p_period_id AND status = 'closed';
  GET DIAGNOSTICS v_updated = ROW_COUNT;

  IF v_updated = 0 THEN
    RETURN jsonb_build_object(
      'success', false,
      'error', 'Période introuvable ou non clôturée (statut attendu : closed)',
      'period_id', p_period_id
    );
  END IF;

  RETURN jsonb_build_object('success', true, 'period_id', p_period_id, 'status', 'approved');
END;
$$;

-- 3) regularize_period : STUB no-op (point d'accroche WP5.3 — répartition
--    excédent/déficit 120/110 -> 450 par quote-part). Signature figée.
CREATE OR REPLACE FUNCTION public.regularize_period(p_copro_id uuid, p_period_id uuid)
RETURNS jsonb
LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public'
AS $$
BEGIN
  RETURN jsonb_build_object(
    'success', true,
    'skipped', 'WP5.3 regularization not implemented',
    'copro_id', p_copro_id,
    'period_id', p_period_id
  );
END;
$$;
```

- [ ] **Step 3 : Demander l'accord, appliquer, relancer l'assertion (PASS)**

Accord utilisateur → `apply_migration` (name `wp5_1_period_functions`). Relancer Step 1 → `has_approve = 1`, `has_regularize = 1`.

- [ ] **Step 4 : Commit**

```bash
git add supabase/migrations/20260601094000_wp5_1_period_functions.sql
git commit -m "feat: approve_period + stub regularize_period, hardening close_period (WP5.1)"
```

---

## Task 6 : RPC open_next_period (à-nouveau idempotent)

**Files:**
- Create: `supabase/migrations/20260601095000_wp5_1_open_next_period.sql`

Cœur de WP5.1. (Re)génère la reprise dans N+1. Points clés : exclut les comptes de report **110/120/119/129** du report de bilan (sinon double comptage du résultat) ; vire le net des classes 6/7 en **120** (110 laissé à 0 — la séparation courant/travaux des comptes de gestion est différée, voir « Hors-périmètre ») ; refuse proprement (`{success:false}`) si N est `approved` ; nettoie un éventuel brouillon résiduel si le post échoue.

- [ ] **Step 1 : Écrire la migration (SQL complet)**

```sql
-- ============================================================================
-- WP5.1 — open_next_period : ouverture de N+1 + reprise des soldes (à-nouveau)
-- ============================================================================
-- Idempotente (DELETE-then-recreate de la reprise opening_balance(source_id=N)).
-- Provisoire tant que N != approved ; figée dès que N = 'approved'.
-- Report : comptes de bilan classes 1/4/5 (HORS 110/120/119/129) à l'identique,
-- en conservant lot_id ; résultat (classes 6/7) viré en 120.
-- Équilibre garanti par construction (la balance de N est équilibrée).
-- ============================================================================
CREATE OR REPLACE FUNCTION public.open_next_period(
  p_copro_id          uuid,
  p_closing_period_id uuid,
  p_new_name          text DEFAULT NULL,
  p_new_start         date DEFAULT NULL,
  p_new_end           date DEFAULT NULL
) RETURNS jsonb
LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public'
AS $$
DECLARE
  v_n            accounting_periods%ROWTYPE;
  v_next_id      uuid;
  v_next_start   date;
  v_next_end     date;
  v_next_name    text;
  v_existing_tx  uuid;
  v_acct_120     uuid;
  v_carry        jsonb;
  v_net67        numeric;
  v_result_entry jsonb;
  v_entries      jsonb;
  v_tx_res       jsonb;
BEGIN
  -- 1) Charger N et refuser s'il est encore 'open' (l'invariant single-open
  --    interdit d'ouvrir N+1 tant que N est ouvert : lock/close N d'abord).
  SELECT * INTO v_n FROM accounting_periods
   WHERE id = p_closing_period_id AND copro_id = p_copro_id;
  IF NOT FOUND THEN
    RETURN jsonb_build_object('success', false,
      'error', 'Exercice de clôture introuvable', 'period_id', p_closing_period_id);
  END IF;
  IF v_n.status = 'open' THEN
    RETURN jsonb_build_object('success', false,
      'error', 'Verrouillez ou clôturez d''abord l''exercice N (lock_period/close_period)',
      'period_id', p_closing_period_id, 'status', v_n.status);
  END IF;

  -- 2) Compte 120 (résultat opérations courantes)
  SELECT id INTO v_acct_120 FROM accounts
   WHERE copro_id = p_copro_id AND code = '120';
  IF v_acct_120 IS NULL THEN
    RETURN jsonb_build_object('success', false,
      'error', 'Compte 120 absent pour cette copropriété (voir normalisation des comptes de report)',
      'copro_id', p_copro_id);
  END IF;

  -- 3) Dates / nom de N+1
  v_next_start := COALESCE(p_new_start, (v_n.start_date + INTERVAL '1 year')::date);
  v_next_end   := COALESCE(p_new_end,   (v_n.end_date   + INTERVAL '1 year')::date);
  v_next_name  := COALESCE(p_new_name,  'Exercice ' || EXTRACT(YEAR FROM v_next_start)::int);

  -- 4) Récupérer/créer N+1, garantir status='open' (N n'est plus 'open' -> invariant OK)
  SELECT id INTO v_next_id FROM accounting_periods
   WHERE copro_id = p_copro_id AND start_date = v_next_start AND end_date = v_next_end;
  IF v_next_id IS NULL THEN
    INSERT INTO accounting_periods (copro_id, name, start_date, end_date, status)
    VALUES (p_copro_id, v_next_name, v_next_start, v_next_end, 'open')
    RETURNING id INTO v_next_id;
  ELSE
    UPDATE accounting_periods SET status = 'open'
     WHERE id = v_next_id AND status <> 'open';
  END IF;

  -- 5) Reprise existante pour ce N ?
  SELECT id INTO v_existing_tx FROM ledger_transactions
   WHERE copro_id = p_copro_id
     AND source_type = 'opening_balance'
     AND source_id = p_closing_period_id;

  -- 5a) Gel : si N approuvé, refuser la régénération PROPREMENT (avant le DELETE).
  IF v_existing_tx IS NOT NULL AND v_n.status = 'approved' THEN
    RETURN jsonb_build_object('success', false,
      'error', 'Reprise figée : exercice source approuvé',
      'period_id', p_closing_period_id, 'tx_id', v_existing_tx);
  END IF;

  -- 5b) Supprimer l'ancienne reprise (autorisé par l'exemption tant que N != approved).
  IF v_existing_tx IS NOT NULL THEN
    DELETE FROM ledger_transactions WHERE id = v_existing_tx;  -- cascade -> entries
  END IF;

  -- 6) Report des comptes de bilan (classes 1/4/5), HORS comptes de report,
  --    agrégés par (compte, lot), en conservant lot_id.
  SELECT jsonb_agg(jsonb_build_object(
           'account_id',  x.account_id,
           'lot_id',      x.lot_id,
           'direction',   CASE WHEN x.net > 0 THEN 'debit' ELSE 'credit' END,
           'amount',      abs(x.net),
           'entry_label', 'Report à-nouveau ' || v_next_name
         ))
    INTO v_carry
  FROM (
    SELECT e.account_id, e.lot_id,
           round(sum(CASE WHEN e.direction='debit' THEN e.amount ELSE -e.amount END), 2) AS net
    FROM ledger_entries e
    JOIN ledger_transactions t ON t.id = e.tx_id AND t.status = 'posted'
    JOIN accounts a ON a.id = e.account_id
    WHERE e.copro_id = p_copro_id
      AND e.period_id = p_closing_period_id
      AND substr(a.code, 1, 1) IN ('1','4','5')
      AND a.code NOT IN ('110','120','119','129')
    GROUP BY e.account_id, e.lot_id
    HAVING round(sum(CASE WHEN e.direction='debit' THEN e.amount ELSE -e.amount END), 2) <> 0
  ) x;

  -- 7) Résultat de l'exercice = net des classes 6/7, viré en 120.
  --    v_net67 = Σ(debit - credit). v_net67 < 0 => excédent => CRÉDIT 120 ;
  --    v_net67 > 0 => déficit => DÉBIT 120.
  SELECT round(coalesce(sum(CASE WHEN e.direction='debit' THEN e.amount ELSE -e.amount END), 0), 2)
    INTO v_net67
  FROM ledger_entries e
  JOIN ledger_transactions t ON t.id = e.tx_id AND t.status = 'posted'
  JOIN accounts a ON a.id = e.account_id
  WHERE e.copro_id = p_copro_id
    AND e.period_id = p_closing_period_id
    AND substr(a.code, 1, 1) IN ('6','7');

  IF v_net67 <> 0 THEN
    v_result_entry := jsonb_build_array(jsonb_build_object(
      'account_id',  v_acct_120,
      'lot_id',      NULL,
      'direction',   CASE WHEN v_net67 < 0 THEN 'credit' ELSE 'debit' END,
      'amount',      abs(v_net67),
      'entry_label', 'Résultat de l''exercice ' || v_n.name
    ));
  ELSE
    v_result_entry := '[]'::jsonb;
  END IF;

  v_entries := coalesce(v_carry, '[]'::jsonb) || v_result_entry;

  IF jsonb_array_length(v_entries) = 0 THEN
    RETURN jsonb_build_object('success', false,
      'error', 'Aucun solde à reporter pour cet exercice',
      'period_id', p_closing_period_id);
  END IF;

  -- 8) Poster la reprise dans N+1 (open) via la route canonique (auto_post + garde CR3).
  v_tx_res := create_ledger_transaction(
    p_copro_id,
    v_next_id,
    v_next_start,
    'À-nouveau — reprise des soldes ' || v_n.name || ' → ' || v_next_name,
    'opening_balance',
    p_closing_period_id,
    v_entries,
    true
  );

  IF NOT coalesce((v_tx_res->>'success')::boolean, false) THEN
    -- Nettoyage d'un éventuel brouillon résiduel (tx créé puis post échoué sans exception).
    DELETE FROM ledger_transactions
     WHERE copro_id = p_copro_id AND source_type = 'opening_balance'
       AND source_id = p_closing_period_id AND status = 'draft';
    RETURN jsonb_build_object('success', false,
      'error', 'Échec de la création de la reprise',
      'detail', v_tx_res, 'next_period_id', v_next_id);
  END IF;

  RETURN jsonb_build_object(
    'success', true,
    'next_period_id', v_next_id,
    'next_period_name', v_next_name,
    'opening_tx_id', v_tx_res->>'tx_id',
    'carry_lines', coalesce(jsonb_array_length(v_carry), 0),
    'result_net67', v_net67,
    'result_to_120', abs(v_net67)
  );
END;
$$;
```

- [ ] **Step 2 : Demander l'accord et appliquer**

Accord utilisateur → `apply_migration` (name `wp5_1_open_next_period`). (La vérification fonctionnelle est entièrement couverte par Task 8.)

- [ ] **Step 3 : Commit**

```bash
git add supabase/migrations/20260601095000_wp5_1_open_next_period.sql
git commit -m "feat: open_next_period - reprise des soldes a-nouveau idempotente (WP5.1)"
```

---

## Task 7 : Brancher l'approbation sur l'orchestrateur AG

**Files:**
- Create: `supabase/migrations/20260601096000_wp5_1_ag_approval_hook.sql`

On modifie **uniquement la branche `APPROVE_ACCOUNTS`** de deux fonctions, en repartant des **defs live** (`pg_get_functiondef`). `prepare_ag_decisions` résout et **stocke `period_id` dans `target_id`** (comme `CREATE_BUDGET`), pour que `activate_ag_decisions` n'ait plus à re-matcher par dates ; `activate_ag_decisions` appelle `approve_period(target_id)` puis `regularize_period`.

> Fragilité connue, **héritée** (non aggravée) : la résolution de période par `(v_vars->>'date_debut')::DATE` suppose un format ISO ; les variables peuvent être en `DD/MM/YYYY` selon la source. On reproduit à l'identique le comportement de `CREATE_BUDGET` ; le durcissement du parsing de dates est un suivi distinct (hors WP5.1). Test 7 crée la résolution en dates ISO pour une résolution déterministe.

- [ ] **Step 1 : Récupérer les defs live des deux fonctions**

Run (lecture) :
```sql
SELECT p.proname, pg_get_functiondef(p.oid)
FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname='public' AND p.proname IN ('prepare_ag_decisions','activate_ag_decisions');
```
Vérifier qu'aucun fichier postérieur à `20260531170500_wp2_prepare_reuse_budget.sql` ne les a redéfinies (les defs live sont la source de vérité).

- [ ] **Step 2 : Assertion (échoue avant)**

```sql
-- target_table de l'action APPROVE_ACCOUNTS deviendra 'accounting_periods'.
SELECT bool_and(pg_get_functiondef(p.oid) LIKE '%approve_period%') AS ok
FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND p.proname='activate_ag_decisions';
```
Attendu AVANT : `ok = false`.

- [ ] **Step 3 : Écrire la migration**

Copier les corps **live** des deux fonctions dans le fichier (en `CREATE OR REPLACE`, signatures et `SET search_path TO 'public'` identiques), puis remplacer **exactement** les branches ci-dessous.

**`prepare_ag_decisions` — remplacer la branche actuelle :**
```sql
    WHEN 'APPROVE_ACCOUNTS' THEN
      INSERT INTO ag_pending_actions (ag_id, resolution_id, action_type, target_table, payload, status)
      VALUES (p_ag_id, v_resolution.id, 'APPROVE_ACCOUNTS', 'budgets', v_vars, 'pending');
      v_actions_created := v_actions_created + 1;
```
**par :**
```sql
    WHEN 'APPROVE_ACCOUNTS' THEN
      -- Résoudre la période N visée (même logique que CREATE_BUDGET) et la stocker
      -- en target_id pour que activate_ag_decisions n'ait plus à re-matcher par dates.
      SELECT id INTO v_period_id FROM accounting_periods
      WHERE copro_id = v_copro_id
        AND start_date = (v_vars->>'date_debut')::DATE
        AND end_date   = (v_vars->>'date_fin')::DATE
      LIMIT 1;

      INSERT INTO ag_pending_actions (ag_id, resolution_id, action_type, target_table, target_id, payload, status)
      VALUES (p_ag_id, v_resolution.id, 'APPROVE_ACCOUNTS', 'accounting_periods', v_period_id, v_vars, 'pending');
      v_actions_created := v_actions_created + 1;
```

**`activate_ag_decisions` — remplacer la branche actuelle :**
```sql
      WHEN 'APPROVE_ACCOUNTS' THEN
        UPDATE budgets b SET status = 'closed'
        FROM accounting_periods ap
        WHERE b.period_id = ap.id AND ap.copro_id = v_copro_id
          AND ap.start_date = (v_action.payload->>'date_debut')::DATE
          AND ap.end_date = (v_action.payload->>'date_fin')::DATE;
```
**par :**
```sql
      WHEN 'APPROVE_ACCOUNTS' THEN
        -- Clôture des budgets de l'exercice (comportement conservé), via target_id.
        UPDATE budgets b SET status = 'closed'
        WHERE b.period_id = v_action.target_id;

        IF v_action.target_id IS NOT NULL THEN
          -- Approuver la période N (closed -> approved) : fige automatiquement la reprise N+1.
          v_res := approve_period(v_action.target_id);
          IF NOT COALESCE((v_res->>'success')::boolean, false) THEN
            RAISE EXCEPTION 'Approbation de la période échouée : %', v_res->>'error';
          END IF;
          -- Point d'accroche WP5.3 (répartition excédent/déficit) — stub no-op.
          PERFORM regularize_period(v_copro_id, v_action.target_id);
        END IF;
```

> Si `activate_ag_decisions` ne déclare pas déjà `v_res JSONB` dans son bloc `DECLARE`, l'ajouter. Tout reste dans la transaction atomique de `finalize_and_activate_ag` (rollback global si l'approbation échoue).

- [ ] **Step 4 : Demander l'accord, appliquer, relancer l'assertion (PASS)**

Accord utilisateur → `apply_migration` (name `wp5_1_ag_approval_hook`). Relancer Step 2 → `ok = true`.

- [ ] **Step 5 : Commit**

```bash
git add supabase/migrations/20260601096000_wp5_1_ag_approval_hook.sql
git commit -m "feat: hook AG approbation des comptes -> approve_period + regularize_period (WP5.1)"
```

---

## Task 8 : Tests de bout en bout (boucle d'or — Le Clos 22222222)

**Files:** aucun (assertions SQL en lecture via `execute_sql`).

> ⚠️ Cette séquence **mute durablement** la boucle d'or (2026 → closed → approved, 2027 figé). À exécuter en connaissance de cause sur la copro de test 22222222 ; ne PAS lancer sur 11111111 (réservée immuable). Demander l'accord avant les Tests 6 et 7 (clôture/approbation + création de données AG).

- [ ] **Test 1 — Ouvrir 2027 (reprise équilibrée)**

```sql
SELECT lock_period('bda8aa71-bba8-4613-9300-214e3d356fd0');               -- N (2026) -> locked (prérequis)
SELECT open_next_period('22222222-aaaa-bbbb-cccc-222222222222',
                        'bda8aa71-bba8-4613-9300-214e3d356fd0');           -- ouvre 2027 + reprise
-- Assertion : grand livre 2027 équilibré et net nul.
SELECT round(sum(total_debit),2) AS d, round(sum(total_credit),2) AS c,
       round(sum(total_debit-total_credit),2) AS net
FROM v_trial_balance tb
JOIN accounting_periods ap ON ap.id = tb.period_id
WHERE ap.copro_id='22222222-aaaa-bbbb-cccc-222222222222' AND ap.start_date='2027-01-01';
```
Attendu : `success=true` ; `d=12500.16`, `c=12500.16`, `net=0.00`.

- [ ] **Test 2 — 450-1 par lot + 512 reportés à l'identique** (filtre `posted` explicite)

```sql
SELECT lot_ref, round(sum(debit-credit),2) AS solde
FROM v_general_ledger gl
JOIN accounting_periods ap ON ap.id = gl.period_id
WHERE ap.copro_id='22222222-aaaa-bbbb-cccc-222222222222' AND ap.start_date='2027-01-01'
  AND gl.status='posted' AND gl.account_code='450-1'
GROUP BY lot_ref ORDER BY lot_ref;
-- + 512 :
SELECT round(sum(debit-credit),2) AS solde_512
FROM v_general_ledger gl
JOIN accounting_periods ap ON ap.id = gl.period_id
WHERE ap.copro_id='22222222-aaaa-bbbb-cccc-222222222222' AND ap.start_date='2027-01-01'
  AND gl.status='posted' AND gl.account_code='512';
```
Attendu : 001=1350.40, 101=1503.94, 102=1748.07, 201=1535.46, 202=1795.29, 301=748.04 ; `solde_512=3818.96`.

- [ ] **Test 3 — Résultat viré en 120 = 12 020,16 € (crédit)**

```sql
SELECT round(total_debit,2) AS d, round(total_credit,2) AS c
FROM v_trial_balance tb
JOIN accounting_periods ap ON ap.id = tb.period_id
WHERE ap.copro_id='22222222-aaaa-bbbb-cccc-222222222222' AND ap.start_date='2027-01-01'
  AND tb.account_code='120';
-- 110 ne doit PAS apparaître (résultat travaux = 0) :
SELECT count(*) AS n110
FROM v_trial_balance tb
JOIN accounting_periods ap ON ap.id = tb.period_id
WHERE ap.copro_id='22222222-aaaa-bbbb-cccc-222222222222' AND ap.start_date='2027-01-01'
  AND tb.account_code='110';
```
Attendu : 120 → `d=0.00`, `c=12020.16` ; `n110=0`.

- [ ] **Test 4 — Vues d'ouverture 2027 cohérentes**

```sql
-- 401 reporté (480,00 crédit) :
SELECT round(total_debit,2) d, round(total_credit,2) c
FROM v_trial_balance tb
JOIN accounting_periods ap ON ap.id=tb.period_id
WHERE ap.copro_id='22222222-aaaa-bbbb-cccc-222222222222' AND ap.start_date='2027-01-01'
  AND tb.account_code='401';
```
Attendu : 401 → `d=0.00`, `c=480.00`. (Vérifier en complément le dashboard trésorerie / solde copropriétaire sur 2027 dans l'UI si pertinent.)

- [ ] **Test 5a — Rejeu idempotent (exerçable)**

```sql
SELECT open_next_period('22222222-aaaa-bbbb-cccc-222222222222',
                        'bda8aa71-bba8-4613-9300-214e3d356fd0');   -- relance à l'identique
SELECT count(*) AS nb_reprises
FROM ledger_transactions
WHERE copro_id='22222222-aaaa-bbbb-cccc-222222222222'
  AND source_type='opening_balance'
  AND source_id='bda8aa71-bba8-4613-9300-214e3d356fd0';
```
Attendu : `success=true` ; `nb_reprises=1` (pas de doublon — DELETE-then-recreate + index unique).

- [ ] **Test 5b — Cut-off tardif dans N : HORS SCOPE WP5.1**

Le sous-cas spec §10.5 « poster une écriture dans N (locked) puis relancer » n'est **pas exerçable** ici (`post_ledger_transaction` exige une période `open`). Il dépend du **posting en `locked` (WP5.2)**. → noté en suivi, **non bloquant** pour WP5.1.

- [ ] **Test 6 — Gel à l'approbation** (accord requis)

```sql
SELECT close_period('bda8aa71-bba8-4613-9300-214e3d356fd0');     -- 2026 locked -> closed
SELECT approve_period('bda8aa71-bba8-4613-9300-214e3d356fd0');   -- closed -> approved
SELECT open_next_period('22222222-aaaa-bbbb-cccc-222222222222',
                        'bda8aa71-bba8-4613-9300-214e3d356fd0');  -- doit refuser proprement
SELECT count(*) AS nb_reprises
FROM ledger_transactions
WHERE copro_id='22222222-aaaa-bbbb-cccc-222222222222'
  AND source_type='opening_balance'
  AND source_id='bda8aa71-bba8-4613-9300-214e3d356fd0';
```
Attendu : `approve_period.success=true` ; `open_next_period` → `{success:false, error:'Reprise figée : exercice source approuvé'}` (retour propre, pas d'exception) ; `nb_reprises=1` (reprise intacte).

- [ ] **Test 7 — Chemin AG complet** (accord requis ; à exécuter à la place du Test 6 sur un état frais, ou après réinitialisation)

Créer (avec accord) une `ag_meeting` + `ag_resolution` `action_type='APPROVE_ACCOUNTS'`, `variables={'date_debut':'2026-01-01','date_fin':'2026-12-31'}` (dates ISO, déterministes), puis :
```sql
SELECT prepare_ag_decisions('<ag_id>');
-- Vérifier la résolution de période :
SELECT action_type, target_table, target_id
FROM ag_pending_actions WHERE ag_id='<ag_id>' AND action_type='APPROVE_ACCOUNTS';
SELECT activate_ag_decisions('<ag_id>');   -- ou finalize_and_activate_ag(<ag_id>, true)
-- Assertion : 2026 approuvée par le chemin AG.
SELECT status FROM accounting_periods WHERE id='bda8aa71-bba8-4613-9300-214e3d356fd0';
```
Attendu : `target_id = bda8aa71-…` (période bien résolue) ; `status='approved'`. (Prérequis : 2026 doit être `closed` avant — l'AG d'approbation suit la clôture, décision 1.)

- [ ] **Commit (suivi des tests)**

Consigner les résultats (PASS/FAIL) dans `.planning/PROGRESS_finance.md` ou la note de session. Pas de commit de code (tests = assertions).

---

## Hors-périmètre WP5.1 (rappel)

- Écritures de **cut-off 408/486** et **posting en `locked`** → WP5.2.
- **Répartition excédent/déficit par lot** (120/110 → 450) à l'approbation → WP5.3 (hook `regularize_period` en place, stub).
- **Séparation courant/travaux des classes 6/7** : tout le net est viré en 120 ; `110` reste à 0 (aucun compte travaux/exceptionnel sur la boucle d'or). À implémenter quand la typologie des comptes de gestion sera tranchée.
- Couche **engagement** → WP5.4.

## Self-Review (couverture spec)

- §3.1 clôture 2 temps → Task 6 (provisoire) + Task 5/7 (fige). §3.2 vraie écriture → Task 6. §3.3 idempotence → Task 6 + Test 5a. §3.4 périmètre borné → stub Task 5/7. §3.5 invariant single-open conservé → non modifié (Task 6 lock N d'abord). §3.6 exemption immutabilité → Task 3.
- §4 sémantique états → Task 5 (approve depuis `closed`) + Task 3. §5 contenu reprise → Task 6 (bilan hors 110/120 + résultat en 120). §6 fonctions/hook → Tasks 5/6/7. §7 garde-fous (single-open conservé, exemption bornée, lot_id 450, unicité) → Tasks 3/4/6. §8 normalisation 110/120 → Task 1. §10 tests → Task 8 (5b/§10.5 explicitement différé WP5.2).
- Pas de placeholder (le seul « stub » `regularize_period` est exigé par la spec §6/§9).
