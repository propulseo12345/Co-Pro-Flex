'use client';

import { Calendar } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export default function AgendaPage() {
  return <PlaceholderPage icon={Calendar} title="Agenda global" />;
}
