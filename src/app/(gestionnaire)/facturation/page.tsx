'use client';

import { Receipt } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export default function FacturationPage() {
  return <PlaceholderPage icon={Receipt} title="Facturation" />;
}
