'use client';

import { FileText } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export default function ModelesPage() {
  return <PlaceholderPage icon={FileText} title="Modèles" />;
}
