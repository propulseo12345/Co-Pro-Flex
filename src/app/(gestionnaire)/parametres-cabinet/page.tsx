'use client';

import { Settings } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export default function ParametresCabinetPage() {
  return <PlaceholderPage icon={Settings} title="Paramètres cabinet" />;
}
