Lance le build du projet et résume les résultats.

Étapes :
1. Exécute `npm run build`
2. Si le build réussit : confirme avec le nombre de pages/routes compilées
3. Si le build échoue : liste chaque erreur avec fichier, ligne et description
4. Propose un plan de correction ordonné par priorité (bloquant d'abord)

Ne corrige rien automatiquement — attends ma validation.
