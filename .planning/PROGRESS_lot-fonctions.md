# PROGRESS — Lot Fonctions (Phase 0 « fonctions »)

Roadmap : `docs/superpowers/plans/2026-06-05-lot-fonctions-roadmap.md` · Mémoire : `[[lot-fonctions-decoupage]]`
Branche : `phase0-db-rebaseline`.
**Cadence/migration** : 3-checks (workflow auteur → 2 relecteurs adversariaux → synthèse) → écrire le fichier → **harnais** (`supabase db reset` + détecteur cascade + vitest) → corriger → **1 commit quand vert**.

## État des 9 migrations
- [x] **0023 authz-helpers** — LIVRÉ + commité `93cd3e4`. 15 helpers (cabinet inline, CS source unique, GED A4, portail). Harnais vert : 0 cascade, anon_exec=0, svc_14=t/link_svc=f, **vitest 75/75**.
- [x] **0024 triggers-integrite-GL — LIVRÉ + commité** (`feat(db): 0024 filet d'intégrité du grand livre`). 8 triggers : immutabilité U+D (en-tête ET lignes), no-insert-posted, consistency intra-copro, is_postable (INSERT+UPDATE), enforce_lot_id_on_45x (A2 sans liste blanche), équilibre (CONSTRAINT TRIGGER DEFERRED) ; 6 fns à lookup en `SECURITY DEFINER set search_path=public`. **Rien sur accounting_periods** (`uq_period_single_open` déjà en 0013). Harnais vert : 8 triggers, 0 cascade, **6 gates fonctionnels pass=6/0** (fixture jetable→ROLLBACK), vitest 75/75. **Consistency cross-copro des autres domaines → distribués en `0025→0028`.** (run v2 `wf_d09fcd2e-21a` ; v1 `wf_84074bed-777` abandonné = périmètre trop gros, leçon en mémoire.) **AMENDÉ commit `75bbd2b`** : + `is_ledger_regen_exempt` (SECTION 0) + exemption câblée dans les 4 triggers d'immutabilité (écritures régénérables opening/closing/result_allocation rejouables tant que la période n'est pas `approved`) — décision B suite à la revue 0025.
**rpc-finance-core SCINDÉ en 5 sous-lots (0025→0029)** — décidé 2026-06-05 pour éviter le volume (cf. échec 0024 v1). Renumérotation aval +4.
- [x] **0025 GL canonique — LIVRÉ + commité** (`7f7d6a3`). 5 RPC : `resolve_lot_tiers_account`, `get_period_for_date`, `provision_copro_chart` (82 comptes, idempotent), `create_ledger_transaction` (route canonique, garde §5.3 + période open, sans WHEN OTHERS masquant), `post_ledger_transaction`. `is_ledger_regen_exempt` + recâblage triggers → **déplacés en 0024** (décision B). Harnais vert : provision 82 · post équilibré · déséquilibre/période-close/45x-sans-lot rejetés · **exemption result_allocation OK + immutabilité call_for_funds OK** · vitest 75/75.
- [x] **0026 Appels & paiements — LIVRÉ + commité** (`268decd`). Périmètre option B (USER) : 5 posteurs (`post_budget_call_for_funds` 10-args plus-grand-reste, `post_owner_payment` 10-args FIFO cloisonné nature + idempotency, `allocate_payment` 3-args INVOKER, `post_supplier_invoice` 14-args adapté `tiers_id`/`service_order_id`, `post_supplier_payment` 8-args idempotent) + **workflow budget** (`submit_budget`/`validate_budget` sans GL, `compute_repartition_shares`, `repartition_key_is_complete`) + helpers statut (`update_call_status` dérive depuis Σ montants, `recalculate_all_call_statuses`, `get_supplier_invoice_paid_amount`) + **10 triggers** (`trg_call_line_status_sync` FUSION anti-récursion, `trg_validate_call_total`/`trg_cff_ledger_required`/`trg_validate_supplier_invoice_total` CONSTRAINT DEFERRED, `trg_validate_payment_allocation` anti-surimput., `trg_allocation_update_line`, `trg_validate_supplier_payment` anti-surpaiement, `trg_update_supplier_invoice_status_after_payment`, `tr_check_budget_line_copro_consistency`, `tr_check_invoice_copro_consistency`). **Corrections LIVE→cible bakées** : `'bank_transfer'`→`'transfer'` (enum), `supplier_id`→`tiers_id`, gardes G-MGR ajoutées, idempotency_key TEXT, suppression recalage manuel `allocate_payment`. Cadence 3-checks (workflow `wf_88afbfde` : auteur + 2 relecteurs adversariaux = **0 blocker/0 major** ; synthèse plantée réseau → terminée à la main). Harnais vert : db reset 0001→0026, 0 cascade, 12 fns + 10 triggers, anon=0 sur posteurs, **gate métier end-to-end** (appel 10000 équilibré, FIFO+idempotency, facture→paid, GL D=C=18400, anti-surimput.), **vitest 75/75**. **HORS périmètre → 0027** : `validate_budget_expense` + cut-off 408/486.
- [x] **0027 Périodes & affectation — LIVRÉ + commité** (`3d323e6`). close/approve/reopen_period (reopen interdit si approved), cut-off art.14-3 (`cutoff_entry_pair`/`post_period_cutoff`/`reverse_period_cutoff` 408/486), `set_opening_balance`/`get_opening_balance`, `open_next_period` (à-nouveau + split résultat 110/120), `validate_budget_expense` RÉÉCRIT (nom via `tiers.name`), `regularize_period` ÉTENDU **D120/C450-1 (courant) ET D110/C450-2 (travaux) par quote-part** + `assert_result_allocation_split`/vue `v_result_allocation_split`. **Bugs revue corrigés** : signe inversé de la vue (450-x = contrepartie de 12x, sens opposés → `abs(450+12x)`), `get_opening_balance.created_at`→`tx_date` (colonne inexistante), gardes idempotence DELETE si exercice approuvé. Gate vert : affectation courant=2000+travaux=1500, `v_split=0`, GL D=C=45000, vitest 75/75.
- [x] **0028 Dérivés, vues & annexes — LIVRÉ + commité** (`24143e8`). 8 vues GL dérivées (`v_trial_balance`, `v_owner_statement_by_lot/by_person`, `v_unpaid_by_lot`, `v_payments_overview`, `v_bank_movements_overview`, `v_lot_vs_gl_mismatch`, `v_unpaid_lot_owner`) + reporting (`get_owner_statement` G-MIXTE, `fn_dashboard_kpis`, `calculate_budget_projection`, `audit_finance_integrity` self-contained, `refresh_bank_movement_status`) + 5 annexes (libellés 3/4/5 corrigés, fan-out 4/5 corrigé EXISTS) + relances (`get_pending_reminders_to_send` RÉÉCRIT lot_owners, `create_payment_reminder`, `mark_reminder_*`, `cancel_stale_reminders`, `is_reminders_paused`) + 2 triggers seed ROBUSTES. Gate : audit=0, GL D=C=21000, impayés OK, vitest 75/75. **✅ Dette réconciliation RÉSOLUE 2026-06-06 (commit `280b8c1`)** : `v_lot_vs_gl_mismatch` filtre le côté GL sur `source_type in ('call_for_funds','payment')` (col. renommée `gl_call_payment_balance`) ; `v_owner_statement_by_lot` reste complet. Validé harnais begin…rollback : Gate A (boucle d'or) audit=0 + **Gate B multi-période** (close→open_next→regularize→approve) audit=0 vs 4 faux écarts avant (preuve différentielle). Test legacy V0 obsolète archivé `.disabled` (commit `89a8712`). cf. `[[audit_reconciliation_multiperiode]]`. **✅ BUG trigger différé CORRIGÉ 2026-06-06 (commit `482af64`)** : `tr_cff_ledger_required` re-quête la ligne courante (au lieu de l'image figée `new.*`) + exempte `cancelled`. Sweep adversarial : 1 seul des 4 triggers contrainte différés était buggé, les 3 autres re-quêtent déjà = sains. **Prouvé à COMMIT RÉEL** (seed committé + audit=0 + cycle ; NEG rejeté ; EXEMPT/EDGE OK ; vitest 75/75). cf. `[[deferred_constraint_trigger_stale_image]]`. Dette future (documentée code) = contre-passation si annulation d'appel posté ; 2 tests SQL legacy à purger (commit séparé, non bloquant).
- [x] **0029 Harnais & seed — LIVRÉ + commité** (`36e265a`) → 🎯 **BOUCLE D'OR**. `seed_golden_loop(copro)` déroule la chaîne canonique (structure + `validate_budget` → `post_budget_call_for_funds` → `post_owner_payment` (1 impayé) → facture+paiement fournisseur → `validate_budget_expense`), pose `onboarding_step=NULL` ; `create_test_copro(_seeded)` (G-SVC), `create_clean_*` = alias. **DROP doublons = RIEN** (reconstruction propre vérifiée). **Décision revue** : seed = exercice vivant conforme **SANS** close/open_next/regularize (sinon mismatch audit, cf. dette 0028) — comme le seed legacy qui ne clôturait jamais ; l'affectation 110/120 reste couverte par le gate 0027. Gate d'acceptation : `create_test_copro_seeded()` → **`audit_finance_integrity = 0 écart`**, GL D=C=37030, idempotent, vitest 75/75.
- [x] **0030 rpc-ag-conseil — LIVRÉ + commité** (`57a4296`). 38 fonctions (17+4+17), 3 vues, 9 triggers, 3 paliers (approche C, 1 fichier). Plan `docs/superpowers/plans/2026-06-06-0030-rpc-ag-conseil-plan.md`. Auteur+relecteurs adversariaux par palier (workflow, retry ×3 anti-socket). Gates : P1 begin/rollback (majorités art.24/25/26/unanimité + passerelle + vues + double-vote 23514 + sans-présence 23514) ; **P2 COMMIT RÉEL** (7 actions auto-peuplées, `audit_finance_integrity=0`, GL D=C=123950, idempotent) **+ tout-ou-rien** (clé incomplète → 23514 + rollback complet) ; P3 (wizard sans `ag_milestones`, correspondance→`ag_votes`, garde live, annuaire 3=1000, bundles). **/code-review high** (42 candidats → 6 correctifs appliqués + re-gate) : garde ex-propriétaire (cast_vote+register), quorum conseil vide, votes membres révoqués, garde APPROVE_ACCOUNTS résultat-nul (miroir open_next_period), budget validé avant appel, **vote-figé** (décision USER : un vote séance/correspondance figé ne se réécrit pas → 23514). vitest 75/75. Décisions différées → `.planning/DEFERRED_USER_DECISIONS.md`.
- [x] **0031 rpc-ged-mutations — LIVRÉ + commité** (`e0fe2d0`). GED : `generate_document_path` (4 args), `create_document_system_folders` (dossiers système, **courrier**), `create_document_version` (pointeur), 3 triggers documents (expiration légale / search_text FR / blocage suppression protégée), vue `v_document_versions`. MUTATIONS (lot-centric) : `validate_mutation` (change `lot_owners`, **0 GL sur 450** prouvé), `initialize_mutation_steps` (6 jalons) + `upsert_mutation_step` (+completed_by), état daté art.5 (`generate_etat_date_payload`/`create_etat_date_snapshot`, 3 parties figées à la date d'effet), vue `v_mutation_detail` ; triggers cohérence intra-copro + immutabilité état daté (différés de 0019). **Cadence atypique** : workflow d'écriture **a planté** (auteur MUTATIONS hang socket non récupérable) → moitié GED récupérée du journal + moitié MUTATIONS écrite **à la main** ; bug attrapé : `is_service_call()` manquant dans 2 gardes GED (corrigé). Revue = self-audit (0 when others, casts OK, revoke/grant OK) + gate fort. Gate begin/rollback vert, vitest 75/75. **DIFFÉRÉ** : recouvrement art.20 (record/settle_opposition = mouvements GL) → `DEFERRED_USER_DECISIONS.md`. **✅ REVUE MULTI-AGENT COMPLÉTÉE 2026-06-07 (commit `5017eb3`)** : workflow 6 angles `wf_ed0b4557-b37` → 7 bugs (4 simples corrigés : volatilité `generate_document_path` IMMUTABLE→STABLE, `is_company` dérivé, gel P3 `issue_date<=v_eff`, garde acquéreur≠vendeur ; +1 garde `v_key null` 23503) ; 1 MAJOR (partie 2 état daté = tantièmes au lieu des sommes dues par le syndicat) + 6 décisions métier → `DEFERRED_USER_DECISIONS.md`.
- [x] **0032 rpc-maintenance-comm — LIVRÉ + commité** (`29300a1`). 20 fonctions + 15 triggers, 3 paliers, **cadence subagent-driven** (skill superpowers : brainstorming → writing-plans → implémenteur+revue spec+revue qualité par palier → revue multi-agent finale `wf_7ad1b7d8-ce4`). Spec `docs/superpowers/specs/2026-06-07-0032-maintenance-comm-design.md` + plan `docs/superpowers/plans/2026-06-07-0032-maintenance-comm-plan.md`. P1 cœur OS (machine à états souple, `generate_service_order_number` advisory lock, `update_service_order_status` jalons+events 0 GL, `create_logbook_from_service_order` idempotent, `delete_service_order`). P2 triggers maintenance (`update_provider_stats` recompute+verrou ordonné+split insert-delete/update-WHEN, `update_contract_status_auto` dérivé écriture, 3 cohérences inter-copro). P3 comm (`mark_conversation_read`, `update_conversation_last_message` anti-régression, compteurs mur plancher 0, 6 cohérences inter-copro). **Correctifs revues** : back-link carnet, `for update`, `order by` anti-deadlock, anti-régression `last_message_at`, ON CONFLICT EvalPlanQual réfuté. Gates P1 5/5 · P2 3/3 · P3 5/5 ; db reset 0001→0032 propre ; vitest 75/75 ; 0 doublon. Décisions métier (sémantique `interventions_count`, mapping catégorie, appelant generate_number) + notes 0034/vues → `DEFERRED_USER_DECISIONS.md`. **CHAÎNE FOURNISSEUR déjà en 0026, exclue (anti-doublon confirmé).**
- [x] **0033 notif-ag-transitoire — LIVRÉ + commité** (`5de57fd`). Île TRANSITOIRE (tables 0018, à droper étape 3). **Décision USER : CIBLE-PURE** (calé sur colonnes 0018 ; edges vivants ag_send_convocations/email_webhook parlent à l'ancien schéma → réécriture edges = étape 3 différée, ne pas ressusciter notification_type/document_id/error_code/provider_message_id/event_timestamp/raw_data/ip/ua). Scoping `wf_861f5eba-682` ; **j'ai attrapé à la main la dérive edge↔cible que le scoping avait ratée** (l'edge appelle create_ag_notification avec p_copro_id/p_notification_type/p_document_id, mark_* avec p_provider_message_id/p_error_code — absents du cible). 5 objets cible-pure : `tr_ag_notif_event_copro_consistency` (+trigger, fonction DÉDIÉE car enforce_copro_consistency 0030 = no-op ici), `create_ag_notification` (copro dérivé, G-MGR), `mark_notification_sent`/`mark_notification_failed` (G-SVC strict + ACL uniforme), `get_ag_recipients` (coproprietaires actifs multicanal, only_missing). **Exclus** : get_ag_sending_stats (0 appel). Gate begin/rollback **9/9** ; db reset 0001→0033 propre ; vitest 75/75 ; 0 doublon. Revue finale `wf_dc94736b-7e3` : 0 vrai bug (1 commentaire trompeur corrigé). Plan `docs/superpowers/plans/2026-06-07-0033-notif-ag-transitoire-plan.md`. **À VÉRIFIER hors 0033** : `check_convocation_delay` (appelé par front `useAgNotifications` — déjà en 0030 ? sinon gap) → DEFERRED.
- [x] **0034 revoke-rls-seed — LIVRÉ + commité** (`d242c14`) → 🎯 **ACCEPTATION PHASE 0**. 173 policies RLS sur 79 tables (patron user_has_copro_access collectif / user_is_copro_manager back-office + volets own lot-centric + confidentialité par contenu via user_can_view_document/can_view_content), bascule ENABLE/DISABLE par app.environment (OFF dev/ON prod) + FORCE sur 5 tables GL, filet REVOKE anon (assertion), seed (assertion work_domain + 6 email_templates système copro_id NULL), `provision_demo_tenant()` (cabinet A/B + gestionnaires câblés). **Cadence** : scoping wf_6bd8c8aa (16 ag, audit adversarial → carte des fuites) → plan → authoring wf_305c3dac (8 ag, policies/lot + infra + provision) → assemblage main → revue wf_ef2fb4c8 (4 lentilles). **2 vrais bugs corrigés** : fuite RIB `tiers` (vue tiers_directory passée SECURITY DEFINER + filtre copro, base manager-only) + **récursion RLS** `collective_loans`↔`collective_loan_shares` (helpers DEFINER `loan_copro_id`/`user_owns_share_in_loan`) ; +2 minor (conversations created_by, pgcrypto→hash littéral). **Décisions USER 2026-06-07** : platform_admin non seedé, indivision visible (coproprietaires p_own_indivision), votes AG par RPC (pas de own-write), events lecture-copro/écriture-mgr. Gate begin/rollback A1→A6 (parcours gestA, 3/3 refus 42501 cross-cabinet, SELECT/INSERT RLS cloisonnés, non-récursion, RIB protégés), vitest 75/75.
- [x] **0035 vues-transverses — LIVRÉ + commité** (`9f03f95`). 4 vues (security_invoker=true) : `v_coproprietaires_overview` (R40 0 doublon, sous-requêtes scalaires), `v_owner_statement_by_lot_detail` (running_balance/lot), `v_alur_fund_summary`, `v_alur_transfers_history`. Revue (code-reviewer) → 3 Major corrigés : solde+total_tantiemes bornés is_primary (cohérence indivision), total_tantiemes borné à la clé générale canonique, v_alur_fund_summary budget courant status='validated'. Vues mortes jamais recréées. Gate (4 vues, 0 morte, R40, cohérence ALUR), vitest 75/75.

## ✅ LOT FONCTIONS COMPLET (0023→0035, 13 migrations) — Phase 0 acceptée 2026-06-07
Reste : merge branche `phase0-db-rebaseline` → main (décision USER) ; refacto edge email_webhook → drop île notif AG (Phase 3) ; portail copropriétaire UI (Phase 3). Dettes : voir DEFERRED_USER_DECISIONS.md (bascule RLS fail-open prod, contrainte clé générale unique, pgcrypto with schema).

## Récupérer 0024 à la reprise (run v2 resserré)
1. Lire `…\tasks\wdlza4aa3.output` → `result.synthesis.migrationSql` (dé-échapper si entités `&gt;`/`&lt;`). (run v1 `w2b9skqdb` abandonné.)
2. Écrire `supabase/migrations/0024_triggers_integrite_gl.sql` (PowerShell : `ConvertFrom-Json` + `[IO.File]::WriteAllText`).
3. **Vérifier soi-même** : chaque colonne vs vraies tables (0012/0013/0014/0016/0017/0019/0020/0021/0022) ET **aucun doublon** avec l'existant (un relecteur peut échouer à rendre le schéma — refaire sa lentille à la main, cf. 0023).
4. Harnais : `db reset` + gate (UPDATE tx posted→RAISE ; entry 45x sans lot_id→RAISE ; insert déséquilibré→RAISE au COMMIT ; 2 periods open→violation ; comptage triggers) + vitest. Commit quand vert.
   - Resume workflow v2 si besoin : `{scriptPath:"…\\author-0024-gl-filet-v2-wf_d09fcd2e-21a.js", resumeFromRunId:"wf_d09fcd2e-21a"}`.

## Gotchas (réutilisables pour 0024→0031)
- **ACL** : Supabase GRANT execute à `service_role` PAR DÉFAUT à la création → `REVOKE … FROM service_role` explicite si on veut « authenticated only ».
- **Déjà présent, NE PAS recréer** : `set_updated_at` + tous `trg_*_updated` ; `handle_new_user`/`on_auth_user_created` ; `tr_lot_copro_consistency` (0008), `tr_lot_owner_copro_consistency`+`tr_lot_owner_shares_sum` (0010), `tr_rkl_copro_consistency` (0009), `tr_invitation_copro_consistency` (0011), `check_tiers_domain_ids` (0015).
- **Reconstruction propre** : enums déjà cible en 0003 → ne pas migrer d'enum ; ne jamais créer les objets abandonnés (user_is_council_member, document_access, bespoke AG, mono-clé…).
- **Harnais** : lancer depuis `Co-Pro-Flex/` ; CLI `npx --no-install supabase …` (v2.105) ; container DB `supabase_db_Co-Pro-Flex` ; DB `postgresql://postgres:postgres@127.0.0.1:54322/postgres`. Services imgproxy/edge/pooler arrêtés = orthogonal.
- **Extraction SQL d'un workflow** : `(Get-Content -Raw out.json|ConvertFrom-Json).result.synthesis.migrationSql` puis `[IO.File]::WriteAllText(path,$sql)` (gère le \n ; vérifier entités HTML).

## Suivi follow-ups (code-review 2026-06-06)
- 🟡 **Micro-opt NON fait (optionnel)** : `trg_cff_ledger_required` (0026) est `after insert or update` sans filtre de colonnes → refire à chaque UPDATE de `call_for_funds`. Le restreindre à `after insert or update OF status, ledger_tx_id` (safe : la contrainte ne peut être violée que par ces 2 colonnes) éviterait un SELECT inutile par UPDATE. Pré-existant, non bloquant.
- ✅ **Fait** : bug `owner_name`/`is_company` corrigé `ac9c022` (v_unpaid_by_lot + v_payments_overview).
