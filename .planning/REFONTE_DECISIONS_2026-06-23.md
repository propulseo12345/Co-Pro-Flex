# Décisions de refonte CoProFlex v2 (grilling 2026-06-22 → 23)

> Décisions actées en session de grilling. Source de vérité des choix structurants de la reconstruction Next.js → TanStack Start.

## Principe directeur
- **On ne copie JAMAIS v1 bêtement** : chaque feature reconstruite = occasion de **corriger les bugs et améliorer** (UX manager-first, justesse comptable, dette, conformité). La **parité** porte sur le **résultat comptable** (le moteur sort la même chose ou mieux), pas sur la reproduction des défauts.

## Structure & stratégie
- Reconstruction **from-scratch** dans `v2-tanstack/` (même repo ; `e2e/` et `supabase/` partagés à la racine). Ancien `src/` **gelé** (référence, jeté au go-live). **Bascule d'un bloc au go-live** (pas d'utilisateurs aujourd'hui).
- **Moteur Supabase identique/partagé** = parité au niveau base. **UI entièrement redessinée** (manager-first).
- **« Feature finie » = écran neuf + test e2e de parité** (clic réel Playwright sur v2 → `audit_finance_integrity = 0` + montants attendus en base).
- **Deux phases** : (1) valider/réparer le **moteur sans UI** (SQL, BEGIN/ROLLBACK) ; (2) reconstruire **écran par écran**.
- **Gouvernance** : Claude applique les migrations via MCP Supabase, protocole 4 étapes (revue cascade → BEGIN/ROLLBACK → apply → vérif audit=0), directement sur le live (données de test jetables).
- **Un seul chemin par feature** (doublons EN/FR morts). **Une seule** biblio UI neuve.

## Stack (verrouillée)
- TanStack Start **1.168.26** + react-router **1.170.16** (famille `@tanstack/*` épinglée EXACTE), React 19, Vite 8, TypeScript strict (jamais `any`).
- **CSS Modules uniquement** (zéro Tailwind). Tokens = `globals.css` (source unique).
- Supabase (`@supabase/ssr` + `supabase-js`), **React Hook Form + Zod**, **`@tanstack/react-query` natif** (le `useAsyncData` maison meurt).
- Polices self-hostées (`@fontsource-variable` Inter/Manrope + Fira Code).
- **Server functions TanStack par défaut** pour les opérations déclenchées par clic ; **Edge Functions réservées** au cron (relances) + webhooks ; vérif feature par feature.
- ✅ `v2-tanstack/` échafaudé (100 dossiers + châssis Supabase/auth), **installe et build vert** (client + SSR).

## Cadrage (A)
- **A1** : 3 espaces **avant go-live**, ordre **gestionnaire → copropriétaire → conseil** ; sécurité 3-rôles figée dès le départ.
- **A2** : conseil syndical = **extension du portail copropriétaire** (cran de droits en plus, pas un espace séparé).
- **A3** : périmètre V1 = **parité + 5 P0 + (PPT, DPE, fiche synthétique, règle 3 mandats art.22)** ; reste P1 en backlog.
  - **P0** : extranet copro (=portail) · boucle annuelle recettée · pièces convocation forcées · état daté branché · mise en demeure LRE réelle.
  - **P1 backlog** : mandat syndic + honoraires · immatriculation RNC · plan d'apurement · mise en concurrence contrats · 105 ALUR éclaté par lot · RGPD formalisé · cohérence inter-annexes.
- **A5** : **Phase 1 = réparer les 3 chaînes cassées** (budget non matérialisé depuis l'AG ; `action_type=NULL` ; cut-off 408/486 sans appelant) **+ prouver le cycle annuel complet en base**, avant toute UI.
- **A6** : ordre de reconstruction **par dépendance de données** : châssis → onboarding → copro/lots → finance → appels → factures → paiements/banque → impayés → AG → clôture/annexes → ventes/état daté → périphérie → dashboard → portail copro.
- **A7** : recette sur la **golden « Domaine des Tilleuls »** + remise du seed `create_test_copro_seeded` à la forme golden (drift 4 lots/1000 → 18/10000). **Playwright sur v2**, feature par feature (PAS sur l'ancienne app).

## Châssis & technique (B) — grilling 2026-06-23
- **B1 — Versions = ÉPINGLAGE EXACT complet** : retirer les `^` sur toute la chaîne critique (cœur TanStack déjà figé + **Vite, TypeScript, react-query, vitest, React**) ; `npm ci` en CI ; MAJ par **lots manuels validés par le golden**. *(Constat POC : cœur TanStack épinglé ✅ mais build — Vite 8 / TS 6 / react-query / vitest — encore flottant.)*
- **B2 — Couche data = react-query PARTOUT + loaders TanStack Start** : **un seul pattern** (décision #7), natif au framework ; **0 `useAsyncData` copié** de v1. *(POC quasi vierge côté data → liberté totale.)*
- **B3 — Auth = MIDDLEWARE GLOBAL deny-by-default** (liste blanche, refuse tout sauf login/vitrine), validation serveur `getUser()`. Les `beforeLoad` restent pour le **confort UI**, pas la sécurité. Ferme la faille **0085**. *(TODO déjà inscrit dans `v2-tanstack/src/routes/_app/route.tsx`.)*
- **B4 — Critère de démarrage du châssis = 3 tests e2e BLOQUANTS** : (1) anonyme sur **toutes** les routes protégées → redirige login, 0 contenu ; (2) route bidon **sans garde** → anonyme redirigé quand même (preuve middleware) ; (3) RLS : vrai user > 0, faux user 0. **Rouge → on ne migre rien.**
- **B5 — RLS ON + FORCE** sur les tables sensibles **dès la baseline v2** *(constat live : 82/87 en ON SANS force)* **+ 2ᵉ cabinet de test PERMANENT** (étanchéité prouvée en réel) **+ fermer la faille `platform_admin`** (E1) avant tout vrai 2ᵉ tenant.
- **B6 — Hébergeur = REPORTÉ, build PORTABLE** (Docker/Node, 0 lock-in Vercel). **Supabase = backend dans TOUS les cas** (la DB, pas l'hébergeur du front). Vercel pour les aperçus pendant la reconstruction ; **VPS OVH dédié = candidat cible** à trancher **avant les vrais clients**. **Souveraineté réelle = région EU du projet Supabase** (pas Vercel/OVH). *(Docker ≠ Supabase : Docker = emballage standard universel.)*
- **B7 — Env + polices** : `NEXT_PUBLIC_*` → `VITE_*` (client) / `process.env.*` (serveur), **audit exhaustif** 1-par-1 (0 `undefined` runtime) ; **polices SELF-HOSTÉES** via `@fontsource` (RGPD : pas d'IP visiteur → Google ; déjà installées dans le POC).
- **B8 — i18n = AUCUN** (mono-langue FR, libellés légaux français). Les doublons EN/FR du code = **dette de nommage** (#7), **pas** de traduction. i18n ajouté plus tard SI international.
- **B9 — CI anti-FAUX-VERT complet** : CI **Linux** (pas Windows exit-0) + **health-check HTTP** (until-200) + pipeline strict `npm ci → tsc → build → health → e2e → vitest → gates` + **preuve EN BASE** (jamais un 200) + **suite e2e unique jouée contre Next ET Start** (golden de parité). **+ boucle d'auto-réparation Playwright avec garde-fous** (cf. DoD § transverses).
- **B10 — Stratégie SEED = « seed-to-golden » (USER, affine la reco)** : **abandon de la copro-jouet** (4 lots/1000/3 clés, non reprise) ; on **réussit d'abord la création E2E de la copro** golden « Domaine des Tilleuls » (18 lots / 10 000 / 6 clés), **puis on capture la SEED depuis cette copro réellement créée** (seed = produit du vrai chemin, pas une fixture écrite à la main) ; les tests suivants consomment cette seed. Garde **« seed == forme golden »** = filet de non-régression. *(Ordre : E2E création fonctionnelle → capture seed → tests dépendants. cf. [[golden_exhaustif_plan]], A7.)*

## Présentation & Visibilité — décisions UX refonte v2 (slugs dédiés)
> **Référentiel dédié `UX-*` / `VIS-*`** pour bannir toute collision avec les « C » d'autres chantiers. ⚠️ **« C1…C9 » sont ambigus** : ils désignent les **paiements/AG dans `DECISIONS.md` (finance)** ET le design dans `REFONTE_QUESTIONS_DEMAIN`. **Règle d'or : un chantier = un préfixe unique.** On ne touche JAMAIS aux « C finance » (trop référencés). Validé via MAQUETTE `.planning/design/maquette-accueil-copro.html` (approuvée USER « parfait »).
>
> | Slug | ex-réf | Décision |
> |---|---|---|
> | `VIS-3CERCLES` | C1-cadrage | 3 cercles de visibilité |
> | `UX-POLISH` | C2-cadrage | langage visuel + polish premium |
> | `UX-DS-SOURCE` | C1-questions | source design-system |
> | `UX-LIGHT` | C2-questions | light theme |
> | `UX-HUBS` | C3-questions | 6 hubs métier |
> | `UX-NAV` | C4-questions | navigation |
> | `UX-WORKLIST` | C5-questions | accueil = file de travail |
> | `UX-SWITCHER` | C6-questions | switcher copro |
> | `UX-MOBILE` | C7-questions | cible mobile |
> | `UX-A11Y` | C8-questions | accessibilité |
> | `UX-COMPONENTS` | C9-questions | composants à figer |

- **VIS-3CERCLES — 3 cercles de visibilité** : Gestionnaire = tout (cabinet) ; Copropriétaire = son périmètre perso + **comptes collectifs de sa copro** (budget, 5 annexes, trésorerie, charges) + docs collectifs ; Conseil = + cran contrôle art.21 (grand livre détaillé, **impayés nominatifs**, devis/contrats). **Seule limite RGPD** : le détail nominatif des soldes des AUTRES copros = conseil + gestionnaire uniquement. RLS = le gardien. *(détaillé en D69)*
- **UX-POLISH** : garder le **langage visuel actuel** comme socle, **polish → premium & mieux organisé** ; preview `claude_design` ; skill de front en CSS Modules.
- **UX-DS-SOURCE ✅** : la maquette part des tokens de `design-system.md` (dark saturé, manager-first) → **`design-system.md` = source unique** ; aligner `globals.css` dessus, supprimer les sources divergentes (§ design de `modules.md`, `da-preview.html`).
- **UX-LIGHT ✅** : light theme **gelé en v2** (dark-first, cohérent maquette) ; réintroduit post-V1 si demande client.
- **UX-HUBS ✅ VALIDÉ (maquette)** : architecture d'info au niveau copro = **6 HUBS MÉTIER + Accueil** — **Accueil · Copropriété** (identité + bâtiments + lots + clés + tantièmes + copropriétaires, cf D62) **· Assemblées · Finance** (+ Factur-X) **· Maintenance** (+ PPT/DPE) **· Documents & comm. · Recouvrement** (impayés + relances + contentieux + ventes/état daté). « Conformité 2026 » dissoute, « Contentieux » fondu dans Recouvrement. Objectif **2 clics pour 90 % des actions** ; remplace les **~12 zones éclatées** actuelles.
- **UX-WORKLIST ✅ VALIDÉ (maquette)** : l'accueil (cabinet ET copro) ouvre sur **« ce qui demande une décision »**, daté et chiffré — pas un tableau de bord passif. Ordre validé : **conformité légale en tête** (D70) → **KPIs essentiels** (GL, D68) → **« À traiter »** trié par priorité → activité récente discrète ([[clean_and_simple_principle]]).
- **UX-NAV ✅ (illustré maquette)** : UN seul système = **sidebar 6 hubs + en-tête contextuel réduit** (switcher copro `UX-SWITCHER`/D66 + recherche + notifications + user réel). Fin du doublon HighBar/UnifiedSidebar (même `MODULES`).
- **UX-COMPONENTS — amorcé (maquette)** : TopBar contextuelle, KPI strip, **carte fonds travaux 2 faces** (D67), panneau « file de travail », badges sémantiques, boutons primary/ghost, pastilles de priorité — tous en **CSS Modules** normés (jamais Tailwind), réutilisés par les 3 espaces.
- **UX-MOBILE ✅** : **desktop-first pour le travail + vue LECTURE allégée mobile** (gestionnaire) ; **portail copropriétaire PLEINEMENT responsive**. Le responsive est un **chantier structurel** (drawer/hamburger, tables à colonne figée), pas un patch ; la maquette amorce (sidebar réduite < 1100px).
- **UX-A11Y ✅** : **WCAG 2.1 AA pragmatique** (focus clavier visible, focus-trap modales/drawers, toasts `aria-live`, contrastes AA, cibles ≥ 44px, `prefers-reduced-motion`), centralisé dans les composants partagés (`UX-COMPONENTS`) ; **pas** de certification RGAA formelle (SaaS B2B). **Soin renforcé sur le portail copro** (grand public). → **Décisions UX refonte COMPLÈTES.**

## Sécurité & justesse (E) — en Phase 1
- **E1** : corriger l'**escalade `platform_admin`** + activer **RLS FORCE** sur les 87 tables.
- **E2** : **unifier la source de vérité des créances sur le GRAND LIVRE** (impayés / dashboard / état daté dérivent du solde 45x, plus du compteur parallèle `call_for_funds_lines`) + invariant de contrôle. Divergence prouvée live : 4950 (GL) vs 4450 (compteur).

### Bloquants détaillés — grilling 2026-06-23 (réf. section E de `REFONTE_QUESTIONS_DEMAIN`, E1→E14)
> ⚠️ Double numérotation E : ci-dessus E1 (platform_admin) = E1-questions ✅ ; E2 (créances GL) = **E6-questions** ✅. Ci-dessous = numérotation du fichier QUESTIONS (suffixe `-q`). E5-q (faux-vert) largement couvert par B4/B9/B10 + DoD.
- **E2-q (edge functions) ✅** : recâbler la logique métier en **server function → RPC SQL** ; **GARDER une poignée d'edge functions** pour le **cron** (relances, alertes d'expiration contrats/diagnostics) **+ webhooks** (banque GoCardless, statut emails). **RECRÉÉES PROPREMENT en v2 (USER)** : pas confiance aux ~27 edges v1 (jamais déployées → 0 sur le live), elles meurent avec v1. Cron chez **Supabase** = renforce la portabilité B6. Règle « qui déclenche ? » : utilisateur → server fn ; horloge → cron edge ; service externe → webhook edge.
- **E3-q (emails) = BREVO (USER)** : fournisseur **français** (souveraineté FR cohérente OVH + EU Supabase, support FR, **email + SMS** pour relances). Recréé proprement (pas le code Resend v1). À configurer : **clé API Brevo** (secret serveur) + **DNS `coproflex.fr` (SPF/DKIM)** sinon 0 email ne part. ⚠️ email « envoyé » **jamais simulé** (cf. D60 : sinon les délais légaux ne courent pas).
- **E5-q (faux-vert) ✅ ABSORBÉ** : aucune action neuve — entièrement couvert par **B9** (CI anti-faux-vert) + **B10** (gate seed-golden) + **DoD** stricte.
- **E7-q (secrets) ✅ reco RÉVISÉE (analyse Claude)** : vérif git → **aucun secret tracké** (`.env*` ignoré ✅) → « rotation immédiate de tous » **ÉCARTÉE** (rien n'a fui via git). Vraies actions : secrets de **PROD** dans le coffre de l'hébergeur (jamais un fichier) ; **retirer le compte démo `password123`** du bundle client avant vrais clients ([[demo_account_login]]) ; base neuve v2 = secrets neufs propres de facto. Rotation seulement si fuite suspectée.
- **E8-q (dates déterministes) ✅** : bannir `new Date()`/`Date.now()` dans toute fonction produisant une **date métier** (tx_date, issue_date, due_date, date d'effet, exercice) → **horloge métier injectable** (`asOf` / `getBusinessDate(periodId)`) branchée sur la période ouverte. Garder `created_at` (horodatage technique). Crucial : calculs justes + tests reproductibles (golden déterministe).
- **E12-q (chaînes AG→finance) ✅ = cœur Phase 1 (A5)** : réparer les 3 ruptures (budget non matérialisé depuis l'AG · `action_type` non câblé · cut-off 408/486 sans appelant) + remplir les payloads (mode, council_members, contract_id, dates) + `validate_ag_variables` en pré-vol PV. **Tranches verticales prouvées e2e** (pas re-livrer le SQL déjà fonctionnel).
- **E13-q (assainir migrations) ✅** = consignes du squash base neuve v2 ([[db_neuve_v2_squash_clean]]) : exclure les outils de test (`force_delete_test_copro`), clarifier le trou 0074, **ne jamais recréer** `post_call_for_funds` (banni), `post_exceptional_call_for_funds` = seul appel hors-budget restant ; **Security Advisor à 0** après chaque DDL.
- **E4-q (annulations / correction d'erreur) ✅ — règle « stylo, jamais gommer » (USER)** : on annule en écrivant la **ligne INVERSE (contre-passation)**, jamais en effaçant (erreur + correction restent visibles, immutabilité) ; un **exercice validé/clos est INTOUCHABLE** → erreur corrigée sur l'**exercice en cours** (régularisation datée). **Conception unifiée** : **1 porte d'entrée « Annuler »** → routage par nature → 4 fonctions métier propres (`reverse_payment` / `cancel_call_for_funds` / `cancel_supplier_invoice` / `reverse_period_cutoff`) + **socle d'invariants commun** (contre-écriture équilibrée, **interdit sur période approuvée**, `audit_finance_integrity=0`, **motif obligatoire**). **`reverse_ledger_transaction` (extourne brute) → INTERNE only** (la « gomme magique » retirée de l'UI) ; **`unallocate_payment` supprimé** (on contre-passe + re-saisit) ; `cancel_stale_reminders` = hors GL (nettoyage relances). Sécurisé **AVANT** toute exposition UI. *(7 voies hétérogènes → 1 système sûr.)*
- **E9-q (état daté) ✅ — consignes de CONSTRUCTION v2 (USER : on ne « supprime » pas, on ne reproduit pas)** : l'état daté = **photo figée à la date d'effet** (art. 5), distincte du solde live. Construire à neuf : (1) calcul « à venir » **scopé à la période de référence** (on ne reproduit pas le cumul multi-années faux qui gonfle la Partie 3) ; (2) afficher **« situation arrêtée au [date] »** (lève la fausse incohérence solde-live vs état-daté-figé) ; (3) **un seul moteur propre** (on ne reproduit pas le chemin bancal `sales/api.ts` = INSERT brut + statut fantôme `final_etat_generated`). Rien à « supprimer » : le front v1 meurt gelé.

## Onboarding / Reprise de mandat (D1→D7)
- **D1** : **deux chemins explicites** « Créer » / « Reprendre » dès le portefeuille + **reprise mi-année dans V1**.
- **D2** : **un seul chemin** = RPC `set_opening_balance` (tag `opening_onboarding`, **vérifié sur le live**) ; **supprimer le writer front doublon** ; l'alerte « reprise à terminer » se base sur `opening_onboarding` + résidu 471/472 ≠ 0. (Aucune migration de données : 0 ligne opening sur le live.)
- **D3** : étapes Documents / Contrats / Carnet = **câblées en reprise** (réutilisant les modules Maintenance/GED), **masquées en création** ; corriger le compteur d'étapes. *(Vérifié : code mort aujourd'hui.)*
- **D4** : **résumé par solde par lot** (pas de reconstitution écriture par écriture des appels historiques) ; **corriger la validation** du nombre d'appels déjà émis (lire le réel en base, pas une saisie libre). *(Vérifié : `alreadyDone` non validé.)*
- **D5** : solde bancaire saisi **une seule fois** (à la reprise, vraie écriture d'ouverture) ; **juste l'IBAN à la création** ; supprimer le champ `initial_balance` fantôme. *(Vérifié : double saisie, métadonnée jamais lue par la compta.)*
- **D6** : ✅ finalisation = **statut « reprise provisoire » + aide au solde CHIFFRÉE** (proposer l'écart probable : banque/ALUR/report). Non bloquant DÉJÀ en place + alerte portefeuille (`RepriseAlertCard`) DÉJÀ là ; à ajouter = vrai statut copro + `EquilibreIndicator` actif (montant + origine chiffrée vs nudge texte). *(Vérifié : Step8 non bloquant, alerte persistante existante.)*
- **D7** : ✅ connexion bancaire **GoCardless conservée en v2** (lecture seule IBAN+solde à l'onboarding, déjà construite) → **rapprochement V2 directe + filet manuel toujours dispo** (consentement persisté + cron quotidien + ingestion idempotente + validation 1-clic ; écrit au GL ⇒ revue adversariale). Visibilité comptes copros = **3 niveaux** : copro (soldes + budget + SA situation, pairs anonymisés) / conseil syndical (détail, contrôle art.21) / syndic (complet). Aligner avec D5 (solde = écriture seulement en reprise). Corriger la **faille IDOR** requisitionId↔copro. *(Vérifié : intégration RÉELLE, onboarding-only aujourd'hui.)*

## Copropriétaires & Lots (D8→D12)
- **D8** : ✅ **indivision = capacité dormante à activer** (`lot_owners` a déjà `share_percent`/`is_primary`/dates, mais UI v1 force 100 %/primary mono-propriétaire). v2 : fiche lot carte « Propriétaires » + bouton **« Gérer l'indivision »** (quote-parts somme=100 %, `is_primary` = **mandataire commun** art.23) ; cas mono-propriétaire **sans friction** (divulgation progressive). Modale de **création de lot simplifiée** (Essentiel visible + Détails repliés + 1 propriétaire). *(Vérifié : aucun écran ne crée d'indivision aujourd'hui.)*
- **D9** : ✅ **source unique du tantième = la ligne de clé** (`repartition_key_lines.weight`). `lots.tantiemes_generaux` = **doublon à supprimer/dériver**. 🔴 Bug latent prouvé : AG (feuille de présence + votes) lit `lots.tantiemes_generaux` alors que la finance lit la clé → divergence (copro créée par onboarding = majorités à 0). + **Capacité manquante** : une résolution se vote sur **la clé qui la gouverne** (pas toujours la générale ; charges spéciales = clé spéciale + seuls les concernés, art.24 II) → ajouter `repartition_key_id` sur `ag_resolutions` (**NULL = générale, défaut, choix MANUEL**), majorité calculée sur le **total de la clé concernée**. *(Vérifié : aucun lien résolution→clé aujourd'hui.)*
- **D10** : ✅ **validation tantièmes = total déclaré par clé + validé en modale par le gestionnaire** (pas de total universel imposé : chaque copro a sa base). Total **stocké sur la clé** (utile état daté/annexes). Après validation : **on ne bloque pas** la gestion. **Filet non bloquant mais visible** : avertissement permanent si la somme s'écarte du total déclaré OU si un lot concerné est sans tantième (éviter l'exemption/le vote silencieux). *(Vérifié : aucune garde serveur aujourd'hui, juste `is_complete` en lecture.)*
- **D11** : ✅ **cycle de vie lot/tantièmes à 2 canaux** (englobe l'archivage et pré-règle D63) :
  - **Changement de propriétaire = la VENTE, déjà automatique** : `validate_mutation` bascule `lot_owners` (clôture ancien / ouvre nouveau, daté, zéro GL). Capacité existante à valoriser, pas à reconstruire.
  - **Modification structure/tantièmes = un MODIFICATIF daté/versionné** (à construire) : ajouter/retirer des lots + re-répartir les tantièmes avec **date d'effet**, sur les colonnes **`repartition_keys.valid_from/valid_to` (dormantes, lues par personne aujourd'hui)**. Les exercices passés gardent leurs anciens tantièmes.
  - **L'archivage est la CONSÉQUENCE** d'un modificatif (lot retiré = archivé à la date d'effet, garde son histoire), pas un geste séparé. Ajouter `archived_at`/`is_active` sur `lots`.
  - **Protection base CONFIRMÉE** (le doc disait à tort « hard delete sans garde ») : `ledger_entries`/`payments`/`call_for_funds_lines`/`mutations`/… sont en RESTRICT/NO ACTION → un lot avec histoire ne se supprime déjà pas. À **harmoniser tout en RESTRICT** + front clair (bouton « Archiver » vs « Supprimer » selon références) au lieu de l'erreur Postgres brute. ⚠️ `repartition_key_lines` est en CASCADE (suppression silencieuse de tantièmes) → géré par le canal modificatif + l'avertissement D10.
  - **Découpe de périmètre (validée)** : *intégrité dès la V1* (date d'effet, versioning, archive, re-validation du total) ; *cérémonie en V1+* (rattachement résolution AG, pièce notariée, assistant guidé, frise d'historique). Version simple = **sous-ensemble** du flux complet, jamais un chemin parallèle. *(Vérifié : mutation→lot_owners câblé ; versioning des clés dormant.)*
- **D12** : ✅ **retirer l'onglet « Locataires » fantôme** (gestion locative = hors métier syndic ; aujourd'hui liste vide + « disponible prochainement »). **MAIS garder une info d'occupant LÉGÈRE** (pas de bail/loyer) : `occupancy_type` sur le lot (propriétaire occupant / loué / vacant) + contact occupant facultatif (nom/tél/email), pour communications & accès. `coproprietaires.is_resident` existe déjà (à réutiliser). Afficher sur la **fiche lot**, pas un onglet copro dédié. *(Vérifié : onglet 100 % fantôme, renvoie `[]`.)*

## Finance — Appels, émission & recouvrement (D13→D16)
- **D13** : ✅ **aucun appel arbitraire** : tout appel porte une **justification** (budget / résolution travaux / avance / urgence art.18). « Budget seul » est trop étroit → **construire les routes manquantes** : `post_exceptional_call_for_funds` (travaux hors budget → 450-2/702) + route **avance art.35** (→ 450-3/1031). **Cœur partagé** (helper + 1 RPC/nature). Urgence art.18 (appel avant vote, ratifié après) = **V1+**. *(Vérifié live : seules `post_budget_call_for_funds` + `cancel_call_for_funds` existent.)*
- **D13-bis — Les appels NAISSENT des décisions d'AG (pilier auto-population)** :
  - Mécanisme existant : résolution → `ag_pending_actions` (porte `resolution_id` + `action_type` + `payload`) → `activate_ag_decisions` (à l'étape PV) exécute et crée budget/ALUR/appels/conseil. L'enum `action_type` contient déjà **`CREATE_EXCEPTIONAL_CALL`** mais **l'exécuteur `post_exceptional_call_for_funds` manque** = **tuyau posé mais débranché** → à finir + brancher.
  - **Manuel = chemin secondaire** (provisions trimestrielles, avance hors nouveau vote), **même moteur partagé**.
  - **Traçabilité** : `call_for_funds` a déjà `budget_id` + `repartition_key_id` ; **ajouter un lien direct `resolution_id`** (l'appel affiche « issu de la résolution X de l'AG du … »).
  - **Modèle résolution→appel (montée en qualité, validée)** : la résolution porte **montant + nature (`resolution_type`) + clé (`repartition_key_id` à ajouter)** ; un **échéancier rattaché** (n appels : date + part %/montant) est **auto-généré** ; à la finalisation, **1 appel par ligne d'échéancier**, à la bonne date, rattaché à la résolution (récupère le montant + crée les dates).
  - **L'échéancier = plan RATTACHÉ à la résolution, PAS une « sous-résolution » votée** (mêmes modalités = même vote). UI = sous-bloc « Modalités d'appel », pas une majorité à part.
  - **Réutiliser/généraliser `budget_payment_schedules`** (a déjà date/montant/phase/`service_order_id`) → échéancier d'appels générique budget+travaux+exceptionnel, **+ `resolution_id`**. Un seul système d'échéancier.
  - **Sortir les valeurs financières du `variables` jsonb** vers des **champs structurés** (c'est de l'argent).
  - **UX « formulaire malin »** (clean & simple) : modèle pré-remplit nature/majorité/clé/échéancier + montant saisi ; échéancier **replié, pré-rempli** (divulgation progressive) ; 90 % des cas = modèle + montant + valider. **MAIS modèles NON figés** : résolution libre (vierge) toujours dispo + tout champ pré-rempli **éditable** (`is_customized` existe). *(Vérifié : `ag_resolutions` sans `repartition_key_id`, montant dans `variables` jsonb ; `budget_payment_schedules` existe.)*
- **D14** : ✅ doctrine échéancier = **défauts intelligents par nature, paramétrables** (budget → **4 appels trimestriels**, 1ᵉʳ jour du trimestre/doctrine ARC ; travaux → selon devis ou unique ; avance/exceptionnel → unique). **PAS de contournement pour les tests** (principe USER fort : *« on solutionne, on ne contourne jamais »*) → le golden garde **4 appels trimestriels** (l'« appel annuel unique » est ABANDONNÉ). Déterminisme **déjà résolu dans le moteur** : `post_budget_call_for_funds` fait l'arrondi cumulatif `sum(weight) over (partition by key_id order by lot_id)` puis `round(cumul)−round(cumul−w)` → **Σ lots = cible exacte** + ordre stable (lot_id unique) ; échéances par cumul aussi. Seule exigence = **UUID de lots FIGÉS dans le seed golden** → valeurs attendues **exactes sur TOUS les lots** (tolérance ±1 cent supprimée). Le centime résiduel est inhérent à la division de monnaie, alloué de façon déterministe. *(Vérifié live : moteur correct ET déterministe.)*
- **D15** : ✅ écriture GL posée à l'**émission** de l'appel (création = écriture, atomique ; **pas** de cycle brouillon→écriture). Échéancier figé à l'AG, puis **option B : chaque appel émis à SA date** (créance inscrite **au fil des trimestres**, art.14-1 ; **PAS** tout d'un coup à l'AG — le comportement onboarding actuel « 4 appels émis d'un coup » est à corriger). **Mode d'émission AU CHOIX du gestionnaire** : automatique (cron, à la date d'échéance) OU manuel (clic « émettre l'appel du trimestre »). Réglage porté par l'échéancier, défaut par copro/cabinet. *(Vérifié : `post_*_call_for_funds` poste l'écriture à la création.)*
- **D16** : ✅ relances **centralisées dans le module Recouvrement** ; grain = **solde global en retard par lot** (niveau J+15/30/60/90 piloté par l'**échéance la plus ancienne**), **abandon du grain par appel** (`call_id`/`call_line_id` sur `payment_reminders` → inutiles/informatifs ; on ne relance pas appel par appel). **Lettre consolidée par PERSONNE** (un copro à 3 lots = 1 courrier listant ses lots, pas 3). La **nature (courant/travaux/ALUR) reste pour l'imputation FIFO cloisonnée** des paiements, **pas** pour le grain de relance. Règles déjà configurables par copro (`payment_reminder_rules` : delay_days / canal / template). *(Vérifié : grain mixte lot+appel aujourd'hui ; vues `v_unpaid_by_lot` / `v_unpaid_with_reminders` existent.)*

## Finance — Factures fournisseurs & paiements (D17→…)
- **D17** : ✅ **strictement les 4 statuts SQL** `draft/posted/paid/cancelled`, pilotés par les écritures (draft=saisie · posted=comptabilisée D6xx/C401 · paid=payée D401/C512 · cancelled). **Supprimer le mock à 5 statuts** (BROUILLON/A_VALIDER/VALIDEE/A_PAYER/PAYEE = code mort dans `src/shared/mock/finance.ts` seulement). Les intermédiaires sont redondants (A_VALIDER = déjà `draft`, A_PAYER = déjà `posted`). Pré-câble D18. *(Vérifié : enum réel = 4 statuts ; les 5 n'existent qu'en mock.)*
- **D18** : ✅ **2-temps** : saisie (`draft`) → validation (`posted`, écriture D6xx/C401 via `validate_supplier_invoice`) → paiement (`paid`, D401/C512 via `post_supplier_payment`). 🔴 **Corriger la faille** : retirer `status` des champs modifiables par `updateSupplierInvoice` (il peut basculer en `posted` **sans écriture** = facture qui ment / rupture silencieuse) → le statut ne change **QUE** par les RPC qui posent l'écriture. **Audit réel** : enregistrer qui valide via `auth.uid()` (pas « Utilisateur courant » en dur). **PAS de séparation des tâches / 4-yeux** (inadapté au syndic solo) → juste un **pop-up de confirmation** à la validation (geste délibéré). *(Vérifié : `validate_supplier_invoice` poste l'écriture ; `updateSupplierInvoice` accepte `status` = faille.)*
- **D19** : ✅ **ventilation multi-poste** (capacité dormante : `supplier_invoice_lines` existe déjà avec `account_id` + `repartition_key_id` + `budget_line_id` + **`operation_id`** + HT/TVA par ligne ; front fait du mono-poste). Requis pour **annexes légales** (ventilation par compte) + **travaux par opération**. **Clean & simple** : défaut **1 ligne**, bouton « + Ajouter une ligne » pour ventiler ; garde de cohérence **total en-tête = somme des lignes**. *(Vérifié : table de lignes complète en base, sous-exploitée par le front.)*
- **D20** : ✅ **option B — réalisé dérivé du GRAND LIVRE** (somme des écritures classe 6 par compte + exercice) ; une facture comptabilisée → consommé ↑ **auto**. **`budget_expenses` déprécié comme source de réalisé** (aujourd'hui `v_budget_lines_overview.validated_spent` vient de `budget_expenses` = 2 mondes parallèles → facture postée au GL ⇒ consommé affiché = 0). **Distinction confirmée par l'expert** : réalisé = GL ; **engagé** (devis/OS signés non facturés, hors GL) = couche séparée → affichage **voté / engagé / réalisé / reste**. *(Vérifié : la vue lit `budget_expenses`, pas le GL.)*
- **D21** : ✅ **choix du compte de trésorerie au règlement** : étendre `post_supplier_payment` avec `p_bank_account_id` ; la modale de paiement liste les vrais comptes 512 (courant / fonds travaux) avec **solde réel** + IBAN bénéficiaire. **Indispensable au rapprochement bancaire (D7)** : créditer le compte d'où l'argent part réellement. (18 copros = 40 comptes 512, ≈2/copro → le choix a du sens.) Vrai virement **SEPA / lot de paiement = P1**. *(Vérifié : `post_supplier_payment` sans paramètre de compte → crédite un 512 par défaut.)*
- **D22** : ✅ **supprimer l'edge `pay_supplier_invoice`** (morte : `paySupplierInvoice` défini jamais appelé, auth cassée, 0 edge déployée sur le live) + son wrapper. **Garder la RPC `post_supplier_payment`** (cerveau atomique). En v2 : navigateur → **server function TanStack** (porte) → **RPC** (double écriture atomique) → DB ; **pas d'edge**. *(Vérifié : wrapper jamais appelé ; doctrine v2 edge=cron/webhooks.)*

## Finance — Clôture / affectation / annexes (D23→…)
- **D23** : ✅ **répartition/affectation du résultat = AUTO à l'approbation des comptes en AG** (résolution `APPROVE_ACCOUNTS` → `regularize_period`, dans l'activation atomique) — seule voie juridiquement correcte (l'affectation EST une décision d'AG). **DÉJÀ CÂBLÉ en base** (la fiche disait à tort « regularize_period appelée nulle part »). Ordre : **à-nouveau (`open_next_period`) AVANT affectation (`regularize_period`)**. Reste = exposer proprement au front. *(Vérifié live : `regularize_period` existe ; `activate_ag_decisions` l'appelle ET gère `APPROVE_ACCOUNTS`.)*
- **D25** : ✅ **exposer le choix report / remboursement** de l'**excédent courant** à l'affectation : **défaut = reste sur le 450** (apuré sur le 1ᵉʳ appel N+1), **option = remboursement** (couvre le copro qui vend/part ou un remboursement voté en AG). Confirme WP5.3. **Distinct de l'apurement travaux** (compte 12 gelé volontairement, `settle_works_balance`). *(décision UX ; modèle déjà en mémoire [[affectation_resultat_copro]].)*
- **D26** : ✅ **câbler la chaîne de cut-off (droits constatés, charges à payer 408) en V1** — obligation légale (compta d'engagement, décret art.14-3). Écran cut-off dans l'assistant de clôture (avant figer), **pré-rempli par la couche « engagé »** (D20 : OS/devis signés non facturés), le gestionnaire ajuste + valide → écritures 408 sur l'exercice qui ferme, **contre-passées auto** à l'arrivée des vraies factures N+1. Branche les RPC orphelines `post_period_cutoff`/`reverse_period_cutoff` + corrige l'idempotence de l'annulation. *(Vérifié : fonctions existent, `period_cutoff_items=0` jamais tourné, aucun appelant front.)*

- **D24** : ✅ **flux de clôture orchestré + étapes de validation visibles** (un point d'entrée unique = activation AG ; séquence atomique figer → à-nouveau → affectation → approbation ; assistant « feu tricolore » par palier). Les 5 RPC existent (`close_period`/`open_next_period`/`regularize_period`/`approve_period`/`reopen_period`). **Cadrage v2 (corrigé par USER)** : le front from-scratch **ne reproduit pas** l'anti-patron UPDATE-direct-du-statut (`approvePeriod`/`rejectPeriod` restent dans v1 gelé, rien à « supprimer ») → le front passe UNIQUEMENT par le flux orchestré ; la **base propre ajoute une GARDE** interdisant tout changement de statut hors orchestration (bypass impossible par construction) + ne reprend pas le statut non canonique `'rejected'`. *(Vérifié : `approvePeriod` = UPDATE direct ; 5 RPC présentes.)*

## AG — préparation, votes, cycle (D27→…)
- **D27** : ✅ **un seul modèle canonique de vote par correspondance** (fin des doublons EN/FR `useVotesCorrespondance`/`useCorrespondenceVotes` + dossiers multiples + onglet préparation). **Une fonction PARTAGÉE** qui marche que ce soit le **gestionnaire** OU le **copropriétaire** qui saisisse (champ `source`/entered_by) → **préparer le chemin portail dès maintenant** (modèle + fonction), l'UI copro se branche plus tard sur le MÊME modèle (jamais un 3ᵉ). **Verrou de précédence** : un vote saisi **par le copro** est figé contre l'écrasement gestionnaire (lecture seule pour lui). **Modifiable jusqu'à J-3** (= délai légal réel, 3 jours francs avant l'AG, décret 67-223) via bouton « Modifier » (copro ET gestionnaire selon propriété) ; **figé après J-3**. Edge moteur à gérer : votant corresp. qui se présente / résolution amendée en séance. *(Vérifié : 2 hooks doublons + fragmentation.)*
- **D30** : ✅ **V1 = gardes SERVEUR jours francs (art.64, délai 21j) + plafond 3 mandats (art.22)** (promouvoir les vérifs front existantes au niveau serveur, non contournables). **Moteur de majorité SQL = SEUL juge** : l'écran affiche `get_ag_live_results`, on NE reproduit PAS le calcul front `checkMajority` (fin de la divergence 500/501 sur la passerelle art.26-1). **P1 (prestataires externes)** : LRE valeur probante d'envoi + signature électronique réelle du PV. *(Vérifié : plafond mandats + jours francs = front-only/avertissement ; `checkMajority` front ∥ `get_ag_live_results` SQL.)*
- **D31** : ✅ **temps réel serveur complet en V1** : persistance **fiable** des votes en base (fin du « fire-and-forget » + du `localStorage` projecteur) + **Realtime Supabase multi-appareils** (coût argent ~0, AG = trafic minime ; effort modéré partiellement déjà payé). **Re-votes passerelle = vrais enregistrements via `bridge_vote_id`** (fin du bricolage client `_dup_` non sauvegardé). Projecteur = **miroir lecture**, la base reste la référence → dégrade en douceur (peut laguer mais **zéro perte de vote**). *(Vérifié : `localStorage` projecteur, persistance fire-and-forget, `_dup_` non persisté.)*
- **D32** : ✅ **corriger le contrat d'erreur d'activation + garde anti-faux-vert**. Aujourd'hui `activate_ag_decisions` ne renvoie pas de bilan d'échec → `usePVPage` lit `result.failed` toujours undefined → **échecs silencieux**. **Mécanisme de correction (demandé USER)** : (1) chaque action a son **statut individuel** dans `ag_pending_actions` (faite/échouée + **raison lisible** + résultat) ; (2) **atomicité PAR ACTION** (savepoint/action) — une action échouée n'annule PAS les réussies (précise/assouplit D29 : pas « tout ou rien sur l'AG entière ») ; (3) la page PV **affiche le bilan** (X faites ✓ / Y échouées ❌ + raison humaine) ; (4) **« Réessayer les actions échouées »** rejoue UNIQUEMENT les non-terminées (**idempotent**, les ✓ sautées, pas de double budget) ; (5) `finalize_ag` **refuse** tant qu'il reste une action en échec / prepared=0 avec résolutions à effet. *(Vérifié : `usePVPage` lit `.failed` jamais renvoyé.)*
- **D28 (RUPTURE RACINE — la plus grave)** : 🔴 le front `createStandardResolutions` crée les résolutions standard **SANS passer `action_type`** ([create-standard-resolutions.ts:135-143](src/lib/ag/create-standard-resolutions.ts#L135)) → résolutions « muettes » (`action_type=null`) → `activate_ag_decisions` ne génère **AUCUNE** action → **tout le pilier AG→copro cassé à la source** (pas de budget/conseil/appels). ✅ **Solution** : en v2, créer les standards via la **RPC `create_ag_with_standard_resolutions`** (vérifié : elle pose bien `action_type`, atomique) — cohérent archi server fn→RPC. **+ Traçabilité (demande USER)** : chaque résolution affiche son EFFET via `action_type` + trail `ag_pending_actions` (avant : « créera le budget 2027 » ; après : « budget créé ✓ le jj/mm » / « ⚠️ échec »). **+ Résolutions personnalisées libres (USER)** : le gestionnaire crée les siennes — soit déclaratives (aucune action), soit déclenchant une action (type choisi **en langage humain**, pas le code brut) ; le chemin custom doit AUSSI porter l'`action_type` (ne pas refaire le bug). *(Vérifié : JS oublie action_type ; RPC SQL le pose.)*
- **D29 (RUPTURE 2)** : 🔴 `budgets.source_ag_id` existe mais **rien ne l'écrit** + `create_budget_from_ag_resolution` **n'existe pas** → à l'activation, la résolution budget cherche un budget inexistant → erreur 23503 → **rollback de TOUTE l'activation**. ✅ **Solution** : le **budget réel naît À L'ACTIVATION** via `create_budget_from_ag_resolution` (crée le budget depuis les postes de la résolution + écrit `source_ag_id`/`linked_budget_id`, atomique, avant `validate_budget` puis `SCHEDULE_BUDGET_PAYMENTS`). Données du budget = dans la résolution jusque-là (affichées en convocation) → **zéro budget fantôme** si AG annulée. Onboarding/1ʳᵉ année : **relier** au lieu de dupliquer. **+ Préparation assistée (USER)** : « reprendre le budget N-1 » pré-remplit les postes ; écran 3 colonnes **voté N-1 / réalisé N-1 (tiré du GL, D20) / proposé N+1**, pleine édition (ajouter/retirer/modifier postes & clés) ; comparaison affichée en convocation. *(Vérifié : colonne source_ag_id présente, fonction absente, validate_budget existe.)*

## Ventes / État daté / Mutations (D33→…)
- **D33 (expert — tranché par USER)** : ✅ **pas d'écriture spéciale de mutation** ; la dette du vendeur se solde par un **paiement classique**. Modèle : (1) `validate_mutation` = **aucune écriture** (juste le changement de propriétaire) ; (2) la dette/le crédit du vendeur est **affiché sur l'état daté** (Partie 1 débiteur / Partie 2 créditeur — DÉJÀ implémenté : `generate_etat_date_payload`/`create_etat_date_snapshot`, `balance_45x_by_nature`) ; (3) à la vente, le **notaire solde les arriérés sur le prix** → le syndic enregistre un **encaissement normal D512/C450** (chemin paiement standard) ; (4) **« clôture compte vendeur » = étape de CONTRÔLE** (vendeur doit finir à 0 ; résidu → recouvrement contre le vendeur parti). **ALUR (450-5) suit l'acquéreur, sans écriture ni remboursement** — reconfirmé. *(Vérifié : `validate_mutation` ne poste rien ; état daté porte le solde 45x ; aucune fonction de régularisation mutation.)*
- **D34** : ✅ **avertissement non bloquant** si le vendeur est débiteur à la validation de la mutation (la vente est un **fait notarié** — bloquer l'appli ne l'annule pas, crée juste un écart réalité/livres ; et la dette se solde par la vente, D33). **Solde réel du vendeur affiché EN ÉVIDENCE dès l'ouverture du dossier** (pas caché en modale). Protection réelle = **opposition art.20-II** (D36), pas le blocage.
- **D35** : ✅ **deux chemins + vrai enregistrement** pour l'acquéreur : choisir un **copropriétaire existant** (annuaire) OU créer une **nouvelle personne** ; à la validation → vrai `lot_owners` + fiche (évite les doublons si l'acheteur possède déjà des lots). **Abandonner le `buyer_draft` jsonb libre** (texte sans annuaire). *(Vérifié : `buyer_draft` jsonb confirmé, `buyer_owner_id` set à la validation.)*
- **D36** : ✅ **exposer l'opposition art.20-II** (capacité dormante : `record_mutation_opposition` / table `mutation_oppositions` câblées en base, **aucune UI**). Écran : le syndic notifie le notaire de **retenir la dette du vendeur sur le prix** (délai 15j) → sécurise l'argent de la copro (= LA protection de D34). **Pré-état daté = livrable distinct** envoyé au notaire (délai art.20), pas un brouillon interne.
- **D37** : ✅ **deux espaces RELIÉS** (USER — affine/écarte la fusion `UX-HUBS`) : **« Recouvrement »** (impayés / relances / litiges) SÉPARÉ de **« Ventes & Mutations »** (état daté / opposition / propriétaire), **reliés au point de contact « dette du vendeur »**. Colle aux 2 tâches mentales distinctes du syndic. **Ne pas reproduire la pile legacy** `VentesProvider`/`useSalesData` en v2 (parité d'abord). *(cf. [[vente_cleanup_chantier]] ; un seul moteur état daté.)*

## Maintenance / Prestataires (D38→…)
- **D38** : ✅ **modules distincts + vues croisées** sous Maintenance : **Carnet d'entretien** (historique) / **Contrats** (renouvellements) / **Assurances en section dédiée** (MRI, RC, dommages-ouvrage, **sinistres**, échéances de prime — obligatoire et à enjeu, ne pas noyer dans le carnet). Source unique = `contracts`/`tiers` Supabase (déjà migré + Realtime ; ne pas reproduire un store mock en v2). *(Vérifié : contrats déjà sur Supabase via `useMaintenanceData`.)*
- **D39** : ✅ **PPT = couche de planification** dans Maintenance, **au-dessus des opérations travaux réelles** (PAS une 3ᵉ source de vérité). Une ligne PPT votée+exécutée se **connecte** à l'opération réelle (`operation_id`) + fonds ALUR + vote d'AG ; **statut dérivé** (prévu → voté → en cours → fait), pas saisi à la main. **DPE → PPT** pré-remplit (travaux recommandés par le diagnostic énergétique).
- **D40** : ✅ **OS : estimation (engagé) saisie à la création + réalisé DÉRIVÉ des factures**. À la création, l'estimation (montant du devis) est saisie/reprise (prévision) ; le **réalisé démarre à 0 et se remplit** à mesure que les factures rattachées (`operation_id`) arrivent — **jamais tapé à la main**. OS affiche **estimé / réalisé / écart**, rattaché à une clé/poste budget (imputation engagé→réalisé). Cohérent D20/D26. *(petit dépannage : enregistrer une dépense réelle, pas un réalisé libre.)*
- **D41** : ✅ **email réel en V1 + templates par sujet** — l'infra EXISTE déjà (USER avait raison) : table `email_templates` (6 modèles, identifiés par `code`, `subject` + `body_html`/`text` + `available_variables`) + **Resend câblé** (`/api/mail/send` → `resend.emails.send`, enregistre `resend_id`). **Gap V1** : domaine vérifié (au lieu du sandbox `onboarding@resend.dev` qui ne touche que le compte) + wirer l'envoi OS/résiliation sur template+send (en v2 = **server function**) + **PDF téléchargeable en secours**. **LRE valeur probante = P1**. Transverse E3 (sert aussi convocations/relances/communications). *(Vérifié : `email_templates`=6 lignes, route Resend réelle mais FROM=sandbox dev.)*
- **D42** : ✅ **geler/masquer la marketplace en V1** (éviter l'effet « fausse démo » qui décrédibilise tout l'outil). Avis vérifiés prestataires + demandes de devis (RFQ) = **backlog**, sortie quand fonctionnel.

## GED / Documents & Communication (D43→…)
- **D43** : ✅ **séparer GED (fichiers) ↔ rendus comptables** : GED = vraie gestion de fichiers (PV, contrats, diagnostics) dans le hub **Documents & comm** ; les rendus du grand livre (balance, grand livre, annexes, clôture) restent dans **Finance** (calculés, **pas des fichiers**). Ne pas reproduire la famille `/documents/*` legacy qui mélange fichier figé et rendu vivant.
- **D44** : ✅ **3 niveaux de confidentialité (USER — réduit de 4 à 3) : public / conseil / gestionnaire**, **droits RÉELS appliqués par RLS** (la base refuse un doc non autorisé — pas une étiquette cosmétique), visibilité **sélectionnée par le gestionnaire à l'AJOUT** du document, accès journalisés (`document_access_log`). Mappe les 3 cercles `VIS-3CERCLES`/D7. Abandon du 4ᵉ niveau « restreint/utilisateur nommé » (simplification). *(Vérifié : 2 modèles concurrents DB/front, aujourd'hui cosmétique.)*
- **D45** : ✅ **vraie entité documents réglementaires + alertes d'expiration** : dates de validité + **tableau de bord d'expiration** (contrôles ascenseur/chaufferie/extincteurs, diagnostics, décennale, DTA) → ne jamais rater un renouvellement obligatoire (matière en base : `v_documents_expiring`, morte → réactiver). **Conservation légale** : soft-delete + `deletion_blocked` + `retention_years` par catégorie (PV/comptes/contrats non supprimables avant terme).
- **D46** : ✅ **deux canaux distincts + dispatch légal dédié** : **chat interne** (échanges courants) + **email transactionnel** (notifications) gardés séparés ; le **canal officiel légal** (convocation/PV) reste un **dispatch dédié avec traçabilité / preuve d'envoi** (mécanisme AG). Ne pas mélanger conversation et acte juridique (sinon AG contestable).
- **D47** : ✅ **mur = publication copro libre + modération a posteriori** (signaler/verrouiller via `is_locked` déjà en base), niveaux de visibilité (tous / conseil / gestionnaires). Arbitrage risque/commercial assumé par USER : esprit communauté, gestion réactive des dérapages.
- **D48** : ✅ **boîte collective PAR COPRO + routage par sous-adresse** (l'adresse destinataire encode la copro → parse → dossier copro ; tout gestionnaire de la copro voit le fil, continuité si changement). Remplace la var figée mono-copro `MAIL_INBOUND_COPRO_ID`. **+ Idée USER : l'adresse copro en COPIE (CC/BCC) = adresse de classement** → auto-capture la correspondance dans le dossier copro (réutilise le routage sous-adresse). Auto-ajout en copie quand l'envoi part de l'appli ; copie manuelle depuis le client mail perso. Nuance : **BCC par défaut** (adresse de classement masquée) vs CC (transparence) — option offerte.
- **D49** : ✅ **calendrier copro en V1** (hub Documents & comm) : AG + interventions programmées (contrats/OS) + échéances légales. Capacité dormante à activer (table `events` + edge existent, **zéro UI**). Attendu syndic, faible coût (la donnée existe).

## Conseil syndical (D50→…)
- **D50** : ✅ **CS simple, dérivé de l'AG (USER)** : le conseil est **voté à chaque AG** (ELECT_COUNCIL) ; **tant qu'il n'y a pas d'AG, le CS ne change pas**. Entité persistée mais **DÉRIVÉE de la décision d'AG** (pas de gestion de mandat complexe ni d'édition manuelle). Affiche composition + président (issus du vote) + date d'élection. Pas d'alerte/cooptation complexe (simplification vs reco initiale). Cohérent pilier AG→copro.
- **D51** : ✅ **retrait seul, pas d'ajout hors AG** : on peut **RETIRER** un membre parti (démission, ou a vendu son lot → plus copropriétaire/inéligible) entre deux AG ; **AJOUTER** reste un vote d'AG (ELECT_COUNCIL). Ajouter la policy d'écriture `council_members` (notée manquante en 0053).
- **D52** : ✅ **espace conseil avec consultation « devis à valider »** (USER s'est ravisé en cours : option **minimale**, pas « rien en V1 »). Le syndic **soumet un devis/travaux** au-dessus du seuil art.21 au CS → le CS donne son **avis** (favorable / défavorable / réserves), **tracé** (qui/quand, art.21). **Nuance juridique** : le CS est **consultatif** (l'AG décide), mais **opérationnellement l'avis favorable = le feu vert** du syndic pour engager au-dessus du seuil → dans l'appli = **demande d'avis au CS = gate pratique** avant d'engager. **Minimal**, calé sur l'espace conseil (A2) ; pas de machine à voter complète. *(`council_decisions`/`council_votes` existent en demi-feature.)*
- **D53** : ✅ **rapport d'activité CS : oui aux trois** — éditeur **rich-text** + **upload réel** des annexes (Supabase Storage) + **annexion auto** à la convocation de l'AGO d'approbation des comptes (art.21, lien existant inutilisé). Câbler le générateur PDF débranché. **Précision format (USER)** : **HTML = format de RÉDACTION/stockage** (éditable), **PDF = format de LIVRAISON** (figé) → conversion HTML→PDF à la génération. Reco : **rich-text à sous-ensemble contrôlé** (gras/listes/titres/liens, pas de HTML/CSS arbitraire) + **vrai moteur HTML→PDF** (Chrome headless en server function) plutôt que jsPDF qui rend mal le HTML. *(Vérifié : modèle stocke HTML mais UI texte brut = incohérence.)*

## Conformité (DPE / PPT / Factur-X) (D54→…)
- **D54** : ✅ **persister le domaine Conformité** (aujourd'hui 100% mock). **DPE** : 1 par bâtiment (multi-DPE sur copro multi-bâtiments), **versionné** (historique), dates/validité → alertes loi Climat (interdiction location G 2025 / F 2028 / E 2034). **PPT** : vue au-dessus des opérations réelles (D39, pas de 3ᵉ source) ; **DPE→PPT** pré-remplit. **e-factures** persistées (cf. D55). → tableau de bord conformité (à jour / en retard / à planifier).
- **D55** : ✅ **Factur-X entrantes d'abord ; appels de fonds HORS CHAMP (confirmé USER expert)**. V1 : recevoir/lire les factures **fournisseurs** en Factur-X (module Factures D17-D22, pas de duplication mock) + générer un **fichier conforme** (PDF/A-3 + XML EN 16931) **sans** transmission PDP/PPF (= P1). **Les appels de fonds = contributions, PAS des factures commerciales B2B → hors champ** ; seuls les **honoraires du syndic** pourraient l'être (P1).
  - **⚠️ Factur-X = session dédiée requise (USER)** : sujet complexe + réglementation MOUVANTE (profils MINIMUM/BASIC/EN16931/EXTENDED ; calendrier de la réforme par paliers ; rôle PDP vs PPF révisé). **NE PAS coder sur la base des souvenirs (cutoff début 2026)** → au moment de l'implémenter, **vérifier les règles à jour sur sources officielles** (recherche web) avant d'écrire. D55 (entrantes + fichier conforme SANS transmission) est volontairement la marche qui **ne dépend pas** des parties volatiles.
- **D56** : ✅ **applicabilité calculée dynamiquement par copro** (âge / taille / **lots principaux** → ne montrer que le pertinent ; nécessite de distinguer lots principaux/annexes dans le modèle) — **MAIS présenté comme AIDE / mise en avant, PAS comme obligation bloquante (USER)**. Le PPT (et assimilés) sont **facilités et signalés** au gestionnaire, **jamais un blocage/gate**. Tableau de bord conformité agrégé multi-copro trié par urgence (informatif). Cohérent philosophie « assister, ne pas bloquer » (D6/D34).

## Contentieux / Litiges (D57→…)
- **D57** : ✅ **contentieux financier en V1 + modèle « affaires » extensible** : V1 = contentieux financier (impayé → procédure, bout de la chaîne de recouvrement D37). Non-financier (voisinage / travaux / dégâts des eaux) = **P1**, mais modélisé dès maintenant comme **UNE table « affaires » polymorphe extensible** (`entities/litiges.ts` existe) → étendre, pas une 2ᵉ feature.
- **D58** : ✅ **« réglé » = obligatoirement un encaissement** (paiement D512/C450 + imputation **FIFO cloisonnée par nature**). **Pas** de bouton « marquer réglé » manuel (anti-patron qui ment, cf. D18). Statut « réglé » **dérivé du paiement**. « Un impayé ne se règle pas, il s'encaisse. » (cf. [[payment_imputation_rules]])
- **D59** : ✅ **hybride + échelle de recouvrement structurée (USER)**. Financier (montant/ancienneté) = **auto depuis le GL** ; stade = **échelle définie** (pas un champ libre, remplace l'heuristique fragile) : **1. Relance amiable · 2. 2ᵉ relance amiable (facultative) · 3. Mise en demeure LRAR (démarre intérêts/frais art.36) · 4. Commandement de payer (huissier) · 5. Assignation · 6. Jugement (titre exécutoire) · 7. Saisie/exécution**. Amiables (1-2) = auto/assisté (D60) ; mise en demeure (3) = acte validé à la main ; judiciaire (4-7) = posé à la main (événements réels). Chaque passage d'étape peut déclencher des **frais** (D61). **Pré-cadre D60/D61.**
  - **Mécanisme d'encaissement (réponse USER) : l'impayé est une VUE sur le GL, pas un état basculé.** Paiement reçu → rattaché à la copro (rappro bancaire D7 / imputation manuelle) → imputé **FIFO cloisonné par nature** sur le 450 → le solde baisse → l'impayé **se re-dérive** (0 ⇒ réglé ; partiel ⇒ reste dû). Jamais de « validation » par bouton.
- **📘 Livrable « Guide client » (USER)** : toutes les règles métier tranchées (échelle recouvrement, imputation FIFO, « un impayé s'encaisse », AG→budget…) devront être **vulgarisées dans un guide d'utilisation pour les syndics clients**. **`REFONTE_DECISIONS` = la matière première** ; produire le guide au fil de l'implémentation.

## Recouvrement — relances & frais (D60→…)
- **D60** : ✅ **amiable au CHOIX du gestionnaire** (auto via cron J+15/30/60/90 / manuel), **via email ou courrier simple** ; **mise en demeure (3) MANUELLE** : V1 **génère le courrier** (le gestionnaire l'envoie en recommandé lui-même), **presta LRE (AR24/Maileva…) = P1** (cohérent D41/D30). **+ Teaser/placeholders de la feature LRE dès V1** (bouton « Envoyer en recommandé électronique — bientôt », argument commercial). **+ Templates de recouvrement data-driven (USER)** : amiable **ET** LRAR, fusionnés depuis les données — **nom/prénom, somme due, période, appel de fonds concerné, DÉCOMPTE détaillé** (généré du GL — légalement requis dans une MED). Réutilise l'infra `email_templates` + `available_variables` (D41). ⚠️ LRAR « envoyée » **jamais** simulée (sinon délais ne courent pas — P0).
- **D61** : ✅ **modéliser + imputer auto au copro DÉFAILLANT** (art.10-1, **pas** sur le collectif), avec écritures. **+ Barème de frais CONFIGURABLE par cabinet/syndic (USER)** selon leur **coût réel / contrat de syndic** (relance X€, MED Y€…) — pas de montants en dur. ⚠️ art.10-1 = frais **nécessaires et justifiés** (pas arbitraires/punitifs) → barème défendable. Cœur (relance/MED) V1 ; huissier/avocat = saisie assistée sur justificatif. Pattern config cabinet (comme `resolution_templates` système/cabinet/copro).

## Paramètres / Dashboard / Portefeuille (D62→…)
- **D62** : ✅ **migrer le paramétrage structure dans le hub Copropriété** (bâtiments + lots + clés + tantièmes + copropriétaires unifiés, là où ça a du sens) ; `/settings` ne garde que le **transverse** (config cabinet, préférences). Fin du doublon `/settings/info` incohérent (mock qui réinvente la structure).
- **D63** : ✅ **interdit de modifier les tantièmes d'un exercice CLÔTURÉ** (passé figé, **garde-fou serveur**) ; sinon **modificatif avec DATE D'EFFET** → les nouveaux tantièmes valent **vers l'avant** (versioning `valid_from`/`valid_to` : ancienne version valide jusqu'à la date, nouvelle à partir ; le calcul prend la version valable à la date concernée). Le passé n'est **jamais réécrit**. Cohérent D9/D10/D11.

## Modèles & White-label (D64)
- **D64** : ✅ **Option A — moteur + module « Modèles » unique, types de rendu DISTINCTS**. Un seul écran « Modèles » et **un seul moteur de variables/aperçu** (merge-fields `{{copro.nom}}`, `{{lot.n}}`, `{{solde}}`… + aperçu sur **données RÉELLES** de la copro), en **généralisant le bon patron `resolution_templates`** (scope **système → cabinet → copro**, variables typées, versionnage, `usage_count`). On **garde des schémas/rendus distincts par type** : email = objet/corps HTML ; PV/courrier = structure → PDF ; résolution = texte légal + majorité. Respecte la décision #7 au niveau **moteur + UI** sans forcer un format de stockage unique (pas de table polymorphe fourre-tout).
  - **État réel constaté (base live `qqfq`)** : `email_templates` = 6 e-mails **système transactionnels** keyés par `code` (ag_convocation, ag_pv_notification, ag_relance, payment_reminder_7/30/60), déclenchés par les edges ; `pv_templates` = **table VIDE** + PV système **en dur dans `pv-template.service.ts`** (anti-patron) ; `resolution_templates` = 100 lignes, **le plus mûr** (scope/variables typées/legal_ref/version). Corrections v2 : **PV système seedé en base, versionné** (fin du « en dur ») ; `email_templates` gagne la **hiérarchie de scope** qui lui manque.
- **D64-bis White-label = COMPLET, niveau cabinet (USER)** : la table `cabinets` n'a **AUCUN champ de marque** aujourd'hui → couche white-label **from-scratch**. Modèle : **contenu neutre (bibliothèque système) + habillage cabinet injecté au RENDU** (on n'entretient qu'**une** bibliothèque, chaque cabinet la voit à son enseigne). Champs (nouvelle table `cabinet_branding`, logo dans **Supabase Storage**) : **logo (+ variante mono) + palette primaire/secondaire/accent + police + en-tête ET pied personnalisés + option « papier à en-tête » PDF**. **Un branding par cabinet** appliqué à **toutes ses copros** ; **override par copro = REPORTÉ** (un cabinet = une identité, pas une par immeuble). **UX/UI de l'écran de paramétrage = à designer PLUS TARD** (configuré **une seule fois** par le syndic, puis plus besoin d'y revenir).
- **Bibliothèque « prête à l'emploi » = ~3 formes (styles) par type de document (USER)** : pour chaque type (PV, relance, convocation, courrier, état daté…), seeder **~3 variantes de mise en forme** versionnées → le syndic en **choisit une** (un peu de choix) puis applique sa charte. Réutilise l'infra `email_templates`/`available_variables` (D41/D60) côté e-mails.
- **Lien transverse PDF** : le white-label complet (logo/couleurs/police dans les PDF) **renforce le choix HTML→PDF** (moteur headless type Chrome en server function) plutôt que jsPDF programmatique — un thème CSS par cabinet est trivial en HTML, pénible en jsPDF. À confirmer avec la **stratégie PDF v2** (point transverse ci-dessous).

## Identité copro & traçabilité (D65)
- **D65** : ✅ **écran « Identité copro » éditable** regroupant l'identité légale au même endroit. **État réel (base live)** : `copros` porte déjà `num_immatriculation` (**RNIC**), `siret`, `name`, adresse, `date_reglement`, `annee_construction`, `exercice_debut`, `previous_syndic_name`. **À AJOUTER** : `regime` (D65-a), **RIB copro IBAN/BIC** (⚠️ **aucune table de compte/RIB n'existe** — seuls `bank_movements` + connexion GoCardless ; à modéliser proprement), **fin/durée d'exercice** (seul le début est stocké). **Saisie manuelle (passive) en V1** ; **connexion téléservice ANAH/RNC = P1**. *Expert (USER) : compléter la liste légale si un champ manque (n° carte pro syndic, médiateur, RCS…).*
- **D65-a Régime = enum INFORMATIF en V1** : `droit_commun` (défaut) / `syndic_cooperatif` / `residence_services` / `asl_aful` / `petite_copro` (≤5 lots). **Affiché sur la fiche, ne pilote AUCUN comportement en V1** — on pourra brancher des règles dessus plus tard (AG simplifiée loi ELAN, syndic coopératif sans syndic pro…).
- **D65-b Journal d'audit CIBLÉ — étendu aux coordonnées (USER)** : trace **qui / quand / ancienne → nouvelle valeur** sur deux ensembles : (1) **paramètres à enjeu** — tantièmes, clés de répartition, RIB, dates d'exercice, immatriculation ; (2) **coordonnées copropriétaire** — téléphone, email, adresse, **pour récupérer un numéro/mail écrasé par inadvertance** (l'ancienne valeur reste dans le journal). **Exclut** le vraiment anodin (notes, préférences, civilité). ⚠️ **Aucune table d'audit générique n'existe** aujourd'hui (le seul « journal » = carnet d'entretien maintenance) → **nouvelle infra** (`param_audit_log` ciblé ou triggers sur colonnes surveillées), **réutilisable** pour d'autres zones sensibles plus tard.

## Dashboard & Portefeuille (D66→…)
- **D66** : ✅ **Portefeuille = page d'accueil cabinet TOUJOURS** (post-login → portefeuille **même à 1 copro** : USER privilégie la cohérence/prévisibilité à l'économie d'un clic) : grille des copros du cabinet + KPIs (solde, impayés, AG à venir). **Switcher copro permanent en en-tête** posant un **contexte « copro active » GLOBAL** hérité par tous les écrans (finance, AG, doc…) → changer de copro sans repasser par la liste. **Dernière copro mémorisée entre sessions.**
  - **🔴 Fix anti-patron v1 (capacité endormie)** : `CoproContext.tsx` est **verrouillé `SINGLE_COPRO_MODE = true`** (« première copro par date ») et le **`CoproSelector` est commenté/désactivé** → un cabinet multi-copro ne peut PAS basculer. v2 = **vrai multi-copro** (réveiller la capacité, cf. [[dormant_capabilities_activation]]) ; on garde le pattern `CoproContext` mais **activé** (liste depuis `memberships`, switcher réel, contexte persistant).

- **D67** : ✅ **carte « Fonds travaux » à DEUX FACES (USER)** — ne JAMAIS afficher un chiffre unique, ni additionner. Face 1 = **montant officiel/réglementaire constitué** = **réserve ALUR `105`** (avec comparaison au minimum légal 5 %) ; Face 2 = **trésorerie réelle dédiée** = **compte bancaire `512100` + Livret A `502`** ; + **voyant de rapprochement** (réserve ↔ banque : s'ils divergent = alerte « le fonds a été ponctionné par la trésorerie courante »). **Raison comptable** : `105` (passif = droits constitués) et `512100` (actif = cash) sont **le même argent vu de deux côtés** → les sommer = double comptage.
  - **État réel** : seul l'ALUR (`105`) est outillé (`v_alur_fund_balance`/`v_alur_fund_summary`) ; **aucune vue de trésorerie multi-comptes n'existe → à créer**. Comptes pertinents : `512000` courant / `512100` travaux / `502` Livret A (actif) ; `105` réserve ALUR / `102` provisions travaux décidés (art.14-2 I) / `1032` avance travaux art.18 (passif) ; `705` affectation du fonds. Cohérent [[alur_affectation_model]] (105 = source unique du solde réserve).

- **D68** : ✅ **une seule définition par métrique, deux formes d'accès (USER)** : chaque chiffre calculé **UNE fois depuis le grand livre** (source de vérité), exposé en **VUE** (1 ligne/copro) pour le portefeuille + listes multi-copro, et **FONCTION** fine réservée au **lourd par période** (budget voté vs réalisé). Chaque écran lit la même source ⇒ mêmes chiffres partout.
  - **🔴 Supprimer le « Frankenstein » actuel** : `getDashboardKpis()` (`src/lib/dashboard/api.ts`) appelle la RPC `fn_dashboard_kpis(p_copro_id, p_period_id)` pour la finance **MAIS** va chercher `unpaid_lots_count` dans la **vue** `v_dashboard_kpis` → deux définitions possibles d'« impayés » qui divergent (cohérent finding audit **GL ↔ `call_for_funds_lines`**). v2 : métriques dérivées du GL, **jamais re-calculées différemment selon l'accès**. (Vues sœurs `v_dashboard_recent_activity` / `v_dashboard_todos` = activité + todos D70.)

- **D69** : ✅ **vues DÉDIÉES au portail (jamais réutiliser les vues gestionnaire)** : `getPortalContext` scopé au copropriétaire, mêmes chiffres du grand livre en dessous (cohérent D68) **projetés sur SES lots**. État réel : portail **pas encore codé** ([[vue_coproprietaire_pending]]), `getPortalContext` **à créer** ; les 3 vues dashboard sont bien `security_invoker` **mais ça ne suffit pas** — elles agrègent la copro entière → réutilisées, soit **fuite** (impayés nominatifs des voisins), soit **total partiel affiché comme total** (trompeur). Les vues dédiées sont précisément ce qui permet de régler « qui voit quoi ».
  - **Modèle de visibilité — 3 CERCLES (`VIS-3CERCLES`), validé** :

    | Information | Gestionnaire | Conseil syndical | Copropriétaire |
    |---|:---:|:---:|:---:|
    | Son compte (solde, appels, paiements) | ✔ tous | ✔ les siens | ✔ **les siens** |
    | Impayés **nominatifs** | ✔ | ✔ *(droit de regard légal)* | ✖ |
    | Santé impayés **globale** (anonyme) | ✔ | ✔ | ✔ |
    | Comptes / budget / **annexes** copro | ✔ | ✔ | ✔ *(communicable)* |
    | Fonds travaux (collectif) | ✔ | ✔ | ✔ |
    | AG : convocation, OdJ, PV, votes | ✔ | ✔ | ✔ |
    | GED collective (règlement, PV, diagnostics, contrats) | ✔ | ✔ | ✔ |
    | Coordonnées des **autres** copros | ✔ | *(CS public)* | ✖ *(RGPD)* |
    | Devis > seuil art.21 | ✔ | ✔ *(avis, D52)* | ✖ |
    | Écritures / actions de gestion | ✔ | ~ *(avis, pas d'écriture)* | ✖ *(lecture)* |

- **D69-bis** : ✅ **copropriétaire = tout le collectif communicable EN CLAIR (USER)** — budget voté + réalisé, **annexes comptables 1-5**, fonds travaux, AG/PV, GED collective, consultables en ligne en permanence (droit légal de consulter les comptes → transparence max, moins d'appels au syndic). Sa situation perso reste filtrée à ses lots ; impayés nominatifs des voisins jamais visibles.

- **D70** : ✅ **DEUX niveaux de todos (USER)** : (1) **Conformité légale en tête, NON paramétrable** (ce sont des délais de loi) — **AG dans les 6 mois de la clôture (art. 64)**, DPE expiré, PPT, **fonds ALUR < minimum légal** ; (2) **Seuils opérationnels = défauts métier sortis du dur vers une config cabinet** — impayés > 60 j, contrats < 30 j, contrôles < 7 j —, **écran de réglage par cabinet en P1**. **État réel** : seuils **60/30/7 codés EN DUR dans `v_dashboard_todos`** ; **aucun todo de conformité légale aujourd'hui** (gap pointé par [[cours_syndic_audit]]) → à ajouter. v2 : seuils opérationnels lus d'une **config cabinet** (bons défauts livrés), **alertes légales calculées des dates réelles** (clôture, validité DPE, cotisation ALUR vs minimum). Cohérent « assister sans bloquer » (D56).

## Décisions transverses (grilling 2026-06-23)
- **🔒 DoD (Definition of Done) PAR FEATURE — niveau STRICT (USER)** : checklist **OBLIGATOIRE** à chaque feature v2 (« feature » = vrai changement fonctionnel ; typo/rename pur exempté).
  - **TOUJOURS** : (1) `tsc` vert (0 `any`) · (2) tests unitaires vitest sur la logique · (3) **1 test e2e qui PROUVE l'effet EN BASE** (count/somme exacts via service-role, **jamais** un 200/redirection seul — anti-faux-vert B9) · (4) **NON-RÉGRESSION** = toute la suite e2e + tous les gates SQL au vert · (5) lint vert + **vérif runtime navigateur (USER)** · (6) 1 PR ciblée + push GitHub.
  - **Si touche la BASE** (migration/RPC/RLS) : + (7) **revue d'impact en cascade AVANT** · (8) test **BEGIN/ROLLBACK** avant apply · (9) Security Advisor rejoué (0 `rls_disabled`).
  - **Si touche la FINANCE/GL** : + (10) `audit_finance_integrity = 0` + équilibre GL · (11) **golden inchangé** (parité résultat) · (12) code review.
  - **Durcissements STRICT (USER, choix explicite)** : **code review multi-agents SYSTÉMATIQUE** (même petites features) **+ revue cascade même sur features UI pures**. Filet maximal assumé (coût tokens/temps accepté).
  - **Mise en œuvre** : niveau 1 **automatisé en CI** (pipeline B9 `npm ci → tsc → build → health-check → e2e → vitest → gates`) ; la non-régression complète tourne **à chaque PR**, le local fait au moins le test de la feature + gates touchés. *(Consolide [[fix_all_before_commit]] · [[cascade_review_before_migration]] · [[playwright_first_testing]] · [[always_push_github]] · [[definition_of_done_v2]].)*
  - **🔁 Cycle par feature (mode AUTONOME de nuit, USER)** : implémenter (TDD) → **boucle d'auto-réparation Playwright jusqu'au VERT RÉEL** avec **garde-fous** [jamais skip/désactiver/truquer un test → sinon il reste ROUGE + signalé ; **limite de tentatives par point** puis on passe au suivant (pas de nuit brûlée sur 1 test) ; distinguer « mon code faux » (corriger+reboucler) de « attendu ambigu/comptable » (STOP + question pour Lyes) ] → **`/simplify`** (nettoyage réutilisation/efficacité/lisibilité) → **re-test** (simplify n'a rien cassé) → **code review multi-agents** → PR + push → **compte-rendu au réveil** (vert prouvé / rouge + pourquoi / questions en suspens).
  - **🧰 Skill maison « méthodo CoProFlex » à CRÉER (USER)** : encapsuler tout le workflow — grill exhaustif → implémentation autonome (boucle test + garde-fous) → `/simplify` → DoD/review — dans un skill réutilisable (via `skill-creator`). **Timing : en FIN de cadrage** (quand la méthodo est figée) ; il pilotera l'autonomie de nuit.
- **⚠️ Stratégie PDF v2 (soulevé D53, à trancher à part)** : aujourd'hui jsPDF programmatique partout (convocation/PV/état daté…) ; option = passer à **HTML→PDF** (templates HTML → moteur headless type Chrome/Puppeteer en server function) pour unifier les modèles et permettre le rendu riche. À arbitrer globalement.
- **Cadrage « v1 = anti-patron, pas code à supprimer » (USER D24)** : v2 front from-scratch ⇒ on ne « supprime » jamais le mauvais code v1 (il meurt avec v1 gelé), on **ne le reproduit pas** ; la base squash+clean **ne reprend pas** l'orphelin/non-canonique et **ajoute des gardes** rendant les bypass impossibles par construction. Formuler les findings v1 ainsi pour tout le reste du grilling.
- **Architecture 3 couches (confirmée D22)** : navigateur → **server function TanStack** (transport/porte, remplace les edge functions) → **RPC Postgres** (logique métier transactionnelle, surtout compta = double écriture atomique tout-ou-rien) → DB. **Edge functions = cron + webhooks UNIQUEMENT.** Ne JAMAIS sortir une double écriture comptable d'une transaction DB (risque grand livre déséquilibré). Patron général pour toute action d'écriture v2.
- **Base de données v2 = projet Supabase NEUF** en atterrissage du grilling : baseline 0001 par **squash + clean** du schéma live (base quasi vide ⇒ coût migration ~0) ; on préserve la correctness (RPC/RLS/fixes prouvés), on jette l'archéologie + doublons. Exécution **après D56**. `qqfq` + v1 gelés en référence jusqu'à la bascule. → mémoire `db_neuve_v2_squash_clean`.
- **Fil rouge « clean and simple »** sur chaque écran/modale (divulgation progressive, défauts malins). → mémoire `clean_and_simple_principle`.
- **Capacités dormantes** : chantier de balayage à faire (schéma riche sous-exploité) → activer, ne pas recopier la sous-exploitation v1. 1er cas = indivision. → mémoire `dormant_capabilities_activation`.

## Findings d'audit majeurs (état au 2026-06-22/23)
- 🔴 Escalade `platform_admin` (multi-cabinet contournable) ; RLS **ON mais pas FORCE** (87 tables).
- 🔴 **0 edge function déployée** sur le live → paiements/AG/emails cassés en prod ; emails = Resend stub (faux succès).
- 🔴 Divergence créances GL ↔ `call_for_funds_lines` (source de vérité à réunifier sur le GL).
- 🔴 GL : `reverse_ledger_transaction` exposé brut + `unallocate/cancel` cassent l'audit.
- 🟢 `audit_finance_integrity = 0` partout, immutabilité GL défendue, base quasi vide (16/18 = fixtures E2E).
- ✅ Surprise positive : la connexion bancaire **GoCardless est RÉELLE** (pas un mock) — mais faille IDOR (contrôle d'appartenance requisitionId↔copro manquant) à corriger.

## Grilling cartographie — 2026-06-24 (écarts recroisés S1-S7 + bloquants transverses T5-T11)

> Salve tranchée sur `CARTOGRAPHIE_CONTEXTE_v2_2026-06-24.md`. Préfixe **G24-**. Affine S1-S5/S7 (journal `GRILLING_CONTEXTE_v2_2026-06-24.md`) et résout les bloquants T-transverses de l'audit.

### Structure & personnes
- **G24-1 (SCI / personne morale, recroise S1+T3)** : **3 champs structurés sur `coproprietaires`** — `entity_type` (physique/morale), `siren`, `legal_rep_name` (+ qualité). **PAS** de table séparée. **SIREN non obligatoire** mais **autocomplétion via l'API publique gratuite `recherche-entreprises.api.gouv.fr`** (tape le nom → SIREN + dirigeants pré-remplis) = feature V1. **Représentant courant seul** ; historiser « qui représentait à la date du PV » = P2. *Fond. : art.23 loi 65-557, D55 Factur-X.*
- **G24-2 (usufruit/NP, recroise S2+T2)** : **flag « lot démembré » + contact NP** (nom/email). NP **convoqué à TOUTES les AG** (sur-convoquer n'est jamais illégal, zéro tri de résolutions). **Vote via UN seul représentant** (mandataire commun, art.23 al.2) — on ne ventile jamais le vote par tête. Répartition charges usufruit/NP réglée entre eux. Pas d'`ownership_type` complet en V1.
- **G24-3 (occupation, recroise S3+D12)** : `occupancy_type` (occupé par le proprio / loué / vacant) porté **sur le LOT**, pas sur la personne. Contact occupant **structuré mais optionnel** ; idéalement saisi **par le propriétaire sur son portail** (repli gestionnaire). Aucune gestion locative. Le flag « loué » = crochet futur loi Climat (DPE G/F/E).
- **G24-4 (lot principal/annexe, recroise S4+T4)** : `lot_type` **dérivé automatiquement du type d'usage** (habitation/commerce/bureau = principal ; cave/parking/box = annexe), **surchargeable**. UI = petite colonne « principal » à cases auto-cochées dans la liste des lots. **Compte de lots principaux stocké en V1** ; les allègements régime « petite copropriété » (CS facultatif, compta simplifiée) = **P2**. *Fond. : art.1 décret 67-223, CCH L711-1, loi ELAN.*

### Cabinet & périmètre
- **G24-5 (identité légale cabinet, recroise S7+T1)** : mentions légales sur la table `cabinets` (n° carte pro Hoguet, garantie financière, RCP, médiateur conso) **injectées en pied de page des actes** ; nécessaire dès la 1re convocation conforme. **Signataire = défaut au niveau cabinet, surchargeable par copro.** Champs gardés **nullable** pour ne rien casser plus tard.
- **G24-SCOPE (USER) : cible = SYNDICS PROFESSIONNELS UNIQUEMENT.** On suppose toujours un syndic pro (mentions toujours présentes) ; branche **syndic bénévole / coopératif ABANDONNÉE** (même P2, sauf décision contraire). Simplifie tout le modèle « cabinet ».

### Finance & doctrine (bloquants transverses)
- **G24-T7 (comptes bancaires)** : table **`copro_bank_accounts` de 1er rang** (IBAN/BIC + type + sous-compte comptable 512/502 + solde rapproché) = **prérequis BLOQUANT avant de bâtir la finance** (fin du « 512 » en dur). **V1 = 2 comptes par copro** (courant + ALUR/fonds travaux) ; **architecture extensible** pour en ajouter plus (livret distinct…) = itération ultérieure **(gardé en mémoire, USER)**. *Fond. : arrêté 14/03/2005, art.18 loi 65-557 (compte séparé).*
- **G24-T6 (couche engagé + cut-off)** : **une seule liste propre** = table `commitments` de 1er rang (extra-comptable, hors GL ; OS + devis signés non encore facturés), **source unique** du reste-à-engager budget, du cut-off et des annexes travaux. **Rattrapage de fin d'année (charges à payer 408 / CCA 486) PROPOSÉ automatiquement** depuis les engagements ouverts, **ajustable** par le gestionnaire. *Fond. : décret 2005-240 art.14-3.*
- **G24-T5 (créances)** : **UNE vue d'ancienneté unique** (montant dû par lot, ventilé par nature × ancienneté) dérivée du GL, lue **à l'identique** par impayés / état daté / portail / dashboard **ET par les relances** (J+15/30/60/90). Évite qu'un écran contredise l'état daté (enjeu juridique). **Perf différée** : si la vue « tout en direct » crée une dette, on l'optimisera (matérialisation/rafraîchissement) plus tard. *Fond. : E2, D68, D16, D58.*
- **G24-T8 (seuils art.21)** : **modèle à 2 seuils / 3 zones par copro**, rattaché aux résolutions d'AG — (1) sous le seuil CS : syndic seul ; (2) au-dessus du seuil CS : avis/accord du conseil syndical ; (3) au-dessus du seuil AG : passage en vote AG. **Garde-fou = avertissement fort + traçable à TOUS les paliers** (le gestionnaire peut passer outre pour les urgences art.18, c'est enregistré). *Fond. : art.21 al.3, art.25, art.18.*
- **G24-T10 (consentement démat, USER)** : **PAS de verrou de consentement dans l'app.** Le syndic recueille l'accord hors-app, **la preuve est classée en GED** (catégorie dédiée). L'app garde seulement une **préférence de canal par copro** (email/papier) pour router les envois. Risque résiduel assumé : défense = document GED, pas un horodatage structuré.
- **G24-META** : on **remonte les noyaux légaux minimaux en V1** (mandat, mentions cabinet) plutôt que d'émettre des actes incomplets — application de la boussole B0.
- **G24-T12 (action, pas décision)** : vérifier **empiriquement** sur le golden 6 clés que `regularize_period` gère l'affectation du résultat multi-clés → **1re action technique de la Phase 1, avant toute UI** ; si RAISE → correctif moteur P0.

### Reste à griller (prochaines sessions, hors scope 2026-06-24)
- **T9** (journal d'actions à valeur probante vs `param_audit_log`), les **8 angles morts** A.3 (stratégie PDF transverse — *cf. décision PDF déjà orientée HTML→PDF* —, numérotation des pièces comptables, avoirs/trop-perçus copro, multi-bâtiment/sous-syndicats/ASL art.27, reprise extra-comptable onboarding, charge/pagination des vues GL, réversibilité SaaS, tantièmes en cours d'exercice), puis les trous fins **Partie C** (147 trous, 18 domaines).

**→ Doctrine de méthode issue de ce grilling (G24-T11) reportée dans le skill `methodo-coproflex` + `rules-v2.md`** : états comptables toujours **CALCULÉS** depuis le GL (jamais stockés en drapeau) ; états **événementiels** stockés mais changés **UNIQUEMENT** par une fonction de transition serveur unique et gardée (généralise D24).

### Salve 2 — T9 + angles morts A.3 (2026-06-24, suite)

- **G24-T9 (traçabilité)** : **deux mécanismes ciblés**, pas de journal universel — (1) journal des **paramètres** sensibles (D65-b, déjà décidé) ; (2) **traçabilité des actes probants** = horodatage « qui+quand » sur l'acte + un **journal des envois légaux IMMUABLE** (impossible à modifier/supprimer après coup), **étendu** des convocations (D46) **aux mises en demeure et relances**.
- **G24-AM2 (numérotation des pièces)** : **séquences serveur sans trou**, par copro × type de pièce (écritures, appels, factures, PV), attribuées à la création ; **remise à zéro par exercice annuel** (2026-0001…). Anti-fraude/conformité, posé dès la V1 (rétro-install douloureuse). *Fond. : art.17 décret 67-223.*
- **G24-AM3 (remboursement vers un copro)** : route de **sortie de trésorerie** vers un copropriétaire en V1 (**D450 / C512**, distincte du paiement fournisseur) pour solder un copro qui part en créditeur / trop-perçu / excédent rendu. **Action volontaire** du gestionnaire ; **défaut = garder le crédit** et l'imputer sur l'appel suivant (cohérent affectation du résultat).
- **G24-AM4 (multi-bâtiment)** : V1 = **un seul syndicat, charges par bâtiment via les clés** de répartition (le golden 2 bâtiments passe). **Syndicats secondaires art.27 + ASL/AFUL = P2.** En plus : **rattachement « bâtiment » léger sur le lot** (adresse, DPE par bâtiment D54, regroupement d'affichage), distinct de la clé comptable.
- **G24-AM5 (reprise extra-comptable, art.18-2)** : à la reprise, la **saisie structurée des éléments ACTIFS** (contrats vivants + échéances, OS/travaux en cours) est **possible mais SKIPPABLE** (non bloquante, complétable plus tard ; à défaut → GED) ; l'historique clos = **PDF en GED**, jamais ressaisi. **Une procédure de recouvrement en cours est reprise en structuré** (stade relance/MED/judiciaire + **ancienneté de la dette par lot**) pour la continuer sans repartir de zéro.
- **G24-AM7 (réversibilité / sortie SaaS)** : feature self-service d'export = **P2** ; mais **capacité d'extraire les données garantie dès la V1** (export technique / archive PDF) pour honorer l'obligation légale de transmission (art.18-2) et le RGPD.
- **G24-AM8 (tantièmes modifiés en cours d'exercice)** : effet **PROSPECTIF, jamais rétroactif** (appels déjà émis inchangés ; nouveaux tantièmes pour les appels suivants). **Tantièmes datés/versionnés** (date d'effet) → chaque appel/exercice utilise la bonne valeur ; sert aussi à figer les exercices clos (D63). *Fond. : art.11 loi 65-557 (modificatif).*
- **Déjà tranchés (rappel)** : AM1 stratégie PDF = **TECH-1 HTML→PDF** ; AM6 perf des vues GL = **différée** (cf. G24-T5).

> **Scope de haut niveau du grilling cartographie = COMPLET** (Partie A + B). Reste pour de prochaines sessions : les **147 trous fins de la Partie C** (18 domaines), domaine par domaine.

### Salve 3 — trous critiques Partie C (2026-06-24)

> Grilling des 6 trous **🔴 OPEN critiques** du `TRIAGE_PARTIE_C_2026-06-24.md`. Préfixe **G24-C\<n\>** (n = numéro de domaine C), sans collision avec G24-1..5 / G24-AM* / G24-T*.

- **G24-C1 (reprise d'un chantier de travaux en cours — face comptable ; complète G24-AM5)** : à la reprise d'un mandat avec chantier voté non clôturé, une étape **« Travaux en cours »** :
  - **crée une vraie OPÉRATION** (`operation_id`) par chantier repris → indispensable pour **annexe 5** (en cours), **annexe 4** (à la clôture) et `settle_works_balance` ; sans opération ces annexes sont impossibles à produire ;
  - **réalisé = solde net obligatoire** (voté / déjà dépensé / reste), **détail par marché-entreprise optionnel et progressif** ; on ne rejoue **jamais** l'historique d'écritures du prédécesseur ;
  - **102 (provision art.14-2 I) + 1032 (avance art.18) = 1 champ principal + 1 optionnel** (en pratique un seul mobilisé), rattachés à l'`operation_id`, jamais en fourre-tout ;
  - **repli = AVERTISSEMENT NON BLOQUANT** : sans le détail, soldes 102/1032 en **comptes globaux** (chemin par défaut) + avertissement « chantier en cours non détaillé → annexes 4/5 incomplètes » ; découpage en opérations plus tard ; jamais bloquant, jamais noyé dans « autres comptes » ;
  - **saisie = écran guidé (chemin principal, données reçues en PDF/papier)** + **gabarit Excel « travaux en cours » optionnel** ré-importable (1 ligne = 1 opération : libellé, AG, voté, réalisé, 102, 1032) — l'Excel n'est **pas** le chemin principal (le confrère fournit surtout du PDF/papier) ;
  - **invariant de contrôle** : Σ(102 affecté aux opérations) ≤ solde 102 global importé ; reliquat non ventilé reste en global + avertissement maintenu.
  *Fond. : décret art.14-2 I (provision travaux décidés), art.18 loi 65-557 (avances), annexes 4/5 arrêté 14/03/2005. S'appuie sur le socle `operation_id` (E4) ; complète G24-AM5 (volet extra-comptable : contrats/OS/recouvrement).*

- **G24-C6 (3ᵉ voie : comptabiliser un mouvement bancaire sans facture)** : 3ᵉ action **« Comptabiliser ce mouvement »** sur la ligne de relevé, distincte du *pointage* (1ʳᵉ voie) et de la *création de paiement copro* (2ᵉ voie). Pose une **écriture équilibrée** (512x + contrepartie) via **RPC SQL atomique** puis **pointe automatiquement** la ligne ; voie qui écrit au GL → **revue adversariale**.
  - **Défauts intelligents éditables** (le gestionnaire valide/corrige ; pas de config par cabinet en V1), mapping sur **codes réels de la base** :
    - **Sorties** : frais de compte / commissions / **agios → 662** · échéance d'emprunt → **661** (intérêts) + **164** (capital) · impôt/taxe prélevé → **632/633/634**.
    - **Entrées** : intérêts compte courant → **716** · subvention → **132** · indemnité d'assurance → **713** · produit divers → **714**.
    - **Intérêts Livret A / fonds travaux → défaut SELON LE COMPTE SOURCE** : fonds travaux (502/512100) → **C 105** (l'intérêt grossit la réserve, reste dans le fonds) ; compte courant (512000) → **C 716**. **Jamais de dilution du fonds dans le courant.**
    - **Mouvement interne** (alimentation du fonds : 512000 → 502/512100) = D/C banque, **zéro charge/produit** (traité comme transfert).
    - **Frais de relance refacturés → HORS 3ᵉ voie** : route art.10-1 (sous-compte 450-6, cf. C.5).
  - ⚠️ Correctifs de mémoire actés au passage (codes vérifiés en base) : **627 = Frais d'AG** (pas « services bancaires ») ; **agios = 662** ; **661 = annuités d'emprunt** uniquement ; **produits financiers = 716** (pas de 762).
  *Fond. : décret 2005-240 art.14-3 (toute opération génère une écriture) ; plan de comptes copro (arrêté 14/03/2005) ; loi ALUR art.14-2 II (intérêts acquis au fonds travaux). Complète la doctrine bancaire existante (pointage + création de paiement).*

- **G24-C9 (mise en concurrence / demande de devis — modèle OS-centré, USER)** : **pas d'entité « consultation » séparée** — l'**ordre de service est central**. À la création, le gestionnaire sélectionne **1 ou N prestataires**.
  - **Table fille `service_order_quotes`** : 1 ligne par prestataire consulté (`tiers_id`, montant, `document_id` PDF, statut reçu/retenu/écarté, trace d'envoi qui/quand).
  - **Devis retenu** → remonte sur l'OS (`tiers_id` + `quoted_amount`) + crée une ligne dans `commitments` (engagé, G24-T6). Les **devis écartés restent** = preuve de mise en concurrence (art. 21).
  - **1 seul prestataire = OS direct** (pas de concurrence) ; **plusieurs = mode demande de devis + comparaison**.
  - **Envoi facilité** : l'app **génère** la demande de devis (descriptif du travail + coordonnées/accès copro) et l'**envoie par email** (système de communication) aux prestataires cochés, avec **trace d'envoi par ligne**. Le presta répond **hors-app** → le gestionnaire saisit le montant + joint le PDF reçu. **Portail prestataire (dépôt en ligne) = P1.**
  - `service_orders.tiers_id` = **nullable** tant qu'aucun devis n'est retenu.
  - Alimente le **gate CS / seuils art.21 (G24-T8)** en devis comparables structurés. **Marketplace nationale (D42) = backlog.**
  *Fond. : art.21 loi 65-557 (mise en concurrence + avis CS). S'appuie sur G24-T8 (seuils/gate) et G24-T6 (commitments) ; distinct de D42 (marketplace). État réel vérifié : `service_orders` porte déjà `estimated/quoted/actual_amount` + `tiers_id` singulier ; aucune table de devis multiples n'existait.*

- **G24-C14-RGPD (socle RGPD — palier minimal V1)** :
  - **V1** : (a) **registre des traitements** statique/pré-rempli (art.30) ; (b) **durée de conservation par catégorie de donnée** ; (c) **export « mes données »** (art.15) déclenché par le **GESTIONNAIRE pour un copropriétaire** (fichier PDF/JSON) — self-service portail = P1.
  - **Partage des rôles** : syndic = **responsable de traitement**, CoProFlex = **sous-traitant** → **DPA art.28** (brique contractuelle à acter, hors code).
  - **Conservation séparée (clé)** : un effacement RGPD = **pseudonymiser les coordonnées** (nom/email/tél) **en CONSERVANT les écritures comptables** (10 ans). Deux durées distinctes : données personnelles (minimisation art.5) vs écritures (obligation légale).
  - **P1** : purge/pseudonymisation automatique à l'expiration. **Plus tard** : module DPO complet.
  *Fond. : RGPD art.5/15/17/28/30 ; décret 2005-240 (conservation comptable). Dépasse la micro-traçabilité B7/D65-b.*

- **G24-C14-FS (fiche synthétique de copropriété — hybride live + snapshot)** :
  - **Vue live** dérivée du GL + identité, consultable à tout moment (au fil de l'eau), toujours à jour. Les ~15 rubriques (décret 2016-1822) se dérivent à ~90 % : GL (chiffres), identité, **RNIC (D65)**, **ALUR (D67)**, **impayés (vue unique G24-T5)**, lots principaux/annexes (**G24-4**). Aucune re-saisie.
  - **Snapshot figé daté** capturé **automatiquement à chaque clôture d'exercice** (photo de la vue live) → **opposable et reproductible**, même logique que l'état daté (D33). Petite table de snapshots.
  - **Rendu HTML→PDF** (AM1) ; annexable en cas de vente.
  *Fond. : loi 65-557 art.8-2, décret 2016-1822 (15 rubriques). Source unique = GL.*

- **G24-C15 (flux d'invitations au portail copropriétaire)** :
  - **État réel** : `copro_invitations` (token, status, expires_at, coproprietaire_id, email) + `memberships` (user_id, copro_id, role) + `link_coproprietaire_account` (acceptation) **existent déjà** ; manquaient les RPC d'**émission**.
  - **Identité multi-copro (confirmée par le schéma)** : **1 compte auth par personne** + **1 membership par copro** → multi-copro natif via le switcher (D66).
  - **Cycle complet en V1** : création **unitaire + en masse** · **envoi email (Brevo, E3-q)** · **expiration auto** du token · **renvoi** · **révocation d'une invitation EN ATTENTE** · **ET révocation de l'ACCÈS d'un membre déjà inscrit** (désactivation du membership : vendeur parti / erreur).
  - RPC à créer : `create_copro_invitation` (unitaire + masse), `resend_copro_invitation`, `revoke_copro_invitation`, désactivation de membership.
  - **Indivision / email partagé** : inviter par défaut le **mandataire `is_primary`** (cohérent G24-2 + résiduel C.3), autres indivisaires en option.
  *Fond. : décret 67-223 art.64-2, loi ALUR art.18 I 5° (extranet). S'appuie sur D66 (switcher), E3-q (Brevo), G24-2 (indivision). Corrige A3 : l'acceptation seule ne suffit pas, le P0 « extranet » exige le flux d'émission.*

> **6 trous 🔴 CRITIQUES de la Partie C = CADRÉS** (G24-C1, G24-C6, G24-C9, G24-C14-RGPD, G24-C14-FS, G24-C15). Reste : les **24 OPEN importants** (surtout C.8 AG, C.12 Ventes, C.5 Recouvrement) puis les **83 PARTIAL** par lots.

### Salve 4 — OPEN importants Partie C : C.8 Assemblées générales (2026-06-24)

> État réel vérifié : le schéma AG est déjà riche. `ag_votes` porte **`is_excluded` + `exclusion_reason`** ; `ag_attendance` porte **`signed`/`signature_data`/`arrived_at`/`left_at`/`tantiemes`/proxy** ; `ag_correspondence_votes(+_details)` existent. Manquent surtout des **règles/UX** et 1-2 champs. Préfixe **G24-C8-\<n\>**.

- **G24-C8-1 (seconde convocation / nouvelle AG art.25-1) — HORS V1, crochet V2.** Précision légale actée : l'art.25-1 a **deux branches** — (a) projet **≥ 1/3** des voix → **même AG, second vote immédiat** à l'art.24 (= « seconde lecture », **déjà couvert** D31, on n'y touche pas) ; (b) projet **< 1/3** → **nouvelle AG dans 3 mois** pouvant statuer à l'art.24. Seule la branche (b) était le trou. **Décision USER : hors V1** (cas rare), **à garder pour V2** : crochet = champ `re_convocation_of_ag_id` + assistant de reprise des résolutions non adoptées (bascule art.24) + alerte délai 3 mois. *Fond. : art.25-1 (et 26-1) loi 65-557.*

- **G24-C8-2 (exclusion du droit de vote) — V1 minimal manuel.** Les champs **existent déjà** (`ag_votes.is_excluded` + `exclusion_reason`). V1 = surfacer une **case « exclure de ce vote » + motif obligatoire** repris au PV ; **rien d'automatique** (détection de conflit = P1). **Garde-fous légaux actés** : (1) **pas de privation de vote pour impayé** (n'existe pas en droit copro) ; (2) la « privation » art.24 II au sens « décision ne concernant que certains » = gérée par la **clé spéciale** (`repartition_key_id` sur la résolution, cf. D9 / partiel C.8), pas par un drapeau ; (3) l'exclusion sert au seul **conflit d'intérêt** (copro partie au contrat voté, syndic candidat…). Valeur = rendre l'AG inattaquable sur ce point. *Fond. : art.24 II loi 65-557 + jurisprudence.*

- **G24-C8-3 (neutralisation votes correspondance sur résolution amendée, art.17-1 A) — V1 manuel.** Rappel légal : dès qu'une résolution est amendée en séance, **TOUS** les votes par correspondance sur cette résolution sont neutralisés (votant = défaillant), la loi ne distingue pas pour/contre. **Décision USER : V1 manuel** — ajout d'un flag `is_amended` (traçabilité/PV) ; quand la résolution est marquée amendée, **le gestionnaire change/neutralise lui-même** les votes des copropriétaires ayant voté par correspondance (via `is_excluded`/motif). **Rappel non bloquant** affiché (« N votes par correspondance sur cette résolution amendée ») pour éviter l'oubli. Neutralisation automatique = amélioration P1 possible. *Fond. : art.17-1 A al.3 loi 65-557.*

- **G24-C8-4 (feuille de présence) — les 3 règles en V1.** Données déjà présentes (`ag_attendance` : `signed`/`signature_data`/`arrived_at`/`left_at`/`tantiemes`/proxy). V1 = (1) **feuille émargée + certifiée par le président = annexe obligatoire du PV** (art.14 décret) ; (2) **`left_at` bloque les votes postérieurs au départ** (sauf pouvoir donné avant de partir) ; (3) **asymétrie indivision conservée** (présence via mandataire unique, vote sur quote-part réelle, jamais ventilé par tête — cohérent G24-2). *Fond. : décret 67-223 art.14 ; G24-2 (indivision).*

**PARTIAL C.8 confirmés (lot G24-C8-P, 2026-06-24) :**
- **P1 (calcul majorité art.24) — ACTION.** Auditer le moteur de calcul du « c'est voté » sur le golden : doit exiger **OUI ≥ plus de la moitié des voix présentes** (présents/représentés), **jamais** un simple `pour > contre`. Corriger si dérive. Point le plus grave (validité de chaque AG).
- **P2 (orchestration 2ᵉ lecture) — assistée.** Le système propose le second vote immédiat (passerelle ≥1/3, art.25-1/26-1), **le président déclenche**, même séance.
- **P3 (vote ciblé / clé spéciale art.24 II) — oui.** Une résolution ne concernant qu'un groupe se vote **sur la clé de répartition liée** à la résolution ; majorité/quorum calculés sur cette clé ; un copro hors-clé ne peut pas voter cette résolution.
- **P4 (pièces obligatoires de convocation) — AVERTISSEMENT non bloquant (USER).** L'app **n'empêche pas** la convocation mais **avertit avec la liste des documents manquants** (devis des travaux votés, 5 annexes comptables). *(Override de la position « bloquant » du triage ; risque de nullité assumé, l'avertissement doit être visible.)*
- **P5 (PV) — 3 règles + ajouts USER.** PV généré depuis les données (opposants/absents nommés) ; **signé** seulement après signature du **bureau, scrutateur(s) inclus** si élu(s) ; envoi tracé (départ du délai 2 mois). **UX : bouton « secrétaire = le gestionnaire »** qui pré-remplit ses infos en 1 clic.
- **P6 (pouvoirs art.22) — loi stricte, non bloquant.** Avertir seulement si **plus de 3 pouvoirs ET total des voix > 10 %** (≤3 toujours OK) ; **jamais bloquant**, proposer de **répartir les pouvoirs sur d'autres personnes**.
- **P7 (éligibilité CS à l'élection) — déjà tranché** par G24-C13-1.

> **Domaine C.8 (AG) = ENTIÈREMENT CADRÉ** (OPEN G24-C8-1..4 + PARTIAL G24-C8-P).

### Salve 5 — OPEN importants Partie C : C.5 Recouvrement / impayés (2026-06-24)

> État réel : `legal_proceedings`, `payment_reminders(+_rules)`, `reminder_settings` existent ; comptes **459/491/492** présents ; **aucune** table `payment_plan`, aucune RPC dépréciation/intérêts. Préfixe **G24-C5-\<n\>**.

- **G24-C5-1 (dépréciation créances douteuses 491/492) — V1 visibilité, écriture P1.** Deux étages distincts : (1) **reclasser** la créance en **459** (déjà via E4-q) ; (2) **écriture de dépréciation** D68x/C491 (mise de côté d'une perte probable, **charge collective**). **Décision : V1 = visibilité seule** (afficher solde **459** + voyant « créance à risque »), **aucune écriture**. La dotation D68x/C491 = **P1, toujours MANUELLE** (jamais barème d'ancienneté auto ; pèse sur le budget courant). *Fond. : décret 2005-240 (prudence), comptes 49x ; prérequis = reclassement 459 (E4-q).*

- **G24-C5-2 (intérêts de retard art.36 / clause pénale) — V1 = tracer la date de MED uniquement.** Règle d'or : **ne rien afficher de faux**. **Décision USER (plus sobre que la reco)** : V1 enregistre **seulement la date de mise en demeure** (point de départ légal des intérêts) ; **pas de calculateur** indicatif en V1. **Reportés P1** : calcul indicatif (taux légal versionné par semestre), **écriture comptable des intérêts** (touche le GL), **clause pénale** (champ optionnel par copro). *Fond. : CC art.1231-6, décret 67-223 art.36.*

- **G24-C5-3 (plan d'apurement / échéancier négocié) — V1 minimal.** Table **`payment_plan`** : échéances datées + statut **actif / respecté / rompu**. **Un seul effet métier** : tant que respecté → **pause ciblée des relances auto** ; **rupture** → **alerte + reprise de l'escalade** (D59) au stade atteint. **AUCUNE écriture comptable** (seul un vrai encaissement réduit le solde 45x ; le plan = calendrier + interrupteur de relances). Rappels auto/suivi visuel = enrichissement P1. *Fond. : CC art.1342-10.*

> **Domaine C.5 (Recouvrement) = CADRÉ** : G24-C5-1, G24-C5-2, G24-C5-3. Restent les PARTIAL C.5 (reclass 459 E4-q, écriture frais 10-1, legal_proceedings RPC, grain relance indivision).

### Salve 6 — OPEN importants Partie C : C.12 Ventes & état daté (2026-06-24)

> Préfixe **G24-C12-\<n\>**.

- **G24-C12-1 (frais d'état daté, plafond 380 € TTC) — champ informatif HORS GL copro.** Les honoraires d'état daté = **recette du SYNDIC**, **pas du syndicat** → **aucune écriture** dans le grand livre copro. **Décision : Option A** — champ `etat_date_fee_amount` plafonné à **380 € TTC**, affiché sur l'état daté, **défaut configurable par cabinet**. **À NE PAS confondre** avec les frais de recouvrement art.10-1 (eux au 450 du débiteur). *Fond. : décret 2020-153 ; art.10-1 II loi 65-557.*

- **G24-C12-2 (créances opposables art.20 — montant « liquide et exigible ») — calcul AUTO.** `record_mutation_opposition` **ne recopie PAS la Partie 1 brute** de l'état daté. **Montant opposable = calculé auto** = solde 450 du vendeur **EXIGIBLE à la date d'avis**, **provisions futures EXCLUES**, causes détaillées par nature (jsonb).
  - **Sources de données (aucune nouvelle)** : (1) **vue unique d'ancienneté G24-T5** (dérivée du **GL = source du montant**) ; (2) **`call_for_funds.due_date`** pour le cutoff échu/non-échu (reste dû ligne = `amount_due − amount_paid`) ; (3) **statut des exercices** pour les reliquats approuvés ; (4) **frais art.10-1** (sous-compte **450-6**).
  - **Privilégié vs chirographaire** : tri par **année d'origine** (fenêtre privilège = année en cours + ~4 ans → privilégié ; au-delà → chirographaire) ; **affiché** depuis la P1, raffinement fin = P1.
  - ⚠️ **Garde-fou** : ne JAMAIS calculer sur `call_for_funds_lines` seul (divergence GL↔lines constatée à l'audit) — le **GL tranche le montant**, les `due_date` ne servent qu'à dater l'échéance.
  *Fond. : art.20/19/19-1 loi 65-557, art.2374 CC ; s'appuie sur G24-T5.*

> **Domaine C.12 (Ventes) = CADRÉ** (volets OPEN) : G24-C12-1, G24-C12-2. Restent les PARTIAL C.12 (RPC opposition record/settle, pré-état daté, répartition art.6-2, clôture compte vendeur, enum mutation_status, RLS gestionnaire-only).

### Salve 7 — OPEN importants Partie C : C.4 Budget & appels (2026-06-24)

> Préfixe **G24-C4-\<n\>**.

- **G24-C4-1 (provision travaux décidés 102 vs appel travaux 702) — V1 = 702 seul, 102 en P1.** Trois tiroirs distincts : **702** = appels travaux votés/exécutés **dans l'exercice** (cas courant) ; **102** = provision **pluriannuelle** pour travaux décidés non encore appelés (art.14-2 I) ; **105** = fonds travaux ALUR (art.14-2 II, autre mécanisme). **Décision : V1 garde travaux → 702** ; le mécanisme **102 = P1**, branché via `operation_id` (cf. G24-C1/E4). **Documenter 102 ≠ 105 ≠ 702** (garde-fou anti-erreur d'imputation faussant l'annexe 5). *Fond. : loi 65-557 art.14-2 I/II, art.14-1.*

- **G24-C4-2 (garde « appel sur budget voté » + reconduction) — verrou RPC + case provisoire.** **(1)** Le contrôle de statut descend **DANS la RPC** `post_budget_call_for_funds` : elle **REFUSE** si le budget ≠ `validated` (verrou serveur incontournable, cohérent B3/D24/D30 ; un appel sur budget non voté = créance illégale art.14-1). **(2)** **Reconduction provisoire art.14-1 al.3** = **drapeau explicite et tracé** sur l'appel/exercice (cas de l'AG en retard : appeler sur la base du budget précédent en attendant le vote), **jamais un bypass du verrou**. *Fond. : loi 65-557 art.14-1 al.3.*

> **Domaine C.4 (Budget & appels) = CADRÉ** (volets OPEN) : G24-C4-1, G24-C4-2. Restent les PARTIAL C.4 (avance art.35 plafond/restitution, révision vs régularisation de budget).

### Salve 8 — OPEN importants Partie C : C.7 Clôture & annexes (2026-06-24)

> Préfixe **G24-C7-\<n\>**.

- **G24-C7-1 (quitus au syndic) — simple résolution AG, aucun lien outil (USER).** Le quitus est **purement moral**, sans effet comptable, indépendant de l'approbation des comptes. **Décision USER (plus sobre que la reco)** : le quitus = **une résolution d'AG comme les autres** (moteur de résolutions/votes existant, tracée au PV), **PAS de champ/entité dédié** sur l'exercice, **PAS de voyant d'alerte** spécifique, **aucun effet GL**. Garde acquise implicite : jamais couplé à `approve_period`. *Fond. : approbation des comptes ≠ quitus (deux votes distincts).*

- **G24-C7-2 (annexe 2 + cohérence 12/478) — courant seul + invariant AVERTISSEMENT avec diagnostic.** **Annexe 2 = résultat COURANT seul** (travaux détaillés en annexes 4/5, jamais dans le résultat de l'annexe 2). **Invariant de contrôle à la clôture = NON BLOQUANT (avertissement)** : vérifie *résultat courant annexe 2 == part reportée en 478* (ex-120) et *total travaux 4/5 == part en 12* (ex-110) vs l'écriture d'à-nouveau. **Décision USER** : ne bloque pas la clôture, mais **affiche l'écart ET localise la source** (quel poste/compte ne colle pas) pour faciliter la correction. *Fond. : décret 2005-240, arrêté 14/03/2005, annexes 2/4/5 ; codes 12/478 (cf. B3).*

> **Domaine C.7 (Clôture & annexes) = CADRÉ** (volets OPEN) : G24-C7-1, G24-C7-2. Restent les PARTIAL C.7 (annexe 1 régularisation, annexe 3 charges/clé, traçabilité AG↔exercice, vote budget par catégorie, régularisation N+1 672/772).

### Salve 9 — OPEN importants Partie C : C.10 GED (2026-06-24)

> État réel : GED **déjà très complète** — `documents` (file_hash, current_version_no, retention_years, deletion_blocked, category, visibility), `document_versions` (version_number, change_summary, file_hash), `document_relations` (entity/relation_kind), `document_folders` (is_system, category_default). Les 3 trous = **capacités dormantes à activer**. Préfixe **G24-C10-\<n\>**.

- **G24-C10-1 (versioning) — version = même acte ; acte neuf = document relié.** **Nouvelle version** = re-upload du **MÊME acte** (`change_summary` obligatoire, ancienne version conservée, **zéro écrasement**). **Acte juridiquement neuf** (RCP modificatif, PV rectificatif) = **NOUVEAU document** relié via `document_relations` ; l'original **reste consultable** (immutabilité, jamais réécrire l'histoire). *Fond. : décret 67-223 art.17/33 ; immutabilité calquée sur le GL.*

- **G24-C10-2 (arborescence dossiers système) — activer.** Semer **~8 dossiers système** non supprimables par copro (AG, Comptabilité, Appels de fonds, Contrats, Diagnostics, Assurances, Mutations, Correspondance), chacun avec un `category_default` ; le **rangement se déduit de la catégorie** du document (surchargeable). Exploite `document_folders.is_system`/`category_default` déjà présents. *Enjeu juridique nul, gain UX.*

- **G24-C10-3 (empreinte SHA-256 / file_hash) — doublons + sceau actes probants.** Calcul **SHA-256 à l'upload** (server function, stocké dans `file_hash` déjà présent) : (1) **doublon exact dans la même copro → avertissement NON bloquant** ; (2) **actes probants** (PV signé, état daté) → **sceau d'intégrité affiché et figé**, re-vérifiable. Limites actées : n'attrape que les doublons exacts ; le sceau ≠ horodatage légal certifié. *Filet anti-doublon + anti-altération, coût quasi nul.*

> **Domaine C.10 (GED) = CADRÉ** : G24-C10-1, G24-C10-2, G24-C10-3. Restent les PARTIAL C.10 (durée de conservation/purge, auto-classement helper register_generated_document, checklist pièces obligatoires, document_access_log).

### Salve 10 — OPEN importants Partie C : C.13 Conseil syndical (2026-06-24)

> Préfixe **G24-C13-\<n\>**.

- **G24-C13-1 (éligibilité au CS) — V1 simplifié (USER) : liste copros + ajout de noms.** Écran d'élection (`ELECT_COUNCIL`) = **liste des copropriétaires cochable** + **champ d'ajout de noms libres** (couvre le cercle élargi : conjoint/PACS, ascendants/descendants, représentant de PM, usufruitier/NP). **Décision USER (plus sobre que la reco)** : pas de blocage dur programmé du syndic (inutile — syndic pro **non copropriétaire**, absent de la liste, cf. G24-SCOPE) ; pas d'avertissement structuré sur le cercle élargi. **Reportés P1 léger** : alerte de retrait non silencieuse quand un membre du CS vend (branchée côté mutation, jamais de delete auto — cohérent D51) ; contrôle fin des incompatibilités art.21. *Fond. : loi 65-557 art.21.*

- **G24-C13-2 (usage comptable du CS, 624/706) — généraliste (USER).** Constat métier : le CS (organe bénévole) **n'a quasiment jamais de frais propres**. **Décision USER : généraliste** — les rares frais du CS = **charge d'administration ordinaire** ventilée sur la clé générale, **pas de traitement spécial**. Le compte **624 « Frais du conseil syndical » existe dans le plan, disponible mais NON imposé** (utilisable pour isoler un coût si le syndic le souhaite). Le **706 « Provisions délégation de pouvoirs au CS » reste dormant** jusqu'à activation de la délégation (D52 = P1). **Aucun module comptable CS dédié.** *Fond. : plan de comptes décret 2005-240 (624/706 vérifiés en base).*

> **Domaine C.13 (Conseil syndical) = CADRÉ** : G24-C13-1, G24-C13-2. Restent les PARTIAL C.13 (FK council_decisions, élection président CS, accès CS aux pièces, délégation de pouvoir dormante, annexion avis CS).

### Salve 11 — OPEN importants Partie C : C.16 Multi-cabinet & pilotage (2026-06-24)

> Préfixe **G24-C16-\<n\>**.

- **G24-C16-1 (facturation SaaS éditeur↔cabinet) — hors V1 + 2 briques.** Billing complet (abonnement/plan + Stripe) = **P1/P2**. **Posé dès maintenant (sans coût)** : (1) **unité tarifaire actée = par lot principal géré** (cohérent G24-4) ; (2) champ **`cabinets.status`** (actif/suspendu/résilié) **nullable**. Décision sans dette technique. *Fond. : modèle SaaS ; G24-4 (lots principaux).*

- **G24-C16-2 (équipe cabinet & rôles internes) — V1 simple + 2 crochets.** **V1** : tous les gestionnaires voient **tout le portefeuille** (cabinet souvent petit au départ). **Posés dès la baseline** (car structurent la RLS, douloureuse à rétro-installer) : (1) **rôle `admin_cabinet`** distinct (réglages sensibles : honoraires, white-label, gestion d'équipe — recoupe `platform_admin`) ; (2) **`responsible_manager_id`** nullable sur la copro (référent / filtre « mes copros »). **Répartition fine des accès (RLS par gestionnaire) = P1.** *Fond. : RGPD (minimisation) ; B5 (RLS baseline) ; recoupe C.3 résiduel (affectation gestionnaire↔copros).*

> **Domaine C.16 (Multi-cabinet) = CADRÉ** : G24-C16-1, G24-C16-2. Restent les PARTIAL C.16 (pilotage portefeuille, indicateurs cross-copro).

### Salve 12 — OPEN important Partie C : C.11 Communication (2026-06-24)

> Préfixe **G24-C11-\<n\>**. État réel : seules `ag_notifications`/`ag_notification_events` existent (AG-spécifiques), aucune table générique.

- **G24-C11-1 (notifications in-app / cloche) — table dédiée V1, temps réel P1.** Vraie table **`notifications` générique** dès la V1 (1 événement → **0..N notifs + 0..1 email**), **distincte du canal email**, alimentée par l'auto-population existante (D31/D32/D69/D70). Distinction actée : **worklist = « quoi faire »** ≠ **notification = « on m'a informé de X »** (X peut ne demander aucune action) → ne pas confondre. **Temps réel (push instantané) = P1** (V1 = rafraîchissement normal). *Fond. : besoin produit ; auto-population D31/D32/D69/D70.*

> **🎉 LES 30 TROUS OPEN DE LA PARTIE C SONT CADRÉS.** Salves 3→12. Récap : 6 critiques (C1/C6/C9/C14-RGPD/C14-FS/C15) + C.8 (×4) + C.5 (×3) + C.12 (×2) + C.4 (×2) + C.7 (×2) + C.10 (×3) + C.13 (×2) + C.16 (×2) + C.11 (×1). **Reste : les 83 PARTIAL** (souvent de simples confirmations), à balayer par lots.

---

## PARTIAL Partie C — confirmations par domaine (2026-06-24)

> Lot des résiduels PARTIAL, validés un par un. C.8-P déjà consigné dans la Salve 4. Préfixe **G24-C\<n\>-P**.

### C.1 — Onboarding & reprise de mandat (lot G24-C1-P)

- **P1 (écriture d'ouverture) — corrigé.** (1) solde de chaque copropriétaire sur son compte perso (450) ; (2) tiroir « report » (478 courant / 12 travaux) **UNIQUEMENT pour un dernier exercice clôturé mais pas encore approuvé en AG** (un exercice approuvé a déjà son résultat réparti sur les 450) ; (3) comptes d'attente (471/472) = tampon pour sommes non rattachables (paiement non identifié, transfert flou), **devant tendre vers zéro, jamais un fourre-tout**.
- **P2 (fonds ALUR) — part par lot dès la reprise.** Capter la ventilation du fonds travaux ALUR **par lot** dès la reprise (nécessaire pour l'état daté) ; UI d'édition fine = P1 ; repli = total global + note par lot.
- **P3 (immatriculation RNIC) — jamais bloquant + tâche de conformité.** Jamais de blocage du démarrage ; tâche « renseigner le RNIC » visible tant qu'il manque (très fortement demandée en reprise, simple rappel en création). *(Affine la position « obligatoire en reprise » du triage.)*
- **P4 (factures fournisseurs) — reprises individuellement** (réglables/pointables une à une), jamais un 401 global.
- **P5 (minimum pour démarrer).** Minimum = **clé générale renseignée** (tantièmes/lot) + ≥1 lot + ≥1 propriétaire/lot + ≥1 compte bancaire ; clés spéciales optionnelles. **Bloquant DUR seulement si écriture comptable impossible** (ex. clé générale absente) ; lot à 0 sur la clé générale = **avertissement** ; lot à 0 sur une clé spéciale = **normal**.
- **P6 (date d'effet + comparatif N-1) — précisé (USER).** Deux modes de date d'effet (1er jour d'exercice OU en cours d'année avec charges courues). En reprise, **importer les VRAIS totaux par poste de l'exercice précédent** (comptes approuvés **dus par l'ancien syndic, art.18-2**) pour un comparatif N-1 réel ; détail complet = **PDF en GED** (G24-AM5). « Pas de comparatif » = exception (copro neuve ou non-transmission). **Jamais de faux chiffres.**

### C.2 — Structure (lot G24-C2-P)

- **P1 (mutation en indivision / démembrement) — option A.** À la **vente**, l'écran accepte directement **N acheteurs** (parts = 100 %, un contact principal/mandataire art.23) + repère démembrement, en **réutilisant le moteur d'indivision existant** (D8/G24-2). Couvre les couples sans étape supplémentaire ; **cérémonie de vente guidée pas à pas = V1+**. *Fond. : art.23 loi 65-557.*

### C.3 — Personnes & rôles (lot G24-C3-P) — COUVERT par décisions antérieures

- **P1 (sous-rôles cabinet + périmètre gestionnaire)** → couvert par **G24-C16-2** (rôle `admin_cabinet` + `responsible_manager_id` + affectation gestionnaire↔copros ; RLS fine P1).
- **P2 (identité multi-copro du compte)** → couvert par **G24-C15** (1 compte auth/personne + 1 membership/copro + switcher D66).
- **P3 (éligibilité CS / incompatibilité syndic-CS)** → couvert par **G24-C13-1** (liste copros + ajout de noms ; incompatibilités fines P1).

### C.4 — Budget & appels (lot G24-C4-P)

- **P1 (avance de trésorerie / fonds de roulement art.35).** (1) Dépassement du **plafond légal de 1/6 du budget** (art.35-1) = **avertissement non bloquant** (l'app signale, n'empêche pas). (2) **Restitution de l'avance au vendeur = P1** (l'avance suit le lot pour l'instant, comme l'ALUR). *Fond. : art.35 / 35-1 loi 65-557.*
- **P2 (révision vs régularisation de budget).** **Régularisation de fin d'exercice = INDISPENSABLE en V1** (comparer charges réelles vs provisions appelées, ajuster chaque compte — cœur légal art.14-1). **Révision / budget rectificatif en cours d'année = P1** (plus rare). Ne pas fusionner les deux logiques (ni avec l'affectation du résultat). *Fond. : art.14-1 loi 65-557.*

### C.5 — Recouvrement / impayés (lot G24-C5-P)

- **P1 (reclassement en créance douteuse 459).** Action **manuelle** « reclasser en créance douteuse » (déplace 450→459, simple présentation, montant inchangé), **motif obligatoire**, **non bloquante**, ancrée aux stades de recouvrement avancés (jugement/risque avéré). Intégrée à la boîte à outils de correction (E4-q, 5ᵉ voie). *Fond. : art.5 décret 2005-240, compte 459.*
- **P2 (frais de recouvrement art.10-1).** Imputés au débiteur sur un **sous-compte dédié (450-6), séparé de ses charges** (hors file d'imputation FIFO) ; contrepartie **produit dédié (714)** ; **fonction comptable atomique avec motif** ; **barème configurable par cabinet**. *Fond. : art.10-1 loi 65-557.*
- **P3 (procédure judiciaire) — fonction à CRÉER.** État réel vérifié : table `legal_proceedings` riche (court, lawyer, debtor_owner_id, nature, status, amount_at_stake…) mais **aucune fonction d'écriture** (seul `tr_legal_copro_consistency`). **Créer `record_legal_proceeding`** (fonction serveur gardée, doctrine G24-T11) pour les stades judiciaires ; stades amiables déduits des relances ; frais huissier/avocat = sortie de trésorerie puis refacturés (route art.10-1) ; passage au contentieux = geste manuel après MED, **sans effet comptable**. *Fond. : G24-T11, art.10-1.*
- **P4 (grain de relance en indivision).** **Relance amiable → au seul contact principal (mandataire)** ; **mise en demeure → à CHAQUE coïndivisaire tenu de la dette** (sinon inopposable à ceux qui ne l'ont pas reçue). *Fond. : solidarité art.815-17 CC.*

### C.6 — Factures fournisseurs & banque (lot G24-C6-P)

- **P1 (modèle de trésorerie V1) — validé USER (modèle « deux poches »).** Une copro V1 = **un compte courant payeur unique** (512) + **un livret d'épargne fonds travaux ALUR** (502, compte séparé légal). **Tous les décaissements partent du courant** ; utiliser l'épargne = **virement interne livret→courant** (502→512) AVANT le paiement, puis le courant paie. **ABANDONNÉ** : mon idée initiale de multi-comptes 512 par nature (512000/512100) + `p_bank_account_id` obligatoire sur toutes les RPC de décaissement = sur-ingénierie (un seul compte payeur, pas de choix à chaque paiement). **Multi-compte courant réel = reporté P1.** *From-scratch : schéma recodé proprement en v2 ; le code v1 `settle_alur_transfer_cash` (D512/C502, libellé « Virement fonds ALUR : Livret A vers compte courant ») sert de **preuve de justesse du modèle**, pas de réutilisation. Fond. : loi 65-557 art.18 (compte séparé syndicat) + art.14-2 II (fonds travaux sur compte/livret séparé).*
- **P2 (cut-off facture→banque + verrou de période) — validé USER.** La **charge** se rattache à l'exercice où le **service a été rendu** (date de prestation, pas date d'émission de la facture) ; le **paiement** se pose sur l'exercice **ouvert** au moment du décaissement (art.14-3, droits constatés). Service relevant d'un exercice clôturé → **régularisation** (charges à payer 408 au cut-off, ou 672/772 datés N+1 — cf. C.7-P5), jamais de réouverture. **Garde = verrou central unique** : `create_ledger_transaction` (0025) refuse déjà toute écriture en période ≠ `open`, et les writers fournisseurs (`create_supplier_invoice`/`validate_supplier_invoice` 0073, paiement) y passent **tous** → protection héritée. **PAS de garde dupliquée** dans chaque writer (source unique, pas de divergence) ; ajouter seulement un **message clair + actionnable à la saisie** renvoyant vers la régularisation (ergonomie, pas 2ᵉ barrière). *Affine la position triage (« garde dans validate ET post ») : le cadenas existe déjà au cœur. Fond. : art.14-3 décret 2005-240 ; doctrine G24-T11.*
- **P3 (traçabilité du moyen de paiement réel) — validé USER (reco).** On garde l'enum `payment_method` (espèces/chèque/virement/carte/prélèvement/autre) + **UN champ `reference` dont l'étiquette s'adapte au moyen côté UI** (« N° de chèque », « Référence virement »…) — corrige le fourre-tout v1 par le contexte, sans empiler des colonnes. **La date comptable = date d'émission** ; le décalage « chèque émis non encore débité » est porté par le **rapprochement bancaire existant** (`bank_movements.status` non pointé tant que la banque ne l'a pas vu), **pas** par un statut de paiement dédié. **Reporté P1** : champs typés par moyen (n° chèque dédié, IBAN, mandat SEPA) + `payment_batch` nullable pour le lot SEPA groupé. *Fond. : état réel `payments(method, reference, status)` + `bank_movements(status)` déjà en place.*
- **P4 (TVA des factures fournisseurs) — validé USER (reco).** V1 = **copro non-assujettie** : la TVA payée au fournisseur reste **fondue dans la charge** (lignes en TTC), pas de récupération — cas de la quasi-totalité des copros. Deux précautions peu coûteuses : (1) **fiabiliser HT/TVA/taux par ligne** (colonnes `montant_ht/montant_tva/taux_tva` déjà présentes) pour la facture électronique **Factur-X** ; (2) ajouter un flag **`is_vat_liable` au niveau copro** (inactif en V1). **Reporté P1** : régime optionnel d'assujettissement avec **TVA déductible (compte 44566)** branché sur ce flag. *Fond. : CGI art.260-2° ; état réel : 0026 lignes 748-812 (« TVA non récupérable en copro »).*
- **P5 (ingestion bancaire auto + expiration consentement DSP2) — validé USER (reco).** Connexion bancaire **lecture seule** (Open Banking/DSP2) avec consentement qui **expire à 90 jours**. Gestion : (1) **alerte de re-consentement à J-7** avant expiration pour reconnecter à temps ; (2) **import manuel CSV/relevé toujours disponible** comme filet — la copro n'est **jamais bloquée** si la connexion auto tombe ou n'est pas activée. *Fond. : DSP2/Open Banking ; vision read-only [[bank_api_readonly_vision]].*

> **Domaine C.6 (Factures fournisseurs & banque) = ENTIÈREMENT CADRÉ** (P1→P5).

### C.7 — Clôture & annexes (lot G24-C7-P)

> **Croisement annexes (2026-06-24)** : vérification sur une **vraie convocation de production** (ASL La Clairière / cabinet LCM Conseil, exercice 2022) + sources officielles. **Notre fac-similé `.planning/FACSIMILE_ANNEXES_2026-06-15.md` est validé quasi à l'identique** sur les annexes 1/2/3/4. **Preuve terrain : 662 (agios) = CHARGES COURANTES** dans l'annexe 2 réelle → confirme la correction (la décision de nuit 0067 « 662→travaux » est fausse, cf. [[b4_b5_gel_multicles_livre]]). Annexes 5 et 6 absentes du PDF (pas de travaux non clôturés ; liste individualisée envoyée à part).

- **P1 (annexe 1 « régularisation par copropriétaire ») — validé USER, CIBLE CORRIGÉE = annexe 6.** La liste individualisée par copropriétaire n'est **pas** l'annexe 1 (= bilan global) mais l'**annexe 6**, indissociable de l'annexe 1 (confirmé par la note (2) du document réel + sources officielles CLCV/ARC/conseil syndical). **Format légal figé** (7 colonnes) : `N° · Nom · Total répartition (quote-part des charges réelles) · Solde précédent (report N-1) · Annulation appels (reprise des provisions appelées) · Solde après répartition DÉBITEUR · Solde après répartition CRÉDITEUR` — débiteur/créditeur **séparés, sans compensation**. **Tout dérivé du grand livre, rien de stocké** (art.11). **Gate de cohérence (lien d'or)** : Σ débiteurs annexe 6 = total créances copropriétaires annexe 1 ; Σ créditeurs annexe 6 = total dettes copropriétaires annexe 1. **Garde-fou build** : test qui reconstitue une annexe 6 réelle (chiffres LCM, annexe 1 équilibrée 60 059,01 / 86 860,16) et vérifie le lien d'or. *Reste à prouver au build (pas au cadrage) : calcul exact de « Annulation appels » + cas copro arrivé/parti en cours d'exercice. Fond. : arrêté 14 mars 2005 (annexe 6) + décret 2005-240 art.11.*
- **P2a (source de vérité du lien charge réelle → clé, pour la colonne « réalisé » de l'annexe 3) — validé USER.** État réel : `budget_lines.repartition_key_id` **NOT NULL** (0016:58) + `budget_expenses.budget_line_id` **NOT NULL** (0016:86) → **toute dépense budgétée a une clé, garanti par la base** ; chaque ligne de budget est **mono-clé** (un compte sur 2 clés = 2 lignes) donc **ventilation exacte, pas de prorata**. Trou : `supplier_invoice_lines` porte déjà `repartition_key_id` ET `budget_line_id` mais **nullables** (0073:97) → une facture peut être sans clé. **Règle = cascade à 3 niveaux** : (1) clé portée sur la ligne d'écriture si présente → (2) sinon clé de la `budget_line` rattachée → (3) sinon clé **par défaut du compte de charge**. **Garantie anti-trou** : aucune charge courante sans clé (sinon annexe 3 ≠ annexe 2 courant) → clé *de fait* obligatoire à la saisie, défaut proposé depuis le budget. **Gestionnaire maître de la clé** : défaut **modifiable** à la saisie ; **correction post-coup = re-étiquetage** (la clé est une dimension de présentation **non-GL**, comme `operation_id` — aucun impact montant/compte/lot) **tant que l'exercice est ouvert** ; **figée à l'approbation AG** (erreur tardive → régularisation N+1). *Confirme la position triage (account_id→budget_line→repartition_key_id) avec précédence ligne + filet. Fond. : annexe 3 art.10 + cohérence croisée annexe 2/3.*
- **P2b (périmètre des colonnes de l'annexe 3) — validé USER (reco).** **5 colonnes dès V1** (alignées sur l'annexe 2, format légal complet, confirmé par le document réel LCM) : `N-1 approuvé · N voté · N réalisé · N+1 voté en cours · N+2 à voter`. Toutes dérivées de données existantes (budgets par exercice + réalisé GL via P2a) ; colonnes futures **affichées vides proprement** si le budget n'existe pas encore (copro neuve / N+2 non proposé). *Révise la position triage (« 3 colonnes V1 ») : les 5 ne coûtent quasi rien de plus et évitent une annexe légalement incomplète. Stub actuel 0075 : vote OK, réalisé + N+1/N+2 à 0 (« chantier ultérieur »).*
- **P3 (traçabilité AG↔exercice + date d'approbation) — validé USER (reco), vérifié.** État réel : `accounting_periods` a `approved_at`/`approved_by` (qui a cliqué) mais **aucun lien vers l'AG** ; `approve_period(p_period_id)` / `regularize_period` ne reçoivent **pas** de date d'AG → écritures d'affectation potentiellement datées du clic. **Décision** : (1) ajouter `approved_by_ag_id` + `approved_by_resolution_id` sur `accounting_periods` (**nullables**, piste d'audit) ; (2) `approve_period`/`regularize_period` reçoivent `held_at` et **datent les écritures de répartition du résultat à la date de tenue de l'AG** (art.14-1). **Source de la date vérifiée** : `ag_meetings.meeting_date` (NOT NULL, 0017:35). **Cas sans AG** (reprise de mandat / correction) : lien AG = NULL + `held_at` **repli sur `end_date` de l'exercice**. *Additif, sans risque. Confirme la position triage. Fond. : art.14-1 décret 2005-240 ; cf. [[affectation_resultat_copro]].*
- **P4 (vote du budget par catégorie — art.24 II) — validé USER (reco), vérifié.** **Vote global par défaut** : une seule résolution sur le montant total du prévisionnel (majorité art.24), **pas** de vote séparé par clé. Le détail par clé/poste vit dans la **structure** (`budget_lines.repartition_key_id`), modèle **multi-clé non figé** (charges spéciales ventilées sur leurs clés sans vote distinct). **Vérifié** : `validate_budget(p_budget_id)` agit sur le **budget entier** + règle « un seul budget validé par copro×exercice×type » (0026:1050) → un budget = un acte global = un vote. Confirmé par le document réel LCM (1 résolution budget pour 156 106 €, détail par clé en annexe 3). *Confirme la position triage. Fond. : art.24 II loi 65-557 + annexe 3.*
- **P5 (régularisation d'un oubli sur exercice approuvé) — validé USER (VERSION CORRIGÉE par vérif).** ⚠️ **Correction de la position triage** : « via 672/772 » était le vocabulaire du **PCG**, faux pour la copro (`672 = Travaux urgents art.18` ; `772 inexistant`). **Bon traitement** : un oubli (charge/produit) découvert APRÈS approbation se régularise en **N+1 comme opération exceptionnelle** — charge sur **678 « Autres opérations exceptionnelles »** / produit sur **718 « Produits exceptionnels »** — **daté N+1**, tracé par un **source_type dédié**, entrant dans le **résultat N+1** (bloc « travaux et op. exceptionnelles » annexe 2). **Jamais de réouverture** (verrou `reopen_period` : exercice `approved` intangible, 0027:185 — confirmé). **ACTION CHART (gap trouvé)** : ajouter **`718` « Produits exceptionnels »** (absent du seed, produits s'arrêtent à 716) + **vérifier `677`** (présent dans la règle `charge_nature` 0059:25 mais absent du seed d'insertion). *Fond. : art.14-3 (intangibilité de l'exercice approuvé) + nomenclature arrêté 14 mars 2005 (678/718).*

> **Domaine C.7 (Clôture & annexes) = ENTIÈREMENT CADRÉ** (P1→P5). Croisé avec une convocation de production réelle (LCM Conseil) + sources officielles. **Findings transverses** : (a) 662 agios = courant (re-confirmé) ; (b) **action chart** : ajouter 718 + vérifier 677 ; (c) annexe 6 (liste individualisée) à implémenter au format légal 7 colonnes.

### C.9 — Maintenance (lot G24-C9-P)

- **P1 (sinistres & gestion des dommages) — validé USER.** État réel : `insurance_policies` existe (police + franchise/`deductible`) mais **aucune entité sinistre/claim**. **V1 = fiche sinistre légère mais bien connectée** (fil conducteur) : `date_survenance · nature (dégât eaux/incendie/tempête/autre) · description · insurance_policy_id · lot_id NULLABLE (vide=commun / rempli=privatif → base du recours) · status · montant estimé · indemnité reçue (nullable) · franchise · lien OS + carnet (`logbook`) + documents`. **Déclenche un OS** de réparation ; **statut piloté par une fonction de transition gardée** (doctrine G24-T11, jamais d'UPDATE éparpillé). Compta **générique en V1** (indemnité→`713` déjà au chart) → GL juste. **P1** : assistant comptable (indemnité/franchise/rapprochement travaux) + **IRSI** + **recours** contre le copropriétaire responsable (via `lot_id`). *Ajouts vs reco initiale : lot_id nullable + statut via transition (anti sous-modélisation). Fond. : art.9-1 loi 65-557, convention IRSI.*
- **P2 (génération auto des OS récurrents) — validé USER, viabilité vérifiée.** **Réveiller la capacité dormante** : `contracts.planned_frequency` + `next_planned_intervention` existent mais rien ne crée l'OS à l'échéance. **Cron crée l'OS en `draft`** (jamais `sent` — l'envoi au prestataire = geste humain, doctrine G24-T11) puis **recale `next_planned_intervention`**. **Viabilité confirmée** : pg_cron déjà en prod (patron `0055` relances impayés, daily idempotent) ; `intervention_frequency` (once/weekly/.../annual) calcule la date suivante (`once`=pas de récurrence) ; `service_orders.contract_id` (0021:116) rattache + déduplique ; `contract_status` filtre (`active`/`to_renew` seulement, jamais expired/terminated/draft). **Garde-fous** : (1) création OS + recalage date **dans une seule transaction** (fonction PL/pgSQL appelée par cron) ; (2) **anti-doublon** (pas de 2ᵉ draft si un est en attente) ; (3) filtres statut + skip `once`. Je consigne. *Fond. : [[dormant_capabilities_activation]], D15.*
- **P3 (carnet d'entretien réglementaire — décret 2001-477) — validé USER (reco).** ⚠️ Distinguer le **journal d'interventions** (`logbook_entries` + vue 0047, le « cahier de bord ») du **carnet réglementaire** (document de synthèse légal). **V1 = carnet réglementaire généré comme VUE AGRÉGÉE read-only** piochant l'existant (identité syndic, contrats d'assurance + échéances, gros travaux réalisés + entreprises, contrats de maintenance, **PPT** `planned_works`), **rendu PDF**. **Rien de stocké/dupliqué** : reconstruit à la demande depuis les données vivantes (même philosophie que les annexes). Toutes les sources existent en base (contracts, insurance_policies, logbook_entries, service_orders, planned_works). *Fond. : décret 2001-477 + art.18-1 loi 65-557.*
- **P4 (résiliation de contrat & préavis Chatel — L215-1) — validé USER (reco).** État réel : `contracts` porte `renewal_date · notice_months (déf.3) · tacit_renewal (déf.true) · end_date · status (avec terminated)` → date limite de dénonciation = `renewal_date − notice_months` calculable. **(a) Alerte Chatel auto** avant la date limite de dénonciation (patron cron, comme 0055) pour ne jamais rater la fenêtre. **(b) Courrier de résiliation** via le système de communication (D64) ; **LRAR = P1**. **(c) Passage `status='terminated'` via RPC de transition gardée** (G24-T11, jamais d'UPDATE direct). **Gouvernance** : résilier pour **non-renouvellement = acte gestionnaire** (gestion courante) ; **choisir un nouveau prestataire au-delà du seuil = mise en concurrence / gate AG (art.21)**. *Fond. : L215-1 code conso (Chatel) + art.21 loi 65-557.*
- **P5 (cohérence du cycle de statut OS) — validé USER.** **Source unique = la BDD** : enum `service_order_status` (9 valeurs : draft/sent/awaiting_provider/scheduled/in_progress/completed/closed/cancelled/refused) + fonction de transition gardée `update_service_order_status` (0032). La doc (`business-rules.md`) et les glossaires = **reflets alignés** dessus (1:1), jamais l'inverse ; `business-rules.md` n'en listait que **7** → **ajouter EN COURS (`in_progress`) / REFUSÉ (`refused`)** + corriger le mapping. Modifier l'enum = via migration si besoin (la doc suit). *Principe général : base+code = vérité, doc/glossaires = reflets. Fond. : G24-T11.*

> **Domaine C.9 (Maintenance) = ENTIÈREMENT CADRÉ** (P1→P5).

### C.10 — GED (lot G24-C10-P)

- **P1 (conservation & suppression des documents) — validé USER (reco).** État réel : `documents.retention_years` (défaut 10, 0020:110) existe, mais **pas de table de règles par type, pas de drapeau permanent, aucun blocage de suppression**. **Décision** : (1) **table de référence des durées légales par type** de document (seedée) → le doc hérite de sa durée ; (2) **drapeau `is_permanent`** (PV / EDD / RCP / carnet, art.8) = **jamais supprimable, par personne** ; (3) suppression dure **BLOQUÉE avant terme** ; après terme, **purge réservée au gestionnaire avec motif tracé** (audit). *À créer : `document_retention_rules` seedée + `is_permanent`. Fond. : art.8 décret 67-223 + obligations légales de conservation.*
- **P2 (auto-classement des pièces générées) — validé USER + GÉNÉRALISÉ À TOUS LES DOCUMENTS.** **Principe global** : `documents` = **LE référentiel unique** de toute pièce (générée ou uploadée) ; **aucune feature ne réinvente son propre stockage**. **Helper unique `register_generated_document`** = **seule porte d'entrée** d'un document généré (crée la ligne `documents` + la classe), appelé par **tous** les générateurs (convocation, PV, état daté, carnet, annexes PDF, courriers, factures, appels/rappels, attestations…). État réel : `ag_documents` (0050) est un **registre parallèle** (storage_path propre + `document_id` **nullable** → orphelins possibles) ; à **dégrader en journal technique** (versions par AG) avec **`document_id` NOT NULL**, fichier dans `documents`. **Action** : inventorier TOUS les générateurs + toute table parallèle de docs → router via le helper (cf. backlog). `technical_documents` (0020) suit déjà le bon modèle (pointe toujours vers `documents`). *Fond. : D43 ; renforce l'entrée glossaire « Document = référentiel unique ».*
- **P3 (checklist des pièces obligatoires) — validé USER (reco).** Rien n'existe (à créer). **Table de référence `document_requirements`** seedée : RCP, EDD (art.8), carnet d'entretien, fiche synthétique (art.8-2), assurance (art.9-1), PV des 3 dernières AG, diagnostics, **PPT** — chacune avec sa **base légale + condition d'applicabilité** (universelle vs conditionnelle selon âge/taille/bâti). **Vue `v_copro_document_compliance`** (présent / manquant / expiré) par copro, **dérivée de la GED** (jamais un statut stocké). **NON bloquante** : aide alimentant les todos du gestionnaire (D70), branchée sur D56. *Fond. : art.8/8-2/9-1 + loi Climat 2021 (PPT).*
- **P4 (journal d'accès aux documents) — validé USER (reco), V1.** **3e mécanisme ciblé** assumé (au-delà des 2 de G24-T9, pas de journal universel). Table **`document_access_log`** alimentée **UNIQUEMENT par la fonction serveur qui délivre l'URL de téléchargement** (point de passage unique = fiable, pas de log éparpillé) ; **on logge SEULEMENT les téléchargements** (pas les consultations/aperçus → pas de bruit) ; **purge auto à 1 an** (RGPD/CNIL, minimisation). Livré en **V1** (coût minime, la porte de téléchargement existe de toute façon). *Fond. : RGPD/CNIL ; cohérent G24-T9 (D44).*

> **Domaine C.10 (GED) = ENTIÈREMENT CADRÉ** (P1→P4).

### C.11 — Communication (lot G24-C11-P)

> État réel vérifié : `ag_envoi_tracking` (legacy 20260314 → schéma dump) porte déjà `sent_at` · `delivered_at` · `tracking_ref` · `status` (`delivery_status`) · `error_message`. `email_templates` (6 modèles) + Resend câblé (cf. D41). Préfixe **G24-C11-P**.

- **P1 (valeur probante de l'envoi : horodatages) — validé USER (reco), 3 horodatages distincts.** On **n'invente jamais** ces dates : elles viennent de **l'API + du webhook du prestataire** (réponse d'envoi + événements `delivered`/`bounced`). Donc 3 cases distinctes pour accueillir les vraies dates : (1) **`sent_at`** = expédition côté app (fait technique) ; (2) **`proof_deposited_at` + `proof_provider`** = horodatage **qualifié du prestataire LRE** (fait juridique opposable, ≠ sent_at) ; (3) **`delivered_at`** = accusé de réception (+ `tracking_ref` = réf. de preuve). **Règle d'or** : le délai légal des **21 jours francs** se lit sur **`coalesce(proof_deposited_at, sent_at)`** via l'**horloge métier** (E8-q), **jamais** sur `created_at`/`updated_at` technique. **Colonnes `proof_*` posées dès la baseline v2** (1 seul sens par colonne = clean), **câblage LRE en P1** (capacité dormante, zéro coût V1). *Fond. : art.13 décret 67-223 (point de départ = expédition) ; LRE valeur probante P1 (D41) ; horloge métier E8-q.*
- **P2 (tracking fournisseur-agnostique) — validé USER, Option A + prestataire V1 = Brevo.** **Drift corrigé** : le v1 est couplé à Resend en dur (`mails.resend_id`, webhooks « format Resend ») → en v2 on **dé-couple**. (1) **Couple neutre** `provider` + `provider_message_id` (remplace `resend_id`) ; le code applicatif ne nomme **jamais** le prestataire. (2) **Une seule table d'événements `delivery_events`** `(provider, provider_message_id, event_type, payload jsonb, occurred_at)` = **source unique de vérité de la livraison**, alimentée par le webhook à **un seul point d'écriture**, qui dérive les vues légales (`ag_envoi_tracking`) ET boîte (`mails`). (3) **1 adaptateur/« traducteur » par prestataire** (seul endroit qui parle Brevo) → ajouter un prestataire = écrire son adaptateur, **zéro migration** sur le reste ; cohabitation possible (Brevo emails ordinaires + prestataire LRE qualifié pour le légal). (4) **Règles de retour** : hard bounce → **repli papier** ; désinscription → **coupe les communications NON légales** (jamais les convocations) ; **signature du webhook obligatoire**. **⚠️ Remplace « Resend câblé » de D41 → prestataire V1 = Brevo.** *Fond. : E3-q, B7 ; un seul chemin par feature ; décision USER 2026-06-24.*
- **P3 (préférences de communication & opt-out RGPD) — validé USER, Option A (interrupteur global).** État réel : seul `coproprietaires.prefers_email` (booléen) existe → minimal. **2 principes non négociables** : (a) communications **LÉGALES** (convocation, MED, appel de fonds) = **jamais désinscriptibles** ; (b) l'opt-out RGPD ne couvre **QUE le facultatif** (mur, événements, rappels optionnels). **Correction de modèle** : séparer **canal** (`preferred_channel` = email/courrier/les_deux, routage pratique, vaut aussi pour le légal) du **consentement** (droit RGPD). **V1** = **un seul drapeau global `accepts_optional_comms`** par **personne** (droit individuel) ; le légal passe toujours. **Extensible vers le par-catégorie (P1) sans migration** (l'interrupteur global = la catégorie « tout le facultatif » ; on ajoute des catégories à côté ensuite). *Fond. : RGPD (consentement/minimisation) ; G24-T10 ; clean & simple + capacité dormante.*

---

## Socle fondateur transverse (C.17) — fin de cadrage 2026-06-25

> Grilling des **doctrines de socle** du `PRE_GRILLING_PACK_2026-06-25.md` (à trancher AVANT baseline/cron/RPC). Préfixe **C17-** = dossiers socle du pack, **namespace distinct des « C1-C9 » finance** (ambigus). Résout les anomalies de socle de `AUDIT_COHERENCE_CADRAGE_2026-06-24.md`.

- **C17-1 — Machine à états AG : guichet unique gardé `set_ag_status` + verrou base** ✅
  - **État réel (live `qqfq`, vérifié MCP 2026-06-25)** : `set_ag_status` **inexistante** (à CRÉER) ; **`ag_meetings.pv_signed_at` ABSENTE** (seul `pv_generated_at` existe → la date de signature du PV, fait juridique central, n'est stockée nulle part) ; enum `ag_status` = 10 valeurs `draft→…→archived` ; policy **`p_mgr_all` = FOR ALL** (UPDATE direct du `status` autorisé au gestionnaire) + **aucun trigger ne valide les transitions** (tout saut d'état accepté par la base). Contournements front réels : `markPvGenerated`/`markPvSent` (`documents.api.ts`) + `updateAgStatus` (`meetings.api.ts`, **bug : pose `pv_generated_at` au lieu d'une date de signature → date perdue**). **Résout ANOM-11** (business-rules.md « pv_* par UPDATE front » vs RPC gardée) **et ANOM-20**.
  - **(a) Forme = porte unique + verrou base** : créer `set_ag_status(p_ag_id, p_new_status, p_signed_at?)` `security definer` gardée, **seule voie d'écriture de `status`** ; **date serveur (`now()`, jamais l'heure du navigateur)** ; **nouvelle colonne `pv_signed_at` + `signed_by`**. Fermeture en base : **trigger `before update` refusant tout changement de `status` hors contexte RPC** (une convention non gardée se recontourne au prochain copier-coller). *(Options B « 3 RPC `mark_pv_*` » et C « UPDATE front + trigger seul » écartées : règle de transition éparpillée et/ou date de signature non stockée.)*
  - **(b) Périmètre = TOUT passe par le guichet (USER)** : `close_ag`/`finalize_ag`/`archive_ag` gardent leur logique métier (calcul votes, vérifs) mais **délèguent l'écriture du `status`** à `set_ag_status` → une seule définition de « transition légale », un seul horodatage. ⚠️ touche des RPC déjà fonctionnelles (dont `close_ag`, proche finance) → **DoD base+finance** : revue cascade AVANT + BEGIN/ROLLBACK + **golden inchangé**.
  - **(c) Refus TOUJOURS expliqué, jamais muet (USER) — RÈGLE TRANSVERSE de toutes les portes gardées C.17** : tout refus renvoie un **message FR lisible** (quoi + pourquoi + quoi faire) **+ un code stable** (l'UI l'affiche en bandeau, jamais une erreur Postgres brute type `23514`). Distingue **refus métier contrôlé** (normal, **PAS** envoyé au monitoring) vs **vrai bug**. Cohérent [[no_silent_refusal]] + doctrine idempotence C17-3 (jamais un `23505` nu).
  - **(d) Transitions = linéaires, sauts refusés** (table de transitions, déjà esquissée front `lib/ag/types.ts`).
  - **(e) « PV signé » V1 = clic daté + dessin in-app (signature SIMPLE eIDAS) ; Yousign (AVANCÉE) en V2 (USER)** : clic gestionnaire → `pv_signed_at` (serveur) + `signed_by` + **capture du dessin** (`SignatureCanvas`, lié au signataire + horodaté + journal de preuve) + **dépôt du PV signé (PDF/scan) en GED = preuve principale en V1** (le dessin = valeur juridique faible). **V2 = Yousign (signature AVANCÉE eIDAS)** déclenchée par **webhook → `set_ag_status`** (simple changement de déclencheur, zéro refonte grâce au guichet unique). Niveau **qualifié** (eIDAS art.25) écarté (non exigé pour un PV d'AG, friction forte).
  - **(f) Marche arrière = repli ENCADRÉ et tracé (USER)** : retours précis (ex. `pv_sent→pv_signed`) **via le guichet uniquement**, **motif obligatoire + trace** ; **ligne rouge = JAMAIS de retour sur une étape ayant alimenté le grand livre** (immuabilité GL). *(« aucune marche arrière » et « marche arrière libre » écartées.)*
  - **(g) Correction d'un PV signé = erreur MATÉRIELLE uniquement (USER)** : toute modif **casse la signature** → **nouvelle version** du PV + **re-signature + re-notification** (le délai de contestation de **2 mois repart** sur la partie corrigée) + **ancienne version conservée** (GED/registre, jamais réécrite — cohérent G24-C10-1). Erreur de **FOND** (décision/vote mal retranscrit) ≠ simple édition → **signalée comme nécessitant une AG rectificative**.
  - **Fond. (loi 65-557 + décret 67-223)** : art.42 al.2 (action en contestation = 2 mois dès **notification** du PV → date fiable serveur) ; décret art.17 (PV **établi et signé** en fin de séance) ; art.18 (notification = **départ du délai** → distinction `pv_signed` ≠ `pv_sent`) ; art.33 (**registre des PV** à valeur probante). La loi impose la **fiabilité de la date** et le **formalisme**, pas l'implémentation SQL — `set_ag_status` en est la bonne traduction technique.
  - **Objets à créer** : colonne `ag_meetings.pv_signed_at` + `signed_by` ; RPC `set_ag_status` (+ table/logique de transitions) ; trigger anti-UPDATE-`status` hors RPC ; (V2) webhook Yousign. **Termes à verser au glossaire à l'implémentation** : « guichet d'état / state gate », « signature simple / avancée / qualifiée (eIDAS) », « PV rectificatif — erreur matérielle vs erreur de fond ».
  - **Observabilité (note Sentry, USER)** : monitoring d'erreurs « Sentry-like » confirmé en **phase bêta** (déjà au `PLAN_MAITRE_FIN_PROJET`), **pas maintenant** ; les **codes de refus stables (c)** pré-câblent le tri « refus métier vs vrai bug » ; **scrubbing RGPD** (noms/montants) obligatoire ; **complémentaire** aux gates SQL (le monitoring attrape les crashes, pas une écriture fausse-mais-réussie). Chantier de durcissement = `CHANTIERS.md`.

- **C17-2 — Audit trail des ACTIONS comptables (qui/quand/pourquoi) : Option A, colonnes typées sur l'objet annulé** ✅
  - **État réel (live `qqfq` + migrations, vérifié 2026-06-25)** : le grand livre sait **QUI a créé** (`ledger_transactions.created_by`/`posted_by`, `payments.created_by`) mais **PAS qui a annulé ni pourquoi**. Les RPC d'annulation **reçoivent** un motif (`reverse_ledger_transaction`/`reverse_payment`/`cancel_call_for_funds`/`cancel_supplier_invoice` ont `p_reason text`) mais il n'atterrit que dans le **`metadata` JSONB de l'extourne** (`0071`/`0087`), **jamais exposé** (ni `v_general_ledger`, ni typé). `unallocate_payment` et `reverse_period_cutoff` n'ont **même pas** de `p_reason`. **Aucune colonne `reversed_by`** nulle part ; **aucune table d'audit générique** ; `reverse_payment` + `cancel_supplier_invoice` n'ont **aucun wrapper front** (motif jamais collecté). **Résout ANOM-20.**
  - **(a) Forme = colonnes typées sur l'objet annulé** : ajouter **`reversed_by` / `reversed_at` / `reversal_reason`** en colonnes propres sur l'**écriture d'origine** (`ledger_transactions`) **+ miroir sur `payments`**, **exposées dans `v_general_ledger`**. *(Option B « table d'audit générique » écartée : contredit la doctrine déjà actée « mécanismes ciblés, pas de journal universel » G24-T9 / G24-C10-P4 ; option C « exposer le JSONB » écartée : « qui a annulé » reste illisible, paiement muet = faux sentiment de conformité.)*
  - **(b) Source unique = le guichet (RPC)** : ces colonnes sont écrites **uniquement par la RPC** (plus de dispersion ; `metadata.reason` cesse d'être la source) ; **`reversed_by = auth.uid()`**, **NULL = service/import tracé « système »** (jamais NOT NULL → ne casse ni cron ni reprise).
  - **(c) Motif OBLIGATOIRE, refusé si vide AU NIVEAU RPC** (verrou, pas une politesse du front — refus expliqué C17-1c) ; **étendu à TOUTES les annulations** : ajouter `p_reason` (obligatoire) à `unallocate_payment` + `reverse_period_cutoff` qui en manquent. **Créer les 2 wrappers front manquants** (`reversePayment`, `cancelSupplierInvoice`) + champ « motif » requis dans l'UI.
  - **Périmètre / report** : couvre **100 % du besoin financier**, lisible direct dans le grand livre, **n'empêche pas** une table d'audit générique plus tard (si AG/GED en ont besoin). **Base quasi vide → aucune reprise d'historique** (pas de backfill). Prolonge C17-1 (`signed_by` + motif, verrou base).
  - **Fond.** : décret 2005-240 art.1-6 (écritures **intangibles** → une correction par contre-passation doit être **justifiable** = motif tracé) ; loi 65-557 art.18 + décret 67-223 art.18-2 (**devoir de rendre compte**). *Honnêteté : aucun texte n'impose littéralement `reversed_by` ; l'obligation est **indirecte** (intangibilité + reddition de comptes).*
  - **Objets à créer** : colonnes `reversed_by`/`reversed_at`/`reversal_reason` sur `ledger_transactions` + `payments` ; exposition dans `v_general_ledger` ; `p_reason` (non vide) sur `unallocate_payment` + `reverse_period_cutoff` ; wrappers front `reversePayment`/`cancelSupplierInvoice` + UI motif.

- **C17-3 — Idempotence des écritures : Option A, socle ciblé sur le noyau financier + empreinte naturelle** ✅
  - **État réel (live `qqfq`, vérifié 2026-06-25)** : idempotence **déjà en place sur les paiements** (`post_owner_payment`/`post_supplier_payment` ont `p_idempotency_key` + index uniques partiels `uq_payments_idempotency`/`ux_supplier_payments_idempotency`/`uq_sadv_idempotent` sur `payments`/`supplier_payments`/`supplier_advances` ; patron `ON CONFLICT DO NOTHING` + relecture + `{idempotent_replay:true}`, **jamais de `23505` nue**). **Trou** : `create_ledger_transaction` (écriture racine, **aucune clé** mais porte déjà `source_type`/`source_id`), `post_budget_call_for_funds` (émetteur d'appels, 10 params, **aucune clé**), créateurs AG (`generate_combined_calls_from_ag`/`create_budget_from_ag`/`create_alur_fund_from_ag` **déclarés non idempotents** en commentaire `0026`), `post_alur_transfer`. **Résout ANOM-20** ; recoupe ANOM-05 (retry d'`activate_ag_decisions` = double-post) / ANOM-03 (divergence GL).
  - **(a) Périmètre V1 = noyau financier** : idempotence **obligatoire** sur `create_ledger_transaction`, `post_budget_call_for_funds`, les créateurs AG, `post_alur_transfer` (paiements déjà faits). **Reste (`set_ag_status`, `reverse_*`, communication) = P1.** *(Option B « champ dédié partout dès V1 » écartée : réécrit trop de RPC, fige un contrat sur des RPC encore cassées ANOM-05/21, colonnes redondantes ; option C « statu quo » écartée : laisse le double-clic d'émission d'appel + le double-post AG ouverts toute la construction.)*
  - **(b) Empreinte naturelle pour l'écriture racine** : `create_ledger_transaction` protégée par sa **cause déjà enregistrée** `(copro_id, source_type, source_id)` (index unique partiel `where source_id is not null`), **pas de colonne `idempotency_key` redondante**. *Invariant assumé : **une cause = une écriture** ; si un flux doit poster 2 tx pour 1 cause → ajouter un petit discriminant (non bloquant).*
  - **(c) Clé obligatoire pour les émetteurs** : `post_budget_call_for_funds` + créateurs AG reçoivent `p_idempotency_key text` **rendu obligatoire** (refus **expliqué** si absent — C17-1c, jamais un `23505`), sur le patron prouvé de `post_owner_payment`. **Fabrication de la clé** : front = UUID client (anti double-clic) ; cron = clé déterministe.
  - **(d) Contrat de réponse standard** pour toute RPC d'écriture : `{ok, tx_id, idempotent_replay}`. **Garde anti-divergence** : même clé mais **paramètres différents** (ex. montant) → **erreur applicative explicite** (`P0001` « idempotency conflict »), **jamais un `DO NOTHING` silencieux** (sinon on avale un vrai bug d'appelant).
  - **(e) Correction d'objet fantôme** : la note de cadrage citait `echeancier_line_id` — **objet INEXISTANT** (ni table `echeancier_lines` ni colonne ; grep négatif). → la décision s'écrit « **clé cron = id de la future ligne d'échéancier** », créée avec l'**échéancier unifié** (D13-bis = généralisation de `budget_payment_schedules` + `resolution_id`). Dépendance, pas un acquis : ne pas coder contre un fantôme (classe d'erreur ANOM-10).
  - **Dépendances** : D15 (« chaque appel à sa date » → cron émetteur idempotent) ; C17-4 (horloge `asOf` : une écriture rejouable doit retomber sur la **même `tx_date`**) ; C17-5 (registre `cron_runs` = volet « jobs » de la même doctrine) ; ANOM-04 (recréer proprement la couche cron/edge, 0 déployée).
  - **Fond.** : décret 2005-240 art.1-5 (comptabilité partie double, **enregistrement chronologique**, grand livre = registre légal → un doublon fausse les annexes votées) ; loi 65-557 art.14-1/14-2 (appels exigibles **à dates fixées** = base métier du cron). *Honnêteté : l'idempotence n'est pas une obligation légale nommée — c'est un **moyen** d'honorer la comptabilité régulière/probante.*
  - **Objets à créer/modifier** : index unique partiel `(copro_id, source_type, source_id)` sur `ledger_transactions` ; `p_idempotency_key` (obligatoire) sur `post_budget_call_for_funds` + créateurs AG + `post_alur_transfer` ; contrat de réponse `{idempotent_replay}` + garde conflit `P0001` généralisés ; (dépend D13-bis) clé cron = id ligne d'échéancier.

- **C17-4 — Horloge métier `asOf` : date explicite validée serveur, « date du jour » automatique supprimée (Option C, 7 colonnes + `NOT NULL`)** ✅
  - **État réel (live `qqfq`, vérifié 2026-06-25)** : **cœur déjà sain** — `create_ledger_transaction` exige `p_tx_date` (aucun DEFAULT) + `p_period_id` explicite + **refuse si période ≠ `open`** ; paiements passent date + période. **Trous** : (1) **7 colonnes `DEFAULT CURRENT_DATE`** = 3 dates métier comptables (`ledger_transactions.tx_date`, `payments.payment_date`, `budget_expenses.tx_date`) + 4 dates d'effet (`repartition_keys.valid_from`, `lot_owners.start_date`, `council_members.start_date`, `technical_documents.added_date`) → filet silencieux ; (2) **année figée en dur `2026`** (`getCurrentBusinessYear`, `src/lib/time/period.ts`) dont part la résolution de période front ; (3) la date **n'est pas vérifiée tomber dans l'intervalle** `[start;end]` de la période (on peut poster 2025 dans la période 2026 ouverte). **Résout ANOM-20.**
  - **(a) Périmètre = Option C, les 7 colonnes (USER « plus propre »)** : retirer `DEFAULT CURRENT_DATE` sur **toutes** les colonnes de date métier/effet → finit la migration d'un coup, aucun état « à moitié nettoyé ». Doctrine pure : **la date est toujours une donnée explicite choisie, jamais un réflexe de la machine**. Tampons techniques (`created_at`/`posted_at`/`updated_at`/`now()`) inchangés.
  - **(b) Retrait du défaut = AVEC `NOT NULL` (refinement clé, USER)** : retirer un défaut sur une colonne nullable remplacerait « aujourd'hui en douce » par « **vide en douce** » (pire : une date d'effet NULL casse le versioning tantièmes/propriété). Chaque colonne dont on retire le défaut devient **obligatoire (`NOT NULL`)** → date oubliée = **erreur bruyante**, pas un silence. Le build vérifie que chaque chemin d'insertion (RPC/import/seed) fournit la date.
  - **(c) Source serveur de l'exercice courant** : remplacer le `2026` figé par une **RPC/vue serveur** (`get_open_period(copro_id)` dérivée d'`accounting_periods`), consommée via react-query ; `getCurrentBusinessYear` = **fallback d'affichage only**, jamais une clé d'écriture. Cohérent « GL = source unique ».
  - **(d) Garde « date dans l'intervalle »** ajoutée dans `create_ledger_transaction` : période `open` **ET** `p_tx_date ∈ [start_date; end_date]` (ceinture + bretelles).
  - **(e) Correction d'exercice clôturé = `reopen_period` (1 seule porte), PAS d'exception « régul »** : la voie de correction reste `reopen_period` (tracé, **refusé si `approved`**). « sauf régul » retiré de l'énoncé (faux besoin) ; option B « voie de régularisation dédiée » écartée (pas de 2ᵉ porte contournant l'intangibilité).
  - **(f) `new Date()` front = classer**, durcir les ~3-4 vraies dates métier (`VentesProvider`, `useSessionVoting`, OS) ; laisser les horodatages techniques.
  - **Coût assumé (Option C)** : test de non-régression **élargi au-delà de la finance** (structure/CS/GED) → chaque chemin d'insertion des 7 tables audité. Gérable (base quasi vide + chemins maîtrisés en v2) ; entre dans la DoD de la migration.
  - **Dépendances** : C17-3 (écriture rejouable ⇒ même `tx_date`) ; C17-5 (cron fournit une `asOf` = date d'effet, pas `current_date` au run) ; immutabilité période `approved` (déjà via `reopen_period`).
  - **Fond.** : décret 2005-240 art.14-3 (**droits constatés** / rattachement à l'exercice = date d'effet, pas date de saisie) ; art.5-6 (annexes **par exercice** : une écriture mal datée fausse l'annexe votée) ; loi 65-557 art.14-1/14-2 (budget voté **pour un exercice**). *Honnêteté : la loi impose le rattachement à l'exercice, pas la suppression d'un `DEFAULT` — c'est notre moyen de le garantir.*
  - **Objets à modifier** : retrait `DEFAULT CURRENT_DATE` + ajout `NOT NULL` sur les 7 colonnes ; `get_open_period(copro_id)` ; garde date-dans-intervalle dans `create_ledger_transaction` ; remplacer les usages de `getCurrentBusinessYear` comme clé d'écriture ; tri des `new Date()` front.

- **C17-7 — Contrat de sécurité des WEBHOOKS : Option 1, patron unique gravé en base, e-mail V1 / banque P1** ✅
  - **État réel (live `qqfq` + code, vérifié 2026-06-25)** : **0 edge déployée** (ANOM-04 confirmé) → tout le code webhook **inerte**. 2 implémentations divergentes (Resend) : `functions/email_webhook` (signature svix sur corps brut, puis **service_role** direct sur `ag_notification_events`, **aucune idempotence**) + `api/mail/inbound` (même patron, **service_role**, copro = `MAIL_INBOUND_COPRO_ID` **hardcodé**, aucun dead-letter). Helper `verifyHmacSignature` **dupliqué** (front + copie edge). **AUCUNE table `webhook_events`** ; **aucun webhook bancaire** (GoCardless = pull lecture seule, « callback » = redirection OAuth) ; `idempotency_key` est sur `payments` (écritures internes), pas sur la **réception** d'event. **Résout ANOM-04** ; recoupe ANOM-16/20/25/01.
  - **(a) Patron unique gravé EN BASE (Option 1)** : table **`webhook_events`** `(provider, event_id, signature_ok, payload jsonb, copro_id null, status[received|processed|dead], received_at, processed_at, error)` + **`UNIQUE(provider, event_id)`** + RPC **`ingest_webhook_event(provider, event_id, payload)` `SECURITY DEFINER`**. « Déjà vu cet event ? » et « quelle copro ? » deviennent des **invariants de base** (testables BEGIN/ROLLBACK, couverts RLS+FORCE). *(Option 2 « tout maintenant » écartée : prestataire paiement non choisi → vérif signature « dans le vide » ; option 3 « durcir sans table » écartée : patron dupliqué + service_role écrit encore en direct.)*
  - **(b) « Jamais service_role qui shunte » = discipline RPC (option a)** : l'edge garde service_role mais n'appelle **QUE** `ingest_webhook_event`, **jamais** d'`INSERT` direct sur `mails`/`payments`/`ag_notification_events`. L'edge se réduit à : **vérifier la signature (corps brut) → appeler la RPC → répondre 200**. *(Rôle DB dédié `webhook_ingestor` = P1. Articule avec C17-8.)*
  - **(c) Anti-rejeu** : clé = **`event_id` du prestataire** sinon **hash du corps brut** ; un rejeu tombe sur `UNIQUE(provider, event_id)` = **no-op idempotent**.
  - **(d) Non rattachable = HTTP 200 + dead-letter, JAMAIS une erreur** (contre-intuitif) : renvoyer 4xx/5xx fait **rejouer le prestataire à l'infini** (auto-DDoS). « Refus métier » (`status='dead'`) ≠ « erreur HTTP ». Rejeu manuel (UI admin) V1.
  - **(e) Livraison 2 temps** : **e-mail Brevo (entrant + sortant) en V1** ; **webhook bancaire/paiement en P1** (suit C.15). Signature unifiée dans **UN seul helper** (supprimer la copie edge).
  - **(f) Aiguillage vers les tables de domaine déjà décidées** : `webhook_events` = **porte d'entrée générique** (sécurité + idempotence + dead-letter) ; la RPC **dispatche** vers le domaine — e-mail → **`delivery_events`** (C.11-P2) ; banque → `allocate_payment` (C.15, P1). Pas de doublon : `webhook_events` = couche sécurité, `delivery_events` = couche domaine.
  - **(g) Routage copro** : V1 = env mono-copro acceptable (golden) ; **table mapping adresse→copro** = dépend du multi-cabinet (C.16), P1.
  - **Dépendances** : ANOM-16 (**Brevo** acté C.11-P2 → contrat provider-agnostique, pilote Brevo) ; C17-2/C17-3/C17-8.
  - **Fond.** : RGPD art.32 (sécurité du traitement → signature obligatoire, refus du non-rattachable) ; art.5-1-f/5-1-c (dead-letter = données perso → **conservation bornée**) ; décret 2005-240 art.14-3 (un webhook de paiement rejoué ne doit pas créer une **double écriture**) ; décret 2019-650 (extranet sécurisé). *Honnêteté : la loi impose le **résultat** (sécurité, sincérité, intégrité), pas « un webhook signé » — le patron en est le moyen.*
  - **Objets à créer** : table `webhook_events` (+ `UNIQUE(provider,event_id)`) ; RPC `ingest_webhook_event` `SECURITY DEFINER` ; helper signature unifié (supprimer la copie edge) ; (P1) webhook PSP + table mapping adresse→copro + rôle `webhook_ingestor`.

- **C17-5 — Contrat des jobs automatiques (CRON v2) : contrat socle figé au Palier 0, jobs construits aux Paliers 4/6** ✅
  - **État réel (live `qqfq` + migrations, vérifié 2026-06-25)** : **UN SEUL cron** = `0055_cron_payment_reminders.sql` (pg_cron 7h → RPC `run_daily_payment_reminders` → `net.http_post` vers edge `run_payment_reminders`). Trois défauts : (1) **pousse dans le vide** (ANOM-04 : 0 edge déployée sur le live) ; (2) **aucun registre applicatif** (`cron_runs`/`run_history` = 0 grep ; seul `pg_cron.job_run_details` interne, jamais lu) → on peut découvrir 3 mois après qu'aucune relance n'est partie ; (3) **fire-and-forget** (`net.http_post` sans lecture de réponse). Idempotence relances/paiements **déjà réelle** (`uq_reminder_lot_delay_active`, `post_owner/supplier_payment` ON CONFLICT) ; **vrai trou** = futur job d'émission (`budget_payment_schedules` sans clé de ligne). `DEFAULT current_date` encore présent (prérequis C17-4). **Résout ANOM-04 + ANOM-20** ; recoupe ANOM-03/26.
  - **(a) Doctrine = poser le CONTRAT au Palier 0, construire les jobs plus tard** (ne pas coder maintenant) ; sinon réécriture rétro de tous les jobs (ANOM-20).
  - **(b) Rattrapage à DEUX VITESSES (USER) — le point comptable central** : si le cron saute des jours, l'**ÉMISSION** d'appels est rejouée **à sa date d'exigibilité passée** (un appel = écriture comptable, date fixée par le calendrier voté art.14-1 ; on date l'**obligation**, pas le courrier → **PAS un antidatage** ; affichage transparent « exigible le 1er · édité le 4 ») ; les **RELANCES** sont rattrapées **à AUJOURD'HUI, jamais antidatées** (la mise en demeure court à l'envoi réel, CC art.1344-1 ; on date l'**acte**). ⚠️ **Garde-fou** : rejeu dans le passé **seulement si la période est ouverte** ; si clôturée (intangibilité) → **alerte gestionnaire**, jamais de forçage. *(« Une seule vitesse, tout à aujourd'hui » écartée : fausse les dates d'exigibilité.)*
  - **(c) Mode d'émission des appels = AU CHOIX du gestionnaire (USER)** : réglage **par copro**, **défaut = « préparé + validation 1 clic »** (le cron prépare + notifie, le gestionnaire valide → coup d'œil avant de facturer, évite une contre-passation) ; **« automatique à échéance » en opt-in**. Le cron lit ce réglage copro par copro.
  - **(d) Registre `cron_runs` = cahier de bord** : 1 ligne par **(job_name, copro_id, business_date)** + statut (running/success/failed) + started/finished + résumé + erreur. **La clé (job, copro, date) EST l'idempotence du rattrapage** (rejouer un jour fait retombe sur la ligne, ne refait rien → **jamais de double facturation**). Granularité **par copro** : un plantage sur 1 copro ne bloque pas les autres (calque atomicité D32).
  - **(e) Idempotence obligatoire sur tout job financier** (jamais un `23505` nu) ; vrai trou = futur job d'émission → clé = **id de la future ligne d'échéancier** (dépend C17-3/D13-bis, objet à créer, pas un acquis).
  - **(f) Alertes INFORMATIVES, jamais bloquantes** : « aucun run réussi depuis N jours » écrit dans `cron_runs`, lu comme KPI à l'écran cabinet ; alertes contrats/carnet restent en **pull** (`v_contracts_alerts`/`v_logbook_alerts` 0047), pas de cron qui pousse en V1.
  - **(g) Qui écrit le résultat = l'EDGE elle-même en fin de run (service_role)** ; pg_cron reste le déclencheur in-db. *(Correction dossier : l'edge actuelle ne persiste RIEN — lignes 440-452 = payload HTTP jeté ; l'écriture dans `cron_runs` est à AJOUTER.)*
  - **Périmètre** : **V1 = émission appels (D15, à créer) + relances (0055)** ; **P1 = bascule contrats to_renew/expired, alertes carnet, balayage `stale`, contrôle min ALUR**. **Cadence des relances (J+15/30/60/90 + ton) PARQUÉE → dossier recouvrement**. Le job relances **lit la vue d'ancienneté GL (G24-T5)**, jamais un 2ᵉ calcul, et **attend EXP-4** (source des créances) pour ne pas relancer sur un faux solde.
  - **Dépendances** : **C17-3** (clé idempotence échéancier) + **C17-4** (asOf : rattrapage à la date d'effet ⇒ écrire à `p_tx_date`, pas `current_date`) — *à figer dans la MÊME passe Palier 0* ; D15/D13-bis ; D14 ; G24-T5 ; ANOM-04/E1-edges ; Brevo (supersede Resend) ; ANOM-03 (EXP-4).
  - **Fond.** : décret 2005-240 art.14-3 (droits constatés → appel rattaché à SA date d'exigibilité = rattrapage par date d'effet pour l'émission) ; loi 65-557 art.14-1 (provisions exigibles le 1er jour du trimestre = calendrier juridiquement déterminé) ; CC art.1344/1344-1 (intérêts moratoires courent de la mise en demeure = relance jamais antidatée) ; décret annexes (chiffres de relance dérivent du GL arrêté). *Honnêteté : `cron_runs`/idempotence/alertes ne sont PAS imposés par un texte copro — ingénierie de robustesse ; la loi impose le RÉSULTAT (appels à la bonne date, relances valablement notifiées), pas le mécanisme.*
  - **Objets à créer** : table `cron_runs` (+ clé `(job, copro, business_date)`) ; règle rattrapage 2 vitesses ; réglage « mode d'émission » par copro ; écriture du résultat côté edge ; (V1) job émission D15 + instrumentation 0055. **Termes glossaire** : « rattrapage à deux vitesses », « date d'exigibilité ≠ date d'édition », « mode d'émission des appels », « registre cron_runs », « alertes informatives ».

- **C17-6 — Cohérence des machines à états SQL : enum technique EN / libellé FR, figer chaque machine avant l'écran** ✅
  - **État réel (live `qqfq` + dump + code, vérifié 2026-06-25)** : enums techniques EN canoniques (`service_order_status` 9 ; `mutation_status` **7 valeurs incl. `sent_to_notary`** — migration 0079, live à 0087, le dump `_archive` à 6 valeurs est **PÉRIMÉ** ; `period_status` 3 = open/closed/approved). **Drift front** : l'OS a **4 listes divergentes** (`statuts.ts` 12 FR + 3 alias dupliqués, `statuts-intervention.ts` 4 FR, `service-order.ts` copie, `business-rules.md` 6) ; mutation = libellés FR **menteurs** (`draft`→« Notifiée », `signed`→« En attente acte »). **Aucune RPC de transition gardée** OS/mutation (UPDATE front cru `sales/api.ts:486` → drift + crash 22P02). Période **déjà gardée** (`approve_period`/`close_period`/`open_next_period`/`reopen_period`). `logbook_status` + `document_visibility` **en FR DANS l'enum SQL**. **Résout ANOM-11/23/25/20.**
  - **(a) Doctrine = l'enum SQL (EN) est la seule horloge de référence de CHAQUE machine ; le FR n'apparaît qu'à l'affichage** via **UNE table de libellés par machine** (label + couleur + ordre timeline + transitions), dérivée du SQL. **Figer une machine AVANT de reconstruire son écran** (sinon on recopie au hasard une horloge contradictoire = dette figée dans le neuf ; « corriger > copier » violé).
  - **(b) OS** : enum SQL 9 = canonique ; **fondre les 4 sources front en 1** (`src/lib/enums/serviceOrder`), aligner `business-rules.md`, **purger les 3 alias dupliqués** + modèles FR concurrents. **refusé ≠ annulé conservés distincts (USER)** (refusé = prestataire décline / annulé = syndic retire). **Périmètre = vie de l'intervention seule** ; **« facturé/payé » NON ajoutés à l'enum** → **statut dérivé (USER)** : l'écran affiche un 2ᵉ voyant (« facture : payée ») **en lisant la facture liée**, jamais une valeur recopiée (« afficher sans recopier », cohérent EXP-7). *Prérequis plomberie : lien OS↔facture (à vérifier).*
  - **(c) Mutation** : **GARDER les 7 valeurs (sent_to_notary inclus, déjà migré 0079)**, **CORRIGER les libellés FR menteurs**. **signé ≠ validé conservés distincts (USER)** (signé = acte notaire = fait juridique ; validé = le syndic acte le transfert du compte à l'acquéreur — séquence légale de la mutation).
  - **(d) Période** : cycle **ouvert → clôturé → approuvé** confirmé **complet (USER, vérifié web)** — ouvert (exercice en cours) / clôturé (le **syndic arrête les comptes** : fige + immutabilité) / approuvé (**l'AG approuve**, art.24, dans les 6 mois en N+1). **Refus d'approbation = PAS de 4ᵉ statut** : l'exercice **reste « clôturé »**, **pas de réouverture automatique** ; `reopen_period` reste un **geste manuel** du syndic en cas d'erreur réelle (interdit si `approved`) ; le refus est tracé **dans la résolution d'AG**, pas dans le statut de période ; continuité comptable préservée (à-nouveaux). Ajouter les **libellés FR** (Ouvert/Clôturé/Approuvé).
  - **(e) Transitions par RPC SQL gardée** (jamais d'UPDATE front cru) — étend `set_ag_status` (C17-1) à OS/mutation ; **~2 RPC à créer** (OS, mutation ; période déjà gardée).
  - **(f) `logbook_status` + `document_visibility` (FR dans l'enum)** : **NE PAS ré-angliciser maintenant** (ALTER TYPE sur enum utilisé = risque pour gain cosmétique, base quasi-vide) → **exemptés, dette notée**.
  - **(g) Garde-fou CI B8** : compare les valeurs d'enum SQL aux clés des maps de libellés front, **casse si divergence** (vaccin anti-drift type `sent_to_notary`/22P02).
  - **Dépendances** : C17-1 (guichet `set_ag_status`, principe étendu) ; D15 (mutation linéaire) ; le palier de reconstruction de chaque écran (OS/ventes/clôture) suit cette décision.
  - **Fond.** : décret 2005-240 art.14-3 (compta d'engagement → période = machine gardée serveur, **par ANALOGIE de bon sens**, pas un mandat littéral) ; loi 65-557 + décret 67-223 art.5 (état daté → mutation jalonne un acte juridique, signed ≠ validated exigé). **AUCUNE loi ne contraint le workflow OS** (workflow produit). *Honnêteté : `sent_to_notary` n'est ni imposé ni interdit par la loi = décision métier (J5) ; distinguo « absent du dump archivé » vs « interdit par la loi » tenu.*
  - **Objets à créer/modifier** : `src/lib/enums/*` (1 map FR par machine) ; fonte des 4 sources OS + alignement `business-rules.md` ; correction libellés mutation ; libellés FR période ; ~2 RPC de transition gardées (OS, mutation) ; test CI B8. **Termes glossaire** : « machine à états », « statut dérivé / afficher sans recopier », « RPC de transition gardée », « garde-fou B8 ».

- **C17-8 — Droits du super-admin (`platform_admin`) : lecture seule par défaut + break-glass dès V1, anti-cumul en base** ✅
  - **État réel (live `qqfq` + migrations 0001-0087, vérifié 2026-06-25)** : modèle actuel = **super-admin tout-puissant (l'inverse de la décision)**. `platform_admin` = **rôle dans `memberships`** (enum `membership_role`, 0003) ; helper `user_is_platform_admin()` (0023) appelé en tête des **DEUX** pivots RLS — lecture (`user_has_copro_access`) ET écriture (`user_is_copro_manager`) → **un compte support écrit dans le GL de tout cabinet client**. Front (`authz.ts`, `layout.tsx`) traite platform_admin **à égalité** avec gestionnaire. **Inexistants** (absents de NOTRE code, pas interdits par la loi) : `profiles.is_platform_admin`, distinction lecture/écriture, **toute table d'audit** (`audit_log`/`break_glass`/`support_session` = 0 grep), garde anti-cumul. RLS **ON sans FORCE** (sauf 5 tables finance prod). **Résout ANOM-01 + ANOM-20.**
  - **(a) Posture = lecture seule par défaut, MAIS capacité d'écrire conservée via break-glass (USER)** : le support voit tout (diagnostic) mais **n'écrit pas par défaut** dans le métier d'un client. *(« Passe-partout permanent » écarté par l'USER : rouvre la faille de l'audit live + écriture hors mandat art.18 + trou RGPD ; « lecture seule pure » non retenu — l'USER veut garder la capacité d'agir.)*
  - **(b) Break-glass DÈS V1 (USER)** : écriture admin **exceptionnelle et encadrée** — fenêtre **bornée dans le temps** + **motif obligatoire** + **tout journalisé** + **gestionnaire notifié**. **Périmètre d'usage = remédiation TECHNIQUE d'urgence** (réparer une écriture qu'un bug a posée de travers), **PAS faire la compta à la place du syndic** (la routine reste au gestionnaire, qui garde la main et peut ratifier/contester). Table `support_session` (admin_id, copro_id, motif, expires_at) consommée par le portier écriture pendant la fenêtre. **Conséquence planning : le journal d'audit devient PRÉREQUIS V1** (assumé) — il arrive de toute façon avec **C17-2**, donc ordre : journal d'abord, break-glass ensuite. *(« Lecture seule V1, break-glass P1 » non retenu.)*
  - **(c) Emplacement** ⚠️ **AMENDÉ le 2026-06-26 par C16-4 → TABLE DÉDIÉE `platform_admins(user_id PK)` hors-tenant** (et NON la colonne `profiles.is_platform_admin` initialement prévue) : la colonne `profiles` serait auto-modifiable (la policy `p_own_update` ne verrouille que `cabinet_id`) → risque d'auto-promotion ; une table dédiée rend l'auto-promotion **structurellement impossible**. `platform_admin` RETIRÉ de l'enum `membership_role` (une seule source de vérité). Posé **hors flux** (seed/console), jamais via l'UI cabinet. *(cf. bloc C16 pour le détail.)*
  - **(d) Lecture seule = bypass admin UNIQUEMENT sur le portier lecture** (`user_has_copro_access`) ; **RETIRÉ du portier écriture** (`user_is_copro_manager`). Écriture admin **seulement sur tables PLATEFORME** (cabinets/profiles/work_domain/templates **système**, `copro_id NULL`), **jamais** le métier client (GL/appels/paiements). ⚠️ **grep des appelants réels** (seed/harnais écrivant via platform_admin) AVANT de couper le bypass (leçon 0033).
  - **(e) Anti-cumul admin/gestionnaire = AU NIVEAU BASE (trigger), pas seulement applicatif (USER)** : jamais juge (éditeur) ET partie (gestionnaire). Trigger sur `memberships` (refuser INSERT `gestionnaire` si `is_platform_admin`) ET sur `profiles` (refuser `is_platform_admin` si déjà gestionnaire). **Vaut aussi pour le fondateur → comptes séparés** (un admin + un gestionnaire).
  - **(f) Compte admin = authenticated NORMAL soumis à la RLS, JAMAIS service_role** (cohérent C17-7) ; **prérequis ANOM-01 : RLS en FORCE généralisé** (sinon lecture seule sans valeur — owner/service contourne).
  - **(g) Front** : **espace support lecture-seule séparé** (bandeau « mode support ») ; **retirer platform_admin** des gardes `requireCoproManager`/`requireAnyManager` (`authz.ts`) + layout gestionnaire (un admin n'est PAS un gestionnaire).
  - **Dépendances** : E1 (livrable technique) + B5 (RLS ON+FORCE + 2e cabinet) ; ANOM-01 (FORCE) ; **C17-2** (le break-glass écrit dans ce journal) ; C.15 multi-rôle (switcher) ; C.16 (admin_cabinet ≠ platform_admin).
  - **Fond.** : loi 65-557 art.18 (le **syndic seul** tient les comptes → écriture éditeur = hors mandat, sans valeur) + art.18-2/20 (conservation des pièces) ; décret 2005-240 (GL = pièce légale ; intangibilité de l'exercice approuvé) ; RGPD art.5-1-f/32 (moindre privilège, traçabilité des accès support) + 5-1-c/25 (minimisation, privacy by default = lecture seule native). *Honnêteté : `profiles.is_platform_admin`, les tables d'audit/break-glass et la distinction lecture/écriture sont ABSENTES de notre code (objets à créer), pas des obligations légales déjà violées.*
  - **Objets à créer** : colonne `profiles.is_platform_admin` + retrait `platform_admin` de l'enum `membership_role` ; refonte helpers 0023 (bypass lecture only) ; triggers anti-cumul (memberships + profiles) ; table `support_session` + RPC break-glass ; **journal d'audit (prérequis V1, cf C17-2)** ; FORCE RLS généralisé ; espace front support + nettoyage `authz.ts`/`layout.tsx`. **Termes glossaire** : « support éditeur / super-admin », « break-glass », « anti-cumul admin/gestionnaire », « journal d'audit ».

> **🧱 SOCLE C.17 COMPLET — 8/8 doctrines tranchées (2026-06-25)** : C17-1 (machine à états AG) · C17-2 (audit trail annulations) · C17-3 (idempotence) · C17-4 (horloge métier) · **C17-5 (cron v2)** · **C17-6 (cohérence machines à états SQL)** · C17-7 (webhooks) · **C17-8 (super-admin / break-glass)**. Le bloc socle est entièrement cadré → place aux arbitrages expert (EXP-4 et suivants).

## Arbitrages expert & ordre conseillé — fin de cadrage (2026-06-25)

> Dossiers du `PRE_GRILLING_PACK_2026-06-25.md` dans l'ordre conseillé, après le socle C.17. Préfixes du pack (**EXP-**, **C16-**, **C15-**…), namespace distinct des « C1-C9 ».

- **EXP-7 — Source de vérité des créances ET du réalisé : Option 2, GL source unique, séquencé (USER)** ✅
  - **État réel (live `qqfq` + code, vérifié 2026-06-25)** : **dérivation GL déjà canonique** pour Dashboard + annexes (réalisé = SUM classe 6 sur `ledger_entries`/`ledger_transactions` posted ; `fn_dashboard_kpis`/`fn_annexe_2/4/5`, migration 0028). **MAIS deux compteurs parallèles fournissent encore des chiffres** : (1) **réalisé** — l'écran Budget lit `v_budgets_overview`/`v_budget_lines_overview` qui somment `budget_expenses.amount` (0036), pas la classe 6 → deux « réalisés » possibles ; (2) **impayés** — `v_unpaid_by_lot`/`v_unpaid_with_reminders` (0028/0036) somment `call_for_funds_lines.amount_due−amount_paid`, **pas le solde 45x du GL**. **Double-posting** (ANOM-12) : `validate_budget_expense` ET `validate_supplier_invoice` postent tous deux D6xx/C401. **Chiffre fantôme** : `reverse_ledger_transaction` (0071) ne propage rien vers `budget_expenses.status`/`supplier_invoices.status`. **`v_lot_vs_gl_mismatch` + `audit_finance_integrity` détectent mais ne résolvent pas** (sondes). **Vérifié 2026-06-25 : `v_lot_vs_gl_mismatch` = 0 ligne** (base quasi vide + correctifs juin) → divergence **structurelle prouvée par le code**, pas active aujourd'hui (les « 4950 vs 4450 / −13550 » = constat audit 22-23/06, non permanent). **Résout ANOM-03 + ANOM-12.**
  - **(a) Doctrine = GL seule source des MONTANTS** : tout MONTANT (réalisé d'un budget, impayé d'un lot) se **LIT dans le grand livre**. `budget_expenses` + `call_for_funds_lines` gardent un rôle de **SAISIE et suivi ligne-à-ligne** (brouillon→validation→pièce→tiers ; qui doit combien sur quelle échéance) mais **ne fournissent plus aucun chiffre de synthèse**. Prolonge la règle maison « GL = source unique » + E2/E6 + G24-T5 (déjà actés) ; EXP-7 = le volet **réalisé** jamais tranché explicitement. *(Option 3 « compteurs maintenus + sonde » écartée : double-vérité, annexes contestables, fantôme persistant.)*
  - **(b) Séquencement = Option 2 (USER, ordre IMPÉRATIF)** : **1) RÉALISÉ d'abord** (autonome) — rebrancher `v_budgets_overview`/`v_budget_lines_overview` (`total_spent`/`validated_spent`/`remaining`/`consumption_pct`) sur la **classe 6 du GL** ; `budget_expenses` reste table de saisie, `.amount` n'est plus un chiffre de synthèse (garde anti-dérive). **2) IMPAYÉS ensuite, APRÈS création de la vue d'ancienneté unique G24-T5** (solde 45x via `get_lot_balance_45x` 0082), puis rebrancher `v_unpaid_by_lot`/`v_unpaid_with_reminders`/`impayes/api.ts`. *(Débrancher avant que G24-T5 existe casserait l'écran impayés + les paliers de relance J+15/30/60/90.)* `call_for_funds_lines` conservées pour le suivi payé/dû d'une ligne (`allocate_payment`) ; `due_date` sert à **DATER**, jamais à fournir le solde.
  - **(c) Double-posting (ANOM-12) = couper, pas fusionner** : **retirer le posting D6xx/C401 à `validate_budget_expense`** → `budget_expenses` devient le palier **« engagé »** (cohérent `CONTEXT.md` « requalifier en engagement » + G24-T6 commitments) ; la charge n'est postée qu'**une fois**, par la facture fournisseur. Séquencer (couper le double-posting d'abord, unifier l'UI Budget/Factures ensuite) — **pas tout d'un bloc**.
  - **(d) Rattachement réalisé→budget** : créer **`v_budget_voted_vs_realized`** au grain budget via **`operation_id`** (ledger ligne, 0060/0073) — PAS l'heuristique `compte 6x + EXISTS budget_lines` (fausse si 2 budgets partagent un 6x). ⚠️ **`operation_id` ne couvre aujourd'hui que les budgets `works`** ; pour le **courant**, étendre `operation_id` aux budgets courants **ou** assumer le rattachement par compte — à expliciter.
  - **(e) Invariant = gate CI bloquante** : `audit_finance_integrity` (sonde) → **gate bloquante** (0 ligne `LOT_GL_MISMATCH`/`CALL_TOTAL_MISMATCH` sur golden Tilleuls, condition DoD finance) **+ sonde « statut fantôme »** (écriture extournée mais `budget_expenses`/`supplier_invoice` encore `validated`). `cancel_call_for_funds` : **contre-passer le GL** (déjà 0071), jamais effacer les lignes.
  - **Dépendances** : E2/E6 (acté) ; **G24-T5 (vue d'ancienneté GL = prérequis des impayés)** ; `operation_id` e4 (0060/0073) ; C17-2 (contre-passation/audit) ; G24-T6 (commitments/engagé) ; modèle facture fournisseur (validate_supplier_invoice).
  - **Fond. (ancrage CORRIGÉ)** : partie double + **droits et obligations constatés** = **décret 2005-240 art. 1 à 5** (+ plan comptable **arrêté 14/03/2005**) — *PAS « art. 14-3 du décret » (confusion : l'art. 14-3 est dans la LOI 65-557 = arrêté/régularisation des comptes)* ; annexes = décret 2005-240 art. 5-6 (doivent dériver du GL) ; **solde 45x = créance légale** (arrêté 14/03/2005) ; CC art. 1342-10 (imputation) = **dater/affecter** seulement, pas établir le montant.
  - **Objets à créer/modifier** : rebranchement `v_budgets_overview`/`v_budget_lines_overview` sur classe 6 ; `v_budget_voted_vs_realized` (via operation_id) ; **vue d'ancienneté unique G24-T5** (solde 45x) puis rebranchement des vues impayés ; retrait posting `validate_budget_expense` ; `audit_finance_integrity` en gate CI + sonde statut fantôme.

- **EXP-4 — Invariant d'équilibre de l'annexe 1 : égalité GLOBALE, alerte non bloquante à 2 visages + trace (USER)** ✅
  - **État réel (migrations 0001-0087, vérifié 2026-06-25 ; dump `_archive` antérieur à 0075 = NON fiable pour les annexes)** : `fn_annexe_1` (0075) calcule déjà les 4 totaux + les 2 totaux généraux (`total_general_creances`/`total_general_dettes`), 450-5 ALUR isolé hors section II (E7), non-compensation par lot. **MAIS aucun bloc « équilibre », aucun gate** : l'égalité créances=dettes n'est imposée nulle part. GL toujours équilibré globalement (`tr_check_transaction_balance` 0024). `gate_0075` prouve shapes/450-5/non-compensation mais **PAS** l'égalité globale. Mécanisme d'équilibre « après répartition » = `regularize_period` (ventile 478→450-1, 12→450-2 par quote-part) ; **tant qu'il n'a pas tourné, l'écart = résultat de l'exercice non affecté** (état transitoire légitime). **Pas de migration 0088** ; helper TS `annexe-equilibre.ts` annoncé « fourni » mais **ABSENT** (notes périmées). **Cadre ANOM-13 (faux risque, cf infra) + ANOM-03 (montants déjà tranchés EXP-7).**
  - **(a) Égalité GLOBALE, pas fine (USER)** : invariant = **Total général créances (trésorerie + créances) = Total général dettes (provisions/avances + dettes)**, au centime. Seule égalité légalement requise (fac-similé `FACSIMILE_ANNEXES_2026-06-15.md` l.105/158 « 15 288 = 15 288 » ; CONTEXT.md l.97). *(Contrôle fin sous-total par sous-total écarté : trésorerie ≠ provisions par nature → bloquerait des copros saines.)*
  - **(b) Armement APRÈS affectation seulement** : l'égalité n'a de sens qu'**après `regularize_period`** (résultat réparti) **ET** prise en compte des **travaux gelés** (le compte 12 garde un solde tant que l'AG ne l'active pas — `p_affecter_travaux=false` par défaut, 0057/0058) → « affecté » = courant ventilé ET (travaux activés OU pas de solde 12). Avant : écart = résultat non affecté, **normal**.
  - **(c) Garde-fou = ALERTE NON BLOQUANTE à DEUX VISAGES (USER, supersede la reco « gate dur »)** : **jamais de blocage** de l'envoi. **Pop-up à l'envoi** dont le message **s'adapte** : 🟢 **avant affectation** = pédagogique (« écart = résultat de l'exercice non encore affecté, normal à ce stade ») ; 🔴 **après affectation** (vrai trou) = alarmant (« annexe 1 déséquilibrée de X € — approbation des comptes **annulable sur recours** (2 mois) »). Message intelligent = pas de fatigue d'alerte (le rouge ne sort que sur un vrai problème). *(« Gate dur » et « gate dur permanent » écartés par l'USER — cohérent doctrine transverse « alerter > verrouiller ».)*
  - **(d) Trace du passage outre (USER)** : cliquer « Envoyer quand même » sur un pop-up rouge **enregistre** (qui/quand/écart) → responsabilité de mandataire assumée + preuve de décision informée si contestation. **Branché sur le journal d'audit C17-2.**
  - **(e) SQL lecture seule, jamais de throw PDF** : `fn_annexe_1` expose un bloc `{creances, dettes, ecart, ok}` + RPC réutilisable `assert_annexe1_balanced(copro, period)` ; **aucune exception levée** (le PDF reste générable, les annexes 4/5 conformes jamais détruites). Le pop-up vit à l'action « envoyer » **côté v2-tanstack** (front Next `src/` gelé).
  - **(f) Périmètre comptes à compléter (correction SÛRE, indépendante)** : ajouter **42/43/44** (autres tiers, ex. État/TVA) côté créances ET dettes, **49** (dépréciation) côté dettes, **vérifier 106** (provision travaux délégation CS) côté provisions — sinon un solde réel déséquilibre l'annexe alors que le GL est juste (faux positif).
  - **ANOM-13 = faux risque** : la **dernière** def de `regularize_period`/`open_next_period` est **0058** (`b5_assertion_multicles`, supersede 0057/0056/0027) et utilise bien les codes **12/478** ; seuls des **noms de variables/commentaires** « 110/120 » résiduels subsistent (à nettoyer dans **0058**, pas 0057). Pas de re-correction du chemin de code.
  - **Test 0088** : prouver l'égalité globale sur la golden « Domaine des Tilleuls » **APRÈS** cycle complet `close → open_next_period → regularize_period → approve` (seul état où l'égalité est dure ; `gate_0075` tourne sans affectation → ne peut pas l'asserter).
  - **Dépendances** : Palier 5 (vues GL + annexes 1-6) ; `regularize_period` (affectation) + immutabilité période ; EXP-7 (GL source des montants) ; C17-2 (trace du passage outre) ; cohérence croisée fac-similé n°2 (annexe 1 compte 12 = annexe 5 total col E).
  - **Fond.** : **arrêté du 14 mars 2005** (modèles annexes 1-5 ; structure actif/passif → égalité des 2 totaux généraux) — *PAS « décret 2005-240 art. 11 » (confusion : art. 11 = décret 67-223, pièces jointes à la convocation)* ; décret 67-223 art. 11 (annexes obligatoires à la convocation d'approbation) ; décret 2005-240 (droits constatés → résultat affecté pour que l'annexe « après répartition » s'équilibre) ; loi 65-557 art.14-1/14-2 (provisions/ALUR = base de la distinction). **Risque = approbation des comptes sur annexe fausse → ANNULABLE sur recours (loi 65-557 art.42, 2 mois)**, pas une nullité automatique. *Honnêteté : le bloc équilibre / la RPC / le helper TS sont ABSENTS de notre code (à écrire), pas des obligations légales déjà violées.*
  - **Objets à créer** : bloc `{creances,dettes,ecart,ok}` + `assert_annexe1_balanced` dans `fn_annexe_1` ; ajout comptes 42-44/49/106 ; pop-up à 2 visages + trace (v2, action envoi) ; test 0088 sur golden après cycle complet ; nettoyage commentaires 110/120 (0058). **Termes glossaire** : « équilibre annexe 1 », « après répartition / écart = résultat non affecté », « pop-up à deux visages », « trace du passage outre ».

## Arbitrages bloquants pré-construction — tranchés 2026-06-26 (USER)

> Les 5 arbitrages strictement USER que l'audit de cohérence (`COHERENCE_PLAN_V2_2026-06-26.md`) signalait comme bloquant le démarrage de la refonte. Tranchés en dialogue le 2026-06-26. Débloquent finance/onboarding/mutation/relances/état daté/portail.

- **ARB-1 — Couplage impayés ↔ grand livre (ANOM-03) : GL seul juge du montant, appels = carnet d'âge** ✅
  - **Décision** : le **solde 45x du grand livre est l'UNIQUE source du montant** d'un impayé (relances, état daté, portail, dashboard). Le détail des appels (`call_for_funds_lines`) **ne fournit plus jamais un total** : il devient un carnet de suivi (quelle échéance, quelle date, quelle nature) servant à l'**ancienneté** et à l'**imputation** (art. 1342-10). **UNE seule vue d'impayés** (vue d'ancienneté G24-T5) = **montant pris dans le GL + âge pris dans les lignes d'appel**. Une **sonde** (`audit_finance_integrity`) **alerte** si GL et détail divergent (jamais bloquant) ; en cas d'écart, on **corrige le détail pour coller au GL**, jamais l'inverse.
  - **Pourquoi** : deux compteurs = deux vérités (audit : GL 4950 vs détail 4450). Écart aujourd'hui = 0 (base quasi vide) → défaut **structurel**, à figer AVANT de coder les impayés. Prolonge EXP-7 (volet impayés) + doctrine états G24-T11 (« conséquence des comptes » toujours dérivée du GL).
  - **Dépendances** : vue d'ancienneté G24-T5 (à créer, Palier 5) = prérequis du rebranchement de `v_unpaid_by_lot`/`v_unpaid_with_reminders`.

- **ARB-2 — Modèle de trésorerie : « deux poches » (ANOM-02)** ✅
  - **Décision** : un **seul compte courant payeur (512)** d'où sortent toutes les dépenses + un **livret ALUR (502)** étanche. Puiser dans le fonds travaux = **virement interne 502→512** avant tout paiement. **Abandon du multi-512 par nature** et du `p_bank_account_id` obligatoire au paiement. Ratifie la décision récente G24-C6-P P1.
  - **Pourquoi** : modèle le plus structurant de la finance (chaîne paiement fournisseur, EXP-6 tuile fonds, D67 carte 2 faces, table `copro_bank_accounts`). Colle à la réalité d'une copro (compte de gestion + livret réglementé), évite de désigner un compte à chaque clic, garde l'argent ALUR visible/étanche.
  - **À faire** : **réécrire D21 / D67 / G24-T7** pour acter « deux poches » comme seul modèle et marquer multi-512 + `p_bank_account_id` **abandonnés**. À figer au Palier 0, avant de créer `copro_bank_accounts`.

- **ARB-3 — Mandat de syndic : minimal en V1 via `contracts` (ANOM-08)** ✅
  - **Décision** : modéliser un **mandat minimal dès la V1** (durée, dates début/fin, lien copro ↔ cabinet) en **réutilisant la table `contracts`** existante (type « syndic ») — **pas de table dédiée** (`syndic_mandate` écartée : `contracts` suffit). Réaligner la vieille note A3 (« mandat plus tard ») : le mandat **minimal** est V1.
  - **Pourquoi** : sans entité mandat, le plafond 3 mandats (D30, art. 22), la tuile « mandats à renouveler » (C16-5), la sortie de mandat (C16-3) et le vote AG « renouvellement syndic » (`APPOINT_SYNDIC`) sont orphelins/no-op. Coût quasi nul via `contracts` = règle « le plus simple qui reste conforme ».
  - **Mandat COMPLET (honoraires détaillés, grille Novelli) = P1**, posé sur la brique minimale.
  - **Idée produit retenue → chantier P1** : **catalogue d'actes facturables auto-comptabilisés** (paramétrage par syndic : envoi LRAR, mise en demeure, état daté, frais de mutation… avec libellé + tarif + **qui paie** + écriture déclenchée). L'acte = un **événement** → écriture **gardée** (idempotente C17-3) → montant dans le **GL** (ARB-1). ⚠️ régime légal par acte : frais de relance/mise en demeure = imputables **au seul copropriétaire défaillant** (art. 10-1, sous condition de mise en demeure préalable) ; état daté = **au vendeur**, **plafonné 380 € TTC** ; forfait de base ≠ actes ponctuels (forfait = C16-2, ligne budget 621 via 701).

- **ARB-4 — Date de référence de l'état daté : date d'effet `v_eff` (EXP-3)** ✅
  - **Décision** : la photo des comptes se fige à **`v_eff` = date d'effet de la mutation (sinon date de signature, sinon date du jour pour un pré-état daté)** = **seule** date de référence. **Pas d'« arrêté de comptes figé »** (exercice clos) ni de `dateReference` saisie librement (reliquat v1, meurt gelé). **Correctif obligatoire** : la projection « à venir » (partie 3, provisions votées non appelées) est **cadrée sur le seul exercice de `v_eff`** (via `period_id`), pour stopper le **cumul multi-années** qui gonfle la facture acheteur (applique E9-q). Garde-fous : état daté **« final »** exige une vraie date d'effet (pas la date du jour) ; document affiche **« situation arrêtée au [date] »**.
  - **Vérifié sur internet 2026-06-26** : conforme à l'**art. 5 décret 67-223** (« photographie de la situation financière du cédant **au moment de sa réalisation** », « d'une manière même approximative et **sous réserve de l'apurement des comptes** ») → l'« arrêté figé » serait **contraire** au texte. Annexe partie 3 = « **deux exercices précédents** » (confirme le bornage par exercice). Modèle pré/final (chacun figé à sa propre date) = déjà conforme.

- **ARB-5 — Minimum légal du fonds travaux ALUR : `MAX(5 % budget ; 2,5 % PPT)`** ✅
  - **Décision** : la référence « minimum légal » (carte fonds travaux D67, todos conformité D70, contrôle cron P1) = **`MAX( 5 % du budget prévisionnel ; 2,5 % du montant des travaux du PPT )`**. Le **5 % budget est le plancher TOUJOURS** ; la **branche 2,5 % PPT** s'active dès qu'un PPT est adopté/saisi. V1 : appliquer le plancher 5 % (toujours calculable) + ajouter la comparaison 2,5 % PPT dès que le montant du PPT est renseigné. **Alerte NON bloquante** (« cotisation sous le minimum légal », jamais de blocage d'AG).
  - **Vérifié sur internet 2026-06-26 (art. 14-2 II loi 65-557)** : sans PPT = 5 % budget ; avec PPT = **2,5 % du montant des travaux du PPT ET 5 % du budget** (double exigence → MAX). **Corrige 2 notes fausses** : « 2,5 % du **budget** » était FAUX (le 2,5 % porte sur le **PPT**) ; « MAX(2,5 % PPT ; 5 % budget) » n'était juste **qu'avec PPT**. ⚠️ PPT **obligatoire pour les copros > 15 ans depuis le 1/1/2025** → la branche PPT devient la norme.

> **🔓 5 ARBITRAGES BLOQUANTS LEVÉS (2026-06-26)** : ARB-1 (impayés↔GL) · ARB-2 (deux poches) · ARB-3 (mandat minimal V1) · ARB-4 (date état daté = v_eff) · ARB-5 (min ALUR = MAX(5% budget ; 2,5% PPT)). **Reste un risque d'ORDRE** (pas un arbitrage métier) : C15-5 multi-rôle à figer AVANT le middleware (Palier 1) — à traiter dans le grilling des dossiers restants.

## C15-5 — Multi-rôle / multi-copro : contexte serveur + source unique + identité = e-mail (USER 2026-06-26) ✅

> Doctrine d'identité/sécurité à figer AVANT le middleware (Palier 1). Risque d'ordre signalé par l'audit de cohérence. Tranché 2026-06-26.

- **(a) Contexte actif côté SERVEUR** : le couple `{copro_id, role}` vit dans un **cookie signé relu serveur** (jamais en sessionStorage client). Le **middleware global revérifie à chaque requête** contre les rattachements RÉELS (`memberships` + dérivations) ; **aucun rôle par défaut** — pas de rattachement = **deny** (corrige le faux-vert v1 `isManager = … || true` / `SINGLE_COPRO_MODE=true`).
- **(b) Source unique = RPC `get_my_contexts()` à créer** : agrège `memberships` (gestionnaire/copropriétaire) + **dérivation `council_members`** (casquette conseil, qui ne vit PAS dans l'enum `membership_role`) + `lot_owners` (qualité de copro). Évite que chaque écran réimplémente la règle (maladie v1 des 3 `.from()` séparés).
- **(c) Identité = l'adresse e-mail (USER)** : **une personne = un e-mail = un compte**, multi-casquette. Pas d'e-mail « spécifique » généré ; le compte copropriétaire naît d'une **invitation envoyée à l'e-mail au fichier** (`link_coproprietaire_account`). Conséquence : **comptes séparés autorisés naturellement** si les e-mails diffèrent (ex. pro pour gérer / perso pour être copro) ; **un seul compte** si même e-mail partout (le switcher montre alors toutes les casquettes). L'appli **supporte les deux, n'impose rien**.
- **(d) Cumul gestionnaire + copropriétaire** : 
  - *Même copro qu'il gère* → **un seul compte**, qualité de copro **dérivée des lots** (pas de 2ᵉ rattachement) ; on **garde la contrainte `UNIQUE(user_id, copro_id)`** (lever = casse les 3 helpers DEFINER, inutile). Séparation nette possible si le syndic préfère via un e-mail perso (= cas autre cabinet).
  - *Copro d'un AUTRE cabinet* → il est un **copropriétaire normal** de cet autre cabinet (portail de cette copro).
- **(e) « 1 cabinet / personne » = vaut UNIQUEMENT pour la casquette GESTIONNAIRE (USER confirmé)** (on travaille pour un cabinet employeur). **N'empêche PAS** d'être copropriétaire dans des copros d'autres cabinets. `get_my_contexts` **unionne** memberships (filtrés cabinet pour le rôle gérant) ET lots/portails (tous cabinets confondus pour le rôle copro). Multi-cabinet pour le rôle gérant = hors V1 (exigerait `profiles.cabinet_id` 1→N).
- **Prérequis NON négociable** : **RLS ON + FORCE** (ANOM-01, aujourd'hui ON sans FORCE) — dernier rempart de l'étanchéité ; + **test e2e d'étanchéité inter-cabinet sur VRAIE session** (pas service_role) attaquant **les `.from()` directs ET les RPC DEFINER** (FORCE ne protège pas une RPC DEFINER → c'est sa logique qu'il faut prouver). Durcir `assert_public_tables_have_rls` (0086) pour vérifier aussi `relforcerowsecurity`.
- **Objets à créer** : RPC `get_my_contexts()` ; cookie de contexte signé ; middleware global deny-by-default (revalidation memberships) ; branchement front de `link_coproprietaire_account` (jamais appelé aujourd'hui) ; durcir 0086 (FORCE). **À purger** : docstring trompeur d'`activeCopro.ts` citant `get_default_copro_id()` (RPC fantôme). **Dépend de** : RLS FORCE (chantier durcissement) ; recoupe C16/C17-8 (platform_admin).

## EXP-1 — Cotisation minimale fonds travaux ALUR : règle à jour + garde-fou non bloquant (USER 2026-06-26) ✅

> Complète ARB-5 (seuil). Vérifié sur Légifrance : **ARB-5 confirmé contre le mémo interne périmé**.

- **(a) Seuil légal À JOUR = art. 14-2-1 loi 65-557** (en vigueur depuis 1er janv. 2023, loi Climat & Résilience 2021) : **avec PPT** = `MAX(5 % budget ; 2,5 % du montant travaux PPT)` ; **sans PPT** = 5 % du budget. **Confirme ARB-5.** Le mémo `docs_obligatoires_convocation.md` (« CORRECTION EXPERT 2026-06-15 = 5 % flat, pas de MAX ») était **périmé** (version ELAN 2018, avant l'ajout de la branche PPT par la loi Climat) → **corrigé le 2026-06-26**. La ligne 42 de `AUDIT_COHERENCE_CADRAGE_2026-06-24.md` (« 2,5 % budget vs MAX… ») est à marquer **tranchée** : les DEUX fausses branches (« 2,5 % du budget » ET « formule isolée ») sont abandonnées.
- **(b) Plancher NON universel = 3 exemptions légales** (drapeau par copro, pas une règle aveugle) : immeuble neuf **< 5 ans** après réception ; copro de **10 lots ou moins** (dispense votée à l'unanimité — « 10 ou moins », pas « moins de 10 ») ; copro dont le **DTG ne prévoit aucuns travaux sur 10 ans**.
- **(c) Garde-fou INTELLIGENT, non bloquant** : 5 % **pré-rempli par défaut** + **avertissement** si l'AG vote en dessous, wording exact = *« en dessous du plancher légal — vérifiez qu'une exemption art. 14-2-1 s'applique, sinon la décision est irrégulière »* (⚠️ PAS « l'AG est souveraine et peut baisser » : on ne peut pas déroger à un minimum d'ordre public hors exemption). **Aucun blocage SQL** : le GL enregistre le vote réel (cohérent doctrine non-bloquante + GL source unique).
- **(d) Montant DÉRIVÉ du budget validé** (jamais saisi à la main = anti faux-WYSIWYG ANOM-17) ; **assiette = budget prévisionnel COURANT seul** (art. 14-1, pas budget+travaux) — vérifier au câblage que `budgetMontant` = le courant.
- **(e) UNE seule source de vérité** pour la règle (5 % + MAX PPT + exemptions) : tuer la **constante morte** `FONDS_ALUR_POURCENTAGE_MIN` (jamais importée) + centraliser les « 5 % » dispersés dans ~6 fichiers (anti ANOM-13).
- **Garde côté écriture** : la cotisation reste D450-5 / C105 (`post_budget_call_for_funds` nature=alur), distincte de l'AFFECTATION du fonds (D105/C705, `alur_transfers` 0037). La garde 5 % vit en **validation front (avertissement)**, jamais en CHECK SQL bloquant.

## EXP-5 — Clôture du compte vendeur à la mutation : simple POINTAGE + 3 garde-fous (USER 2026-06-26) ✅

- **Décision = pointage de jalon, PAS d'écriture comptable de transfert.** Le solde 450 porte la dimension `lot_id` (« il suit le lot ») : `validate_mutation` ferme la ligne vendeur (`lot_owners`) et ouvre l'acheteur, le 450 ne bouge pas → **rien à transférer**. Une écriture vendeur→acheteur **doublerait** une réalité déjà portée par lot_owners + lot_id (deux vérités sur la même créance). Le transfert de propriété n'est **pas une opération comptable** (ni charge, ni produit, ni encaissement) = fait juridique enregistré par jalon (décret 2005-240 art.5). C'est déjà l'état du code (`gl_posted=false`). *(Option « écriture de transfert » écartée.)*
- **Garde-fou 1 — trace SERVEUR si solde ≠ 0** : aujourd'hui l'alerte n'est qu'un texte de modale qui disparaît (zéro trace). Créer une **issue/tâche persistante** (« solde vendeur de X à traiter : retenue notaire / reprise acquéreur ») = preuve que le solde a été vu/traité (triage C.12 jalon 6 « issue forcée si solde≠0 »). **Petit chantier** (table/colonne ou `mutation_steps.payload`), pas un simple câblage.
- **Garde-fou 2 — LIVE pour l'alerte / FIGÉ pour l'état daté** : `get_lot_balance_45x` (live) pour l'avertissement gestionnaire (« soldé maintenant ? ») ; l'état daté reste figé à `v_eff` (ARB-4). Deux dates, deux usages, assumé.
- **Garde-fou 3 — opposition art. 20 = SEUL mécanisme à écriture, distinct** : la seule vraie écriture touchant le 450 à la vente = versement notaire (D512/C450) via l'opposition. ⚠️ **RPC `record_mutation_opposition` / `settle_mutation_opposition` PAS encore codées** (tables présentes, fonctions à créer — chantier C.12). Ne pas présenter l'opposition comme opérationnelle.
- **Dépendance** : fiabilité de l'alerte conditionnée à ARB-1 (GL source unique). Cas créditeur (trop-perçu/avances dus au vendeur) → arbitrage remboursement vs reprise acheteur à inclure dans l'issue (à ne pas laisser implicite).

## EXP-6 — Tuile « Fonds travaux » : compte 105 seul + carte à deux faces (USER 2026-06-26) ✅

- **Décision = la tuile « Fonds travaux » montre le compte 105 STRICT** (vrai fonds ALUR, art. 14-2 II), via `v_alur_fund_balance` (vue déjà votée « source unique » 2026-06-08), **imposée aux 3 surfaces** (dashboard, balance, vue) — fini les 3 chiffres divergents. **Bug corrigé** : le dashboard sommait **103 (Avances) + 105** → mélange une **dette envers les copropriétaires** (avances remboursables art. 35/18) avec la **réserve acquise** (ALUR non remboursable). Trompe les votes AG + fausse le contrôle du minimum 5 % (ARB-5, qui se calcule sur le 105 seul) + contredit l'état daté.
- **Carte à DEUX FACES (jamais additionnées)** : **Face 1 = réserve constituée = 105** (engagement comptable, comparée au minimum légal) ; **Face 2 = trésorerie réellement épargnée = compte 502 SEUL** (le livret). Les additionner = double comptage (105 = promesse, 502 = cash ; ils divergent : cotisation appelée non encaissée, ou virement déjà fait). ⚠️ **Correction** : le compte « 512100 » cité ailleurs est un **fantôme** (jamais seedé) → Face 2 = **502 uniquement**, pas « 502 + 512100 ». Afficher le rappel « virement livret→courant non effectué » à côté.
- **Le 102 (provisions travaux décidés, art. 14-2 I) n'est PAS agrégé** : notion juridiquement distincte du 105. **Dormant en V1** (les travaux votés passent par 702 + budget travaux ; le 102 n'est jamais mouvementé → solde 0). Le **report des travaux non terminés est assuré par le gel du résultat travaux** (compte 12 + `settle_works_balance`, déjà acté) → **boucle finance JUSTE sans le 102 en V1**. Le 102 = **présentation bilancielle plus orthodoxe** + suivi **par opération** → **chantier P1** (avec le palier `operation_id`), **validé par un test golden de travaux à cheval sur 2 exercices**. **Pas un bloqueur de justesse V1** (arbitrage USER : à remonter si un vrai syndic/CS exige la ligne 102 au bilan dès V1).
- **À corriger aussi** : la balance range à tort le **703 (Avances, produit)** dans la famille « fonds-travaux » et **omet le 705 (Affectation du fonds ALUR)** → aligner sur 105/705. Supprimer le commentaire faux « fonds de réserve 103 » (≥ 2 occurrences dans 0028). Non bloquant (lecture seule).

## EXP-2 — Immutabilité de l'exercice approuvé + régul N+1 exceptionnelle 678/718 (USER 2026-06-26) ✅

> Prolonge C17-4 (horloge) et le gel travaux. Vérifié sur internet 2026-06-26 (Légifrance arrêté 14 mars 2005 + jurisprudence approbation des comptes).

- **(a) Gel TOTAL à `approved` (ratifié, déjà codé)** : exercice approuvé = **intangible** (aucune écriture, aucune régénération d'à-nouveau/affectation rétroactive ; même les `source_type` régénérables se figent via `is_ledger_regen_exempt` qui se ferme dès qu'une période est `approved`). **`reopen_period` interdit si `approved`** (déjà gardé). Statut **`closed` reste réouvrable** avant l'AG (corriger puis re-clôturer) ; `approved` = définitif.
- **(b) Correction d'un oubli post-AG = RÉGUL EN AVANT (N+1), jamais de réouverture** : opération **exceptionnelle datée dans l'exercice ouvert suivant** — **charge → 678, produit → 718**. **Bannir 672/772** (vocabulaire PCG entreprise erroné : 672 = travaux urgents art.18 ; 772 inexistant en copro). **Tracée par un `source_type` dédié** (ex. `exceptional_regularization`) — visible dans le grand livre, isolée dans le bloc « travaux et opérations exceptionnelles » de l'annexe 2. ⚠️ ce `source_type` NE doit PAS entrer dans la liste blanche `is_ledger_regen_exempt` (régul = écriture définitive normale de N+1).
- **(c) Vérif légale (Légifrance)** : **intangibilité confirmée** — comptes approuvés non rouvrables. **Seule exception = judiciaire et individuelle** : un copropriétaire peut contester **sa répartition de charges** devant le tribunal sous **5 ans** (ou fraude) → ce n'est PAS une réouverture du GL par le syndic, c'est une révision judiciaire de répartition (hors moteur). ➡️ **Option C « break-glass » écartée (contraire au droit)** ; **Option B « régul par nature » écartée** (mélange une charge N-1 dans le résultat courant N+1 sans signal, fausse la lecture).
- **(d) Nomenclature confirmée (arrêté 14 mars 2005)** : **678 = « Charges exceptionnelles »** (classe 6) ; **718 = « Produits exceptionnels »** (classe 7, famille 71). ⚠️ libellé du seed « Autres opérations exceptionnelles » pour 678 = variante à **harmoniser** sur l'intitulé officiel « Charges exceptionnelles » (numéro bon).
- **(e) Gap chart à réparer (déjà au backlog CHANTIERS)** : **ajouter 718** (absent de tout le seed) + **corriger 677** (référencé dans `charge_nature` mais jamais inséré) + finir le renommage **110→12 / 120→478 dans `open_next_period`** (ANOM-13, sinon on fige le verrou sur du vocabulaire mort).
- **(f) Déclencheur du gel = à l'AG d'approbation des comptes** (fait juridique daté, cohérent C17-1/C17-6). Le **câblage front de `approve_period`** (aujourd'hui zéro appelant) = **chantier séparé** (le moteur existe). **Termes glossaire** : « intangibilité de l'exercice approuvé », « régularisation N+1 / opération exceptionnelle », « 678 charges exceptionnelles / 718 produits exceptionnels (≠ 672/772) ».

## Dossiers socle « rejoués » — tranchés 2026-06-26 (USER)

> Les 6 dossiers socle jamais consignés (EXP-2 déjà traité ci-dessus). Vérifs internet faites pour C14-3 / C14-4 / C15-2.

- **C11-P4 — Anti double-convocation (envois en masse)** ✅ : écrire l'envoi **`'pending'` AVANT** d'appeler le prestataire e-mail + **contrainte `UNIQUE(ag, copro, canal[, version])`** → un rejeu (cron qui repart, double-clic) retombe dessus = **no-op idempotent** (calque D32). Filet en BASE, pas seulement côté écran. Rejeu doux sur `ag_envoi_tracking` ; abandon de la pile transitoire. *(Pas de dimension légale.)*

- **C14-1 — Journal d'accès aux documents** ✅ : aujourd'hui le « qui a vu quel acte » vit dans un **store RAM perdu au reload** (fantôme `document_access_log`, ANOM-10). → **journal MINIMAL réel** : table `document_access_log` (RLS gestionnaire-only, purge 1 an, alimentée par la **seule** server function) + RPC **`set_document_visibility`** sur les **3 niveaux SQL existants**. **Abandon du mock 4-niveaux + ACL** (supersédé par D69-bis). Cohérent RGPD art. 32 (traçabilité) + décret 2019-650 (extranet). La table d'accès est elle-même un traitement → registre art. 30 + info art. 13.

- **C14-3 — DTG document dédié, jamais 3e source travaux** ✅ (Option 1) : **DTG = `technical_documents` type='dtg'** (l'enum existe déjà), **JAMAIS une 3e source de données travaux** (respecte D39 : PPT dérivé des opérations réelles). **V1** = rendre le type réel + l'afficher dans la check-list de conformité. **Pré-remplissage assisté DTG→PPT = P1**. **Vérifié (CCH L.731-1 à L.731-5)** : DTG = **constat** en 4 volets (état PC, conformité, améliorations, DPE collectif) qui **alimente** le PPT (≠ s'y substitue) ; si DTG sans travaux sur 10 ans → dispense de PPT possible. ⚠️ Calendrier : le **PPT** est progressivement obligatoire (2023→2025 toutes copros) ; le **DTG** n'est obligatoire que dans des cas précis (mise en copro immeuble >10 ans, insalubrité, vote AG) → **pas de règle « DTG obligatoire pour tous »**.

- **C14-4 — Factur-X dès la V1 : champs prêts + INGESTION entrante (USER, avantage concurrentiel)** ✅ : périmètre V1 = **(1)** champs requis à la saisie (SIRET exigé à la validation avec porte de sortie `vat_number`, **HT+taux→TVA dérivés & persistés**, `currency DEFAULT 'EUR'`) **+ (2) ingestion d'un Factur-X entrant** (importer une facture fournisseur Factur-X → **pré-remplit toute la saisie**). **Génération sortante + ventilation multi-taux EN 16931 (BG-23) = P1.** TVA **scalaire** par ligne en V1 ; GL reste **TTC** (copro non-assujettie, TVA = charge ; flag `is_vat_liable` dormant). **Vérifié (calendrier officiel)** : **réception obligatoire pour TOUTES les entreprises au 1er sept. 2026** (émission grandes/ETI 2026, PME/TPE 2027 ; sanction 15 €/facture) → l'**ingestion** est l'obligation la plus proche + le gain de temps qui fait vendre. Réf : ord. 2021-1190, LF 2024 art. 91, CGI art. 289/289 bis, EN 16931. **Débrancher/étiqueter « démo » la page Factur-X mock** (anti faux-vert ANOM-10).

- **C15-2 — Paiement des charges : modèle SEPA posé en V1, prestataire AGNOSTIQUE (penchant direct-ICS), live en P1** ✅ : 
  - **Modèle (acté)** : prélèvement **SEPA** (pas CB), **provider-agnostique** (doctrine webhook C17-7, 1 adaptateur/presta). Plomberie : table `sepa_mandates` + écriture GL **uniquement sur paiement CONFIRMÉ** via **`post_owner_payment`** (jamais d'INSERT brut, jamais au clic — SEPA asynchrone J+2/J+5, rejet jusqu'à 8 semaines), **contre-passation `reverse_payment`** sur rejet/recall (R-transaction). Webhook **signé dédié** + `webhook_events` (idempotence `event_id`). **V1 = modèle posé**, prélèvement **live = P1**.
  - **Prestataire = NON tranché, décision à l'activation P1 sur chiffres réels.** **Penchant état final = DIRECT ICS** (le syndicat prélève via son **ICS** vers son **compte séparé** : **0 commission**, pas de fonds de tiers, idiomatique — ce que font les logiciels syndic ; coût = dev `pain.008` + retours bancaires). **GoCardless Payments = option MVP rapide** (API/webhooks clés en main, ~1 % plafonné 2 €, montage multi-créancier). **Lemonway/MangoPay (cantonnement) ÉCARTÉS** (overkill : chaque copro encaisse sur son propre compte). ⚠️ Mémoire « SEPA GoCardless déjà intégré » **partiellement fausse** : on n'a que **Bank Account Data (lecture seule)**, pas le moteur de prélèvement.

> **Bilan grilling 2026-06-26** : 5 arbitrages bloquants (ARB-1→5) + C15-5 + bloc EXP (EXP-1/3/5/6) + EXP-2 + 5 socle extras (C11-P4, C14-1, C14-3, C14-4, C15-2) = **17 décisions tranchées et consignées**. Reste à griller : C16 (5), C15-1/3/4, C12 (6), C13 (5), C14-2/5/6, C11-P5/P6.

## Bloc C16 — Cabinet / multi-cabinet — tranché 2026-06-26 (USER)

> Le mandat (ANOM-08) est déjà tranché par ARB-3 (réutiliser `contracts`). Vérif internet faite pour la passation (C16-3, art. 33 décret 67-223).

- **C16-1 — Mandat = `contracts` domaine « syndic », cabinet inscrit comme tiers** ✅ : confirme ARB-3. Réutiliser la table `contracts` (start/end/renewal_date, tacit_renewal, notice_months, slug `'syndic'` déjà seedé `0004`) → hérite gratuitement de l'alerte renouvellement (`v_contracts_alerts` 0047), des vues et du moteur AG (`MANAGE_CONTRACT` 0030). **Conséquence ACCEPTÉE (USER)** : `contracts.tiers_id` NOT NULL → le **cabinet gestionnaire est inscrit comme un `tiers` de type 'syndic'** dans chaque copro. Travail V1 : seeder un `contracts 'syndic'` à `create_copro` (durée défaut 3 ans modifiable, jamais bloquée si <3 ans) ; re-router `APPOINT_SYNDIC` (no-op aujourd'hui) vers la logique `MANAGE_CONTRACT`. ⚠️ « plafond 3 mandats » = faux ami (= 3 pouvoirs de vote/mandataire art.22, déjà codé 0065 — rien à voir avec la durée).

- **C16-2 — Honoraires de syndic = ligne de budget sur 621 (noyau V1, zéro objet)** ✅ : compte **621** « Rémunération du syndic » (+ 6211/6212/6213/622) déjà seedé (`0025`), classé `charge_nature='courant'` (`0059`). Chaîne « ligne budget 621 → validate_budget → post_budget_call_for_funds » (D450-1/C701) marche génériquement (prouvé golden loop 0029). **Zéro objet nouveau** ; seul ajout : pré-suggérer la ligne 621 dans tout nouveau budget. Prestations hors forfait (état daté, recouvrement) = saisie sur 622. On comptabilise la charge copro, **jamais le CA cabinet** (GL par-copro). **Grille Novelli complète = P1** (ALUR 2014 + décret 2015-342). ⚠️ honoraires TOUJOURS via le budget prévisionnel, jamais le wizard d'appel libre.

- **C16-3 — Sortie de mandat : PASSATION ENRICHIE en V1 (export complet) + statut archivé gardé serveur** ✅ (reco révisée à la hausse) : 
  - **Statut de cycle de vie** : ajouter `copros.status (active|archived)` + archived_at/by/reason via **RPC gardée `set_copro_status`** (jamais d'UPDATE front). « Lecture seule » **RÉELLE imposée serveur** (les RPC d'écriture refusent si `status=archived`), réversible+tracée, **ne touche jamais `cabinet_id`** (reprise par un autre cabinet = ré-onboarding). Filtrer AUSSI les KPI portefeuille sur le statut (sinon compte les mandats perdus).
  - **Dossier de passation V1 = export complet de l'existant** (les données existent déjà → c'est une fonction d'export, pas 6 modules) : comptes (grands livres général/fournisseurs/copropriétaires + balance + comptes annuels + annexes du dernier exercice + relevés/soldes bancaires) + **soldes 45x + état des impayés** + **liste copropriétaires/lots/TANTIÈMES** + contrats en cours + **fonds** (trésorerie/ALUR/avances) + carnet d'entretien/diagnostics + **pièces GED (ZIP)** + **liste récapitulative**. Couvre l'obligation légale (**décret 67-223 art. 33** + loi art. 18-2, transmission **sous 1 mois**) ET = argument de vente (« dossier de passation en 1 clic »).
  - **P1** : le **vrai transfert intra-plateforme** (réaffectation cabinet) + formatage légal léché. Déclencheur V1 = manuel (auto sur date de fin = P1, dépend du mandat). ⚠️ base légale = décret 67-223 art. 18-2/art. 33 (pas « loi art. 18-2 »).

- **C16-4 — Escalade super-admin : TABLE DÉDIÉE `platform_admins` (affine C17-8)** ✅ : **faille active** — `user_is_platform_admin()` (`0023`) fait un `exists` sur `memberships` **sans regarder `copro_id`** → un seul rang = bypass GLOBAL R+W sur tous les cabinets. **Décision = sortir le flag dans une table dédiée `platform_admins(user_id PK)` HORS-tenant** (≠ colonne `profiles` initialement prévue C17-8) : **auto-promotion structurellement impossible** (l'utilisateur n'a aucun accès écriture à cette table) — vs la colonne `profiles` qui exigerait de durcir `p_own_update` (vigilance, oubli = faille). Bypass = **lecture seule tracé**, écriture exceptionnelle = **break-glass** (motif + audit), **cumul platform_admin+gestionnaire INTERDIT**. Créer **`admin_cabinet`** dans le même chantier. **Patch sur le LIVE (0088+) AVANT** d'activer un 2e cabinet (faille active = prérequis E1/B5). Corriger les **4 helpers** (dont `user_is_cabinet_manager` 0042). Vérifier 0 rang live avant de débrancher ; enum laissé mort. ⚠️ RGPD art. 5-2/32 (accountability/sécurité). **→ AMENDE C17-8(c) : table dédiée, pas colonne profiles.**

- **C16-5 — KPIs portefeuille : `cabinet_id` à la source + agrégat SUM en base** ✅ : tuer l'anti-patron `usePortefeuille.calculateKPIs()` (re-somme 14 KPI en JS côté client). (1) **ajouter `cabinet_id` DANS `v_dashboard_kpis`** (join `copros`, au plus bas niveau pour que le filtre sécurité s'applique partout) ; (2) `v_cabinet_overview` = **pur `SUM/GROUP BY cabinet_id`** (hérite de la justesse du GL) ; (3) **supprimer le recalcul JS** (l'écran additionne une seule fois) ; (4) **re-matérialiser d'abord les vues feuilles** absentes du live (sinon « somme du vide » = faux-vert) ; (5) tuile « mandats à renouveler » alimentée par l'entité mandat (acté ARB-3/C16-1). **Prérequis BLOQUANT : RLS FORCE** (`cabinet_id`) + ANOM-03 figée (total impayés). ⚠️ base légale durée mandat = **décret 67-223 art. 28** (pas loi art. 28) ; « sommer pas recalculer » = D68 interne.

## Bloc C12 — Ventes & mutations — tranché 2026-06-26 (USER)

> Très cohérent avec EXP-5 (le 450 suit le lot), EXP-3 (date d'effet), ARB-1 (GL source unique), C17-1 (machine à états gardée), baseline B1 (FORCE). ANOM-03/ARB-1 = préalable cité par 5 des 6.

- **C12-1 — Opposition art. 20 : `settle` = wrapper sur le paiement standard** ✅ : créer `record_mutation_opposition` (fige le montant **exigible** dû, statut `opposed`, ventile privilège/chirographaire — déclaration, pas d'argent) + `settle_mutation_opposition` qui **réutilise `post_owner_payment`** (D512 courant / C450, imputation FIFO, idempotence déjà faits), récupère le `ledger_tx_id` et le lie à l'opposition → `paid`. **Un seul chemin d'écriture** (pas de `source_type='mutation'` dédié = 2e porte interdite). **Touche TOUJOURS le 512 courant, jamais 105/450-5 ALUR** (fonds acquis au lot). `reconstitute_buyer_advances` câblé dans le même chantier (P3). ⚠️ **Bloquant ARB-1** (montant opposable lu du GL). ⚠️ `get_lot_balance_45x` inapte tel quel (inclut 450-5, ne distingue pas échu/non-échu). Base légale = **art. 2402 CC** (ex-2374) + loi 65-557 art. 19/19-1 (privilège).

- **C12-2 — Avis de mutation = événement autonome sur `mutations`** ✅ (validé sauf objection) : ajouter `avis_mutation_date` sur `mutations` (saisissable tôt, déclenche le pré-état daté) + **todo dérivé** dans `v_dashboard_todos` (pas de table notifications). **Supprimer le faux compteur « 15 j » de `v_mutations_overview`** (compté depuis `requested_at`, aucune base légale). Le « 15 j » de courtoisie (production du pré-état daté) ≠ le **15 j légal art. 20** (qui reste sur `mutation_oppositions`). Statut « avis reçu » = présence de la date (pas de nouvelle valeur d'enum).

- **C12-3 — Répartition vendeur/acquéreur art. 6-2 : nommer dans la pièce probante, zéro pro rata** ✅ (validé sauf objection) : la règle est déjà bien appliquée (charges à la date d'effet, résultat au copropriétaire à l'approbation des comptes, pas de pro rata auto) mais jamais nommée. → **figer une `repartition_note` art. 6-2 + enrichir `legal_reference` (art.5 + art.6-2)** dans le payload **immuable** de l'état daté (opposable). **Zéro calcul, zéro pro rata** (le partage conventionnel relève de l'acte notarié = coder un pro rata serait une FAUTE). ⚠️ slot front `note` déjà occupé par `alur_note` → 2e slot/fusion. ⚠️ **ne pas publier au notaire avant ARB-1** (la partie 3 lit encore `call_for_funds_lines`).

- **C12-4 — Clôture du compte vendeur : vrai contrôle de solde, préventif jamais bloquant** ✅ : (1) ajouter `p_as_of date` à `get_lot_balance_45x` (cut-off, rétro-compatible) ; (2) créer `close_seller_account` qui lit le solde du lot **à `effective_date`** (même horloge que l'état daté final, EXP-3), **hors 450-5 ALUR** ; (3) **découpler** = retirer `cloture_compte` du WHERE de `validate_mutation`. **Solde ≠ 0 (débiteur OU créditeur, USER) → trace une « issue » dans le payload jsonb** (débiteur = à recouvrer/opposition ; **créditeur = remboursement dû au vendeur ou reprise acquéreur**). ⚠️ **TOUJOURS PRÉVENTIF, JAMAIS BLOQUANT (USER)** : on alerte, on ne gèle pas la vente (cohérent doctrine « alerter > verrouiller » + EXP-5 : clôture = pointage, zéro écriture GL). Issue = step `in_progress` + payload (pas de nouvelle valeur d'enum, contrainte `ck_step_completed`). Tout part de **0076** (pas 0031 mort).

- **C12-5 — Statuts mutation : `signed` ≠ `validated` ratifié + machine VERROUILLÉE (C17-1)** ✅ : garder la distinction (acte signé ≠ bascule `lot_owners` = qui paie/vote/est convoqué). (1) **remplacer les 2 UPDATE front** (dans les 2 familles de code) par **RPC gardées** (`set_mutation_sent_to_notary`, `mark_mutation_signed`) ; (2) garde « entrée = `signed` » dans `validate_mutation` + **sauts nommés** donation/succession ; (3) écrire `status` ET `mutation_steps` par la **même** RPC (source = `mutations.status`) ; (4) **régénérer `supabase.ts` + corriger `DbMutationStatus`** (valeur fantôme `final_etat_generated`, risque 22P02). Enum = **7 valeurs LIVE** (`sent_to_notary` inclus, 0079) ; ne pas documenter sur le dump/types périmés (6 valeurs).

- **C12-6 — RLS ventes/état daté = gestionnaire-only + FORCE partout** ✅ (USER) : confirmer les 4 tables (`mutations`, `mutation_steps`, `mutation_oppositions`, `etat_date_snapshots`) **gestionnaire-only, AUCUNE policy copro**, `etat_date_snapshots` insert-seul (immuable art.5), notaire = fiche `tiers` (jamais un user). **Appliquer FORCE** (aligné baseline B1, défense en profondeur sur données nominatives — la reco initiale « retirer FORCE » est ÉCARTÉE car elle rouvrait B1) + **revoke anon table-level explicite** + futures RPC d'opposition en **DEFINER + manager + revoke anon**. Accès copropriétaire au portail = **RPC DEFINER dédiée**, jamais une policy table. ⚠️ base légale minimisation = RGPD art. 5.1.c (PAS « art. 6-2 loi 65-557 », qui régit la répartition).

## Bloc C13 — Conseil syndical — tranché 2026-06-26 (USER)

> Contexte transverse : les tables `council_decisions`/`council_votes` existent (0017/0030) mais sont **inertes côté front** (aucun écran ne les écrit ; l'edge `council-workflow` non câblée/non déployée). Chaque fiche porte donc son coût de (re)construction d'un chemin d'écriture en v2 (chantier « espace CS »).

- **C13-2 — Élection du président du CS : porte unique gardée + verrou d'unicité** ✅ : la désignation du président peut se faire **à l'AG (optionnelle)** *ou* **en cours d'année par le gestionnaire** depuis l'espace de la copro (cas réel : si le CS n'a pas encore choisi à l'AG, il le mentionne au gestionnaire qui a la main pour poser/modifier le rôle). Quand le CS change de président, **l'ancien redevient simple membre** (reste au conseil). Plomberie : `ELECT_COUNCIL` pose les membres ; **même si un président est indiqué dans le payload AG, il transite par la MÊME fonction gardée** `set_council_president` (jamais un INSERT brut sans contrôle) — exige membre actif (art. 22 + copropriétaire actif), rétrograde l'ancien en `member`, idempotente, tracée, horloge serveur. **Index UNIQUE PARTIEL** au prédicat identique à `is_council_president` (`is_active AND end_date IS NULL AND role='president'`) = filet anti-deux-présidents. Sans président = non bloquant. **V1 = président seul** (pas de `set_council_role` générique = piège à copie ANOM-23). ⚠️ ne pas confondre avec `ag_meetings.president_id` (président DE SÉANCE).

- **C13-1 — Avis du CS sur OS/contrat : avis unique gestionnaire, ZÉRO nouvelle FK** ✅ : **ne pas ajouter** `linked_service_order_id`/`linked_contract_id`. Réutiliser les crochets existants : `council_decisions.linked_resolution_id` (déjà là, déjà utilisé par l'edge) + table polymorphe `council_documents` (`linked_type`/`linked_id`, couvre déjà `contract`/`service_order`). **Avis V1 = avis UNIQUE saisi par le gestionnaire** (favorable/défavorable/réservé dans `status` + description), consultatif (D52, non opposable). Le **scrutin membre-par-membre reste dormant pour P1** (par TÊTE — 1 membre = 1 voix, jamais par tantièmes ; `compute_decision_result` 0030 déjà conforme). **Aucune migration de structure.** ⚠️ RESTRICT était à l'envers (bloquerait la suppression d'un OS à cause d'un vieil avis) — la convention de la table est SET NULL.

- **C13-3 — Accès du CS aux pièces : grand livre NOMINATIF complet (choix USER expert)** ✅ : l'art. 21 donne au CS un accès **large** à toute pièce → le conseil voit le **grand livre nominatif complet** de SA copro (pas d'agrégation/masquage des 45x). Garde-fous **techniques** (pas de restriction de contenu) : (1) **journalisation des consultations** dans **UNE table d'access-log générique** (tracée à l'ouverture d'écran, purge RGPD 1 an — résout aussi le fantôme ANOM-10) ; (2) **étanchéité inter-copros** : la vue GL conseil doit reposer la garde `copro_id` et être la SEULE porte (jamais fuiter le GL d'une autre copro). Contrats = vue `security_invoker` (RLS déjà collective) ; devis = vue DEFINER sur `service_orders` au-dessus du seuil. **Prérequis : RLS FORCE** sur council_*/ledger_*/contracts (ANOM-01) + **dériver de `v_general_ledger`** (existe en code 0036/0071, drift à réaligner sur le live, PAS à recréer — ANOM-19). ⚠️ « demande de pièce » = P1 (crochet UX seul en V1).

- **C13-5 — Avis CS sur marché art. 21 + alerte convocation : NON BLOQUANTE, calculée serveur** ✅ : alerte greffée sur `validate_ag_variables` (au pré-vol convocation, lister les résolutions « marché > seuil art. 21 » sans avis CS lié), **jamais un verrou** (l'absence d'avis CS n'est PAS une cause de nullité de l'AG ; les copros sans conseil ne doivent pas être bloquées). **Silencieuse si 0 membre CS actif.** Source de vérité **SQL**, pas le front cosmétique (`useConvocationAnnexes` = cases décoratives à ne pas croire). V1 = avis unique gestionnaire lié via `linked_resolution_id`. **Prérequis à séquencer AVANT** : (a) RPC d'écriture d'avis CS (n'existe pas) ; (b) **stocker le seuil art. 21 par copro** (absent) ; (c) trancher ANOM-07 (acter le non-bloquant globalement). ⚠️ corriger l'incohérence interne « art. 64 »/« art. 13 » pour le délai 21 j ; rapport annuel CS = table `rapports_activite_cs` (0053), à ne pas confondre avec l'avis ponctuel.

- **C13-4 — Délégation de pouvoir au CS (art. 21-1) : ZÉRO migration en V1, report P1** ✅ : **ne rien poser en base** (pas de table, ni colonne, ni valeur d'enum dormante). Un crochet dormant `DELEGATE_TO_COUNCIL` serait un **piège à copie** (ANOM-23) : l'AG voterait une délégation que `activate_ag_decisions` ne traiterait pas = **échec silencieux** (le pire en juridico-financier). On documente l'intention, on code tout d'un bloc en P1 une fois tranchés le **plafond obligatoire** + le périmètre des actes (uniquement décisions art. 24 ordinaires, jamais comptes/budget/gros travaux) + le **compte-rendu annuel** à l'AG (art. 21-1, voté en art. 25). Comptes **624/674/706 restés dormants, acté noir sur blanc.** Garde anti-confusion : l'avis CS > seuil art. 21 est obligatoire à recueillir mais **ne vaut JAMAIS autorisation d'engager** (seule l'AG, ou une délégation art. 21-1 votée, engage).

## Bloc C14 — Conformité — tranché 2026-06-26 (USER)

> Vérif internet faite (RNIC : open data ANAH MAJ quotidienne depuis avril 2026, clé = n° d'immatriculation, expose une date de dernière mise à jour ; pas d'API d'écriture, déclaration purement déclarative via téléservice).

- **C14-2 — Immatriculation RNIC : V1 saisie + lien + statut dérivé ; recoupement auto en P1** ✅ : le statut « à jour / en retard » est **100 % DÉRIVÉ, jamais stocké** (déclaration vide OU antérieure à `approved_at` du dernier exercice approuvé ⇒ « due » ; ancrage sur **`approved_at`** de `accounting_periods`, PAS `closed` ni `new Date()`). **V1** = `date_derniere_declaration_rnic` saisie à la main + **mini-enum factuel `rnic_status`** (`non_immatriculee/immatriculee/radiee`, ne sert qu'à porter radiation/dispense que la date ne dit pas) + **lien direct vers le téléservice** (régularisation) + **recherche du n° d'immatriculation par adresse à l'onboarding (USER : auto en V1)** — recherche **ASSISTÉE** (on propose les copros trouvées dans l'open data à cette adresse, **le gestionnaire confirme**, on stocke le numéro ; jamais de remplissage silencieux car l'adresse est une clé molle : formats divergents + plusieurs syndicats possibles à une même adresse). Alerte = 6e branche **passive** de `v_dashboard_todos` (zéro cron). Champ sur la fiche Identité (D65), pas de 4e onglet. **P1 = contrôle de fraîcheur annuel automatique** (recoupement open data : on relit le registre → si à jour on valide, sinon on alerte le gestionnaire « erreur ? sinon voici le lien pour régulariser ») : **faisabilité CONFIRMÉE** (clé = n° d'immatriculation déjà stocké en V1 ; proxy = date de mise à jour du registre = bon indicateur pour une **alerte non bloquante**, pas une preuve gravée). Morceau P1 réel (ingestion/refresh 528k lignes) mais sans inconnue technique. ⚠️ numérotation CCH : annuel = **L711-3**, syndic déclarant = **L711-4**, astreinte ANAH = **L711-6**.

- **C14-5 — Cloisonnement documents (décret 2019-650) : TOUT LE BLOC bloquant au go-live du portail (USER, option 2)** ✅ : le portail copropriétaire n'ouvre qu'avec les **trois** livrables — (1) **cloisonnement RLS** (4e branche dans `user_can_view_document` : si `coproprietaire_id` renseigné → gestionnaire OU ce copro via `coproprietaires.user_id=auth.uid()`, indivision via `lot_owners`/`get_user_lot_ids`) ; (2) **journal d'accès réparé** (créer `document_access_log` SINGULIER, aligner l'edge `get_document_url`, **supprimer le catch silencieux** qui avale l'erreur, purge 1 an — résout ANOM-10) ; (3) **check-list conformité 2019-650** (vue dérivée de la GED, jamais un statut stocké). **Modèle de visibilité (spec USER)** : documents **communs** = convocations, PV d'AG, **factures/pièces comptables consultables** (droit de communication art. 18-1) → visibles de **tous les copropriétaires** ; documents **nominatifs** = appels de fonds, relevés individuels, état daté → **réservés au propriétaire** (+ gestionnaire). L'enum `visibility` reste à 3 valeurs (la 4e branche est un prédicat additionnel, pas un 4e niveau). **Préalables** : portail (Palier 12) + `link_coproprietaire_account` ; **`cron_runs` à CRÉER** (purge, n'existe pas) ; abandonner le front v1 `NiveauConfidentialite` 4 niveaux. ⚠️ cloisonnement = RGPD art. 32 ; journal = art. 30 + reco CNIL.

- **C14-6 — Conservation légale + registre des PV : barème éditable + numéro PV matérialisé (option 1, USER)** ✅ : **aucun 2e modèle de durée** — le barème = exactement **C.10-P1** (table éditable `document_retention_rules` seedée + `is_permanent`, fusion même PR) ; le trigger `calculate_document_expiration` **lit la table** (10 ans en filet) au lieu des 6 catégories hardcodées (non conformes : un PV « à vie » redevenait purgeable à 10 ans, art. 8). Grain via un **`legal_doc_type` dédié** (l'enum `document_category` mélange juridique/pratique). `is_permanent` hérité de la règle, surchargeable par doc, verrouille la purge à vie. **Registre PV = vue `v_pv_register` dérivée de `ag_meetings`**, MAIS numéro **`pv_sequence_no` MATÉRIALISÉ** sur la ligne (jamais `row_number()` qui se renumérote → perte de valeur probante), gravé à la **génération** du PV via **G24-AM2** (séquences sans trou), clé `(copro_id, 'pv', année de meeting_date)` ; AGO + AGE de la même année partagent le compteur. **Livré APRÈS G24-AM2** (vrai bloquant V1). Resserrer le FK `pv_document_id` en **ON DELETE RESTRICT** (sinon numéro sans fichier). Purge gestionnaire + motif tracé. ⚠️ numérotation cotée = **bonne pratique anti-fraude qu'on s'impose**, PAS une obligation légale dure (art. 17 décret 67-223 vise la comptabilité) — ne pas survendre.

## Bloc C15 — Portail copropriétaire / votes — tranché 2026-06-26 (USER)

> C15-5 (multi-rôle : contexte serveur + `get_my_contexts` + identité=e-mail) déjà tranché en amont. Les 3 fiches partagent la MÊME table de journal d'accès (mutualisée GED+portail+CS, résout ANOM-10) et supposent multi-rôle (fait) + RLS ON+FORCE (ANOM-01).

- **C15-1 — Vote par correspondance / neutralisation du défaillant (art. 17-1 A) : chaîne complète saisie gestionnaire, neutralisation À LA MAIN** ✅ : **V1 = saisie gestionnaire uniquement** (le vote self-service du copropriétaire en ligne = P1, avec le portail Palier 12 — en V1 le portail n'existe pas, donc 100 % des VPC papier/formulaire sont saisis par le syndic). 4 morceaux : (amont) garantir un `ag_attendance(presence_type='correspondence')` avec tantièmes dans `register_correspondence_form_votes` → **le correspondant compte au quorum** (non négociable, sinon conformité illusoire) ; (aval) RPC gardée `mark_resolution_amended(resolution_id, reason)` qui pose `is_amended` + bascule `is_excluded=true`/`exclusion_reason='art.17-1 A'` sur TOUS les `vote_source='correspondence'` de CETTE résolution ; (lecture) agrégat informatif `excluded_correspondence` dans `get_ag_live_results` (ne touche pas le calcul) ; (preuve) `form_document_id` = formulaire reçu/signé archivé GED (pas de snapshot de texte). **NUANCE MÉTIER MAJEURE (USER)** : la neutralisation **n'est JAMAIS automatique** sur une édition de texte → distinction **amendement substantiel** (le sens change ⇒ défaillance art. 17-1 A) vs **simple correction de forme/nomenclature/coquille** (PAS un amendement ⇒ **voix MAINTENUE**). **UX (USER)** : à l'enregistrement d'une modif de résolution, **SI et seulement si des VPC existent** sur cette résolution → question « amendement substantiel ? neutraliser les VPC ? » **sans rien pré-cocher** (choix conscient) → [Oui] appelle la RPC, [Non] maintient les voix. Aucune question si 0 VPC. Choix + motif **tracés** (protège le syndic). Aucune incidence grand-livre. Revue cascade + BEGIN/ROLLBACK + Advisor 0.

- **C15-3 — Périmètre RGPD du portail : 2 mécanismes code + finition page (option 1) ; DPO officiel & AIPD en P1** ✅ : séparer 2 natures. **(1) CONTENU/PUBLICATION** (charge USER, ni code ni base) : finaliser `/confidentialite` (raison sociale, adresse, **vrai contact RGPD** — pas de DPO fictif —, **AJOUTER le sous-traitant e-mail Brevo**), **RETIRER la mention cookies/consentement** si zéro traceur (pas de bannière requise). **(2) CODE+BASE = seulement DEUX mécanismes** : journal d'accès docs (UNE table d'audit transverse, RPC SECURITY DEFINER, **partagée C14/C15-3/C15-4**) + self-rectification **bornée aux COORDONNÉES** (e-mail/tel/adresse ; e-mail = **double opt-in**, c'est le canal de convocation légale ; tantièmes/soldes/lots → syndic seul). Durées : mention + champ suffisent ; **purge auto = P1**. **DPO officiel + AIPD allégée + registre des traitements = P1** (NI DPO NI AIPD obligatoires à ce stade : pas de données sensibles art. 9, pas de surveillance/profilage à grande échelle ; à préparer avant le 1er syndic payant). **Prérequis durs** : (a) réconcilier l'enum visibilité sur les **3 valeurs SQL** AVANT de coder le journal ; (b) multi-rôle C15-5 (fait) ; (c) **RLS ON+FORCE** (sinon la mention « RBAC » de la page ment). ⚠️ ne PAS fonder sur art. 10 RGPD (un impayé ≠ donnée pénale) → fondement = caractère **non occasionnel** (art. 30.5). ⚠️ v2-tanstack VIDE → ces artefacts sont à RE-ÉCRIRE (pas finaliser le `src/` gelé) ; re-vérifier zéro traceur (Vercel Analytics) embarqué par défaut.

- **C15-4 — Espace conseil syndical : journal d'accès + bandeau art. 21 (option 1)** ✅ : **UNE table d'accès canonique** côté serveur (RPC SECURITY DEFINER, IP serveur, grain **consultation réelle** view/download), **mutualisée GED + portail + CS** (résout ANOM-10 d'un coup, pas 3 objets divergents). Colonnes : acteur, `role_contexte` (`council`), copro_id, objet_type/id, action, finalité, IP, horodatage. Au login conseil : **bandeau INFORMATIF non bloquant** (finalité art. 21 contrôle de gestion + interdiction de diffusion), **PAS un clic-consentement** (le droit de regard du CS est légal, pas optionnel). **Rétention 1 an** (alignée sur le journal docs C14-5). Journal = bloquant go-live (de l'espace CS) ; bandeau = V1 léger. **Préalables** : C15-5 (fait) conditionne `role_contexte` + déclencheur bandeau ; RLS ON+FORCE ; **la table d'accès est elle-même un traitement** → inscrire registre art. 30 + information art. 13 (ne pas surveiller en silence). ⚠️ impayés/soldes = données **patrimoniales** (vigilance), PAS sensibles art. 9. ⚠️ décret 67-223 art. 26 : accès CS subordonné à un avis préalable au syndic (à cadrer, peut être un événement journalisé).

## Bloc C11 — Communication — tranché 2026-06-26 (USER)

- **C11-P5 — Source unique des destinataires : UNE projection canonique (vue socle + RPC dérive)** ✅ : tuer les **3 chemins** divergents (RPC `rpc_get_ag_coproprietaires`, vue `v_coproprietaires_overview`, lectures directes). **Vue socle** = seule à connaître nom/tantièmes/solde/**adresse complète** (`address_line2`)/éligibilité/canal, via une fonction SQL `display_name(coproprietaire)` partagée ; la **RPC scopée-AG ne recalcule plus rien**, elle lit la vue et ajoute sa garde DEFINER (conserver la garde = pas de fuite annuaire au portail). **Arbitrages tranchés** : (1) **INDIVISION (USER)** = mandataire commun `is_primary` = **UNE voix**, poids du lot compté **une fois** (art. 23) ; la RPC cesse de couper en demi-voix (modèle vue = le juste). Destinataire = ce mandataire. (2) **Personne morale (USER)** = **raison sociale + « à l'attention de » le représentant** → la source doit porter/saisir le représentant légal (gérant). (3) fallback nom = « Inconnu ». (4) **exclure les anciens propriétaires**. (5) destinataires = titulaires actifs d'au moins un lot. (6) flag `is_eligible_recipient` + canal préféré **DANS la source** (clé d'idempotence des envois C11-P4 stable). **Préalable dur** : **figer la non-détermination de la clé générale** (contrainte d'unicité « une seule clé générale active ») AVANT de figer la signature, sinon la « source unique » ment encore. ⚠️ ANOM-19 (12 vues d'agrégat à recréer, dont celle-ci). ⚠️ commentaire « la vue retourne des doublons » = PÉRIMÉ (sous-requêtes scalaires, 0 doublon).

- **C11-P6 — Modération du mur : soft-delete auditable (contrat de design v2)** ✅ : remplacer le `DELETE` direct front (cascade destructeur, 0 trace) par un **masquage auditable**, gravé comme contrat de la future table mur v2 (rien sur le Next gelé). (1) `is_hidden` sur `wall_posts` ET `wall_comments` ; (2) masquage via **RPC gardée** (`hide_wall_post`/`hide_wall_comment`), jamais `.delete()`/`.update()` front ; (3) `v_wall_feed` filtre `is_hidden = false` (invisible aux copros, relisible par le gestionnaire) ; (4) **hard-delete séparé** réservé gestionnaire pour le droit à l'effacement RGPD (art. 17) ; (5) motif = enum (spam/insulte/hors-sujet/donnée perso) + libre optionnel ; (6) **`is_locked`** (fermer la discussion) **distinct de `is_hidden`** (retirer le contenu) ; (7) trigger sur UPDATE de `is_hidden` pour les compteurs ; (8) `hidden_by` + horodatage tracés. **QUI peut masquer (USER)** : **gestionnaire + auteur de son propre contenu** (l'auteur ne touche QUE ses messages ; masquer un post n'efface jamais les commentaires de tiers — c'est tout l'intérêt vs CASCADE). **Signalement structuré (USER)** = **hors V1, table dédiée P1** (V1 = modération réactive). **Rétention d'un post masqué = alignée sur le barème C.14** (pas de durée propre). **Préalable non négociable** : retirer `GRANT ALL ON wall_posts TO anon` + **RLS FORCE** (ANOM-01), sinon modération cosmétique. ⚠️ LCEN art. 6 (hébergeur) = sur-interprétation pour un mur interne ; fondement réel = accountability RGPD art. 5.2. ⚠️ ne pas forcer l'unification avec `mails.is_deleted` (corbeille perso ≠ masquage par un tiers).

> **Bilan 2026-06-26 — CADRAGE DES BLOCS CLOS** : + C16 (5) + C12 (6) + C13 (5) + C14 (3) + C15 (3) + C11 (2) = **41 décisions tranchées et consignées** ce jour (au-dessus des 17 arbitrages/EXP/socle du matin). **Tous les blocs de ratification (C11→C16 + EXP + ARB) sont traités.** Prochaine étape = sortir du cadrage : prioriser les paliers de build (V1) à partir de ces décisions.
