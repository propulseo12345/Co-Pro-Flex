# CHAINES INTER-FEATURES — DETAIL (10 chaines, 2026-06-22)

## Chaine 1 — AG resolution "budget previsionnel" votee -> close_ag -> prepare_ag_decisions -> activate_ag_decisions (etape PV) -> CREATION/disponibilite du budget de l'exercice -> generation des appels de fonds

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **AG - creation de l'AG ordinaire (wizard etape 1)** -> Le gestionnaire saisit les postes budgetaires de l'exercice a venir ; ils sont stockes UNIQUEMENT en JSON dans `ag_resolutions.variables.budget_postes` (+ montant total formate). AUCUNE ligne `budgets` n'est creee, AUCUN `linked_budget_id` ecrit. -> `createStandardResolutions` / `addResolution` / `updateResolution` (src/lib/ag/create-standard-resolutions.ts:128, src/features/ag/session/hooks/useAgSessionPage.ts:742 handleSaveBudgetPostes) — cote SQL aucune fonction, simple INSERT/UPDATE `ag_resolutions` -> `ag_resolutions` (resolution_type='budget', action_type='CREATE_BUDGET', majority_type='art24', variables={budget_postes:[...], montant:'...'}). Rien dans la table `budgets`.
2. **AG - vote en seance** -> Le coproprietaire vote sur la resolution budget. `cast_vote` derive les tantiemes (cle generale active) et insere le vote. -> `cast_vote` (0030_rpc_ag_conseil.sql:369) -> `ag_votes` (resolution_id, coproprietaire_id, vote, tantiemes, vote_source) ; `ag_resolutions.status` -> 'voting'.
3. **AG - cloture de seance** -> `close_ag` fige TOUTES les resolutions non figees via `calculate_resolution_result` : la resolution budget passe a 'approved' (si for_t>against_t en art24). Passe l'AG en status='closed'. -> `close_ag` -> `calculate_resolution_result` (0030_rpc_ag_conseil.sql:781 et :145) ; appelants : `finishAgSession` (src/lib/ag/api/meetings.api.ts:161) et edge ag_close/index.ts:156 -> `ag_resolutions.status='approved'`, voted_at, threshold_tantiemes, is_bridgeable ; `ag_meetings.status='closed'`, closed_at, session_ended_at.
4. **AG - pivot post-cloture** -> `prepare_ag_decisions` parcourt les resolutions 'approved' avec action_type mappe et materialise une `ag_pending_actions` par resolution. CREATE_BUDGET -> target_table='budgets', target_id=NULL, payload=variables (donc budget_postes mais PAS d'id de budget). Idempotent on conflict. -> `prepare_ag_decisions` (0030_rpc_ag_conseil.sql:1417, mapping ligne 1459) ; appele APRES `close_ag` par finishAgSession (meetings.api.ts:164) et ClosureRecap.tsx:150 -> `ag_pending_actions` (ag_id, resolution_id, action_type='CREATE_BUDGET', target_table='budgets', target_id=NULL, payload=variables, status='pending').
5. **AG - activation des decisions (etape PV, validation des signatures)** -> `activate_ag_decisions` traite les pending 'pending'. Branche CREATE_BUDGET : elle ne CREE PAS le budget — elle cherche un budget DEJA EXISTANT via `r.linked_budget_id`, sinon via `budgets(source_ag_id=p_ag_id, budget_type='current')` puis appelle `validate_budget(budget_id)`. Comme aucun budget n'a jamais ete cree ni lie, v_budget_id est NULL -> RAISE 23503 'budget current introuvable pour l'AG' -> rollback tout-ou-rien de TOUTE l'activation. -> `activate_ag_decisions` branche CREATE_BUDGET (0030_rpc_ag_conseil.sql:1790-1808) -> `validate_budget` ; appele par usePVPage.ts:739 handleSendSignatureRequests -> ATTENDU : `budgets.status='validated'` puis appels de fonds. REEL : aucune ecriture — RAISE puis rollback. L'UI affiche 'L'activation des decisions de l'AG a echoue : aucun budget / appel de fonds n'a ete genere'.
6. **Finance - disponibilite du budget pour generer les appels** -> `generate_calls_from_ag_payload` (declenche pour SCHEDULE_BUDGET_PAYMENTS / CREATE_ALUR_FUND dans la meme activation) resout le budget par payload.budget_id > linked_*_budget_id > budgets(source_ag_id, type, status='validated'). Exige un budget VALIDE sur periode 'open' avant de boucler `post_budget_call_for_funds`. -> `generate_calls_from_ag_payload` (0030_rpc_ag_conseil.sql:1523) -> `post_budget_call_for_funds` (0026) -> ATTENDU : call_for_funds + ledger_transactions/ledger_entries (D450-1/lot, C701). REEL : jamais atteint car la branche budget echoue en amont (et meme isole, budget introuvable -> RAISE 23503).

**Categories traversees** :
- AG / gouvernance (ag_meetings, ag_resolutions, ag_votes, ag_pending_actions)
- Finance / budget (budgets, budget_lines)
- Finance / appels de fonds (call_for_funds)
- Comptabilite / grand livre (ledger_transactions, ledger_entries, accounting_periods)

**STATUT** : casse

**Points de rupture** :
- RUPTURE PRINCIPALE — aucun ecrivain ne materialise le budget : nulle part dans le code (src ni SQL, repo == LIVE) une ligne 'budgets' avec source_ag_id n'est creee a partir de `ag_resolutions.variables.budget_postes`, et `linked_budget_id` n'est JAMAIS ecrit. Verifie sur le LIVE qqfqrcolzmcbsvfaumiq : budgets.source_ag_id non NULL = 0, ag_resolutions.linked_budget_id non NULL = 0.
- `activate_ag_decisions` branche CREATE_BUDGET (0030_rpc_ag_conseil.sql:1790-1808) : VALIDE un budget cense exister (b.source_ag_id=p_ag_id), ne le CREE pas. v_budget_id NULL -> RAISE 23503 -> rollback de toute l'activation (tout-ou-rien).
- `createBudget` (src/lib/budget/api.ts:293-327) : n'accepte ni ne pose source_ag_id, ni n'ecrit budget_lines a partir des postes ; cree un budget 'draft' deconnecte de l'AG.
- `BlocBudget` (src/features/ag/finalisation/components/BlocBudget.tsx) : commentaire 'cree a l'etape PV' TROMPEUR — composant LECTURE SEULE qui ne fait que lire variables.budget_postes ; ne cree aucun budget. Idem prepare_ag_decisions pose target_id=NULL pour CREATE_BUDGET.
- Symetrie : meme apres un eventuel budget, `generate_calls_from_ag_payload` (0030:1602-1605) RAISE 23503 si aucun budget n'est resoluble — donc la generation d'appels est cassee par la meme racine.

**Assertion de parite de bout en bout** : Un test e2e doit prouver en base, sur une copro neuve : (1) creer une AG ordinaire avec une resolution CREATE_BUDGET portant des postes (variables.budget_postes), voter POUR, close_ag, prepare_ag_decisions, puis activate_ag_decisions ; (2) APRES activation, attendre EXACTEMENT 1 ligne budgets avec source_ag_id = l'AG, budget_type='current', status='validated', et ag_resolutions.linked_budget_id renseigne pointant cette ligne ; (3) budget_lines doit contenir une ligne par poste, somme(budget_lines.amount) = somme des budget_postes = ag_resolutions.variables.montant ; (4) ag_pending_actions(CREATE_BUDGET).status='activated' avec result_data referencant le budget_id ; (5) si une resolution SCHEDULE_BUDGET_PAYMENTS suit, call_for_funds cree et ledger_transactions/ledger_entries equilibrees (D450-1/lot, C701) dont la somme = budget vote. ETAT ACTUEL : ce test echoue des l'etape (2) — activate_ag_decisions leve 23503 'budget current introuvable pour l'AG' et ne cree 0 budget (confirme : 0 budget source_ag_id sur le LIVE).

**Implication ordre de construction** : Avant toute campagne de test du cycle annuel et avant de coder les appels de fonds depuis l'AG, il faut INSERER l'etape manquante de materialisation du budget. Ordre v2 recommande : (1) ecrire une fonction SQL canonique `create_budget_from_ag_resolution(p_resolution_id)` [ou etendre prepare_ag_decisions/activate_ag_decisions] qui, pour CREATE_BUDGET, cree la ligne budgets (source_ag_id=ag, budget_type='current', version, status='draft'), eclate variables.budget_postes en budget_lines (compte + cle de repartition), pose ag_resolutions.linked_budget_id, PUIS valide ; cette creation DOIT se faire AVANT que activate_ag_decisions appelle validate_budget et AVANT generate_calls_from_ag_payload (l'ordre interne CREATE_*=0 avant SCHEDULE_*=1 est deja bon, il manque juste le INSERT du budget en tete de branche CREATE_BUDGET). (2) Decider ou se cree le budget : a la saisie wizard (createBudget enrichi de source_ag_id + budget_lines, le plus tot = feedback le plus tot, tranche verticale) OU dans activate_ag_decisions (auto-population pure AG->GL, coherent avec le principe directeur du fichier 0030). (3) Tant que (1) n'est pas livre, la generation d'appels depuis l'AG reste inutilisable — ne pas construire d'UI d'appels au-dessus. (4) Corriger le commentaire trompeur de BlocBudget et le wording 'cree a l'etape PV'. (5) Verrou financier : la creation/validation doit rester dans la transaction tout-ou-rien de activate_ag_decisions pour que budget+appels+ecritures GL soient atomiques.

## Chaine 2 — AG -> activate_ag_decisions : auto-population generique de tous les action_types vers le grand livre / l'etat de la copro

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **AG - creation (UI reelle)** -> useAgCreateForm.handleSubmit -> createAgInSupabase({include_standard_resolutions:false}) -> createStandardResolutions() -> addResolution()/edge ag_add_resolution -> edge ag_create (insert ag_meetings, status draft) + edge ag_add_resolution / addResolutionDirect (insert ag_resolutions) -> `ag_meetings` (1 ligne), `ag_resolutions` (N lignes) MAIS action_type=NULL, linked_budget_id=NULL ; AUCUN budgets.source_ag_id ecrit. **RUPTURE 1 ici.**
2. **AG - cloture de seance** -> ClosureRecap / meetings.api.closeAgMeeting -> rpc close_ag puis rpc prepare_ag_decisions (ordre impose, OK cote front) -> `public.close_ag` (0030) puis `public.prepare_ag_decisions` (0030) -> close_ag: ag_resolutions.status=approved/rejected + voted_at + threshold_* (via calculate_resolution_result) ; ag_meetings.status=closed. prepare_ag_decisions: insere ag_pending_actions UNIQUEMENT pour les resolutions approved AVEC action_type non NULL (filtre 'action_type is not null'). Comme action_type=NULL (RUPTURE 1), prepared=0 -> 0 ligne ag_pending_actions.
3. **AG - PV / validation des signatures** -> usePVPage.handleSendSignatureRequests -> garde 471/472 -> rpc activate_ag_decisions -> `public.activate_ag_decisions` (0030, LIVE = identique au repo, verifie via pg_get_functiondef) -> Boucle ag_pending_actions status=pending ordonnee (budgets=0, appels=1, APPROVE_ACCOUNTS=2). Comme 0 ligne pending -> activated=0, AUCUNE ecriture, no-op silencieux. Quand des lignes existent (path SQL), dispatch par action_type ci-dessous.
4. **action_type CREATE_BUDGET / CREATE_WORK_BUDGET** -> activate -> validate_budget(budget courant/works lie a l'AG) -> `public.validate_budget(v_budget_id)` ; budget resolu via ag_resolutions.linked_budget_id/linked_work_budget_id PUIS fallback budgets(source_ag_id, budget_type) version desc -> Passe budgets.status -> validated. FIABLE *si et seulement si* un budget a ete cree avec source_ag_id OU la resolution porte linked_budget_id. Or AUCUN code front n'ecrit ces liens (RUPTURE 2) -> RAISE 23503 'budget introuvable' -> rollback tout-ou-rien de TOUTE l'AG. Ne poste PAS le GL (le GL part par SCHEDULE_*).
5. **action_type CREATE_ALUR_FUND** -> activate -> validate_budget(alur) puis generate_calls_from_ag_payload -> `public.validate_budget` + `public.generate_calls_from_ag_payload` -> `public.post_budget_call_for_funds` (boucle selon payload.mode) -> Valide le budget alur + poste l'appel D450-5/C105 (call_for_funds + ledger). FIABLE *si* budget alur source_ag_id existe ET periode 'open' ET payload.mode present. Sinon RAISE (budget alur introuvable / periode non open / budget non validate). Depend de RUPTURE 2.
6. **action_type SCHEDULE_BUDGET_PAYMENTS / CREATE_EXCEPTIONAL_CALL / SCHEDULE_ALUR_PAYMENTS** -> activate -> generate_calls_from_ag_payload -> `public.generate_calls_from_ag_payload` -> boucle `public.post_budget_call_for_funds` (UNIQUE/SEMESTRIEL/TRIMESTRIEL/PERSONNALISE) -> Cree les appels de fonds + ecritures GL (D450-x/C70x). C'est l'ETAPE QUI POSTE REELLEMENT LE GL. FIABLE *si* budget cible validated + periode open + payload.mode fourni. CREATE_EXCEPTIONAL_CALL exige un budget type 'works' source_ag_id (jamais cree par l'UI). Idempotence sur uq_call_idempotent. Depend de RUPTURE 2 + payload mode (RUPTURE 3).
7. **action_type APPROVE_ACCOUNTS** -> activate (execute EN DERNIER, ordre 2) -> close_period + open_next_period + regularize_period + approve_period sur target_id -> `public.close_period` / `open_next_period` / `regularize_period` / `approve_period` (target_id = periode resolue par prepare_ag_decisions via date_debut/date_fin) -> Cloture N, cree a-nouveau N+1, affecte le resultat, passe periode -> approved. open_next/regularize conditionnes a l'existence d'un solde reportable (sinon sautes). FIABLE cote moteur (le plus aboutie). Pre-requis: garde 471/472 cote PV (fail-closed) + prepare a su resoudre la periode (sinon RAISE 23503). action_type/date_debut/date_fin doivent etre en base (RUPTURE 1).
8. **action_type ELECT_COUNCIL** -> activate -> desactive l'ancien conseil + upsert des elus -> update council_members (is_active=false) + insert ... on conflict (copro_id,coproprietaire_id,start_date) -> council_members renouveles (FIABLE et autonome, n'a besoin que de payload.council_members[] avec coproprietaire_id valides + lot actif). Mais l'UI ne remplit pas variables.council_members ni action_type -> jamais materialise (RUPTURE 1).
9. **action_type MANAGE_CONTRACT** -> activate -> active le contrat vote + expire l'ancien actif du domaine -> update contracts status=active + update contracts status=expired (meme domain_id, start_date anterieur) -> contracts bascule active/expired (FIABLE et autonome, n'a besoin que de payload.contract_id). Refuse de ressusciter un 'terminated'. Depend de RUPTURE 1 pour etre declenche.
10. **action_type APPOINT_SYNDIC / GRANT_QUITUS / DESIGNATE_BUREAU** -> activate -> no-op informatif (DESIGNATE_BUREAU exclu des prepare) -> aucun (jsonb 'informatif') ; DESIGNATE_BUREAU filtre par prepare_ag_decisions -> AUCUNE ecriture metier (volontaire/fantome). APPOINT_SYNDIC et GRANT_QUITUS ecrivent juste result_data informatif sur ag_pending_actions. Mandat syndic differe post-finance (memoire syndic_mandate_deferred).
11. **AG - finalisation (page Finalisation + finalize_ag)** -> loadPendingActions (lecture seule) puis markAgFinalized -> rpc finalize_ag -> `public.get_ag_pending_actions` (0039, lecture) + `public.finalize_ag` (0040) -> finalize_ag passe ag_meetings.status -> finalized UNIQUEMENT si status pv_signed/pv_sent ET 0 ag_pending_actions non-activated. Avec 0 pending (RUPTURE 1), la garde 'non activee' est trivialement satisfaite -> finalise une AG qui n'a rien auto-peuple (faux positif).

**Categories traversees** :
- AG / gouvernance (ag_meetings, ag_resolutions, ag_votes, ag_pending_actions)
- Finance / budgets (budgets, budget_lines, validate_budget)
- Finance / appels & grand livre (call_for_funds, ledger_transactions, ledger_entries, post_budget_call_for_funds)
- Finance / periodes & cloture (accounting_periods, close_period, open_next_period, regularize_period, approve_period)
- Conseil syndical (council_members)
- Maintenance / contrats (contracts)

**STATUT** : casse

**Points de rupture** :
- RUPTURE 1 (racine, bloque TOUT) : la creation d'AG cote UID (src/features/ag/new/hooks/useAgCreateForm.ts handleSubmit -> src/lib/ag/create-standard-resolutions.ts createStandardResolutions) appelle createAgInSupabase avec include_standard_resolutions:false PUIS addResolution SANS action_type. addResolutionDirect (src/lib/ag/api/resolutions.api.ts:72) ecrit action_type=input.action_type||null et l'edge supabase/functions/ag_add_resolution/index.ts (insert l.244-255) n'a meme pas de champ action_type. Resultat: ag_resolutions.action_type=NULL -> prepare_ag_decisions (0030 l.1451 'action_type is not null') ignore tout -> 0 ag_pending_actions -> activate_ag_decisions no-op. Le moteur SQL marche, mais l'UI ne l'amorce jamais.
- RUPTURE 1bis : updateResolution (src/lib/ag/api/resolutions.api.ts:114) n'expose PAS action_type dans son payload (l.128-143) -> impossible de reparer un action_type apres coup via l'edition de resolution.
- RUPTURE 2 (finance) : aucun chemin front n'ecrit budgets.source_ag_id NI ag_resolutions.linked_budget_id/linked_work_budget_id. useBudget.ts:354 ne fait que LIRE source_ag_id. Donc meme avec action_type correct (path SQL create_ag_with_standard_resolutions), CREATE_BUDGET/CREATE_WORK_BUDGET/CREATE_ALUR_FUND/SCHEDULE_*/CREATE_EXCEPTIONAL_CALL leveraient 23503 'budget introuvable' dans activate_ag_decisions (0030 l.1804/1817) -> rollback tout-ou-rien de toute l'activation.
- RUPTURE 3 (payload echeancier) : generate_calls_from_ag_payload lit payload.mode (UNIQUE par defaut) mais l'UI ne remplit ni variables.mode ni variables.council_members[] ni variables.contract_id ; validate_ag_variables (0030 l.1075) detecterait ces manques mais n'est appele nulle part dans le flux PV.
- RUPTURE 4 (remontee d'erreur PV) : src/features/ag/pv/hooks/usePVPage.ts:757 lit result.activated et result.failed, mais activate_ag_decisions ne renvoie JAMAIS 'failed' (tout-ou-rien: toute erreur = RAISE -> error). typedResult.failed est toujours undefined ; le recap d'activation peut donc afficher un faux 'partiel'.
- RUPTURE 5 (faux vert finalisation) : finalize_ag (0040 l.34-40) ne verifie que l'absence de ag_pending_actions non-activated ; avec 0 pending (RUPTURE 1) il finalise une AG ou rien n'a ete auto-peuple -> l'AG parait 'terminee' sans budget/appel/conseil cree.

**Assertion de parite de bout en bout** : Un test e2e doit prouver, sur une copro neuve (cf. create_test_copro_seeded en BEGIN/ROLLBACK), qu'apres le PARCOURS UI REEL (creation AG -> resolutions standard -> vote 'for' -> close_ag -> prepare -> activate) la base contient : (1) ag_resolutions.action_type NON NULL et coherent pour chaque resolution porteuse (CREATE_BUDGET, CREATE_ALUR_FUND, ELECT_COUNCIL, APPROVE_ACCOUNTS...) ; (2) au moins une ligne ag_pending_actions par resolution approuvee a effet, toutes en status='activated' (prepared>0 ET activated=prepared) ; (3) budgets.status='validated' avec source_ag_id=AG pour les budgets votes ; (4) call_for_funds + ledger_transactions/ledger_entries equilibrees (somme debits=credits) effectivement postees pour les SCHEDULE_*/CREATE_ALUR_FUND, avec les comptes attendus (D450-x/C70x courant, D450-5/C105 ALUR) ; (5) accounting_periods N en status='approved' et periode N+1 'open' avec ecritures d'a-nouveau pour APPROVE_ACCOUNTS ; (6) council_members actifs = exactement la liste votee pour ELECT_COUNCIL ; (7) contracts.status='active' sur le contrat vote et 'expired' sur l'ancien pour MANAGE_CONTRACT. Aujourd'hui ce test echoue des l'assertion (1)/(2) : prepared=0, activated=0, aucune ecriture (verifie sur le LIVE : 2 ag_meetings, 0 ag_resolutions, 0 ag_pending_actions, 0 budgets source_ag_id).

**Implication ordre de construction** : Le moteur SQL d'activation (activate_ag_decisions, prepare_ag_decisions, generate_calls_from_ag_payload, finalize_and_activate_ag) est deja ecrit et LIVE-conforme : NE PAS le reconstruire en premier. L'ordre de construction v2 doit etre : (1) REPARER L'AMORCAGE (RUPTURE 1) en cablant action_type lors de la creation des resolutions standard cote UI — soit basculer createStandardResolutions sur create_ag_with_standard_resolutions (SQL, qui pose deja action_type), soit propager action_type via addResolution + champ action_type dans l'edge ag_add_resolution + updateResolution ; c'est la tranche verticale qui debloque TOUT le reste. (2) CABLER LE LIEN FINANCE (RUPTURE 2) : a la creation/validation d'un budget issu d'une AG, ecrire budgets.source_ag_id ET/OU ag_resolutions.linked_budget_id/linked_work_budget_id, sinon CREATE_BUDGET/SCHEDULE_*/ALUR rollback. (3) REMPLIR LES PAYLOADS (RUPTURE 3) : variables.mode (echeancier), variables.council_members[], variables.contract_id, date_debut/date_fin, et brancher validate_ag_variables en pre-vol du PV. (4) Puis fiabiliser la remontee d'erreur PV (RUPTURE 4 : supprimer le faux 'failed') et durcir finalize_ag pour refuser une AG ou prepared=0 alors que des resolutions a effet existent (RUPTURE 5). Construire ces tranches AG->finance->conseil->contrat de bout en bout une par une (vertical), preuve e2e a chaque palier, plutot que de re-livrer le SQL deja fonctionnel.

## Chaine 3 — Appels (creances 45x) -> encaissement copro -> imputation FIFO cloisonnee par nature -> etat des impayes & releves individuels

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **Appels de fonds (emission)** -> Le gestionnaire emet un appel agrege multi-cles depuis un budget valide (wizard finance / edge generate_call_for_funds). -> `post_budget_call_for_funds`(10 args) — migrations/0026_rpc_appels_paiements.sql §G (lignes 418-611) -> INSERT call_for_funds (status='issued') + 1 ligne call_for_funds_lines par (lot×cle) avec amount_due ; ecriture GL via create_ledger_transaction: D 450-x/lot (agrege) · C 701/702/105 ; ledger_tx_id repose sur l'appel (trg_cff_ledger_required, DEFERRED, exige tx NOT NULL au commit).
2. **Encaissement coproprietaire** -> Le gestionnaire saisit un reglement (PaymentModal front -> edge record_payment, ou RPC directe). Idempotent via idempotency_key. -> `post_owner_payment`(10 args) — migrations/0026 §H (lignes 625-739) ; edge functions/record_payment/index.ts appelle cette RPC -> INSERT payments (ON CONFLICT (copro_id, idempotency_key) DO NOTHING = idempotence) ; puis ecriture GL D 512 (banque) · C 450-x par nature lettree · trop-percu -> C 450-3 (avances) ; ledger_tx_id repose sur le payment.
3. **Imputation FIFO cloisonnee par nature** -> Appelee par post_owner_payment juste apres l'INSERT payment. Balaye les lignes impayees du MEME lot, nature par nature ('current' PUIS 'works' ; ALUR jamais en auto), FIFO intra-nature (cf.issue_date, cf.id). Override manuel art.1342-10 si p_call_line_ids fourni. -> `allocate_payment`(p_payment_id, p_call_line_ids, p_nature_filter) — version 0072_payment_cloisonnement_avance.sql (remplace la 0026 §F qui faisait un FIFO GLOBAL franchissant les cloisons) -> DELETE puis INSERT payment_allocations (idempotent). NE poste PAS le GL. Reliquat non impute laisse tel quel -> post_owner_payment le bascule en avance 450-3.
4. **Chaine de triggers (mise a jour du compteur parallele)** -> Declenchee automatiquement par l'INSERT/UPDATE/DELETE sur payment_allocations. C'est le SEUL pont entre l'imputation et l'etat des impayes. -> `trg_allocation_update_line` -> tr_allocation_update_line() puis `trg_call_line_status_sync` -> tr_call_line_status_sync() -> update_call_status() — migrations/0026 §L2/L3/C -> UPDATE call_for_funds_lines.amount_paid = Σ payment_allocations de la ligne ; UPDATE call_for_funds_lines.status (paid/partial/unpaid) ; UPDATE call_for_funds.status en-tete (issued/partially_paid/paid).
5. **Etat des impayes + relances (ecran /ventes-impayes)** -> Le front liste les impayes et leur severite (J+15/30/60/90). Lit le COMPTEUR PARALLELE call_for_funds_lines, PAS le GL. -> Vues `v_unpaid_by_lot` (0028) et `v_unpaid_with_reminders` (0036) ; lues par src/lib/impayes/api.ts (listUnpaidByLot, listUnpaidWithReminders, getImpayesStats, getUnpaidLinesForLot) -> Lecture seule. total_unpaid = Σ(cfl.amount_due − cfl.amount_paid) WHERE cfl.status<>'paid' AND cf.due_date<current_date AND copro.onboarding_step IS NULL. Source = call_for_funds_lines.amount_paid (compteur), NON les 45x du GL.
6. **Releve individuel coproprietaire (etat date / portail)** -> Affiche le solde d'un lot/personne. Lit le GRAND LIVRE (45x), source DIFFERENTE de l'ecran impayes. -> Vues `v_owner_statement_by_lot` / `_by_person` (0028) ; `get_owner_statement`(...) (0028 §B.8) -> Lecture seule. balance = Σ(debit−credit) sur comptes a.code LIKE '45%' portant lot_id, tx posted. Inclut TOUTES sources (appels, paiements, a-nouveau, affectation, reprise mandat), pas seulement appels/paiements.

**Categories traversees** :
- Finance — Appels de fonds
- Finance — Comptabilite / Grand livre
- Finance — Impayes & relances
- Ventes & Impayes (ecran /ventes-impayes)
- Edge functions (record_payment, generate_call_for_funds)
- Portail coproprietaire / Etat date (releves individuels)

**STATUT** : partiel

**Points de rupture** :
- DIVERGENCE STRUCTURELLE (cœur de la question) : l'ecran impayes lit un COMPTEUR PARALLELE et le releve individuel lit le GL — deux sources distinctes verifiees sur le LIVE. v_unpaid_by_lot (migrations/0028_derives_vues_annexes.sql:197-235) et v_unpaid_with_reminders (0036_vues_drift_finance.sql:632) calculent total_unpaid = Σ(call_for_funds_lines.amount_due − amount_paid). v_owner_statement_by_lot (0028:115-151) calcule balance = Σ(debit−credit) sur les 45x du GL. Confirme LIVE : v_unpaid_* reads_cfl=true/reads_gl=false ; v_owner_statement_by_lot reads_gl=true/reads_cfl=false.
- POINT DE COUPLAGE UNIQUE = la chaine de triggers payment_allocations -> amount_paid (tr_allocation_update_line, 0026:1207-1240). Si cette chaine est desactivee, contournee (UPDATE direct de amount_paid, ou DELETE d'allocation sans repasser par le trigger), ou si une ecriture 45x est passee au GL SANS creer d'allocation (a-nouveau, affectation de resultat, reprise de mandat, OD manuelle, mutation), le compteur call_for_funds_lines NE bouge PAS alors que le GL bouge -> l'ecran impayes et le releve individuel se contredisent. C'est exactement la classe d'ecarts que v_lot_vs_gl_mismatch (0028:324-369) restreint volontairement a source_type IN ('call_for_funds','payment') pour eviter les faux positifs.
- MOITIE ENCAISSEMENT JAMAIS EXERCEE SUR LE LIVE : payments=0, payment_allocations=0 (alors que calls_issued=11, call_lines=486, unpaid_rows=36). La branche emission->impayes tourne, mais encaissement->imputation FIFO->extinction d'impaye n'a produit AUCUNE donnee. Les 36 impayes affiches n'ont jamais ete confrontes a un paiement reel. La concordance actuelle (v_lot_vs_gl_mismatch = 0 ligne, 0 divergence) est donc VACUE : les deux compteurs sont d'accord parce que aucun paiement n'a encore touche un seul des deux.
- PIEGE ONBOARDING (faux-vert) : v_unpaid_by_lot et v_unpaid_with_reminders EXCLUENT les copros avec copros.onboarding_step NON NULL (0028:233). DEFAULT 0 (0007) => une copro fraichement creee affiche 0 impaye meme avec des appels echus. Un test e2e doit forcer onboarding_step = NULL avant d'asserter, sinon la gate impayes passe a vide.
- BUG MINEUR FRONT (non bloquant mais code mort) : src/lib/impayes/api.ts:382 getUnpaidLinesForLot — la clause .lt('amount_paid', supabase.rpc ? undefined : undefined) est un no-op (toujours undefined), le filtrage 'reste a payer' est en realite fait en JS plus bas. A nettoyer, mais ne casse pas la chaine.

**Assertion de parite de bout en bout** : Un test e2e en base (sur copro avec onboarding_step=NULL et periode 'open') doit prouver, APRES un encaissement partiel via post_owner_payment : (1) la ligne d'appel ciblee a amount_paid = Σ payment_allocations de cette ligne — verifie LIVE : 0 ligne out-of-sync aujourd'hui, mais a re-prouver une fois payments>0 ; (2) call_for_funds_lines.status et call_for_funds.status ont bascule en 'partial'/'partially_paid' via la chaine de triggers ; (3) le GL porte D 512 · C 450-x par nature + C 450-3 pour le trop-percu, ecriture equilibree et posted ; (4) INVARIANT DE PARITE CENTRAL : pour le lot, Σ(amount_due − amount_paid) de v_unpaid_by_lot == solde 450 du GL RESTREINT aux source_type ('call_for_funds','payment') de v_owner_statement_by_lot — c'est-a-dire v_lot_vs_gl_mismatch.difference DOIT rester = 0 apres le paiement (le compteur parallele et le GL n'ont pas diverge) ; (5) le cloisonnement : un encaissement 'current' n'eteint JAMAIS un appel 'works' emis plus tot (verifier qu'aucune allocation ne franchit la nature) ; (6) idempotence : rejouer post_owner_payment avec la meme idempotency_key ne cree NI payment NI allocation supplementaire et laisse impayes + GL identiques.

**Implication ordre de construction** : Ordre de construction v2 impose par les dependances : (1) D'ABORD figer la regle « le GL est la source unique de verite » et decider si l'ecran impayes doit etre DERIVE du GL (45x source_type calls+payments) plutot que du compteur call_for_funds_lines — sinon on perpetue deux sources qu'il faut maintenir synchrones a la main via triggers (dette fragile). Tant que le double compteur reste, v_lot_vs_gl_mismatch DOIT etre cable comme garde-fou actif (gate CI + alerte), pas juste une vue de diagnostic. (2) ENSUITE la zone strangler 'encaissement' ne peut etre migree AVANT la zone 'appels' : allocate_payment et la chaine de triggers presupposent que call_for_funds_lines existe avec amount_due/amount_paid/status et budget_type resolvable (jointure budgets pour la nature). (3) L'ecran impayes/relances et le portail coproprietaire (releve individuel) sont des CONSOMMATEURS terminaux : a migrer en DERNIER, apres que la paire appels+encaissement ecrit des donnees reelles — imperatif car aujourd'hui payments=0 rend tout test de parite vacue. (4) Avant toute campagne de test golden : poser des paiements reels (partiel, total, trop-percu, multi-nature) pour sortir du faux-vert 'aucun paiement = aucune divergence' et exercer effectivement le pont trigger qui relie les deux moities de la chaine.

## Chaine 4 — Facture fournisseur : saisie brouillon -> validation (D6xx/C401) -> paiement (D401/C512) -> realise budgetaire & rapprochement bancaire

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **Finance / Factures fournisseurs — saisie (route canonique /finance/factures/new)** -> Le gestionnaire saisit une facture mono-poste (fournisseur + compte 6xx + montant TTC), avec post_immediately=true -> useNewFacturePage.handleSubmit -> useCreateSupplierInvoice -> financeApi.createSupplierInvoice -> edge create_supplier_invoice -> RPC `post_supplier_invoice`(LIVE: p_copro_id,p_period_id,p_tiers_id,...,p_lines,p_post_immediately) -> supplier_invoices (status posted, ledger_tx_id), supplier_invoice_lines (1 ligne, account_id 6xx, budget_line_id NULL), ledger_transactions + ledger_lines (D6xx / C401). AUCUNE ligne budget_expenses.
2. **Finance / Factures fournisseurs — saisie alternative (modal liste, chemin V1 useFacturesPage encore present)** -> handleCreateFacture cree un brouillon SANS ligne ni ventilation (montant d'en-tete uniquement) -> useFacturesPage.handleCreateFacture -> useCreateSupplierInvoiceDirect -> financeApi.createSupplierInvoiceDirect -> insert direct table -> supplier_invoices (status 'draft', total_amount) UNIQUEMENT. PAS de supplier_invoice_lines, PAS d'ecriture GL. NB: la liste V2 routee a retire le bouton de ce modal (page.tsx l.51-53) mais le code reste appelable.
3. **Finance / Factures fournisseurs — validation (draft -> posted)** -> Comptabiliser le brouillon : generer l'ecriture D6xx (par ligne) / C401 (total) -> useFacturesPage.handleSendToAccounting -> financeApi.validateSupplierInvoice -> RPC `validate_supplier_invoice`(p_invoice_id) [migration 0046, present a l'identique sur le LIVE] -> ledger_transactions + ledger_lines (D6xx / C401 ; source_type='supplier_invoice'), supplier_invoices.status='posted' + ledger_tx_id + total_amount recalcule depuis les lignes. EXIGE des supplier_invoice_lines (RAISE 23514 'brouillon sans ligne' si 0). N'ecrit RIEN dans budget_expenses.
4. **Finance / Factures fournisseurs — detail (workflow lateral de la fiche)** -> Bouton 'Valider la facture' / 'Mettre en paiement' / 'Marquer comme payee' sur la fiche [id] -> useFactureDetailPage.handleChangeStatut -> useUpdateSupplierInvoice -> financeApi.updateSupplierInvoice (.update({status}) NU sur supplier_invoices) -> supplier_invoices.status uniquement (draft->posted->paid). AUCUNE ecriture GL : ni D6xx/C401, ni D401/C512. **RUPTURE** — flip de statut nu, exactement le trou que 0046 devait fermer cote liste mais qui subsiste sur la fiche.
5. **Finance / Factures fournisseurs — paiement (posted -> paid)** -> Enregistrer le reglement (PaymentModal de la liste) : sortie de banque -> useFacturesPage.handlePaymentComplete -> financeApi.postSupplierPayment -> RPC `post_supplier_payment`(LIVE: D401/C512 + idempotency) [route session, contourne l'edge pay_supplier_invoice] -> supplier_payments, ledger_transactions + ledger_lines (D401 / C512), supplier_invoices.status='paid' quand solde, idempotency_key. Le compte 512 (Banque) est mouvemente ICI, a la saisie.
6. **Finance / Rapprochement bancaire** -> Pointer la ligne de releve contre le reglement fournisseur deja saisi -> edge reconcile_bank_movement -> RPC `reconcile_bank_movement` [migration 0066, LIVE] -> bank_matches (INSERT) + bank_movements.status='matched'. ZERO ecriture GL (pointage pur — le 512 est deja mouvemente par post_supplier_payment). Lien banque correct et coherent.
7. **Finance / Budget — suivi realise (onglet Budget, vues v_budgets_overview / v_budget_lines_overview)** -> Afficher consomme / realise / reste vs previsionnel par ligne de budget -> lib/budget/api -> SELECT `v_budgets_overview` & `v_budget_lines_overview` (LIVE) qui agregent budget_expenses (total_spent, validated_spent FILTER status='validated', consumption_pct) -> Lecture seule. Calcule le realise EXCLUSIVEMENT depuis budget_expenses. Ne lit NI supplier_invoices, NI supplier_invoice_lines, NI ledger_lines classe 6. Le seul peupleur de budget_expenses en prod = validate_budget_expense (onglet Depenses du budget) ; cote factures fournisseurs, le seul ecrivain de budget_expenses sur le LIVE est seed_golden_loop (test).

**Categories traversees** :
- Finance — Factures fournisseurs (cycle depense fournisseur)
- Finance — Grand livre / comptabilite (partie double, classe 6 / 401 / 512)
- Finance — Budget previsionnel & suivi du realise (budget_expenses)
- Finance — Rapprochement bancaire (512 / bank_movements)
- Maintenance — Tiers fournisseurs (supplier = tiers is_supplier)

**STATUT** : partiel

**Points de rupture** :
- RUPTURE MAJEURE (reponse directe a la question) — Le realise fournisseur NE remonte PAS au suivi budgetaire. supabase/migrations + LIVE: v_budgets_overview et v_budget_lines_overview calculent total_spent/validated_spent/consumption_pct UNIQUEMENT depuis budget_expenses. Or post_supplier_invoice, validate_supplier_invoice (0046) et post_supplier_payment (0026) n'ecrivent JAMAIS budget_expenses (verifie sur le LIVE : 0 occurrence dans les 3 corps ; seule fonction qui INSERT budget_expenses = seed_golden_loop, test). Une facture fournisseur comptabilisee (D6xx au GL) laisse donc le budget afficher consomme=0. Deux mondes paralleles : World A budget_expenses (onglet Depenses, validate_budget_expense -> GL + vues OK) vs World B supplier_invoices (Factures fournisseurs -> GL OK, vues budget aveugles).
- Le pont structurel existe mais est inerte : supplier_invoice_lines.budget_line_id EXISTE (verifie LIVE) et createSupplierInvoice expose meme un budget_line_id dans le payload edge, mais (a) l'UI /finance/factures/new ne le renseigne pas (lines = [{account_id,label,amount}] sans budget_line_id), et (b) aucune vue budget ne lit supplier_invoice_lines ni ledger_lines. Le hook est pose, jamais branche.
- RUPTURE SILENCIEUSE sur la fiche detail — src/features/finance/invoices/useFactureDetailPage.ts:handleChangeStatut appelle useUpdateSupplierInvoice (updateSupplierInvoice = .update({status}) nu) au lieu de validateSupplierInvoice / postSupplierPayment. Valider ou payer DEPUIS la fiche [id] flippe le statut sans AUCUNE ecriture GL (ni D6xx/C401, ni D401/C512). C'est exactement le trou que 0046 ferme cote liste, reste ouvert cote fiche. Route /finance/factures/[id]/page.tsx l.44 (bouton getActionLabel).
- Incoherence de saisie residuelle — createSupplierInvoiceDirect (api.ts:691, encore appele par le V1 useFacturesPage.handleCreateFacture) cree un brouillon SANS supplier_invoice_lines. Si un tel brouillon atteint validate_supplier_invoice, la RPC leve 23514 'brouillon sans ligne'. La liste routee V2 a neutralise le bouton (commentaire page.tsx l.51-53) mais la fonction et le mapping de statut 'draft' restent, donc tout brouillon cree hors /new est invalidable.
- Deux features 'facture' coexistent (dette doublon EN/FR) : src/features/finance/invoices (V1) et src/features/finance/factures (V2 qui re-emballe juste useFacturesPage de invoices). Migration a finir : risque de copier le mauvais chemin (le naked-flip de useFactureDetailPage).

**Assertion de parite de bout en bout** : Un test e2e en base (sur une copro de harnais create_test_copro_seeded, en BEGIN/ROLLBACK avec claim service_role) doit prouver, a partir d'un budget current avec 1 ligne (account_id=701/6xx, planned_amount=1000) et 0 depense : (1) APRES post_supplier_invoice(post_immediately=true) d'une facture 600 € imputee sur ce compte 6xx : ledger_lines contient bien D6xx 600 / C401 600 (chain GL OK) ; (2) MAIS v_budget_lines_overview.validated_spent reste 0 et consumption_pct=0 pour cette ligne — c'est l'assertion-cle qui materialise la rupture : le realise du GL ne remonte pas au suivi budgetaire. Un systeme correct exigerait validated_spent=600 / consumption_pct=60. (3) APRES validate_supplier_invoice sur un brouillon SANS ligne : RAISE SQLSTATE 23514. (4) APRES post_supplier_payment 600 € : ledger_lines D401 600 / C512 600 et supplier_invoices.status='paid'. (5) APRES reconcile_bank_movement contre ce paiement : bank_matches=1 ligne, bank_movements.status='matched', et COUNT(ledger_lines) INCHANGE (pointage pur, zero double ecriture du 512). (6) REGRESSION fiche detail : appeler le chemin handleChangeStatut (updateSupplierInvoice status->posted) puis verifier que ledger_transactions ne contient AUCUNE ecriture source_type='supplier_invoice' pour cette facture (prouve le naked-flip). Invariants comptables transverses : Σ debits = Σ credits sur chaque ledger_transaction ; tout 45x porte lot_id ; immutabilite apres post.

**Implication ordre de construction** : Ordre de construction v2 (tranches verticales finance-first) : 1) D'ABORD reparer la rupture silencieuse de la fiche detail (useFactureDetailPage.handleChangeStatut DOIT router vers validateSupplierInvoice puis postSupplierPayment selon la transition, comme la liste — sinon valider/payer depuis la fiche corrompt l'etat metier sans GL). C'est un bug de correction pure, prerequis a toute campagne de test factures. 2) ENSUITE trancher le modele d'unification du realise budgetaire (decision USER requise, c'est un choix metier, pas un bug a patcher en aveugle) : soit (A) la validation de facture fournisseur cree/MAJ une budget_expense liee (reutiliser le ledger_tx_id + budget_line_id, qui existe deja sur supplier_invoice_lines) pour que les vues budget la voient ; soit (B) refondre v_budget_lines_overview/v_budgets_overview pour deriver le realise directement des ledger_lines classe 6 (par account_id + period), source legale unique, et deprecier budget_expenses comme source de realise. L'option B est plus alignee avec la doctrine 'GL = source unique' du projet ; l'option A garde le pont deja cable structurellement. 3) Le cablage du budget_line_id a la saisie (/finance/factures/new) est un prerequis de l'option A (relier la ligne 6xx a la ligne de budget). 4) Finir la migration doublon invoices(V1)/factures(V2) AVANT d'ajouter du code, pour ne pas dupliquer le naked-flip. 5) Le rapprochement bancaire est SAIN et ne doit pas bouger (toute ecriture GL ajoutee au pointage doublerait le 512) — le geler comme invariant de non-regression.

## Chaine 5 — Cloture d'exercice : close_period -> open_next_period (a-nouveaux + report resultat 478/12) -> regularize_period (affectation courant D478/C450-1, travaux GELE par defaut) -> approve_period

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **AG — etape PV (resolution APPROVE_ACCOUNTS)** -> Le gestionnaire active les decisions votees depuis la page PV ; le moteur AG execute la sequence de cloture sur la periode N cible -> usePVPage.ts:739 -> rpc('activate_ag_decisions', p_ag_id) [0030_rpc_ag_conseil.sql bloc 'when APPROVE_ACCOUNTS' ~1830-1876] -> ag_pending_actions consommees ; orchestre close/open_next/regularize/approve. Garde idempotente : si periode deja 'approved' -> no-op (pas de double affectation).
2. **Cloture technique exercice N** -> Si N est 'open', passage en 'closed' (cloture comptable, pas l'approbation AG) -> `close_period`(p_period_id) [0027 §1] appelee ligne 1848 ; aussi front closePeriod (api.ts:1083) bouton compta useComptabilitePage.ts:261 -> accounting_periods.status='closed', closed_at, closed_by. Transition gardee open->closed uniquement.
3. **Ouverture N+1 + a-nouveaux + report du resultat** -> Cree/rouvre N+1, reporte soldes bilan (1/4/5) par compte×lot, SOLDE le resultat 6/7 vers comptes d'attente : net courant -> 478, net travaux/exceptionnel -> 12. Contre-passe le cut-off de N dans N+1. -> `open_next_period`(p_copro_id, p_closing_period_id) [0056 §4 / LIVE confirme codes 478 et 12] appelee ligne 1870 ; appelle reverse_period_cutoff -> ledger_transactions source_type='opening_balance' (period_id=N+1, source_id=N) : a-nouveaux 1/4/5 + D/C 478 (resultat courant) + D/C 12 (resultat travaux) ; + ecriture 'closing' d'extourne cut-off en N+1. Idempotent par remplacement si N et N+1 non approuves.
4. **Affectation du resultat aux coproprietaires** -> Lit le solde 478 (courant) et 12 (travaux) de N+1 apres a-nouveau, deverse par quote-part (cle generale active). COURANT toujours affecte ; TRAVAUX GELE par defaut (p_affecter_travaux=false) -> le 12 reste et se reporte. -> `regularize_period`(p_copro_id, p_period_id) appelee en 2-args ligne 1871 -> p_affecter_travaux=false [0057 B4 / LIVE confirme reg_works_gated=true] ; garde-fou assert_result_allocation_split en fin -> ledger_transactions source_type='result_allocation' (period_id=N+1, source_id=N) : D478/C450-1 par lot (courant). PAS d'ecriture travaux par defaut -> solde 12 conserve. Arrondi cumulatif = Σ lots exact au centime.
5. **Gel definitif des comptes (approbation AG)** -> Passe N en 'approved' : exercice intangible (ferme l'exemption is_ledger_regen_exempt sur N et N+1) -> `approve_period`(p_period_id) [0027 §2] appelee EN DERNIER ligne 1873. NB : approvePeriod front (api.ts:1184) est un UPDATE direct hors RPC, NON cable en UI -> accounting_periods.status='approved', approved_at, approved_by. Ensuite regularize/open_next refusent toute reprise (RAISE metier avant trigger 0024).
6. **Apurement travaux differe (hors cloture annuelle)** -> Plus tard, hors chaine de cloture, deversement manuel du solde 12 gele vers 450-2 quand l'operation travaux est definitivement close (multi-exercices) -> settleWorksBalance (api.ts:1143) -> `settle_works_balance`(p_copro_id, p_period_id) [0057 B4 / LIVE confirme] ; ecran 'operations a apurer' via v_works_pending_settlement -> ledger_transactions source_type='works_settlement' ADDITIVE sur l'exercice OUVERT : D12/C450-2 par quote-part. Marche avant comme apres approbation, ne touche jamais un exercice fige.

**Categories traversees** :
- AG (moteur d'activation des decisions)
- Finance / Comptabilite (cycle des exercices, grand livre)
- Affectation du resultat / comptes d'attente 478-12
- Repartition (cle generale, quote-part par lot)
- Cut-off / droits constates (extourne en N+1)

**STATUT** : fonctionne

**Points de rupture** :
- DIVERGENCE PROMPT vs REALITE (pas un bug) : le prompt dit 'D478/C450-1 courant, D12/C450-2 travaux par quote-part'. Depuis 0057 (B4, LIVE confirme), la branche TRAVAUX est GELEE par defaut (p_affecter_travaux=false). regularize_period n'ecrit QUE le courant D478/C450-1 ; le 12 reste et se reporte. Le D12/C450-2 ne se fait QU'A la demande via settle_works_balance, jamais auto a la cloture (conformite arrete 14/03/2005, travaux multi-exercices).
- CODE MORT DANGEREUX — src/lib/finance/api.ts:1184 approvePeriod : UPDATE direct accounting_periods status='approved' (PAS rpc('approve_period')). CONTOURNE close_period + open_next_period + regularize_period. Aujourd'hui NON cable (aucun appelant) donc inerte, mais branche tel quel il approuverait un exercice SANS a-nouveaux N+1 ni affectation = rupture silencieuse.
- CASSE — src/lib/finance/api.ts:1205 rejectPeriod : pose status='rejected'. L'enum period_status du LIVE = (open|closed|approved), 'rejected' n'existe PAS -> tout appel echoue (invalid enum). Non cable aujourd'hui, a supprimer/corriger.
- CHEMIN PARTIEL LEGITIME — useComptabilitePage.ts:261 -> closePeriod appelle close_period SEUL (open->closed), n'enchaine PAS open_next/regularize. Etat 'closed' intermediaire valide ; chaine completee SEULEMENT si l'AG passe ensuite APPROVE_ACCOUNTS. Sans AG, l'exercice reste 'closed' sans N+1 ni affectation (dependance inter-feature a surveiller).
- GARDE DE SAUT — 0030 ligne 1853-1869 : open_next+regularize ne tournent QUE s'il existe un solde bilan 1/4/5 net OU un resultat 6/7 net non nul. Exercice totalement nul -> approve_period seul (pas de N+1). Cas legitime mais a couvrir par test.

**Assertion de parite de bout en bout** : Sur une copro avec exercice N 'open' ayant des ecritures 6/7 (resultat non nul) et des soldes de bilan, executer activate_ag_decisions sur une AG dont une resolution APPROVE_ACCOUNTS cible N, puis prouver EN BASE : (1) N.status='approved' + un N+1 'open' cree/rouvert ; (2) ecriture opening_balance (period_id=N+1, source_id=N) reportant les soldes 1/4/5 par compte×lot + soldant le net courant sur 478 et le net travaux sur 12 ; (3) ecriture result_allocation (period_id=N+1, source_id=N) avec D478/C450-1 par lot, Σ(450-1)=|solde 478| au centime, et AUCUNE ligne 450-2 (travaux gele) ; (4) solde du compte 12 en N+1 = report du resultat travaux, NON deverse ; (5) v_result_allocation_split renvoie 0 ligne pour (copro, N+1) ; (6) chaque ecriture equilibree (Σ debit=Σ credit) et balance N+1 d'ouverture equilibree ; (7) rejouer activate_ag_decisions = no-op (idempotence) ; (8) apres approbation, open_next_period/regularize_period RAISE (fige). Complement : settle_works_balance deverse le 12 -> D12/C450-2, Σ(450-2 par lot)=|solde 12|, 12 ramene a 0.

**Implication ordre de construction** : Ordre intra-chaine IMPERATIF et deja respecte : close_period AVANT open_next_period (pas de report depuis 'open') ; open_next_period AVANT regularize_period (regularize lit les soldes 478/12 de N+1 issus de l'a-nouveau ; sans open_next ils sont nuls et regularize skip silencieusement = a-nouveau-AVANT-affectation) ; approve_period EN DERNIER (sinon l'exemption d'immutabilite se ferme et open_next/regularize sont refuses). Pour v2 : (a) garder activate_ag_decisions comme SEUL point d'entree complet ; ne PAS exposer un bouton 'approuver l'exercice' branche sur approvePeriod (UPDATE direct qui court-circuite l'orchestration) ; recabler approvePeriod sur rpc('approve_period') ou le supprimer ; (b) supprimer/reparer rejectPeriod (enum 'rejected' inexistant) ; (c) construire et tester cette chaine AVANT le portail coproprietaire et tout ecran de bilan (soldes 450-1/450-2/478/12 en dependent) ; (d) l'apurement travaux (settle_works_balance) est une feature SEPAREE de la cloture annuelle, a construire/tester independamment (manuel, multi-exercices).

## Chaine 6 — Droits constates art.14-3 : cut-off 408/486 fin N -> extourne auto a l'ouverture N+1

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **Facture fournisseur non payee fin N (Finance > Factures)** -> Saisie + validation de la facture : D6xx / C401. Si non payee au 31/12 elle reste un solde crediteur 401 (dette certaine). -> create_supplier_invoice / validate (route writes.ts — D6xx/C401), PAS post_period_cutoff -> ledger_entries D6xx (charge rattachee a N via period_id=N) + C401. La charge EST deja dans le resultat N par sa date — une 401 certaine n'a PAS besoin de cut-off 408 ; le cut-off ne sert qu'aux charges A PAYER non encore facturees (CAP->408) ou constatees d'avance (CCA/PCA->486).
2. **Cut-off de fin d'exercice (droits constates art.14-3) — AUCUNE UI** -> Le gestionnaire devrait, sur l'exercice N encore OUVERT, declarer les items CAP/CCA/PCA/PAR (charges/produits a rattacher non factures). -> `post_period_cutoff`(p_copro_id, p_period_id, p_items jsonb) — EXISTE LIVE, signature OK -> period_cutoff_items (1 ligne/item, posting_tx_id) + 1 ecriture GL equilibree source_type='closing', source_id=period_id N (ex. D6xx/C408 pour une CAP). LIVE : 0 ligne dans period_cutoff_items, 0 tx 'closing' -> jamais execute.
3. **Cloture de l'exercice (Finance > Comptabilite > Cloturer)** -> handleValiderCloture : verifie equilibre + mouvements categorises, puis ferme. Ne propose PAS de poster le cut-off avant. -> financeApi.closePeriod -> rpc('close_period') — pur flip 'open'->'closed', ne declenche NI post_period_cutoff NI open_next_period -> accounting_periods.status='closed', closed_at, closed_by. Aucune ecriture GL.
4. **Ouverture de l'exercice N+1 (a-nouveau + extourne cut-off) — AUCUNE UI** -> Devrait etre appelee apres close_period pour reporter les a-nouveaux ET extourner le cut-off de N dans N+1. -> `open_next_period`(...) — EXISTE LIVE (version 0060, contient bien charge_nature et l'appel interne a reverse_period_cutoff). AUCUN appelant : absent de src/lib/finance/api.ts, des edge functions et de cron.job. -> Si appelee : ecriture opening_balance (a-nouveaux bilan + resultat 478/12) sur N+1, PUIS via reverse_period_cutoff une ecriture 'closing' source_id=N portee par N+1 inversant chaque item auto_reverse=true (ex. extourne C6xx/D408). LIVE : jamais produite.
5. **Contre-passation auto (reverse_period_cutoff)** -> Sous-etape interne de open_next_period — jamais atteinte faute d'appelant amont. -> `reverse_period_cutoff`(p_copro_id, p_period_id) — EXISTE LIVE, garde N+1 ouverte + refus si N ou N+1 approuve + idempotence par DELETE de l'extourne precedente -> period_cutoff_items.reversal_tx_id + ecriture GL inversee sur N+1 (cutoff_entry_pair p_reverse=true). LIVE : 0.

**Categories traversees** :
- Finance/Factures fournisseur (D6xx/C401)
- Finance/Comptabilite — cloture d'exercice (close_period, UI)
- Finance/Periodes — droits constates & a-nouveau (post_period_cutoff, open_next_period, reverse_period_cutoff — SQL only)
- Grand livre / ledger_transactions (source_type='closing' + 'opening_balance')
- AG — approbation des comptes (approve_period, fige l'immutabilite)

**STATUT** : casse

**Points de rupture** :
- RUPTURE PRINCIPALE — Aucun point d'entree pour le cut-off : post_period_cutoff n'a AUCUN appelant. Present uniquement dans src/types/supabase.ts (types generes), absent de src/lib/finance/api.ts, des edge functions (supabase/functions) et de cron.job. Une facture/charge a payer non facturee ne peut donc PAS etre rattachee a N par l'app aujourd'hui.
- RUPTURE PRINCIPALE — open_next_period n'a AUCUN appelant applicatif (idem : types only, pas dans api.ts, pas d'edge, pas de cron). Or l'extourne (reverse_period_cutoff) n'est declenchee QUE depuis open_next_period (migration 0060, ligne 289). Sans ouverture pilotee de N+1, l'extourne ne s'execute jamais -> un cut-off, meme poste manuellement, resterait non extourne.
- RUPTURE UI — src/features/finance/comptabilite/hooks/useComptabilitePage.ts:241 handleValiderCloture appelle uniquement closePeriod ; le flux de cloture ne propose ni le cut-off prealable (sur N ouverte) ni l'ouverture/a-nouveau de N+1. La chaine legale n'est pas materialisee dans le parcours gestionnaire.
- PREUVE LIVE (qqfqrcolzmcbsvfaumiq) — period_cutoff_items = 0 ligne, ledger_transactions source_type='closing' = 0 : la chaine n'a JAMAIS tourne en production.
- DETTE CONNEXE (hors chaine, a signaler) — src/lib/finance/api.ts:1184 approvePeriod fait un UPDATE direct sur accounting_periods au lieu d'appeler la RPC approve_period (0027), court-circuitant la garde d'immutabilite is_ledger_regen_exempt ; rejectPeriod idem. Risque de figer/defiger une periode hors du contrat SQL.

**Assertion de parite de bout en bout** : Un test e2e doit prouver, sur une copro de test (ex. boucle d'or / Domaine des Tilleuls) avec exercice N OUVERT : (1) saisir un item de cut-off CAP via post_period_cutoff(N, [{kind:'CAP', account_id:6xx, counterpart_code:'408', amount:A}]) => exactement 1 ligne period_cutoff_items(period_id=N, posting_tx_id NOT NULL) + 1 ledger_transactions source_type='closing' source_id=N period_id=N equilibree D6xx/C408 ; le resultat de N inclut A. (2) close_period(N) => status='closed', GL inchange. (3) open_next_period(copro, N) => cree/rouvre N+1 'open' + ecriture opening_balance sur N+1, ET en cascade reverse_period_cutoff a ecrit 1 ledger_transactions source_type='closing' source_id=N period_id=N+1 inversee (C6xx/D408) + period_cutoff_items.reversal_tx_id renseigne. (4) Verifier la NETTING : sur N+1 la charge constatee est annulee (solde 408 = 0 apres extourne, en attendant la facture reelle qui soldera 408/401), et le resultat N+1 ne porte plus la charge de N. (5) Idempotence : re-appeler open_next_period (N et N+1 non approuves) ne duplique ni l'a-nouveau ni l'extourne. (6) Garde immutabilite : approve_period(N) PUIS open_next_period => exception 'reprise figee / extourne figee'. Aujourd'hui ce test echoue des l'etape (1) cote app car aucun bouton/route n'appelle ces RPC ; il ne passe qu'en SQL direct.

**Implication ordre de construction** : Pour la v2, l'ordre de construction de la boucle annuelle est contraint par les gardes de statut des fonctions (toutes verifiees LIVE) : (A) la facture/charge se saisit pendant que N est 'open' ; (B) le cut-off (post_period_cutoff) DOIT etre poste AVANT close_period — la fonction exige period N status='open' (sinon 'non ouverte — rouvrir avant le cut-off') ; (C) close_period (N 'open'->'closed') ; (D) open_next_period exige N 'closed' (refuse si 'open') et declenche en interne reverse_period_cutoff qui exige N+1 'open' et REFUSE si N OU N+1 = 'approved' ; (E) approve_period (AG) DOIT venir EN DERNIER : il ferme l'exemption d'immutabilite et fige a-nouveau ET extourne. Donc l'ordre canonique est cut-off -> cloture -> ouverture N+1 (a-nouveau + extourne) -> approbation AG. Consequence chantier : avant de coder l'UI, il faut (1) ajouter les wrappers manquants postPeriodCutoff() et openNextPeriod() dans src/lib/finance/api.ts, (2) inserer dans le parcours de cloture un ecran cut-off (sur N ouverte) puis un declencheur d'ouverture N+1, (3) corriger approvePeriod/rejectPeriod pour passer par les RPC approve_period (respect de l'immutabilite). Tant que ces wrappers n'existent pas, toute campagne de test ne peut valider la chaine qu'en SQL direct, pas via le rendu client (incoherence a signaler immediatement par rapport a la regle 'tests Playwright = preuve client reel').

## Chaine 7 — Fonds de travaux ALUR : cotisation (D450-5/C105) -> affectation budget travaux (D105/C705) -> reglement cash Livret A vers courant (D512/C502), solde via v_alur_fund_balance

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **AG - vote cotisation ALUR (resolution CREATE_ALUR_FUND, art.25) OU Onboarding reprise de mandat (solde d'ouverture 450-5/105)** -> A l'activation du PV : activate_ag_decisions branche 'CREATE_ALUR_FUND' valide le budget ALUR puis emet l'appel de cotisation art.14-2 II. (Voie alternative : reprise saisit un solde d'ouverture sur 450-5/105.) UI : src/lib/constants/resolutions.ts:229 (resolution), src/app/(dashboard)/ag/[id]/finalisation + BlocALUR.tsx ; reprise = src/components/features/onboarding/reprise/useRepriseSoldes.ts:34 -> `activate_ag_decisions` (0030, ligne 1811 branche CREATE_ALUR_FUND) -> validate_budget -> generate_calls_from_ag_payload -> `post_budget_call_for_funds` (0026, ligne 482 : budget_type 'alur' => compte credit '105') -> create_ledger_transaction (0025). Voie reprise : set_opening_balance. -> ledger_transactions (source_type call_for_funds) + ledger_entries D 450-5/lot (agrege toutes cles) C 105 total (reserve art.14-2 II) ; ligne call_for_funds 'issued'. Le 105 (credit-normal) monte = fonds disponible.
2. **Finance > Budgets > onglet ALUR > modale Affectation (TransferModal)** -> Le gestionnaire saisit un montant et choisit un budget travaux VOTE (statut validated) comme destination, puis confirme l'affectation du fonds. -> UI TransferModal.tsx -> page.tsx onTransferALUR -> useBudget.handleTransferALUR (ligne 481) -> postAlurTransfer (src/lib/budget/api.ts:693) -> RPC `post_alur_transfer` (0037). Gardes : gestionnaire, advisory_xact_lock, budget=works+validated, periode ouverte, p_amount <= solde 105 cumule (meme calcul que la vue). -> ledger_transactions (kind transfer, source_id=budget_id) + ledger_entries D 105 / C 705 du montant affecte, dans l'exercice du budget travaux ; ligne alur_transfers (destination 'works', cash_settled=false, ledger_tx_id). Le 105 baisse => le disponible diminue d'autant (anti double-affectation).
3. **Finance > Budgets > onglet ALUR > banniere 'en attente de virement' (ALURTab) -> bouton 'Marquer le virement effectue'** -> Le gestionnaire clique sur le rappel d'une affectation non encore reglee en tresorerie pour acter le virement reel Livret A -> compte courant. -> UI ALURTab.tsx handleSettle -> page.tsx onSettleAlurCash -> useBudget.handleSettleAlurCash (ligne 500) -> settleAlurTransferCash (src/lib/budget/api.ts:714) -> RPC `settle_alur_transfer_cash` (0037). Gardes : FOR UPDATE anti double-virement, gestionnaire, cash_settled deja=false, exercice ouvert. -> ledger_transactions (kind transfer, source_id=transfer_id) + ledger_entries D 512 (compte courant) / C 502 (Livret A) du montant, dans l'exercice ouvert ; UPDATE alur_transfers SET cash_settled=true, cash_settled_at, cash_ledger_tx_id. Sort de la banniere v_alur_transfers_pending_cash.

**Categories traversees** :
- AG (vote/activation des decisions)
- Onboarding/Reprise de mandat (voie alternative d'alimentation 450-5/105)
- Finance - Budgets (budget travaux vote, onglet ALUR)
- Comptabilite - Grand livre (create_ledger_transaction, ledger_entries, partie double)
- Tresorerie (comptes 512 courant / 502 Livret A)

**STATUT** : fonctionne

**Points de rupture** :
- Aucune rupture de cablage : la chaine est complete et coherente du front au grand livre, et le repo est EN PHASE avec le live (definitions des 3 fonctions + 3 vues identiques sur qqfqrcolzmcbsvfaumiq).
- SILENCE EMPIRIQUE (pas une casse de code) : sur le live, total solde 105 = 0 sur 18 copros, 0 ligne alur_transfers, 0 pending cash. La chaine n'a JAMAIS ete exercee de bout en bout sur le live — aucune cotisation ALUR n'a credite 105, donc affectation et reglement n'ont jamais pu se declencher. A prouver par un test, pas une regression.
- Point de fragilite d'ergonomie (pas un bug) : useBudget.handleCreateBudget ne mappe que 'current'/'works' (ligne 528) ; un budget ALUR ne se cree donc PAS par le bouton 'Nouveau budget' mais UNIQUEMENT via l'AG (CREATE_ALUR_FUND) ou la reprise. Si le testeur cherche a amorcer la cotisation hors AG, il ne trouvera pas d'entree directe.
- Asymetrie d'exercice a verifier : post_alur_transfer poste D105/C705 dans l'exercice DU BUDGET TRAVAUX (cut-off), tandis que settle_alur_transfer_cash poste D512/C502 dans l'exercice OUVERT le plus recent. Si l'affectation et le reglement tombent sur deux exercices differents, les deux ecritures vivent dans des periodes distinctes — comptablement defendable mais a confirmer cote test multi-periode.
- 705 jamais contre-passe : l'affectation credite 705 (produit d'affectation du fonds) sans mecanisme de reversal si l'operation travaux est annulee apres affectation — hors-scope ici mais a garder en tete pour le cycle de vie complet.

**Assertion de parite de bout en bout** : Un test e2e (copro de test, ex. create_test_copro_seeded en BEGIN/ROLLBACK) doit prouver en base, dans l'ordre : (1) APRES cotisation ALUR : v_alur_fund_balance.balance = montant cotise > 0, egal a SUM(credit-debit) du compte 105 poste, et il existe des ledger_entries D 450-5/lot agreges C 105 dont les debits par lot somment au credit 105 (equilibre partie double). (2) APRES post_alur_transfer(montant M sur un budget works valide) : un ledger_transactions transfer avec entries D105/C705 de M ; v_alur_fund_balance.balance a BAISSE de M (le disponible se consomme) ; une ligne alur_transfers cash_settled=false visible dans v_alur_transfers_pending_cash ; et un 2e appel post_alur_transfer de (solde+0.01) est REJETE (borne sur solde 105). (3) APRES settle_alur_transfer_cash : ledger_entries D512/C502 de M ; alur_transfers.cash_settled=true + cash_settled_at + cash_ledger_tx_id non nul ; la ligne disparait de v_alur_transfers_pending_cash ; un 2e settle du meme transfer est REJETE (anti double-virement). (4) Invariant global : la balance generale reste equilibree (sum debit = sum credit), 105 n'est plus touche par le settle (le disponible ne bouge pas a l'etape 3), et 502+512 restent nets a zero sur le mouvement (virement interne de tresorerie).

**Implication ordre de construction** : Ordre de construction v2 impose par les dependances de donnees, strictement sequentiel : (0) Socle grand livre partie double + create_ledger_transaction + plan comptable avec 450-5/105/705/502/512 + accounting_periods (statut open/closed) AVANT tout. (1) Source d'alimentation du 105 : soit le moteur AG (resolution CREATE_ALUR_FUND -> validate_budget -> post_budget_call_for_funds avec credit 105), soit la reprise de mandat (set_opening_balance 450-5/105). Sans cette etape, RIEN en aval ne peut s'amorcer (le live le prouve : 105=0 => chaine muette). (2) post_alur_transfer (D105/C705) ne peut etre construit/teste qu'APRES (1) ET apres l'existence d'un budget travaux au statut validated (donc apres le cycle de vote budget travaux). (3) settle_alur_transfer_cash (D512/C502) depend de l'existence d'une ligne alur_transfers => APRES (2), et d'un exercice ouvert. (4) v_alur_fund_balance est la SOURCE UNIQUE et doit etre la borne partagee front+serveur des le depart : ne jamais introduire un 2e calcul de disponible (l'ancienne v_alur_fund_summary par exercice divergeait — proscrite). Implication pratique pour le plan de test v2 : amorcer obligatoirement la cotisation via AG ou reprise (pas via le bouton Nouveau budget qui ignore le type 'alur'), sinon l'onglet ALUR affichera solde 0 et la modale d'affectation se bloquera ('fonds non encore alimente').

## Chaine 8 — Vente de lot : soldes 45x du lot -> etat date art.5 (snapshot immuable) -> mutation (transfert proprietaire)

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **Ventes/Mutations - ouverture de la validation (avertissement solde vendeur)** -> Le gestionnaire clique 'Valider' sur la fiche mutation ; le hook charge le solde 45x reel du lot pour AVERTIR (jamais bloquer) si le vendeur n'est pas solde -> `get_lot_balance_45x`(p_copro_id, p_lot_id) [migration 0082] appelee via mutationsApi.getLotBalance45x -> useMutationDetail.loadSellerBalance -> page.tsx onValidate -> ValidationModal(sellerBalance) -> AUCUNE ecriture (lecture seule). Lit ledger_entries x ledger_transactions(status='posted') x accounts(code like '45%') filtre lot_id+copro_id, round-then-sum PAR compte. VERIFIE LIVE : pas de cut-off tx_date (has_cutoff=false, mentions_current_date=false) -> solde LIVE 'maintenant', inclut 450-5 ALUR.
2. **Etat date art.5 - generation du payload (3 parties legales)** -> onGeneratePreEtat('pre') / onGenerateFinalEtat('final') -> useMutationDetail.handleGenerateEtatDate -> mutationsApi.generateEtatDate -> RPC create_etat_date_snapshot, qui appelle en interne generate_etat_date_payload -> `generate_etat_date_payload`(p_copro_id, p_mutation_id, p_snapshot_type) [LIVE = version 0080, def_len 10462, identite gelee Option B] -> AUCUNE ecriture (STABLE, lecture seule). Lit le GL fige a v_eff = coalesce(effective_date, signature_date, current_date) : meme requete 45x que get_lot_balance_45x MAIS avec cut-off t.tx_date <= v_eff (VERIFIE sur le LIVE). P1=debiteur (bal>0), P2=crediteur hors 450-5, P3=avances 450-3 + provisions appelees non echues + H3 (quote-part budgets votes) ; blocs identite copro/syndic/lot/seller/notaire geles.
3. **Etat date art.5 - figeage du snapshot immuable** -> create_etat_date_snapshot INSERE le payload dans etat_date_snapshots et fait avancer le workflow de la mutation -> `create_etat_date_snapshot`(p_copro_id, p_mutation_id, p_snapshot_type) [migration 0031, LIVE def_len 2095] -> INSERT etat_date_snapshots(copro_id, mutation_id, lot_id, snapshot_type, effective_date=v_eff, payload, generated_by). CHECK ck_etat_date_payload_parts (3 cles). UPDATE mutations.status -> 'pre_etat_generated' (pre) ou 'etat_generated' (final). UPDATE mutation_steps step 'pre_etat_date'/'etat_date' = completed. Immutabilite posee par tr_etat_date_immutable (seul document_id modifiable ensuite).
4. **Mutation - validation / transfert de propriete (lot-centric)** -> handleValidate -> useMutationDetail.handleValidateMutation -> mutationsApi.validateMutation -> RPC validate_mutation (chemin alternatif useSalesMutations.validateSale rebranche sur la MEME RPC, PLAN_J5 T5 #18) -> `validate_mutation`(p_mutation_id, p_signature_date, p_effective_date, p_buyer_owner_id, p_buyer_first_name, p_buyer_last_name, p_buyer_company_name, p_buyer_email, p_buyer_is_company) [migration 0031, LIVE def_len 3734] -> UPDATE lot_owners SET end_date=v_eff (cloture des proprietaires actifs) ; INSERT lot_owners (acquereur, share 100, start_date=v_eff) ; eventuel INSERT coproprietaires (acquereur a la volee) ; UPDATE mutations (buyer_owner_id, dates, status='validated') ; UPDATE mutation_steps signature_acte+cloture_compte=completed. ZERO ecriture GL : retourne gl_posted=false, le solde 450 reste attache au LOT (A3).

**Categories traversees** :
- Ventes/Mutations (features/ventes)
- Comptabilite / Grand livre (ledger_entries, ledger_transactions, accounts 45x)
- Etat date art.5 / GED (etat_date_snapshots, document_folders 'Etats dates')
- Copropriete / Lots (lot_owners, coproprietaires, repartition_keys)
- Budgets / Appels de fonds (budgets, call_for_funds_lines pour P3)

**STATUT** : fonctionne

**Points de rupture** :
- NON-RUPTURE (asymetrie ASSUMEE et documentee) : get_lot_balance_45x (supabase/migrations/0082_get_lot_balance_45x.sql) lit le GL LIVE SANS cut-off (verifie sur le LIVE : aucun filtre tx_date), alors que generate_etat_date_payload (LIVE = 0080_etat_date_identity.sql) applique t.tx_date <= v_eff. Donc l'avertissement de la modale et le total P1 de l'etat date peuvent differer si des ecritures 45x existent entre v_eff et aujourd'hui. C'est intentionnel (en-tete 0082 L.10-12 : 'AUCUN cut-off... le plus honnete'), mais un testeur naif le lira comme une incoherence -> a expliciter dans l'UI.
- DETTE MORTE (pas dans le chemin actif mais piege a la copie) : src/lib/sales/api.ts createEtatDateSnapshot fait un INSERT client brut dans etat_date_snapshots (contourne l'immutabilite + payload non calcule cote SQL) ET utilise updateMutationStatus('final_etat_generated') -> valeur ABSENTE de l'enum mutation_status (LIVE : draft|pre_etat_generated|etat_generated|sent_to_notary|signed|validated|cancelled). Cette fonction n'est PLUS appelee (useSalesMutations.createEtatDate a ete rebranche sur la RPC), mais elle reste exportee et planterait si recablee. A supprimer (parite avant suppression, cf. memoire vente_cleanup_chantier).
- COUPLAGE FAIBLE (pas une rupture) : create_etat_date_snapshot ne re-verifie pas l'etat de l'acquereur ni le solde au moment du figeage ; il fige a v_eff qui depend des dates saisies sur la mutation. Si effective_date/signature_date sont nulles au moment du pre-etat, v_eff retombe sur current_date (coalesce) -> le pre-etat peut etre fige a une date differente de l'etat final. Comportement correct mais a tester explicitement.

**Assertion de parite de bout en bout** : Un test e2e (golden, copro seedee via create_test_copro_seeded en BEGIN/ROLLBACK, claim service_role) doit prouver EN BASE, pour un lot avec des ecritures 45x reelles (un appel D450-1 impaye + un paiement partiel + une cotisation ALUR D450-5) : (1) get_lot_balance_45x(copro, lot) = round-then-sum signe par compte des ledger_entries postes 45x, ET egale EXACTEMENT le total partie_1 - partie_2 du payload genere QUAND aucune ecriture n'existe entre effective_date et current_date (sinon ils divergent legitimement = preuve de l'asymetrie de cut-off a documenter) ; (2) create_etat_date_snapshot insere une ligne etat_date_snapshots dont payload->'partie_1_sommes_dues_vendeur'->>'total' = somme des bal>0 a tx_date<=effective_date et payload->'partie_2'->'items' EXCLUT le code '450-5' ; (3) un UPDATE direct du snapshot (hors document_id) leve l'exception tr_etat_date_immutable (immutabilite art.5) ; (4) validate_mutation cloture lot_owners.end_date=effective_date et ouvre l'acquereur start_date=effective_date SANS creer la moindre ledger_transaction (assert : count des tx source_type='mutation' INCHANGE, retour gl_posted=false), le solde 450 du lot etant INCHANGE avant/apres la mutation ; (5) apres validation, generate_etat_date_payload regenere a la meme v_eff renvoie le MEME total 45x (le transfert de propriete ne touche pas le GL).

**Implication ordre de construction** : Ordre de construction v2 (dependances dures du chemin) : (1) Plan comptable accounts avec codes 45x + colonne nature, et ledger_entries.lot_id NON NULL pour le 450 (enforce_lot_id deja a 450%) - socle GL AVANT tout ; (2) repartition_keys/_lines (cle generale active OBLIGATOIRE : generate_etat_date_payload leve 23503 sans elle -> casserait l'etat date) ; (3) budgets validated + call_for_funds_lines (partie 3 H3 / provisions appelees) ; (4) tables mutations/etat_date_snapshots/lot_owners + triggers d'immutabilite et de coherence (0019 schema pur PUIS 0031 fonctions/triggers) ; (5) generate_etat_date_payload (0031 base -> enrichie 0076 H2/H3 -> 0080 identite) ; (6) create_etat_date_snapshot (depend de generate_etat_date_payload) ; (7) validate_mutation (independant du GL, depend de lot_owners) ; (8) get_lot_balance_45x (0082) APRES generate_etat_date_payload car il REPLIQUE volontairement sa formule round-then-sum-par-compte - toute evolution de la formule de l'etat date doit etre repercutee ici en miroir (sinon l'avertissement ment). Cote front : rebrancher/supprimer la dette src/lib/sales/api.ts (raw insert + enum 'final_etat_generated' fantome) AVANT d'ouvrir le chemin a un vrai syndic.

## Chaine 9 — Budget vote -> appels de fonds (ventilation par cle/tantiemes -> creances 45x + produit 701/702/105)

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **Finance > Budgets (creation/edition budget vote)** -> Le gestionnaire cree un budget (courant/travaux/ALUR) et y attache des lignes budgetaires, chacune portant un compte de charge, une CLE de repartition et un montant. handleCreateBudget (useBudget.ts) cree le budget puis UNE seule ligne via createBudgetLine, en piochant la 1re cle dispo (repartition_keys ... limit(1)). Le seed golden et l'onboarding (onboarding/api.ts) creent eux des lignes MULTI-cles (9 lignes/6 cles sur le live). -> mutations.createBudget -> INSERT budgets ; mutations.createLine -> createBudgetLine (lib/budget/api.ts) -> INSERT budget_lines(repartition_key_id, amount, account_id) -> budgets (1 ligne, status draft) ; budget_lines (1..N lignes : repartition_key_id + amount par poste). C'est l'UNIQUE source de la ventilation cote RPC.
2. **Finance > Budgets (machine a etat)** -> submit_budget puis validate_budget font passer le budget draft->submitted->validated. Pre-condition verifiee : repartition_key_is_complete sur chaque cle utilisee (toute la copro couverte, poids > 0). Aucune ecriture comptable ici (le vote ne touche pas le grand livre). -> `submit_budget` / `validate_budget` (0026) -> budgets.status='validated', validated_by/at ; unicite 1 valide par copro×periode×type.
3. **Finance > Appels de fonds (wizard CreateCallWizard, 4 etapes)** -> Le gestionnaire choisit Type (exceptional/travaux) + budget rattache (StepType), puis SAISIT un montant total libre + choisit UNE cle de repartition (StepAmount). useCreateCallWizard recalcule une ventilation par lot a l'ecran (montant × weight/Σweight, dernier lot ajuste de l'arrondi) et l'affiche en recap (StepRecap). -> aucun a ce stade (calcul 100% front, dans le hook) -> rien (etat React seulement) — la ventilation montree a l'utilisateur est un APERCU front, pas ce qui sera poste.
4. **Finance > Appels de fonds (soumission)** -> submit() appelle createCall une fois (mode single) ou N fois (mode multiple, avec installment_index/count). createCall (lib/finance/api.ts) exige budget_id et appelle la RPC. ATTENTION : il force p_fraction=1.0 et NE TRANSMET NI total_amount NI repartition_key_id (commentaire explicite : « deviennent informatifs »). -> useCreateCall.mutate -> createCall -> supabase.rpc('post_budget_call_for_funds', {p_copro_id, p_period_id, p_budget_id, p_label, p_trimester, p_issue_date, p_due_date, p_fraction:1.0, p_installment_index, p_installment_count}) -> appel RPC ; aucune ecriture directe cote front.
5. **Moteur SQL — post_budget_call_for_funds (0026, identique au LIVE qqfqrcolzmcbsvfaumiq)** -> Garde G-MGR. Derive la nature du budget. Verifie repartition_key_is_complete sur chaque cle du budget. Recalcule le total a appeler = Σ par cle de (budget_lines.amount × fraction OU par echeance i/N). Emet 1 ligne par (lot × cle) en arrondi cumulatif « plus grand reste » sur repartition_key_lines.weight (Σ lots = cible par cle au centime). Total/lignes derives UNIQUEMENT de budget_lines + repartition_key_lines — total_amount/cle du wizard sont ignores. -> `post_budget_call_for_funds` -> resolve_lot_tiers_account, repartition_key_is_complete, create_ledger_transaction -> call_for_funds (en-tete, repartition_key_id=NULL=agrege, total_amount=v_total recalcule, status='issued') ; call_for_funds_lines (1 par lot×cle : amount_due, repartition_key_id, weight_snapshot).
6. **Moteur SQL — ecriture au grand livre** -> Construit les ecritures : D 450-x PAR LOT (somme des lignes du lot, toutes cles agregees, avec lot_id) ; C 701 (current) / 702 (works) / 105 (ALUR, reserve art.14-2 II) au total. Poste via create_ledger_transaction (auto_post=true). Renseigne call_for_funds.ledger_tx_id. Triggers d'integrite : trg_validate_call_total (Σ lignes = total en-tete), trg_cff_ledger_required (issued => tx NOT NULL), filet 0024 (lot_id obligatoire sur chaque 45x). -> create_ledger_transaction (0025) + triggers tr_validate_call_total / tr_cff_ledger_required -> ledger_transactions (1) ; ledger_entries (N debits 450-x avec lot_id + 1 credit 70x/105) ; call_for_funds.ledger_tx_id.

**Categories traversees** :
- Finance > Budgets (budget_lines + cles de repartition)
- Coproprietaires/Lots > Cles de repartition (repartition_keys, repartition_key_lines)
- Finance > Appels de fonds (call_for_funds, call_for_funds_lines)
- Finance > Comptabilite / Grand livre (ledger_transactions, ledger_entries, comptes 450-x/701/702/105)

**STATUT** : partiel

**Points de rupture** :
- RUPTURE PRINCIPALE (front<->back, contrat divergent) — src/lib/finance/api.ts:351 createCall : la RPC est appelee SANS total_amount ni repartition_key_id (p_fraction fige a 1.0). La ventilation reellement postee est recalculee 100% cote SQL depuis budget_lines (multi-cles, plus-grand-reste). Le montant saisi par le gestionnaire et la cle qu'il choisit dans le wizard sont IGNORES. Consequence : l'apercu de StepRecap (mono-cle, montant libre) ne correspond PAS aux creances 45x effectivement ecrites. Faux WYSIWYG.
- RUPTURE amont (qualite de la ventilation) — src/hooks/modules/useBudget.ts:556-571 handleCreateBudget : un budget cree depuis l'UI ne pose qu'UNE budget_line avec la 1re cle trouvee (repartition_keys ... limit(1)). Toute la ventilation par poste/cle voulue par la loi est donc ecrasee par une cle unique arbitraire. La ventilation multi-cles ne marche que pour les budgets seedes/onboardes (confirme live : front=1 ligne/1 cle, golden=9 lignes/6 cles).
- RUPTURE wizard (mode multiple) — src/features/finance/appels-fonds/hooks/useCreateCallWizard.ts:301-321 : en mode 'multiple', le front fabrique ses propres montants d'echeance (buildDefaultInstallments) mais transmet installment_index/count a la RPC qui REFRACTIONNE le budget par i/N. Les montants affiches (state.installments[i].amount) et les montants postes divergent des que le total saisi != total du budget.
- MAILLON MORT (parcours travaux) — src/hooks/modules/useBudget.ts:696-703 handleTransformToAppele et :710-713 handleGenerateProchainAppel : boutons 'Generer appels depuis budget' = alert()/TODO non cables. Le seul chemin reel passe par le wizard generique. Pas de rupture silencieuse (message honnete), mais feature absente.
- INCOHERENCE de saisie — StepAmount.tsx demande un montant total libre alors que la RPC derive le montant du budget vote : un montant qui ne correspond pas au budget passera la validation front mais produira un appel d'un montant different (potentiellement bloque si budget sans lignes : 'budget sans montant a appeler').

**Assertion de parite de bout en bout** : Un test e2e doit prouver en base, apres post_budget_call_for_funds(budget valide) : (1) call_for_funds.total_amount = Σ call_for_funds_lines.amount_due = Σ(par cle) round(budget_lines.amount × fraction) au centime ; (2) il existe exactement 1 ligne par couple (lot, cle) present dans repartition_key_lines des cles du budget, weight_snapshot = repartition_key_lines.weight fige ; (3) pour chaque cle, Σ amount_due des lots = montant-cible de la cle (invariant plus-grand-reste, 0 centime perdu — trg_validate_call_total passe) ; (4) GL : Σ debits = Σ credits = total_amount ; chaque debit est sur 450-x et porte un lot_id NON NULL (0 debit sans lot, filet 0024) ; le debit agrege par lot = Σ des lignes d'appel de ce lot ; le credit unique est sur 701 (current) / 702 (works) / 105 (alur) selon budget_type ; (5) call_for_funds.ledger_tx_id NOT NULL et status='issued'. Verifie manuellement sur le live (Appel T4, golden) : 12500.00 = header = Σlignes = GLdebit = GLcredit ; 60 lignes = 18 lots × 6 cles ; 18 debits 450-x tous avec lot_id ; credit sur 701. PARITE SQL OK. A ajouter en plus : un test prouvant que le montant et la cle saisis au wizard n'influencent PAS le resultat (anti-regression de la divergence front).

**Implication ordre de construction** : Pour v2 : (A) Le moteur SQL est la source de verite unique et correcte de la ventilation — le reconstruire A L'IDENTIQUE en premier (post_budget_call_for_funds + repartition_key_is_complete + create_ledger_transaction + triggers trg_validate_call_total / trg_cff_ledger_required + filet lot_id 0024), AVANT toute UI. (B) budget_lines (cle + montant PAR poste) doit etre posable AVANT les appels : donc dans l'ordre, repartition_keys/repartition_key_lines -> budget_lines multi-cles -> submit/validate_budget -> post_budget_call_for_funds. (C) Corriger la creation de budget cote UI pour poser une budget_line PAR poste avec sa vraie cle (supprimer le limit(1)) — sinon la ventilation reste mono-cle meme avec un moteur correct. (D) Realigner le contrat wizard<->RPC : soit l'UI derive son apercu du budget vote (lecture de budget_lines via compute_repartition_shares) au lieu de demander un montant/cle libres, soit on bascule sur une RPC a montant explicite ; ne PAS afficher une ventilation mono-cle que le backend n'ecrira jamais. (E) Cabler enfin les boutons travaux (handleTransformToAppele / handleGenerateProchainAppel) ou les retirer. Regle d'or : poser le moteur + les invariants (triggers) en premier, l'UI ensuite, et figer la parite par test avant d'ouvrir le wizard aux vrais syndics.

## Chaine 10 — Onboarding / reprise de mandat : set_opening_balance (opening_onboarding) -> soldes d'ouverture (residu 471/472) -> assiette lue par les features aval

Maillons (feature -> action -> fonction backend -> donnees ecrites) :

1. **Onboarding wizard - creation copro (etape 1, /onboarding/create)** -> Le gestionnaire cree la copro reprise (nom + previous_syndic_name + exercice_debut) -> RPC `create_copro` (SECURITY DEFINER, 0083) -> appelle provision_copro_chart pour le plan comptable canonique (450-1/2/3/4/5, 471, 472, 105, 12, 478, 401, 512x...) -> copros (onboarding_step), copro_members (gestionnaire), accounts (plan comptable complet, dont 471/472 et sous-comptes 450-x). src/lib/onboarding/api.ts:createCopropriete.
2. **Onboarding etapes 2-4 (Coproprietaires, Lots/Cles, Comptes)** -> Saisie des coproprietaires, lots, cles de repartition, comptes bancaires (512000/512100) -> Inserts directs (coproprietaires, lots, repartition_keys/lines, accounts) ; pas de RPC GL -> coproprietaires, lots, lot_owners, repartition_keys, repartition_key_lines, accounts (512x). Ces tables sont l'identite que TOUTES les vues aval joignent (v_lots_with_owners, v_owner_statement_by_lot).
3. **Onboarding etape 5 (Step5Budget)** -> Creation/recuperation de la periode d'onboarding + saisie du budget previsionnel -> getOrCreateOnboardingPeriod (cible la periode portant deja opening_onboarding, sinon l'unique open, sinon cree l'exercice contenant aujourd'hui via deriveExerciceYearForDate) + createOnboardingBudget -> accounting_periods (status open), budgets (current, draft), budget_lines (compte de charge 6xx resolu). src/lib/onboarding/api.ts:397,470.
4. **Onboarding etape 6 (Step6AgAppels) - POST-AS-YOU-GO** -> Emission des appels de fonds de l'exercice repris (le wizard poste les appels AVANT la reprise des soldes) -> postOnboardingCalls -> RPC `post_budget_call_for_funds` par echeance (verifie repartition_key_is_complete au prealable) -> call_for_funds, call_for_funds_lines (1 ligne par lot x cle), ledger_transactions source_type='call_for_funds' (D 450-1/lot agrege, C 701), budgets.status='validated'. src/lib/onboarding/api.ts:595.
5. **Onboarding etape 7 (RepriseSoldes / useRepriseSoldes) - MAILLON CENTRAL** -> Saisie des soldes d'ouverture par lot (450-1 courant / 450-2 travaux / 450-5 ALUR / 103 avance) + globaux (105, 401, 12, 478, banques 512x, et 6x/7x si reprise mi-annee) -> setOnboardingOpeningBalance -> RPC `set_opening_balance` (0027) : une SEULE ecriture equilibree atomique, idempotente PAR REMPLACEMENT (DELETE de l'ancienne opening_onboarding puis repost). resolve_lot_tiers_account pour les 450-x ; le complement d'equilibre va en 471 (debiteur) / 472 (crediteur) NON BLOQUANT via create_ledger_transaction(p_auto_post=true) -> ledger_transactions source_type='opening_onboarding' (source_id=period_id, AU PLUS 1 par copro/periode via index unique partiel) + ledger_entries (450-x/lot, 105, 401, 12, 478, 512x, et le residu 471/472). C'EST l'assiette d'ouverture du grand livre. src/.../reprise/useRepriseSoldes.ts:306, api.ts:834.
6. **Onboarding etape 7-bis (detail residu art.10, optionnel)** -> Detail extra-comptable du residu 471/472 (creances/dettes non encore imputees) -> `set_opening_balance_residual_detail` (0077) ; opening_residual_gl recalcule le residu GL net ; flag stale si le GL bouge -> opening_balance_residual_items (TABLE COMPAGNE HORS-GL, ne touche jamais le grand livre). Warning si detail != residu, jamais bloquant.
7. **Onboarding etape 8 (Step8Finalisation) - VERIFICATION, NE POSTE RIEN** -> Verifie la coherence du grand livre puis termine l'onboarding (onboarding_step -> NULL) -> auditOnboardingBooks (RPC audit_finance_integrity) + getValidatedBudgetCallProof ; decision pure computeFinalizationDecision ; finishOnboarding (completeOnboarding: copros.onboarding_step=NULL) -> copros.onboarding_step=NULL (la copro devient 'live'). Le residu 471/472 et LOT_GL_MISMATCH/CALL_TOTAL_MISMATCH sont des AVERTISSEMENTS ; seuls LEDGER_UNBALANCED et LOT_ID_MISSING_45X bloquent (liste blanche audit-rules.ts).
8. **Aval A - Releve par lot/personne, solde 45x, ALUR, dashboard, etat date, mutation vente (lecteurs GL)** -> Lisent le solde reel des coproprietaires depuis le grand livre POSTE, toutes sources confondues -> v_owner_statement_by_lot / v_owner_statement_by_person, get_lot_balance_45x (0082), v_alur_lot_contributions.alur_ledger (0036), v_trial_balance, fn_dashboard_kpis, generate_etat_date_payload, mutationsApi -> LECTURE SEULE. CES vues INCLUENT les ecritures opening_onboarding (filtre status='posted' + a.code like '45%', sans filtre de source_type) -> la reprise EST leur assiette. Parite OK avec une from-scratch sur ce versant.
9. **Aval B - Impayes / relances / suivi des appels (lecteurs call_for_funds_lines)** -> Calculent ce que le lot 'doit' a partir des LIGNES D'APPEL uniquement -> v_unpaid_by_lot (0028, exclut copros en onboarding step non NULL), v_unpaid_with_reminders, v_calls_overview, v_call_lines_detailed (0036), chaine de relances (src/lib/impayes/api.ts, useImpayesData) -> LECTURE SEULE. CES vues NE LISENT PAS le GL opening_onboarding : elles agregent call_for_funds_lines. La dette de pre-reprise saisie en 450-1 d'ouverture N'Y APPARAIT PAS -> divergence d'assiette structurelle vs from-scratch.

**Categories traversees** :
- Onboarding / reprise de mandat
- Finance - Grand livre (comptabilite d'engagement, partie double)
- Finance - Budgets & Appels de fonds
- Finance - Impayes & Relances
- Finance - Periodes & affectation du resultat (cloture, a-nouveau)
- Ventes & Mutations (etat date)
- Dashboard / KPIs
- ALUR / Fonds de travaux

**STATUT** : partiel

**Points de rupture** :
- DEAD CODE / piege de copie : src/lib/onboarding/api.ts:720 postOnboardingOpeningBalances poste source_type='opening_balance' (collision avec l'a-nouveau open_next_period qui utilise AUSSI 'opening_balance') et n'a ZERO appelant dans src. La voie reelle est setOnboardingOpeningBalance -> set_opening_balance (source_type='opening_onboarding'). Ces deux fonctions coexistent dans le MEME fichier -> risque qu'un futur ecran appelle la mauvaise. A supprimer (migration a moitie faite, regle CLAUDE.md 'finir les migrations').
- DIVERGENCE D'ASSIETTE (le maillon 'features aval' n'est complet QUE sur le versant GL) : la dette de pre-reprise vit uniquement en 450-1 d'ouverture (GL opening_onboarding). v_unpaid_by_lot / v_unpaid_with_reminders / v_calls_overview / v_call_lines_detailed et toute la chaine de relances lisent call_for_funds_lines SEULEMENT (0028 L.226, 0036 L.84/127). Resultat : un coproprietaire qui devait deja de l'argent a l'ancien syndic N'apparait PAS dans les impayes/relances apres reprise, alors que son solde 450 (v_owner_statement_by_lot, get_lot_balance_45x, etat date) le montre debiteur. Une from-scratch a sa dette dans les DEUX (l'appel cree call_for_funds_lines ET le GL).
- DOUBLE EXCLUSION pendant l'onboarding : (a) v_unpaid_by_lot filtre c.onboarding_step IS NULL -> tant que step!=NULL la copro reprise ne remonte aucun impaye ; (b) les appels Step6 sont postes AVANT la reprise Step7 -> ordre inverse d'une logique 'a-nouveau d'abord'. C'est volontaire (post-as-you-go) mais cela signifie que l'impaye de reprise ne devient visible nulle part automatiquement, meme apres finalisation (cf. point precedent).
- NON-BLOCAGE assume mais a surveiller : le residu 471/472 (set_opening_balance) reste non solde sans empecher la finalisation (Step8Finalisation.tsx + audit-rules.ts liste blanche = LEDGER_UNBALANCED + LOT_ID_MISSING_45X seulement). Une copro reprise peut donc devenir 'live' avec un 471/472 != 0 ; v_lot_vs_gl_mismatch exclut explicitement opening_onboarding (0028 L.317) donc l'audit ne le signale jamais comme erreur. Ce n'est pas un bug mais cela rompt la parite stricte avec une from-scratch (residu = 0 par construction).
- FRAGILITE de resolution de periode : getOrCreateOnboardingPeriod / readOnboardingPeriod s'appuient sur l'unicite de la periode 'open' et sur le tri posted_at NULLS LAST (api.ts:340). En cas de double periode open (anormal) la resolution prend la plus recente -> la reprise pourrait se poser sur une periode differente de celle des appels Step6 si l'ordre des creations divergeait.

**Assertion de parite de bout en bout** : Un test e2e doit prouver EN BASE, sur le perimetre repris, l'EGALITE des soldes GL entre les deux chemins. Construire deux copros : (1) FROM-SCRATCH = copro creee + budget + appel post_budget_call_for_funds + paiements partiels ; (2) REPRISE = copro creee + set_opening_balance posant en 450-1/2/5 par lot exactement le solde net que la from-scratch presente apres ses appels/paiements (a-nouveau equivalent), as_of_date = periodStart. Assertions : (A) PARITE GL POSITIVE : pour chaque lot, get_lot_balance_45x(repro) == get_lot_balance_45x(scratch) et v_owner_statement_by_lot.balance identiques ; v_trial_balance par compte 45x identique ; fn_dashboard_kpis et generate_etat_date_payload coherents. (B) INVARIANT D'EQUILIBRE : la tx opening_onboarding est equilibree (Sigma debit = Sigma credit) et audit_finance_integrity ne renvoie AUCUN LEDGER_UNBALANCED ni LOT_ID_MISSING_45X (toute ligne 450-x porte un lot_id). (C) RESIDU TRACE : opening_residual_gl == net 471/472 de la tx, et reprise-alert getRepriseResidual le retrouve a l'identique. (D) IDEMPOTENCE : rejouer set_opening_balance avec les memes lignes laisse AU PLUS 1 tx opening_onboarding (index unique partiel) et un solde inchange. (E) DIVERGENCE ATTENDUE A DOCUMENTER (red flag a transformer en decision) : v_unpaid_by_lot(repro) NE remonte PAS la dette de pre-reprise alors que v_owner_statement_by_lot(repro) la montre debitrice -> le test doit ASSERTER cette divergence et tracer si c'est voulu (la dette de reprise n'est pas un 'impaye d'appel') ou un trou (le copro debiteur echappe aux relances).

**Implication ordre de construction** : Ordre de construction v2 dicte par les dependances reelles observees : (1) PLAN COMPTABLE D'ABORD - provision_copro_chart (450-1/2/3/4/5, 471, 472, 105, 12, 478, 401, 512x) est un PRE-REQUIS dur : set_opening_balance, resolve_lot_tiers_account et createOnboardingBudget echouent explicitement si un compte manque. (2) IDENTITE (lots, lot_owners, repartition_keys/lines) AVANT toute ecriture 450-x : la regle lot-centric impose lot_id sur 45x (trigger trg_enforce_lot_id_on_45x) et toutes les vues aval joignent lots/lot_owners. (3) MOTEUR GL (create_ledger_transaction + triggers d'integrite 0024) AVANT set_opening_balance : la reprise n'est qu'un appelant de la route canonique. (4) PERIODE 'open' AVANT la reprise : create_ledger_transaction exige status='open'. (5) DECIDER LE COUPLAGE IMPAYES<->REPRISE AVANT de cabler les relances : tant que v_unpaid_by_lot ne lit que call_for_funds_lines, toute feature de recouvrement construite dessus ignorera la dette de reprise. Si v2 veut la parite stricte, il faut soit (a) materialiser la dette de reprise en call_for_funds_lines (appel d'a-nouveau), soit (b) etendre l'assiette impayes a v_owner_statement_by_lot - ce choix doit etre tranche TOT car il conditionne le schema des relances. (6) SUPPRIMER postOnboardingOpeningBalances (source_type='opening_balance') AVANT d'ajouter de nouveaux ecrans de reprise, pour ne pas perenniser le doublon. (7) GARDER post-as-you-go mais documenter que la finalisation (onboarding_step=NULL) est le point ou la copro entre dans les vues aval - donc tester la parite APRES finalisation, pas pendant.

## Tests de chaine a ecrire

| Chaine | Statut | Priorite |
|--------|--------|----------|
| 1 — AG budget previsionnel -> CREATE_BUDGET -> appels de fonds | casse | rouge |
| 2 — AG -> activate_ag_decisions : auto-population generique de tous les action_types | casse | rouge |
| 3 — Appels 45x -> encaissement -> imputation FIFO cloisonnee -> impayes & releves | partiel | orange |
| 4 — Facture fournisseur : saisie -> validation D6xx/C401 -> paiement D401/C512 -> realise & rappro | partiel | orange |
| 5 — Cloture d'exercice : close_period -> open_next_period -> regularize_period -> approve_period | fonctionne | jaune |
| 6 — Droits constates art.14-3 : cut-off 408/486 fin N -> extourne auto N+1 | casse | rouge |
| 7 — Fonds de travaux ALUR : cotisation -> affectation -> reglement cash (v_alur_fund_balance) | fonctionne | jaune |
| 8 — Vente de lot : soldes 45x -> etat date art.5 (snapshot immuable) -> mutation | fonctionne | jaune |
| 9 — Budget vote -> appels de fonds (ventilation par cle -> creances 45x + produit 701/702/105) | partiel | orange |
| 10 — Onboarding / reprise de mandat : set_opening_balance -> soldes d'ouverture -> assiette aval | partiel | orange |
