# CHANTIERS — Tour de contrôle CoProFlex v2

> **But** : ne plus jamais perdre un chantier commencé puis oublié quand on change d'angle.
> **À consulter en début de session** (`/token-saver start`) et **à mettre à jour dès qu'un chantier change d'état**.
> Règle : un chantier qui passe en `📐 design validé` ou `⏸️ parké` SANS prochaine action datée est un signal d'alerte — soit on le relance, soit on l'archive explicitement.

## Légende statuts
- 💡 **Idée** — évoquée, pas encore cadrée.
- 📐 **Design validé** — brainstorming/spec faits, **pas encore implémenté** (zone de risque d'oubli n°1).
- 🚧 **En cours** — implémentation/cadrage actif.
- ⏸️ **Parké** — commencé puis arrêté (à relancer ou archiver — **ne pas laisser pourrir**).
- ✅ **Livré** — fini + vérifié (DoD).
- 🗄️ **Archivé** — abandonné volontairement (garder la trace pour ne pas re-proposer).

---

## Chantiers actifs / en attente

| Chantier | Statut | Dernière activité | Prochaine action | Réf |
|---|---|---|---|---|
| **🛡️ Sécurité native baseline** (ex-« sécurité live hors-bande ») | 📐 Exigence baseline 0001 (PAS un patch live) | 2026-06-26 : **décision Lyes — on NE patche PAS le live** (`qqfq` quasi vide, meurt gelé). Recon read-only = anti-pattern confirmé À NE PAS reproduire : `user_is_platform_admin()` lit `memberships.role='platform_admin'` **sans cloisonnement cabinet** → bypass écriture global, court-circuité en tête de `user_is_copro_manager`/`user_has_copro_access` | Intégrer dans la **baseline 0001** de la base neuve : (1) table `platform_admins` dédiée (C16-4) au lieu d'un rôle dans `memberships` ; (2) RLS ON+FORCE sur TOUTES les tables + 0 grant anon + gate `relforcerowsecurity` ; (3) zéro compte démo en clair. **Cohérent SESSION/BILAN « sécu native, pas de patch jetable ».** | `BILAN_CADRAGE_2026-06-26.md` §2c/§5 · `REFONTE_DECISIONS` C16-4/C17-8 · mémoire `db_neuve_v2_squash_clean` |
| **Purge contradictions + registre supersedes (pré-build)** | 🚧 Découverte LIVRÉE, tri à faire | 2026-06-26 : **balayage exhaustif multi-agents (2 vagues, ~310 agents, lecture seule) → 107 pièges à copie** (🔴18 · 🟠54 · 🟡28 · ⚪7), vérifiés en adversarial. `SUPERSEDES.md` créé + `business-rules.md:66` corrigé + 2 plans archivés (déjà fait) | **TRIER les 107 findings** (vrai/faux positif/différé) avec Lyes → corrections en PR séparées par THÈME (RLS-off mémoires & DECISIONS.md = urgent car auto-chargé ; provider Resend→Brevo ; statuts/enum ; finance/Edge ; machine à états AG `set_ag_status` ; plan comptable inventé ; live gelé/migration). Blueprints `db-cible/*` = corrigés **JIT au palier** (SUPERSEDES §C) | `AUDIT_CONTRADICTIONS_2026-06-26.md` · workflows `wnhc4412u`+`wr2p7og1a` · `BILAN_CADRAGE` §2b/§3 |
| **Bilan de cadrage + backlog de build séquencé** | ✅ Livré (2026-06-26) | Audit multi-agents 12 agents : couverture domaine par domaine + ~50 objets de socle/domaine avec `depends_on`/palier 0→12 + paliers de build avec DoD | Servir de référence de chantier pour le build (ne pas reperdre) | `BILAN_CADRAGE_2026-06-26.md` · sortie workflow `wqvq17a2r` |
| **Grilling cadrage PARTIE C** (blocs macro CLOS, reste trous fins) | 🚧 Blocs clos / trous fins à faire | **2026-06-26 : CADRAGE DES BLOCS CLOS** — 5 ARB + EXP + socle C.17 8/8 + C16/C12/C13/C14/C15/C11 tous tranchés (41 décisions ce jour, consignées) | **Reste = les 147 trous fins de la Partie C (18 domaines) + 8 angles morts** auto-déclarés par le registre (numérotation pièces, pagination vues GL, tantièmes en cours d'exo, T9 journal probant…) — passe de grilling domaine par domaine, à planifier | `BILAN_CADRAGE_2026-06-26.md` §2a · `REFONTE_DECISIONS_2026-06-23.md` l.288-289/304 |
| **Audit cohérence + complétude cadrage** | ✅ Livré (nuit 2026-06-25) | Workflow recon 7 lecteurs → 26 anomalies (6 bloquantes), registre supersedes, ordre 13 paliers ; + drift HORS-FINANCE audité (comble ANOM-25) | Résoudre les anomalies pendant le grilling (cf. plan Phase 3) | `AUDIT_COHERENCE_CADRAGE_2026-06-24.md` · `AUDIT_DRIFT_HORS_FINANCE_2026-06-25.md` · plan `docs/superpowers/plans/2026-06-24-cadrage-base-saine.md` |
| **GLOSS- Glossaires v2 — phase 1 (socle)** | 🚧 Commité, à pousser | 2026-06-24 : `CONTEXT.md` + `glossaire-technique.md` + câblage commités (`9d93046`) | **push** (`gh auth switch lyestriki-29`) | `docs/superpowers/specs/2026-06-24-glossaires-v2-design.md` |
| **GLOSS- phase 2 — enrichissement a posteriori** | ✅ Livré, à pousser | 2026-06-24 : workflow 41 agents, 230 termes vérifiés → +42 métier / +41 technique, condensés (commit `eeeb360`) ; auto-import retiré | **push** | spec §5 ; output `w7oi1i5l8` |
| **Anti-fragmentation du code** (modules profonds, deletion test) | 💡 Idée | 2026-06-24 : noté hors-scope du glossaire | brainstorming → spec dédiée | spec glossaire §8.1 |
| **Capitaliser les 5 principes de conception** dans `methodo-coproflex` | 💡 Idée | 2026-06-24 : noté hors-scope du glossaire | brainstorming → spec dédiée | spec glossaire §8.2 |
| **Golden exhaustif « Domaine des Tilleuls »** (tests) | 📐 Design validé | grilling 2026-06-22 : plan + valeurs attendues | EN ATTENTE GO USER pour dérouler | `.planning/tests/PLAN_GOLDEN_EXHAUSTIF.md` |
| **Portail copropriétaire** (8 pages, server-first) | 📐 Design validé | 2026-06-10 : design validé, zéro implémentation | `/writing-plans` session dédiée | `docs/superpowers/specs/2026-06-10-portail-coproprietaire-design.md` |
| **Refonte UI/UX manager-first** + espaces copro/CS | 💡 Idée (direction) | direction posée | brainstorming → frontend-design (stack v2 Tailwind + shadcn, DA « Indy ») | mémoire `ui_ux_refonte_direction` |
| **S0 — Faille RLS anon du live** | ✅ Vérifié 2026-06-25 | Vérif empirique MCP (lecture seule) : **87 tables, 0 sans RLS, 0 anon-exploitable** ; les 2 tables nominatives ont RLS ON + 0 grant anon ; advisor = 0 finding RLS. **Fausse alerte** (déjà bouché par 0085/0086, 21 juin). Mémoire `coproflex-cloud-live` corrigée | Aucune — **clos**. Reliquat de durcissement → ligne dédiée ci-dessous | `AUDIT_COHERENCE_CADRAGE_2026-06-24.md` ANOM-01 (périmé) · mémoire `coproflex-cloud-live` |
| **Durcissement sécurité** (FORCE généralisé + advisors) | 💡 Idée (non urgent) | 2026-06-25 : issu de la vérif S0 — FORCE sur **5/87** seulement (cœur finance), 82 ON-sans-FORCE ; advisor = 169 WARN + 1 ERROR (vue `tiers_directory` DEFINER **voulue**) | Cadrer plus tard **avec revue cascade** (FORCE touche aux 154 RPC definer) : FORCE généralisé + tri advisors (13 `search_path`, `pg_net` hors `public`, activer protection mots de passe compromis) | mémoire `coproflex-cloud-live` MAJ 2026-06-25 |
| **Migration TanStack Start** (`v2-tanstack/`) | 🚧 En cours | from-scratch en cours, moteur=parité | dérouler features par catégorie + ordre de dépendance | `REFONTE_CARTOGRAPHIE/CHAINES/VERIFICATIONS_2026-06-22.md` |
| **Plan comptable copro COMPLET** (référentiel arrêté 14/3/2005) | 📐 Catalogue prêt, intégration différée | 2026-06-29 : workflow `w3ehk01ib` → **84 comptes** sourcés Légifrance, mappés à notre schéma, vérifiés (codes golden = exacts). Auto-challenge → on N'intègre PAS le plan complet maintenant (friction « maigre » + ~60 `charge_nature` non prouvables car non exercés). POINT 3 réglé autrement (option A : `account_id` nullable) | **Intégrer au fil de l'eau** (chaque compte entre quand une feature l'exerce → prouvé par test) OU en bloc avec validation experte Lyes de la colonne courant/travaux. 718/677 déjà notés backlog ci-dessous | `.planning/PLAN_COMPTABLE_COPRO_PROPOSE.md` · `REFONTE_DECISIONS` § Plan comptable |
| **Catalogue d'actes facturables auto-comptabilisés** | 💡 Idée (retenue P1) | 2026-06-26 : idée USER pendant ARB-3 ; paramétrage par syndic (LRAR, mise en demeure, état daté, frais mutation…) = libellé + tarif + **qui paie** + écriture déclenchée. S'appuie sur mandat complet (P1) | Brainstorming/spec en P1 : régime légal par acte (art.10-1 défaillant ; état daté plafonné 380€), acte=événement→écriture gardée idempotente→GL | `REFONTE_DECISIONS_2026-06-23.md` (ARB-3) · dossiers C16-1/C16-2 |
| **FRONT — Bascule Tailwind+shadcn + DA « Indy » + organisation** | 🚧 En cours | 2026-06-28 : règles alignées ✅ ; **DA « Indy » CHOISIE** par **concours codé** (5 DA × 4 écrans banc d'essai = Dashboard/Finance/AG/Copro, maquettes Tailwind statiques + viseur local port 5174) ; **mode sombre token-first** + **white-label cabinet borné (20 teintes curées)** actés + **preuve** clair/sombre sur modal+calendrier ; **recherche 2026** organisation/harnais (workflow `w4tt9fu4o`) → **`coproflex-v2/docs/ORGANISATION.md` écrit** + correction lint v3→v4. FRONT-7/8/9 consignés | **Étapes 1-3 LIVRÉES** (Tailwind v4+shadcn+tokens Indy clair/sombre · `DESIGN_TOKENS.md`+20 teintes · login+démo · lint 0/tsc 0/build OK). **Gates ACTIFS** : anti-couleur-en-dur, `strict-type-checked` (no-any), react-hooks, `reportUnusedDisableDirectives`. ✅ **Gate frontières `boundaries` RÉPARÉ + PROUVÉ** (2026-06-28, revue de code multi-agents) : resolver `eslint-import-resolver-typescript` + `settings['import/resolver'].typescript` ; règles migrées v6 `element-types`+`entry-point` → **`boundaries/dependencies`** (0 dépréciation). Subtilité découverte empiriquement : `internalPath` = **chemin COMPLET** du fichier → motif `!src/features/*/index.@(ts|tsx)` (seul l'index RACINE = entrée publique ; **sous-barrel `…/components/index.ts` bloqué**, trou trouvé par la revue). Prouvé par sondes éphémères (shared→route rejeté · sous-barrel rejeté · index racine OK). ✅ **mdp démo SORTI du code** → `VITE_DEMO_*` dans `.env.local` (gitignoré), bloc démo conditionnel (disparaît si var vide). ✅ **DA Indy on-accent figé** `#1b1714` clair=sombre (FRONT-8, doc alignée). **NEXT = étape 4 : pilote finance** : (b) primitive shadcn `Input` ; (c) `loginFn` validé **Zod**. **Follow-ups différés (revue 2026-06-28)** : ① épingler `typescript <6.1` (peer typescript-eslint) ; ② tokens `--sidebar-*` (conventionnels shadcn) à ajouter au 1er composant installé ; ③ brancher `better-tailwindcss` (ordre des classes, déjà en dép.) ; ④ **durcir login démo** = server fn lisant `process.env` **non-`VITE_`** (sinon mdp inliné au bundle si build avec var présente — risque assumé court terme, compte dev-only) ; ⑤ risque opé = clé **service-role du LIVE** dans `.env.local` → envisager clé/projet dev distinct. CI Actions, hook Stop | `REFONTE_DECISIONS` § FRONT (0→9) · concours `coproflex-v2/.planning/da-concours/` · workflow `w4tt9fu4o` |
| **FONDATION DB v2 — base neuve finance-first → pilote « Appels de fonds »** | 🚧 En cours (Palier 0 lancé) | 2026-06-28 : cadrage méthodo + revue des séances de grilling (2 sous-agents). Décisions verrouillées : **base neuve d'abord** · **finance-first ciblé** · projet = réutiliser **`oiozjlvlsfzvkmvltiue`** (restauré ACTIVE_HEALTHY, EU/Francfort) · specimen = écran **Appels de fonds** (FRONT-3) · golden **Tilleuls**. **Pré-vol** : SUPERSEDES complété (AUTORISATION.md faille super-admin + glossaire L52) · BILAN_CADRAGE corrigé (CSS Modules→Tailwind). **0.1 FAIT** : oio réactivé + `.env.local` repointé (clé service-role live RETIRÉE). Accès lecture schéma `qqfq` débloqué (88 tables, RPC finance mûr reconnu). | **FAIT** : garde-fou anti-copie + registre tranché + **brouillons `0001` (enums+socle) / `0002` (tables finance) + `PLAN_MIGRATION_0001-0004`** poussés (coproflex-v2 `c0bbe5b`, **NON appliqués**). **FAIT (suite 2026-06-29)** : `0001`/`0002`/`0003` écrits + revus (multi-agents) + corrigés, **NON appliqués** ; **extraction qqfq R1** (19 corps → `coproflex-v2/.planning/rpc-qqfq/`) ; **grilling R1 : R1-01 tranché = MAIGRE chemin courant (12 fn)** = `0004` (GL core/périodes helpers/plan/répartition) + `0005` (appels/paiements) ; clôture/à-nouveau→`0006` (dép. cut-off), corrections+reprise différées. Grilling + phase B FAITS : `0004` (6 fn) / `0005` (5 fn) écrites, audit cascade adversarial + **4 corrections triées appliquées** (doublons retirés, garde p_nature_filter, fail-loud auto_post, Hugo→imputation ciblée Gate 1). **APPLIQUÉ RÉEL (2026-06-29)** : `0001→0005` committés sur `oio` via runner `--apply` (**option 2** : le runner inscrit lui-même `schema_migrations`, format Supabase). État base = **31 fn / 26 tables / 33 triggers / 26 RLS ON (26/26) / carnet 5 lignes** `0001…0005`. **Advisors sécurité triés avec Lyes** : `rls_disabled=0` (seul bloquant DoD) ✅ ; **2 vrais défauts corrigés à la source + prouvés** = `search_path` figé sur `set_updated_at` (0001) · `revoke execute … from public` (0003 — le `from anon, authenticated` ne fermait rien, PUBLIC garde le grant par défaut) ; **84 alertes restantes (0 erreur) DIFFÉRÉES** = périmètre de la future **migration RLS+droits** (policies + `revoke execute from public` généralisé + FORCE), équivalent neuf de l'ancien `0034` (cf. chantier « Sécurité native baseline » ligne 21). **FAIT : audit `minor_only` → reset+ré-apply propre (carnet fidèle, Gate 1 re-vert) · runner durci (garde mono-shot) · glossaire R1 · 5 commits NON poussés.** NEXT = décision push/PR (branches très en avance de main) + migration RLS+droits (84 advisors différés). Détail vivant = `PROGRESS_v2-migrations.md`. | `coproflex-v2/.planning/PLAN_PILOTE_FINANCE.md` · `PLAN_GOLDEN_EXHAUSTIF.md` · `BILAN_CADRAGE` paliers |
| **🌐 VIT — Vitrine « coming soon » coproflex.fr** (chauffe domaine + liste d'attente + blog SEO) | 📐 Cadré, plan écrit (2026-06-29) | **2026-06-29 : grilling complet** — 14 décisions VIT-A→N. Réutilise la tranche marketing v1 (Next.js) dans un **repo dédié `coproflex-vitrine`** ; positionnement honnête « accès anticipé » (zéro faux témoignage/tarif) ; leads → projet Supabase dédié `bqla` + Brevo (email bienvenue + cadeau = guide PDF digitalisation/IA) ; Vercel (connecteur MCP OK, équipe Propulseo) ; DNS OVH méthode enregistrements (MX intacts). `env.vitrine.example` créé. | **Exécuter P0→P6** (repo+copie → élagage/honnêteté → capture leads → Brevo → SEO/légal → déploiement → domaine). **À fournir par Lyes** : accès `bqla`, compte Brevo, infos légales Propul'SEO. | `.planning/VITRINE_LP_PLAN_2026-06-29.md` · `env.vitrine.example` |

---

## Actions techniques repérées au fil du grilling (backlog build)
> Petites actions concrètes découvertes pendant le cadrage, à ne pas perdre (≠ chantiers).

- **Chart** : ajouter le compte `718` « Produits exceptionnels » (absent du seed) + vérifier `677` (dans la règle `charge_nature` 0059 mais absent du seed). *(C.7-P5, 2026-06-24)*
- **Annexe 6** : implémenter la liste individualisée des copropriétaires au format légal 7 colonnes. *(C.7-P1, 2026-06-24)*
- **RPC à créer** : `record_legal_proceeding` (table `legal_proceedings` sans fonction d'écriture). *(C.5-P3)*
- **GED unifiée** : inventorier TOUS les générateurs de documents + toute table parallèle (au-delà de `ag_documents`) → router via un helper unique `register_generated_document` ; `documents` = référentiel unique ; `ag_documents.document_id` → NOT NULL. *(C.10-P2, 2026-06-24)*
- **Mémoire** : `MEMORY.md` dépasse la limite (25,1 KB > 24,4 KB) → compacter (1 ligne/entrée, fusionner/élaguer les entrées périmées). *(2026-06-24)*

### Drifts repérés par l'enrichissement glossaire (ultracode, 2026-06-24)
> Écarts « conçu/décidé mais pas codé » remontés par la vérification adversariale du code. Détail complet : `…/tasks/w7oi1i5l8.output`. Plusieurs sont déjà connus (mémoires sécurité/drift) — ne pas re-traiter en double.

**Finance / écriture (RPC à créer ou réunifier) :**
- **Compte 102 « Provisions pour travaux décidés » (art. 14-2 I) = P1** : dormant en V1 (travaux via 702 + gel du résultat travaux compte 12). À câbler en P1 **avec le suivi par opération** (`operation_id`, palier E4) pour une présentation bilancielle orthodoxe (annexe 1/5) + réserve par chantier. **Pré-requis de mise en route** : test golden de travaux **à cheval sur 2 exercices** prouvant que le report via le 12 ne perd rien. *(EXP-6, 2026-06-26 ; pas un bloqueur de justesse V1.)*
- `post_exceptional_call_for_funds` (appel travaux dédié) — spécifiée design 0037, jamais codée ; aujourd'hui via le poseur générique.
- Appel **avance art.35** (D450-3/C1031) — aucune route d'émission (pas de `budget_type='advance'`).
- **Réalisé budgétaire = 2 sources** (budget_expenses vs classe 6 du GL) → réunifier vers le GL.
- **Double-posting** : `validate_budget_expense` ET `validate_supplier_invoice` postent D6xx/C401 → requalifier `budget_expenses` en **engagement** (couche « engagé » non matérialisée).
- Reclassement **459** (créance douteuse) + dotation **491/492** — comptes présents, aucune RPC.
- `718` produits exceptionnels — **à ajouter au plan** (déjà listé ci-dessus).
- `record_legal_proceeding`, `record/settle_mutation_opposition`, `create/resend/revoke_copro_invitation` — tables présentes, RPC à créer.
- Frais recouvrement **450-6** + plan d'apurement + intérêts art.36 — à câbler.

**AG / cadrage :**
- **RUPTURE `action_type=NULL`** : `createStandardResolutions` (front) ne pose pas `action_type` → résolutions « muettes », 0 décision matérialisée. *(à vérifier en priorité, casse l'auto-population)*
- `create_budget_from_ag_resolution` inexistante (budget créé en amont → risque fantôme sans `source_ag_id`).
- `activate_ag_decisions` tout-ou-rien (pas de savepoint/reprise partielle).
- Vote par **clé spéciale** + `is_amended` + `repartition_key_id` sur `ag_resolutions` : colonnes absentes (vote toujours sur tantièmes généraux).
- Émission appels **en bloc à l'AG** (option B « au fil des trimestres » non implémentée) ; reconduction provisoire art.14-1 al.3 non tracée.

**Annexes / maintenance :**
- Cascade clé **Annexe 3** (réalisé par clé) figée à 0 ; pas de `fn_annexe_6` (porté par `fn_annexe_1`).
- **OS récurrent + alerte Chatel** : colonnes dormantes, aucun cron/générateur.
- 3ᵉ voie bancaire (« comptabiliser un mouvement ») non implémentée.

**Sécurité / dette (déjà en mémoire — pour mémoire) :** RLS ON sans FORCE (hors 6 tables), faille anon `0085`, escalade `platform_admin`, IDOR GoCardless, ~61 fonctions SQL désync (repo non reproductible), HTML→PDF non fait (jsPDF), dette front (2 arbres `features`, ~13 formateurs euro, pages EN/FR, `PaymentModal` homonyme).

---

## Journal des changements d'état
- **2026-06-29 (audit adversarial + reset propre + glossaire + commits R1)** : audit multi-agents `wa4miyfis` (17 agents) sur l'apply + les 2 correctifs + l'outillage → verdict **`minor_only`, rien de cassé**. **Tri Lyes (AskUserQuestion)** : **Q1 = reset + ré-apply propre** d'`oio` → le forward-fix de l'entrée précédente est **SUPERSEDÉ** (carnet désormais **fidèle** aux fichiers : 0001=15973 / 0003=19818 ; **Gate 1 re-vert** ; apply à neuf PROUVÉ ; 0 donnée) ; **Q2 = garde-fou mono-shot** du runner (refuse si une version est déjà au carnet, + cause d'échec affichée), `scenario.mjs --applied` ajouté. **Glossaire R1** (CONTEXT « Imputation proposer/valider/graver » recadré + 2 entrées technique). **5 commits créés, NON poussés** : coproflex-v2 `21760ff`→`1a1124d` (imputation 0002/0005 · sécu advisor 0001/0003 · outillage runner/scenario · Gate 1) + Co-Pro-Flex `ac3875d` (glossaire). **NEXT = décision push/PR** (branches très en avance de main : v2 ~22, docs 126) **puis migration RLS+droits** (84 advisors différés).
- **2026-06-29 (APPLY RÉEL 0001→0005 sur oio + tri advisors)** : runner adapté **option 2** (inscrit lui-même `supabase_migrations.schema_migrations` au format Supabase, DANS la transaction d'apply, `create schema/table if not exists` en garde-fou). **DRY vert** (BEGIN/ROLLBACK) puis **`--apply` COMMIT** : `oio` = **26 tables / 31 fn / 33 triggers / 26 RLS ON / carnet 5 lignes** (`0001:enums_and_socle` … `0005:calls_and_payments`). **Advisors sécurité triés avec Lyes (AskUserQuestion, 3 décisions)** : `rls_disabled=0` (seul bloquant DoD) ✅. **2 vrais défauts corrigés à la source + PROUVÉS en base** : (1) `set_updated_at` sans `search_path` → figé `'public'` (0001) ; (2) `revoke execute … from anon, authenticated` **inopérant** (le grant PUBLIC par défaut restait) → `from public` (0003) ⇒ `is_ledger_regen_exempt` désormais 0 exécution anon/auth/public. **84 alertes restantes (0 erreur ; 58 WARN definer-executable + 26 INFO rls-no-policy) DIFFÉRÉES** = future **migration RLS+droits** (policies + grants + FORCE). Forward-fix appliqué sans reset/DROP (`create or replace` + `revoke`), fichiers `0001`/`0003` mis à jour pour rester cohérents. **Rien commité côté git** (apply = base seulement). **NEXT = glossaire R1 + commits séparés coproflex-v2 + PR.**
- **2026-06-29 (compile-test 0001→0005 VERT + outillage apply direct)** : le MCP Supabase a décroché en cours (connecteur claude.ai, réglé par « Developer: Reload Window ») ; **constat : le MCP ne peut PAS pousser ~100 Ko inline** → bascule sur **connexion directe Postgres** = runner `coproflex-v2/scripts/db/runner.mjs` (lit `OIO_DB_URL` + CA Supabase `.planning/rpc-qqfq/supabase-ca.crt`, **TLS vérifié, jamais désactivé** ; mdp à caractère spécial → parser découpe au dernier `@`). **COMPILE-TEST BEGIN/ROLLBACK VERT** : `0001→0005` s'appliquent proprement (functions=30 tables=26 triggers=33 policies=0), `oio` reste **0/0**. **Rien commité, rien appliqué.** Comptes clarifiés : claude.ai=`lyestriki@yahoo.fr` / Supabase oio+qqfq=`lyes.triki@propulseo-site.com` / ancien `iyfes…`=`contact@coproflex.fr` (NE PAS supprimer sans backup, GED/Edge). **NEXT (session NEUVE) = `runner.mjs --apply` après feu vert Lyes [décider tracking schema_migrations] → scénario Gate 1 (77 560, Hugo ciblé).**
- **2026-06-29 (phase B — authoring 0004/0005 + audit cascade)** : workflow ultracode `wdazr2npa` (51 agents, ~4 M tokens, 11 min) → `0004_gl_core_and_periods.sql` (6 fn) + `0005_calls_and_payments.sql` (5 fn) écrites (**NON appliquées**). **3 bugs objectifs corrigés à l'écriture** (cast bigint→boolean qui plantait chaque appel · garde d'accès manquante sur `allocate_payment` SECURITY DEFINER · ON CONFLICT à prédicat partiel incompatible avec la contrainte unique totale 0002). **Audit cascade adversarial 8 dim + juges** → 15 pistes, 13 réfutées, **2 confirmées + 1 doublon**. **Tri Lyes → 4 corrections appliquées** : retrait doublons `get_period_for_date`/`get_open_period` de 0004 (vivent en 0003) · garde `p_nature_filter` (allocate_payment) · fail-loud auto_post déséquilibré (create_ledger_transaction) · **Hugo = imputation ciblée à CÂBLER dans Gate 1** (code RPC inchangé, copie fidèle qqfq). **NEXT = phase C : compile-test BEGIN/ROLLBACK 0001→0005 sur oio → Gate 1 (77 560) → apply.** Détail = `PROGRESS_v2-migrations.md` § Phase B.
- **2026-06-29 (suite — extraction qqfq + grilling R1)** : **extraction** des 19 corps RPC R1 de qqfq → `coproflex-v2/.planning/rpc-qqfq/qqfq-rpc-bodies.sql` (bundle base64 → fichier, hors contexte). **Grilling R1 (skill methodo)** : **R1-01 tranché = R1 MAIGRE, chemin courant dans l'année (12 fn)** ; clôture/à-nouveau → `0006` (`open_next_period` dépend de `reverse_period_cutoff`/cut-off — fait dur) ; corrections (reverse/cancel) + reprise (`set_opening_balance`) → tranches dédiées. **Plomberie confirmée par lecture des corps** : `reversal_of_tx_id`=colonne (pas metadata), `call_for_funds_lines` sans `amount_paid`/`status`, `post_budget_call_for_funds` générique 3 natures (701/702/105), `repartition_key_is_complete`=garde dur (adapter `coverage_mode`→`repartition_category`). **OUVERT** : Q3 périmètre Gate 1 (reco 77 560), Q4 structure workflow. Détail vivant = `PROGRESS_v2-migrations.md`. **Rien commité ; `rpc-qqfq/` non suivi.**
- **2026-06-29 (préparer le terrain de `0001` — garde-fou anti-copie + registre tranché)** : leçon de la session précédente = **on a failli recopier les vieilles migrations** (oubli du from-scratch). Garde-fou posé : `PLAN_PILOTE_FINANCE` retourné en **additif** (fini « squash du live »), **bannières voie 1/voie 2** sur `.planning/qqfq-extract/`. **Registre d'inclusion** généré (workflow ultracode `wwidv6rkf`, 19 agents : classer → avocat du diable → clôture FK/vues/enums/comptes → synthèse) = **403 retenus / 229 exclus**, écrit dans `coproflex-v2/.planning/REGISTRE_INCLUSION_0001.md`. **3 blockers + flags factuels résolus par lecture des corps de RPC qqfq** (cut-off = `closing` et non `od` ; à-nouveau = `opening_balance` ; `resolve_lot_tiers_account` prend du **texte** → drop `loan`/`doubtful` sans casse ; statut d'appel **dérivé en vue**, pas stocké). **4 décisions métier tranchées** (avoir fournisseur INCLUS · budget complémentaire = **version** · état daté **différé** prio n°1 ventes · `payment_method` = {transfer,check,direct_debit,other}). Consigné dans `REFONTE_DECISIONS` (bloc « BL — Résolution registre 0001 »). **Puis brouillons `0001` (20 enums + 13 tables socle) / `0002` (14 tables finance bigint) + `PLAN_MIGRATION_0001-0004` écrits et poussés (coproflex-v2 `c0bbe5b`, NON appliqués) ; 3 conflits flagués pour la revue cascade. NEXT = `0003` (RPC, fetch qqfq + adaptation bigint) + `0004` (RLS/vues) → revue cascade ultracode → apply `oio`.**
- **2026-06-28 (nuit — build socle front, étapes 1-3 du plan d'exécution)** : sur `coproflex-v2` (repo v2). **Étape 1** : Tailwind v4 + `@tailwindcss/vite` + shadcn (cn/components.json) installés ; `globals.css` = **tokens Indy clair+sombre** (convention `.dark`, défaut clair) ; **3 `.module.css` supprimés** (fin de coexistence CSS Modules). **Étape 2** : `DESIGN_TOKENS.md` (3 étages + mode sombre + white-label) + **catalogue 20 teintes de marque** + planche `brand-palette.html` ; marque cabinet = couleur(20) + texte blanc/noir **au choix** (sans jugement affiché — USER). **Étape 3** : **harnais ESLint** (flat, ESLint 10) — frontières `boundaries`, **gate anti-couleur-en-dur** (`no-restricted-syntax`), `strict-type-checked` (no-any), react-hooks, exports nommés, `reportUnusedDisableDirectives`. Lancé → 11 erreurs sur NOS fichiers de démarrage (pré-linter, pas hérités) **corrigées à la source** (env `requireEnv` au lieu de `!`, `FormEvent` déprécié retiré, `??` inutile, accolades, `throw redirect()` autorisé précisément via type `@tanstack/router-core`). **lint 0 err · tsc 0 · build OK · dev 200.** Login + **boutons démo** (Gestionnaire 1-clic / Copropriétaire grisé). **Règle de méthode ajoutée (USER)** : « cause racine, jamais de contournement » (tout bug) → `rules-v2` 2-ter + skill methodo + `REGLES_CODE` G11 + mémoire + **mécanisée** (`ban-ts-comment`, `reportUnusedDisableDirectives`). 2 serveurs locaux tournent (dev 3001, concours 5174). **Rien commité.** **Revue de code (2 relecteurs indépendants)** : socle propre/aligné A→G ; 1 vrai défaut = **gate frontières `boundaries` INERTE** (pas de resolver → fix prérequis étape 4) ; 1 sécu = **mdp démo en clair à NE PAS committer** ; 2 différés (primitive `Input`, Zod sur `loginFn`). **Aucune correction appliquée** — instruction USER « si y'a des risques on change rien » + rien d'urgent (app verte, 0 feature). Détail : la surestimation « frontières actives » corrigée (les autres gates marchent).
- **2026-06-28 (soir — design des pages + organisation v2)** : **Phase 1 du PLAN_MAITRE bien avancée.** Méthode « design des pages » cadrée (liste re-dérivée des parcours, gestionnaire d'abord, ordre des 12 sections — **FRONT-7**). **DA verrouillée = « Indy »** via **concours codé** : 5 DA distinctes × 4 écrans banc d'essai (Dashboard/Finance/AG/Copro), maquettes Tailwind statiques + viseur local + galerie dans `coproflex-v2/.planning/da-concours/`. **Mode sombre** acté token-first (paire clair/sombre garantie par tokens-à-la-racine + shadcn possédés + gate anti-couleur ; **preuve** sur modal+calendrier) + **white-label cabinet borné** = 20 teintes curées (**FRONT-8**). **Recherche web 2026** (workflow `w4tt9fu4o`, 5 angles + synthèse) sur l'organisation d'un codebase TanStack écrit par IA → verdict « API publique par feature = **frontière OUTILLÉE** » + harnais ESLint (boundaries v5, better-tailwindcss v4, hook Stop, CI) + garde-fous vibe-coding → **`ORGANISATION.md` écrit**, `REGLES_CODE` corrigé (plugin Tailwind **v3→v4**), **FRONT-9** consigné. **Piège trouvé** : bascule Tailwind décidée mais **pas exécutée** (repo encore CSS Modules). NEXT = valider le **plan d'exécution séquencé** (install Tailwind/shadcn + tokens + harnais + pilote). 6 mémoires-glossaire + index à jour.
- **2026-06-28 (FRONT — bascule stack style + refonte DA)** : décision Lyes — **bascule CSS Modules → Tailwind v4 + shadcn/ui** (critère = optimiser le codeur IA, même logique que TanStack) + **refonte DA complète « Indy »** (clair/chaleureux/corail). Workflow `wfmayn3fw` (7 agents) = inventaire des ~40 artefacts « jamais Tailwind » + setup TanStack+Tailwind+shadcn + bonnes pratiques agentiques. **Règles ACTIVES alignées** (hook `rules-v2`, `CLAUDE.md` [+ Next.js→TanStack, pas de coexistence], skill `methodo`, README v2, `REGLES_CODE` §C, anti-piège `PLAN_SOCLE`, 4 mémoires + index `MEMORY.md`, ligne stack `REFONTE_DECISIONS`). Décisions **FRONT-0→6** consignées (§ FRONT). Prototype DA en localhost. NEXT = verrouiller DA + docs neufs + composants + Claude Design.
- **2026-06-27 (Phase 3 socle connaissance v2 + grilling règles de code + double-check)** : **registre de disposition des 107 findings** construit (107/107 étiquetés ; 59 neutralisés par non-migration ; 6 canoniques à corriger + 18 bandeaux db-cible). **Double-check expert adversarial** (Légifrance, workflows `w83wut1p3`/`wu4ara3lg`) des 4 contestés : 008 confirmé (mdp déjà bcrypt en base, vrai défaut = secret versionné + escalade super-admin), 023 nuancé (réf = **art.6-2 décret 67-223** pas loi ; EXP-5 vendeur créditeur ventilé par nature : 103 remboursable / trop-perçu à l'approbation / 105 jamais), 028 confirmé (+ **FORCE manquant** 8 tables comm + garde 0086 à durcir sur `relforcerowsecurity`), 030 nuancé (**double plancher** MAX(5% budget ; 2,5% PPT) + exemptions ALUR changées post-2023). **Grilling règles de code A→G ACTÉ** (7 dimensions) → `coproflex-v2/docs/REGLES_CODE.md`. Recherche web skills (`wmkakw048`) = catalogue 27 + socle à installer (Supabase agent-skills, Playwright, typescript-eslint, ts-reset, stylelint-css-modules, tanstack…). Branche **`socle-connaissance-v2` @ `0bc1b86`** (nouveau repo) : 3 docs-socle committés (`REGLES_CODE`, `REGISTRE_DISPOSITION_107`, `PLAN_SOCLE_CONNAISSANCE_v2`). **Leçon workflow** : 2 workflows simultanés = throttle serveur Anthropic → 1 à la fois + `resumeFromRunId`. NEXT = atterrir 6 canoniques nettoyés + 18 bandeaux (workflow ultracode), dossier arbitrages ouverts, re-grep=0, puis Phase 4/5.
- **2026-06-26 (repo v2 dédié — DÉCISION + migration infra)** : décision Lyes — **la v2 part dans un repo Git dédié** `propulsFlex/Coproflex` (local `Desktop/Code/coproflex-v2`), code + docs canoniques. **Supersède** la décision « même repo / bascule au go-live » ([[migration_tanstack_start]]). Raison : grep 100 % propre + moment le moins cher (v2 à peine commencée). **Phases 1-2 FAITES** : `v2-tanstack/` promu en repo indépendant (commit `8b46139`, 92 fichiers, `.env.local` gitignoré vérifié), `typecheck`+`vite build` verts, poussé sur GitHub (HTTPS, compte `propulsFlex`). **Reste** : Phase 3 = base `docs/` canonique (= atterrissage de la purge des 107 findings) ; Phase 4 = `CLAUDE.md` v2 + hook + re-clé mémoire + correction des mémoires empoisonnées ; Phase 5 = pointeur sur l'ancien repo (v1 gelé). Backend Supabase cloud `qqfqrcolzmcbsvfaumiq` **inchangé** (partagé). `v2-tanstack/` reste dans l'ancien repo (doublon gelé jusqu'au pointeur Phase 5).
- **2026-06-26 (audit cohérence exhaustif)** : balayage anti-pièges « tout sauf déchets » en **2 vagues** (vague 1 = 9 grappes ; vague 2 = 23 grappes relancées après un throttling serveur transitoire ; ~310 agents, ~18 M tokens, lecture seule). Résultat = **`AUDIT_CONTRADICTIONS_2026-06-26.md`, 107 findings** (🔴18 🟠54 🟡28 ⚪7), vérifiés adversarialement (panel de 3 jurés sur les 🔴). **Leçon méthode** : un workflow à fort fan-out (>30 grappes) sature l'API → découper en sous-vagues séquentielles. **0 correction appliquée** (lecture seule) ; tri/ratification à suivre. NB : le registre `SUPERSEDES` (10 périmés) était très incomplet — l'audit a multiplié la couverture ×10.
- **2026-06-26 (reprise post-déménagement)** : projet déplacé vers `Desktop\Code\Co-Pro-Flex`, **mémoire migrée** vers la nouvelle clé projet (104 fiches, vérifiées). **Contradiction tranchée** : le chantier « Sécurité live hors-bande » est **annulé en tant que patch live** — le live `qqfq` **meurt gelé** (quasi vide, voué au remplacement), la sécurité devient **native dans la baseline 0001 de la base neuve** (cohérent SESSION/BILAN « sécu native, pas de patch jetable »). La recon read-only a documenté l'anti-pattern `platform_admin` à ne pas reproduire. Restent les autres « pièges à copie » → chantier **purge des contradictions**.
- **2026-06-26 (soir)** : **CADRAGE DES BLOCS CLOS** (41 décisions ce jour : 5 ARB + EXP + socle C.17 + C16/C12/C13/C14/C15/C11). Puis **audit de couverture multi-agents** (12 agents, vérifs en base) → `BILAN_CADRAGE_2026-06-26.md`. Verdict : cartographie + décisions macro solides, mais **0 objet v2 construit** + **2 failles sécurité ACTIVES en prod** + **147 trous fins Partie C** restants. Prochaine action = sécurité live hors-bande, puis purge de texte + supersedes, puis Palier 0.
- **2026-06-26 (matin)** : prep de nuit livrée (`MORNING_BRIEF_2026-06-26.md` = 35 fiches de décision + `COHERENCE_PLAN_V2_2026-06-26.md`). **5 arbitrages bloquants TRANCHÉS** (ARB-1 impayés↔GL · ARB-2 deux poches · ARB-3 mandat minimal V1 + idée catalogue d'actes P1 · ARB-4 date état daté=v_eff · ARB-5 min ALUR=MAX(5%budget;2,5%PPT)) → consignés dans `REFONTE_DECISIONS`. Reste : C15-5 multi-rôle (risque d'ordre) + ratifier les 30 autres fiches du brief.
- **2026-06-25** : S0 « faille RLS anon » **vérifié empiriquement = fausse alerte** (déjà bouché 0085/0086) → **clos** ; reliquat de durcissement (FORCE généralisé + advisors) versé en chantier séparé non urgent. Mémoire `coproflex-cloud-live` corrigée (FORCE = 5/87 cœur finance, pas 87 ni 0).
- **2026-06-24** : création du registre. GLOSS- phase 1 (socle) + phase 2 (enrichissement ultracode, 230 termes) livrées ; auto-import retiré. Grilling PARTIE C : C.6 + C.7 + **C.9 entièrement cadrés** (P1-P5). Drifts versés au backlog.
