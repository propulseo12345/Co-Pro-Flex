# PLAN DE BATAILLE — Nuit autonome 2026-06-15 → réveil

> **But** : abattre le maximum de **J2-bis** (recâblage hors-finance) + **démarrer les fondations J5**
> (conformité comptable) sur une **branche jetable**, avec revue adversariale, sans rien pousser ni
> toucher le live. Au réveil : branche pleine de commits + ce fichier mis à jour + un **RAPPORT** en tête.
>
> **Gouvernance (Option A, rappel)** : aucune migration sur le live `qqfqrcolzmcbsvfaumiq` ; je livre les
> `.sql` + gates ; **Lyes applique** au déploiement délibéré ; tests sur **rejeu jetable Docker** uniquement.
> **Autonomie totale accordée** sur branche jetable (commits sans validation). **Rien poussé** (seul `lyestriki-29` pousse).
>
> **Effort** : `ultra` — workflows multi-agents pour l'écriture des tranches indépendantes + **revue
> adversariale** systématique aux points grand livre. **Watchdog** : un superviseur vérifie que chaque
> agent avance ; si un agent traîne/se fige (problème socket Windows connu), je suis notifié, je vais voir,
> je répare (reprise depuis le journal / écriture manuelle).

---

## 0. Décisions figées pour la nuit (pour ne pas deviner)

| # | Sujet | Décision exécutée cette nuit | Source |
|---|---|---|---|
| D-a | **Paiements C2/C3** | **EXCLUS** (Lyes parti sans trancher C4/B7). B7 = 🟢 verrouillé « ne pas ré-imputer le trop-perçu ». → arbitrage en tête du rapport. | DECISIONS.md B7, red team |
| D-b | **C6** (doc/seuil feuille de présence art.24) | **FAIT** (trivial, pas de risque GL). | cadrage |
| D-c | **charge_nature 711** | **RESTE 'courant'** (le passer travaux casse `gate_charge_nature_e2e` + contredit cadrage #6 + commentaire 0059). La bascule travaux du 711 se fait via la précédence `operation_id` (E4), pas via charge_nature. | red team A |
| D-d | **charge_nature 661/662/704** | **→ 'travaux'** (cadrage #6, autorité du 15). 661/662 comptablement discutables → **à confirmer au réveil** ; réversible via `set_account_charge_nature`. | cadrage #6 + red team A |
| D-e | **E9 rattachement** | **PROSPECTIF only** : l'immutabilité GL (`tr_ledger_entry_immutable`, 0024) interdit le backfill des écritures postées. Jamais élargir `is_ledger_regen_exempt`. Ordre : front → filet vue → garde RPC ; **pas** de contrainte NOT NULL dure. | red team A |
| D-f | **Intégration E4** | `git checkout e4-operation-id-ledger -- <3 fichiers>` (migration 0060 + gate + 1 ligne db-test). **JAMAIS `git merge e4`** (importerait bouton démo + Playwright + docs). | red team A |
| D-g | **Numéros migrations** | main top = 0059, E4 = 0060 → **la nuit démarre à 0061**. | red team A |
| D-h | **Rapprochement bancaire** | `reconcile` = **pointage pur** (INSERT `bank_matches` + `bank_movements.status='matched'`, **zéro écriture GL** sur paiement existant — le 512 est déjà mouvementé à la saisie). + garde cohérence copro des 3 ids (anti-IDOR). « Encaissement jamais saisi » = **hors scope** du payload (pas de lot_id/period_id). | red team B |
| D-i | **Mutations storage** | notaire → créé comme `tiers(is_notary)` → `notaire_id` ; `buyer_owner_id` **NULL** (acquéreur pas encore copro) ; saisie acquéreur préservée dans une colonne **`buyer_draft jsonb` nullable** (migration additive, zéro impact existant). Corriger les **2** `createMutation` + `updateMutation`. | red team B |
| D-j | **AG jalons** | créer RPC `get/save_ag_milestone` sur `ag_milestones.milestone_key` (cadrage #3) **ET** corriger le SELECT fautif `milestone_type`→`milestone_key` (`useAGDelais:149`). Aucun `milestone_type` résiduel. | red team B |
| D-k | **AG pouvoirs** | RPC sur `ag_attendance` (proxy) ; **UPSERT** sur `uq_ag_attendance_owner (ag_id, coproprietaire_id)` (le pouvoir = lignes MANDANT, pas une ligne mandataire) ; garde art.22 serveur (≤3 `proxy` par `represented_by_id`). | red team B |
| D-l | **GED** | reloger **types + runtime** (`LinkedEntityType`, `ExtractedDocumentData`, `LINKABLE_MODULES`, helpers de détection) dans `src/lib/documents/linking.ts`, réexporter, repointer les **6** importeurs, mapper les 3 types manquants (IMPAYE/INTERVENTION/APPEL_FONDS), brancher versioning sur `v_document_versions` (existe, 0031:266), **supprimer le mock EN DERNIER**. | red team B |

---

## 1. Risques de cascade & garde-fous (synthèse revue adversariale)

1. **Double encaissement bancaire** → reconcile = pointage pur, jamais d'écriture sur paiement existant. (D-h)
2. **charge_nature reseed** → corriger **la fonction `provision_copro_chart`** + les données dans la **même** migration. (D-c/d)
3. **Effacement silencieux de la précédence E4** → toute migration qui touche `open_next_period`/`v_result_allocation_split` doit repartir du **corps 0060**, pas 0059. **Idéalement ne pas les toucher** (elles lisent déjà `charge_nature`).
4. **Backfill GL interdit** → E9 prospectif only. (D-e)
5. **Régénération types** → **une seule fois en fin de nuit**, après la dernière migration, depuis rejeu jetable. Toute regen intermédiaire = faux-vert.
6. **Gate par migration** → chaque `00xx` de la nuit a sa gate (`supabase/tests/`), enregistrée dans `scripts/db-test.mjs` (`GATES[]`), exercée **dans les deux sens** + **anti-vacuité** + montant **non divisible**. `rebaseline-check` vert à chaque étape.
7. **GED tsc** → reloger AVANT de supprimer le mock (6 importeurs, types+runtime).
8. **Mur UX** → retirer les +1 optimistes (likes:265 ET comments:320) + **refetch ciblé** du compteur (pas de régression du +1). `author_name` à l'écriture = déjà OK, reste une lecture morte à nettoyer.
9. **Mutations FK** → buyer NULL, notaire en tiers, jsonb draft. (D-i)
10. **IDOR bancaire** → garde copro(bank_movement)==copro(target)==payload.copro_id. + corriger bug adjacent `categorizeBankMovement` (colonnes mortes).

---

## 2. Ordre d'exécution (par risque croissant + dépendances)

**Point de vérif standard `[CHK]`** = `npm run db:test` (gates bloquantes vertes) + `bash scripts/rebaseline-check.sh` (rejeu + smoke audit=0) ; `tsc` aux étapes front/types.

### Vague 1 — J2-bis SÛRES (parallélisables, fichiers disjoints)
- [ ] **T1 Mur** : retirer double-comptage likes+comments + refetch ciblé + nettoyer lecture morte `author_name`. `tsc`. commit.
- [ ] **T2 Conseil/Membres** : migration `v_council_members_detail` (JOIN council_members→coproprietaires/profiles, COALESCE) + gate `gate_00xx_council_members` + rebrancher `useConseilSyndicalPage`. `[CHK]` + `tsc`. commit.
- [ ] **T3 Maintenance TODO** : `interventions.service.ts:12` (api.ts déjà supprimé) → brancher sur `useMaintenanceData`/vue. `tsc`. commit.
- [ ] **T4 AG jalons** : RPC `get/save_ag_milestone` (migration) + corriger SELECT `milestone_type`→`milestone_key` + rebrancher `useAGDelais`. gate + `[CHK]` + `tsc`. commit.

### Vague 2 — J2-bis MOYENNES
- [ ] **T5 GED** : `src/lib/documents/linking.ts` (types+runtime relogés) → repointer 6 importeurs → mapper 3 types → versioning sur `v_document_versions` → supprimer mock. `tsc` (multi-fichiers). commit.
- [ ] **T6 AG choix d'envoi** : RPC `get/save_ag_envoi_tracking` (rebrancher l'existant 0030 ; multi-canal ; distinguer canal légal vs copie) + rebrancher `useAgEnvoiPage`. gate + `[CHK]` + `tsc`. commit.
- [ ] **T7 Mutations** : migration `mutations.buyer_draft jsonb` + corriger 2×`createMutation` + `updateMutation` (notaire→tiers, buyer NULL, draft jsonb). gate + `[CHK]` + `tsc`. commit.

### Vague 3 — J2-bis À ENJEU (revue adversariale obligatoire)
- [ ] **T8 AG pouvoirs** : RPC sur `ag_attendance` (UPSERT unique + garde art.22 serveur) + rebrancher `usePouvoirs` + aligner shape justificatifs. gate (2 sens, garde art.22) + `[CHK]` + `tsc`. **revue ultra**. commit.
- [ ] **T9 Rapprochement bancaire** : 2 edge functions `import_bank_movement` (RPC DEFINER gardée) + `reconcile_bank_movement` (pointage pur) sur pattern `record_payment` + garde copro + corriger `categorizeBankMovement`. gate + `[CHK]` + `tsc`. **revue ultra**. commit (PR isolée logique).

### Vague 4 — J5 FONDATIONS (séquentiel, GL-sensible, revue ultra)
- [ ] **T0 Intégrer E4** : checkout 3 fichiers (D-f). `[CHK]` (filet : prouver 0060 + gate verts avant d'empiler).
- [ ] **T10 `0061` charge_nature corrective** : `CREATE OR REPLACE provision_copro_chart` (661/662/704→travaux, 711 reste courant) + `UPDATE` existant. gate `gate_0061_charge_nature_corrective` (asserte 661/662/704=travaux, **711=courant**, anti-vacuité) + non-régression `gate_charge_nature_e2e`. `[CHK]`. **revue ultra**. commit.
- [ ] **T11 `0062` `set_account_charge_nature`** : RPC manager, respect CHECK (refus propre hors 6/7), traçabilité. gate. `[CHK]`. commit.
- [ ] **T12 E9** : (a) front sélecteur `operation_id` (budgets `works`) `tsc`/build → (b) `0063` vue `v_works_entries_unlinked` + garde RPC clôture d'opération (PAS contrainte) + gate (2 sens, anti-vacuité). `[CHK]`. **revue ultra**. commit. (c) durcissement RPC « operation_id requis si nature travaux » **après** (a). Contrainte dure **différée**.
- [ ] **T13 C6** : doc/enums/seuil art.24 (gate si SQL touché). `[CHK]`. commit.

### Clôture
- [ ] **T14 Régénérer `src/types/supabase.ts`** UNE FOIS (après dernière migration, rejeu jetable). `tsc` global. `[CHK]` final.
- [ ] **T15 RAPPORT de réveil** en tête de ce fichier + mise à jour `SESSION.md` + `PLAN_MAITRE_FIN_PROJET.md` (cocher) + mémoire (faits durables).

---

## 3. Mécanisme d'orchestration & watchdog

- **Intégrateur = boucle principale (moi)** : gère l'état git (branche, commits ciblés, gates, rebaseline, tsc) en **séquentiel fiable**. `git add` **toujours ciblé** (jamais `git add .` — 54 fichiers-déchets à la racine).
- **Ouvriers = sous-agents** : écriture lourde par tranche (souvent en parallèle sur tranches à fichiers disjoints). Je teste/commite leur sortie.
- **Revue = Workflow `ultra`** : fan-out de vérificateurs adversariaux sur chaque tranche GL (bancaire, charge_nature, E9, pouvoirs) avant commit définitif.
- **Watchdog** : à chaque jalon je vérifie l'avancement (git log, gates). Un agent figé (socket Windows) → je le repère (sortie nulle / pas de progrès), je récupère le travail depuis le journal `agent-*.jsonl` ou je réécris à la main (leçon `workflow_authoring_cadence`). Split des auteurs + retry ×3.
- **Continuité** : j'enchaîne les tranches sans rendre la main jusqu'au rapport. Filet de reprise = section 4.

## 4. Reprise / état (mis à jour en continu)

- Branche de travail : `nuit-2026-06-15` (depuis `j2bis-nettoyage-front`, base main+E4).
- **Environnement test** : conteneur Docker `supabase_db_Co-Pro-Flex` (DB SEULE — `supabase start`
  complet crashe par OOM sur cette machine ; relancer via `docker start supabase_db_Co-Pro-Flex`).
  Gates = `npm run db:test` ; rejeu = `bash scripts/rebaseline-check.sh`.

### FAIT + commité (branche nuit-2026-06-15)
- `3cad144` docs cadrage · `08de512` E4 (0060) intégré (checkout fichiers, pas merge).
- **T1 Mur** `1b91d7c` — double-comptage likes/comments supprimé (refetch v_wall_feed).
- **T2 Conseil** `5ecfe2d` — vue `v_council_members_detail` (0061) + gate ; noms/emails résolus, rôle EN→FR.
- **T4 Jalons** `5699744` — RPC get/save_ag_milestone (0062) + gate ; colonne `milestone_key` corrigée.
- **T5 GED** `5c2f554` — sortie des mocks document-linking/versioning → Supabase + `linking.ts` ;
  bonus : bug pré-existant `ENTITY_TYPE_FROM_LEGACY` corrigé. AccessBadge mort supprimé.
- **T7 Mutations** `a33da72` — 0064 `buyer_draft` + notaire→tiers (RPC find-or-create) ; bouton réparé.
- **T6 AG envoi** `fc905df` — 0063 choix dans `ag_meetings.envoi_choices` (séparés des traces légales).
  ⚠️ cascade « purge des preuves d'envoi recommandé » ATTRAPÉE en revue adversariale puis corrigée.
- **Preuves** : `npm run db:test` = **23/23 gates bloquantes vertes** ; `tsc` 0 ; rejeu 0001→0064 (en cours).

### RESTE (ordre d'attaque)
- **T3 Maintenance** (trivial) : TODO `interventions.service.ts:12` (le fichier `lib/maintenance/api.ts` est déjà inexistant).
- **T8 AG pouvoirs** (à enjeu) : RPC sur `ag_attendance` (UPSERT `uq_ag_attendance_owner` + garde art.22 serveur) — revue ultra.
- **T9 Rapprochement bancaire** (le + à enjeu) : 2 edge functions ; reconcile = **POINTAGE PUR** (zéro écriture GL),
  garde IDOR copro, + corriger `categorizeBankMovement` (colonnes mortes) — revue ultra.
- **J5 fondations** : T10 charge_nature (`provision_copro_chart` + données, 661/662/704→travaux, **711 reste courant**) ·
  T11 `set_account_charge_nature` · T12 E9 (front `operation_id` → vue filet + garde clôture, **prospectif**) · T13 C6 (doc art.24).
- **T14** régénérer `src/types/supabase.ts` UNE FOIS (fin) · **T15** rapport réveil.

### ⚠️ Prochains numéros de migration : **0065+** (0060 E4, 0061 conseil, 0062 jalons, 0063 envoi, 0064 mutations = pris).

## 5. Hors périmètre nuit → arbitrage Lyes au réveil

1. **C2/C3 + C4/B7** (paiements) — conflit décisionnel, priorité #1.
2. **661/662** travaux — confirmer la classification comptable.
3. **Annexes E2/E7/E8** + **état daté H1/H2/H3** + **F8** (3 briques) + **F9 contre-passation** — reste de J5 (nuits suivantes).
4. **Mutations workflow complet** (opposition art.20, multi-acquéreurs) — J9.
5. **Backlog `providers→tiers`** (manques mineurs non bloquants).
6. **Portail copropriétaire (J3)** — seulement après J5 soldé + validé.
