# Audit drift HORS-FINANCE — 2026-06-25

> **Source** : même workflow (`wf_326431c2-0b2`), 5 auditeurs (AG, maintenance, GED, communication, ventes). Complète l'audit drift qui ne couvrait que la finance (`AUDIT_DRIFT_FINANCE.md`). Comble ANOM-25 (« photo incomplète »).
> But : inventorier les orphelins « à câbler vs à purger » par domaine, AVANT de reconstruire en v2.

## ag

**Références cassées / fantômes** :
- GHOST COLUMNS — src/lib/ag/api/resolutions.api.ts:72-80 (addResolutionDirect, le fallback EFFECTIVEMENT emprunté en prod faute d'edge) insère dans ag_resolutions 7 colonnes INEXISTANTES au schema : tantiemes_for / tantiemes_against / tantiemes_abstention / voters_for / voters_against / voters_abstention / vote_details. 0 occurrence de ces 6 compteurs dans _schema_dump.sql ; vote_details n'existe QUE comme table ag_correspondence_vote_details, pas comme colonne. La vraie table (dump 1633-1656) n'a que threshold_tantiemes/threshold_voters. => l'insert direct lève 'column does not exist' (PGRST204).
- action_type NON PROPAGÉ — src/lib/ag/create-standard-resolutions.ts:135-143 appelle addResolution({...}) SANS clé action_type, alors que le template lu en porte une (template.action_type lu mais jamais transmis, ligne 128). addResolutionDirect met donc action_type = null (resolutions.api.ts:72). Conséquence : prepare_ag_decisions (0030:1451) filtre 'action_type is not null' => 0 ag_pending_actions => activate_ag_decisions n'active rien. C'est la rupture racine AG→finance (ANOM-05 #1) confirmée dans le réel.
- RPC create_budget_from_ag_resolution — ABSENTE de toutes les migrations (grep 0 hit) et jamais appelée par le front ; n'existe que comme nom dans l'audit/mémoire (orphelin D29 à créer). Le budget n'est jamais matérialisé depuis ag_resolutions => activate_ag_decisions branche CREATE_BUDGET (0030:1804-1806) lève 23503 'budget introuvable'.
- EDGE FUNCTIONS NON DÉPLOYÉES (ANOM-04) appelées comme chemin PRINCIPAL par le front AG : castVote→ag_cast_vote (src/lib/ag/api/votes.api.ts:34), addResolution→ag_add_resolution (resolutions.api.ts:35), createAg→ag_create (meetings.api.ts:113), closeAg→ag_close (meetings.api.ts:120). 0 edge déployée sur le live => chaque appel retombe sur un fallback direct-DB non-équivalent (voir ghost columns).
- RPC set_ag_status — ABSENTE des migrations (aucune fonction de statut canonique). Le front pose le statut par UPDATE brut sur ag_meetings.status (updateAgStatus meetings.api.ts:200-203 ; cancelAgSession:218-225 ; ~10 fichiers font from('ag_meetings')) — contredit la doctrine C.17 (set_ag_status unique, aucun UPDATE front) ; ANOM-11.
- council_decisions / council_votes / council_documents — référencés UNIQUEMENT dans src/types/supabase.ts (généré). AUCUN code de feature ne lit/écrit ces tables ; seule v_council_members_detail est lue. L'edge council-workflow n'est JAMAIS invoquée par le front (0 hit). Aucune RPC d'écriture conseil n'existe en migrations (seuls is_council_member/is_council_president = gardes RLS).
- DESIGNATE_BUREAU — valeur d'enum ag_action_type SANS branche d'exécution : non mappée dans prepare_ag_decisions (0030:1458-1471) et filtrée explicitement (0030:1452) ; tomberait sinon dans le else 'action_type non géré' de activate_ag_decisions (0030:1954). Intentionnel (constitution du bureau = sans effet data) mais c'est un enum à effet nul.

**Stubs menteurs (succès sans effet)** :
- send-convocation-email/index.ts:39-45 — renvoie {success:true, messageId:'stub-...', stub:true} quand aucune clé API n'est configurée : succès mensonger, aucun email parti (ANOM-04).
- activate_ag_decisions branche APPOINT_SYNDIC (0030:1914-1915) — renvoie {note:'informatif — mandat syndic différé'} et marque le pending_action 'activated' SANS aucun effet : une résolution 'Renouvellement du contrat de syndic' votée est rapportée comme activée mais ne change rien (copros n'a pas de colonne syndic).
- activate_ag_decisions branche GRANT_QUITUS (0030:1951-1952) — {note:'informatif — quitus accordé'} : marqué 'activated' sans aucun effet réel (acceptable métier, mais c'est un succès sans trace persistée du quitus).
- castVoteDirect (src/lib/ag/api/votes.api.ts:54-137) — fallback qui upsert ag_votes en RECALCULANT les tantièmes côté JS (somme tantiemes_generaux des lots) et SANS passer par cast_vote RPC : ignore is_excluded/exclusion_reason, le trigger tr_ag_vote_requires_attendance et la cohérence d'attendance ; renvoie {success:true} même si le calcul de quorum/majorité divergera. Faux-vert silencieux.
- addResolutionDirect (resolutions.api.ts:54-108) — renvoie {success:true, resolution_id} après un insert qui, sur le vrai schéma, échouerait (ghost columns) ; sur un schéma tolérant il insère action_type=null => résolution muette pour le moteur de décisions.

**Capacités dormantes (schéma riche non câblé)** :
- Tout le cycle CONSEIL SYNDICAL autonome : tables council_decisions, council_votes, council_documents + enums council_decision_status, council_role, council_doc_link_type + RPC compute_decision_result (0030:287) + edge council-workflow = schéma riche, ZÉRO câblage front (hors élection via ELECT_COUNCIL en AG). Cas typique 'schéma riche non exploité'.
- ag_correspondence_vote_details (dump 1440) + ag_correspondence_votes (1460) : modèle détaillé de vote par correspondance (statut, reception_method, integrated_vote_id, recorded_by). Le front utilise des chemins parallèles (save_votes_correspondance / get_correspondence_eligible_owners) ; la granularité 'détail par résolution' + traçabilité d'intégration semble sous-exploitée.
- ag_meetings.president_id/secretary_id/scrutineer1_id/scrutineer2_id (FK personnes) coexistent avec *_name (texte libre). Le bureau est saisi en texte (designation) ; les FK structurées sont largement dormantes.
- ag_meetings : remote_meeting_url / remote_meeting_provider (AG à distance, décret visioconférence) — colonnes présentes, non câblées au front.
- ag_milestones / ag_notification_events / ag_notifications / ag_envoi_tracking : infrastructure de suivi d'envoi et d'événements riche (delivery_status, tracking_ref, provider_ref) largement en avance sur l'usage front (envoi = stub email).
- ag_action_type contient CREATE_WORK_BUDGET et CREATE_EXCEPTIONAL_CALL pleinement implémentés côté activate_ag_decisions, mais le chemin de création JS (createStandardResolutions) ne génère que 5-7 résolutions standard sans action_type => ces capacités d'activation restent dormantes faute d'amorçage correct.

**Doublons / 2 chemins** :
- CRÉATION AG — DEUX chemins : (A) RPC canonique create_ag_with_standard_resolutions (0030:670) qui pose AG + 7 résolutions AVEC action_type correct ; (B) chemin front réel = edge ag_create(include_standard_resolutions:false) (useAgCreateForm.ts:238) PUIS createStandardResolutions JS (ligne 261) qui insère SANS action_type. Le front emprunte (B), le mauvais.
- AJOUT RÉSOLUTION — edge ag_add_resolution (resolutions.api.ts:35) vs fallback addResolutionDirect (insert SQL direct, ligne 54). Deux implémentations, payloads divergents (l'edge n'écrit pas non plus action_type).
- VOTE — edge ag_cast_vote (votes.api.ts:34) vs RPC cast_vote (0030:369) vs fallback castVoteDirect (upsert direct, ligne 54). Trois chemins pour le même acte ; le front choisit l'edge (absente) puis l'upsert direct, jamais la RPC cast_vote.
- CLÔTURE/ACTIVATION — orchestrateur 1-transaction finalize_and_activate_ag (0030:1990, calculate→prepare→activate atomique) vs chemin front éclaté : finishAgSession=close_ag+prepare_ag_decisions (meetings.api.ts:161-164) puis activate_ag_decisions séparé (usePVPage.ts:739). Deux orchestrations, le front fragmente.
- FIN DE SÉANCE — edge ag_close (meetings.api.ts:120) vs RPC close_ag (finishAgSession, meetings.api.ts:161). Deux portes pour clore.
- VOTES CORRESPONDANCE — au moins 3 hooks/RPC parallèles : save_votes_correspondance (useVotesCorrespondance.ts, useVotesCorrespondanceCoproPage.ts), register_correspondence_form_votes (useCorrespondenceVotes.ts:196), get_votes_correspondance vs get_correspondence_eligible_owners. Chevauchement EN/FR et doublons fonctionnels.

**Verdict à créer / à purger** :
- À CREER — create_budget_from_ag_resolution (D29) : matérialiser le budget depuis ag_resolutions avant activation (débloque CREATE_BUDGET dans activate_ag_decisions).
- À CREER — set_ag_status RPC gardée unique (doctrine C.17) + purger les UPDATE bruts de ag_meetings.status côté front (updateAgStatus, cancelAgSession et ~10 hooks).
- À PURGER puis REBRANCHER — chemin JS createStandardResolutions (create-standard-resolutions.ts) : remplacer par l'appel à la RPC canonique create_ag_with_standard_resolutions (qui pose action_type). Code actuel = piège (action_type perdu).
- À PURGER — ghost columns dans addResolutionDirect (resolutions.api.ts:73-80) : tantiemes_for/against/abstention, voters_*, vote_details n'existent pas ; les retirer (ou supprimer le fallback direct et fiabiliser la RPC).
- À PURGER (faux-vert) — castVoteDirect et addResolutionDirect fallbacks direct-DB : remplacer par les vraies RPC (cast_vote / addResolution-as-RPC) une fois les edges recréées proprement ; ne pas garder un upsert qui shunte triggers et exclusions.
- À PURGER (stub) — send-convocation-email mode stub success:true : doit échouer franchement (ou file d'attente) tant que le provider Brevo n'est pas configuré.
- À CREER (si périmètre conseil V1 retenu) — RPC d'écriture du cycle council_decisions/council_votes (submit/decide/vote) + câblage UI ; sinon ASSUMER dormant et le documenter. council-workflow edge à recréer ou abandonner.
- À CONSERVER tel quel (no-op assumé) — branches APPOINT_SYNDIC et GRANT_QUITUS : informatives par décision métier (mandat syndic différé), MAIS marquer le caractère 'sans effet' dans le résultat front pour ne pas mentir à l'utilisateur.

**Notes** : Méthode : schema dump _schema_dump.sql (structure seule, bodies de RPC dans supabase/migrations) + grep des appelants réels dans src + lecture des migrations 0030/0039/0040/0050/0061-0065. Tables AG du dump : ag_meetings(1506), ag_resolutions(1633), ag_votes(1684), ag_pending_actions(1609), ag_attendance(1410), ag_correspondence_votes(1460)+_details(1440), ag_envoi_tracking(1484), ag_milestones(1554), ag_notifications(1589)+_events(1572), ag_session_drafts(1665), council_decisions(2153)/council_votes(2219)/council_members(2198)/council_documents(2178). VERDICT GLOBAL : le schéma AG et les RPC migrations sont RICHES et largement COHÉRENTS (close_ag, prepare_ag_decisions, activate_ag_decisions, finalize_and_activate_ag, create_ag_with_standard_resolutions existent et sont corrects). Le drift est presque entièrement CÔTÉ FRONT : il emprunte des chemins parallèles (edges non déployées + fallbacks direct-DB) qui (a) perdent action_type, (b) écrivent des colonnes fantômes, (c) shuntent triggers/exclusions, et bypassent les RPC canoniques. La 'rupture racine AG→finance' (ANOM-05) est confirmée empiriquement : action_type=null via createStandardResolutions→addResolution. DISTINCTION absent/loi : aucune des absences relevées n'est une interdiction légale — ce sont des manques de code (create_budget_from_ag_resolution, set_ag_status) ou des doublons à purger. Le seul 'enum à effet nul' légitime est DESIGNATE_BUREAU (constitution du bureau, sans effet data — voulu). Le CONSEIL SYNDICAL autonome est la plus grosse capacité dormante (tables+enums+edge+compute_decision_result présents, 0 câblage). Non vérifié faute de périmètre : bodies exacts des edges ag_* (lus seulement pour send-convocation-email et l'absence d'action_type dans ag_add_resolution) et la valeur live des données (base quasi vide d'après l'audit, donc tests de parité vacués — ANOM-26).

---

## maintenance

**Références cassées / fantômes** :
- contract_status enum mort : useContractsPage.ts:48 fait `.from('contracts').select('id,end_date,updated_at').eq('status','pending_renewal')` mais l'enum public.contract_status (schema_dump L266-272 + 0021) ne contient PAS 'pending_renewal' (valeurs reelles: draft/active/to_renew/expired/terminated). La requete renvoie 0 ligne en permanence (ou erreur 22P02 invalid_input_value) -> le chargement des 'pending renewals' depuis la base ne marche jamais, retombe sur localStorage. Le code ecrit d'ailleurs 'to_renew' (L87) -> 'pending_renewal' est une valeur 100% fantome.
- insurance_sub_type : mapping casse. DB enum = multirisque/dommages_ouvrage/rc/protection_juridique/autre (schema_dump L532-538). Le front (useAssuranceDetailPage.ts:62) fait `(data.sub_type).toUpperCase()` et attend MRI/RC_SYNDICAT/DOMMAGES_OUVRAGE/PROTECTION_JURIDIQUE/AUTRE (SOUS_TYPES_ASSURANCE L11-17). 'multirisque'->'MULTIRISQUE' (jamais 'MRI'), 'rc'->'RC' (jamais 'RC_SYNDICAT') : les libelles de sous-type d'assurance ne s'affichent jamais correctement.
- insurance_policies n'a NI start_date NI end_date (schema_dump L2414-2429) mais useAssuranceDetailPage lit dateDebut depuis created_at (L66) et laisse dateFin='' (L67). Toute la logique d'expiration/alerte d'assurance (getAssurancesStats compare new Date(a.dateFin)) tourne donc sur une date vide -> echeances d'assurance non calculables depuis la base. Colonne attendue par le front ABSENTE du schema (pas un interdit legal : simple trou de modele).
- v_service_orders_overview.documents_count est hard-code a `0` (0047 L266). Le front le lit (useServiceOrdersListPage.ts:53 `documents_count ?? 0`) et l'affiche comme compteur de PJ : il vaut toujours 0 dans la liste. (La fiche detail recompte a part via .from('documents'), donc la liste ment mais pas la fiche.)
- Sinistres : AUCUNE table sinistre/claim dans le schema (grep schema_dump = 0). Un 'sinistre' n'existe que comme logbook_entry_type='incident' (enum L671-677). Le domaine 'sinistres' annonce dans le cadrage n'a pas de modele dedie -> rien de casse cote front (pas de page sinistre branchee) mais capacite metier absente.

**Stubs menteurs (succès sans effet)** :
- EDITION ASSURANCE = stub mensonger. useAssuranceDetailPage.handleSave (L121-159) appelle updateAssurance() de @/lib/services/assurances.service, qui mute un tableau en memoire `assurancesState` (assurances.service.ts:24,112-140) puis affiche succes + router.refresh(). Or le CHARGEMENT lit la vraie table insurance_policies (L52-53). Resultat : on edite une police, l'UI dit 'enregistre', router.refresh() recharge depuis la base = modif PERDUE. addAssurance/deleteAssurance/addDocumentToAssurance idem (tout le service est un mock, commentaire L7 'En production, ce sera remplace par des appels Supabase').
- ENVOI D'OS PAR EMAIL = succes mensonger. useMaintenanceData.sendOrderEmail (L480-493) et handleEmailSend (useServiceOrdersListPage.ts:228 'Email envoye avec succes') ne font QUE rpc('update_service_order_status', new_status:'sent', comment:'Email X envoye'). Aucun email reel (aucune edge function appelee, aucun insert mails). Le commentaire en base pretend qu'un email est parti. Idem handleSend (useNewServiceOrderPage.ts:368 'Ordre de service envoye a X').
- completeAndLog (useMaintenanceData.ts:518-527) NE LOGUE PAS. Le nom promet de cloturer ET d'inscrire au carnet d'entretien ; il ne fait que rpc('update_service_order_status', new_status:'closed'). La RPC create_logbook_from_service_order EXISTE (0032 L86) mais n'est jamais appelee -> aucune entree carnet creee a la cloture d'un OS, contrairement a ce que le nom de la fonction affirme.
- handleConfirmResiliation (useContracts.ts:163-186) resilie un contrat UNIQUEMENT dans le store mock (updateContrat statut RESILIE) + toast 'resilie avec succes' (+ message 'Recommande postal envoye') : aucun terminateContract Supabase ici (contrairement a useContractsPage.handleResilierContratExpire qui, lui, appelle bien terminateContract). Selon le bouton emprunte, une resiliation est persistee ou purement cosmetique.
- handleAddContrat/handleSaveContrat (useContracts.ts:113-160) ecrivent d'abord dans le mock (addContrat/updateContrat) PUIS tentent supabaseCreateContract/Update dans un try/catch qui avale l'erreur (console.error only). Si l'ecriture base echoue, le toast 'cree/modifie avec succes' s'affiche quand meme : faux positif possible.
- handleTelecharger / handleExport 'PDF' (useContracts.ts:189-258, 296) : genere un .txt deguise en PDF (Blob text/plain, download .txt) et toast 'Export PDF genere'. handleDownloadSyndicPDF (L262) = setTimeout 1500ms + toast 'Fichier telecharge' sans aucun fichier. PDF mensongers.

**Capacités dormantes (schéma riche non câblé)** :
- create_logbook_from_service_order (RPC 0032 L86-119) : code complet et solide (idempotent, back-link, statut derive) mais ZERO appelant dans src/ (grep = 0 hit hors migrations). Capacite 'auto-alimenter le carnet d'entretien depuis un OS realise' entierement codee cote base, jamais cablee cote front.
- contracts.auto_generate_orders + planned_frequency + planned_day_of_month + next_planned_intervention (0021 L75-79, schema_dump L2006-2009) : colonnes prevues pour generer automatiquement des OS recurrents depuis un contrat. AUCUN cron/RPC ne les lit (grep migrations: seul 0055 cron existe, et c'est pour les relances impayes, pas les OS). Schema riche, mecanique de generation d'OS recurrents inexistante -> colonnes dormantes.
- service_order_events.payload (jsonb, schema_dump L2970) : jamais ecrit (update_service_order_status n'insere que from/to/comment, 0032 L78). Slot d'evenement structure dormant.
- service_orders : emergency_ceiling, is_art18_emergency, acknowledged_at/quoted_at/validated_at/started_at/refused_at (schema_dump L2995-3013) modelisent un workflow art.18 (urgence travaux) + horodatage fin. is_art18_emergency est cable en dur a false partout (useNewServiceOrderPage L338, useContractsPage L195) ; emergency_ceiling n'est jamais ecrit -> garde-fou plafond d'urgence art.18 dormant.
- insurance_policies.related_works / works_reception_date / deductible / guarantees (schema_dump L2423-2425) : works_reception_date (point de depart de la garantie decennale/Dommages-Ouvrage) n'est jamais lu/ecrit par le front (handleSave envoie travauxConcernes/dateReceptionTravaux a un mock, pas a la base). Suivi decennale dormant.
- contracts.renewal_date (distinct de end_date, schema_dump L2001) : jamais lu par les vues ni le front (les alertes raisonnent sur end_date - notice_months*30). Colonne renewal_date dormante.
- logbook_entries.document_id / budget_category / next_due_at : next_due_at pilote v_logbook_alerts (utilise) mais document_id (lien GED d'une entree carnet) n'est jamais ecrit par updateEntry/createEntry cote front -> rattachement document du carnet dormant.

**Doublons / 2 chemins** :
- DEUX piles de LECTURE des contrats sur la meme vue v_contracts_overview : (A) src/lib/services/contracts.service.ts (loadContracts L173) -> store externe via useSyncExternalStore dans hooks/modules/useContracts.ts (c'est CE store mock-style qui alimente l'affichage de la page contrats) ; (B) hooks/modules/useMaintenanceData.useContracts (fetchContracts L222) utilise par useContractsPage et useNewServiceOrderPage. Deux chemins, deux formats (legacy FR ContratDetaille vs ContractOverview EN), risque d'incoherence d'etat.
- DEUX hooks 'useContracts' homonymes : @/hooks/modules/useContracts (legacy, mock-store + sync Supabase best-effort) ET @/hooks/modules/useMaintenanceData#useContracts (Supabase pur). useContractsPage.ts:5 et :13 importe LES DEUX en aliasant (useContracts + useContractsSupabase). Source de confusion EN/FR typique de la dette doublons.
- ECRITURE contrat dupliquee : handleAddContrat/handleSaveContrat (useContracts.ts) ecrivent mock + Supabase ; handleRenouvelerContrat/handleResilierContratExpire (useContractsPage.ts) ecrivent Supabase via terminateContract/updateContract ; handleConfirmResiliation (useContracts.ts L163) ecrit SEULEMENT le mock. Trois voies de mutation contrat avec des garanties de persistance differentes.
- STATUT OS : double mapping legacy<->Supabase maintenu a deux endroits (useServiceOrdersListPage.ts:10-23 mapStatusToLegacy ET useServiceOrderDetailPage.ts:16-46 SUPABASE_TO_LEGACY/LEGACY_TO_SUPABASE). Le 2e ajoute des cles legacy fantomes (A_ENVOYER, ACCEPTE, PLANIFIE, REALISE, FACTURE, PAYE) qui n'existent plus dans le workflow cible -> tables de correspondance redondantes et partiellement mortes.
- ASSURANCE : lecture reelle (insurance_policies) + ecriture mock (assurances.service) = chemin de donnees incoherent (cf. lyingStubs).
- FALLBACK localStorage systematique sur OS (newOrdresService, custom_ordres_service) fusionne avec les donnees Supabase (useServiceOrdersListPage.ts:71-116) : 2e source de verite cote client, deja signalee comme dette.

**Verdict à créer / à purger** :
- A CREER (cabler demain) : appel de create_logbook_from_service_order depuis completeAndLog/updateOrderStatus quand l'OS passe completed/closed. La RPC est prete (0032), il manque juste le .rpc() cote useMaintenanceData -> capacite 'carnet auto' livrable immediatement.
- A CREER : persistance reelle des assurances. Remplacer @/lib/services/assurances.service (mock) par des .from('insurance_policies') (insert/update/delete) + AJOUTER colonnes start_date/end_date a insurance_policies (migration) pour que les alertes d'echeance fonctionnent. C'est un vrai trou de modele, pas un interdit legal.
- A CREER : generateur d'OS recurrents (cron pg_cron analogue a 0055) qui lit contracts.auto_generate_orders/planned_frequency/next_planned_intervention. Schema deja prevu pour ca -> activer la capacite dormante.
- A CREER : palier 'engage' (table commitments, ANOM-18) pour rendre un OS de 8000 EUR visible au budget (cut-off 408/486). N'existe nulle part en base -> a modeliser.
- A CREER : envoi d'email reel des OS (edge function / insert mails) au lieu du faux 'sent' ; sinon retirer les toasts 'Email envoye'.
- A PURGER : la valeur 'pending_renewal' dans useContractsPage.ts:48 (n'existe pas dans contract_status, requete morte) -> aligner sur 'to_renew' (deja la valeur ecrite L87).
- A PURGER / corriger : mapping insurance_sub_type cote front (MRI/RC_SYNDICAT...) a aligner sur l'enum DB (multirisque/rc...) ou ajouter une vraie table de correspondance ; le toUpperCase() naif est mort.
- A PURGER (refonte v2) : les exports 'PDF' qui produisent du .txt et handleDownloadSyndicPDF (faux telechargement) ; doublon useContracts (legacy mock-store) une fois la pile Supabase unifiee ; les fallback localStorage OS si on assume Supabase obligatoire.
- A PURGER : cles legacy mortes des tables de statut OS (A_ENVOYER/ACCEPTE/PLANIFIE/REALISE/FACTURE/PAYE) une fois le legacy retire.

**Notes** : Methode : le _schema_dump.sql (2026-06-23) est STRUCTURE SEULE (79 tables, 1 vue, 8 fonctions) — il ne contient NI les vues d'agregat NI les RPC. La verite vues/RPC = supabase/migrations (0021/0032/0047). Toutes les vues consommees par le front (v_providers_overview, v_contracts_overview, v_contracts_alerts, v_service_orders_overview, v_logbook_overview, v_logbook_alerts, v_maintenance_stats) et toutes les RPC (generate_service_order_number, update_service_order_status, delete_service_order) EXISTENT et matchent le contrat de colonnes du front (0047). Donc cote OS/contrats/carnet la lecture est SAINE — le drift est ailleurs : (1) assurances 100% mock cote ecriture alors que lecture reelle ; (2) enum pending_renewal/insurance_sub_type mal mappes ; (3) RPC create_logbook_from_service_order et colonnes auto_generate_orders dormantes ; (4) faux envois email + faux PDF. 'commitments' et 'sinistres' : confirmes ABSENTS du modele (pas d'interdit legal, juste non modelises). Je n'ai PAS pu interroger le live (pas d'acces DB execute ici) : l'existence des vues/RPC est prouvee par migrations, pas par la base live — si une migration 0021/0032/0047 n'etait pas appliquee sur qqfqrcolzmcbsvfaumiq, TOUT le module OS/contrats/carnet tomberait en fallback localStorage silencieux (a verifier avec un \\d / list_migrations sur le live). Le hook portail (v_dashboard_kpis/v_maintenance_stats) porte deja un commentaire d'alerte : non fiable en security_invoker pour un coproprietaire (compteurs partiels presentes comme totaux).

---

## ged

**Références cassées / fantômes** :
- IMPORTANT meta-constat : le dump c:/.../_archive/2026-06-23/_schema_dump.sql est PERIME pour la GED (anterieur aux migrations 0050/0052) : il NE contient PAS ag_documents, v_ag_documents, pv_templates, ni les 6 vues de compat v_documents_*. Verification faite sur la base LIVE (qqfqrcolzmcbsvfaumiq) : ces objets EXISTENT bien en prod. Donc PAS de broken ref de ce cote -- ne pas conclure 'orphelin' a partir du dump seul.
- document_access_log : invoque par la doc/cadrage (ANOM-10, AUDIT_COHERENCE_CADRAGE_2026-06-24.md:108) et type IDocumentAccessLog (src/types/models/document-access.ts). ABSENT de la base LIVE (verifie information_schema) ET du dump ET d'absolument tout SQL du repo (aucune migration ne le cree). C'est notre code/doc qui le suppose existant, PAS la loi. Le hook src/hooks/modules/useDocumentPermissions.ts ecrit dans un store RAM (accessLogsStore l.19) au lieu d'une table.
- register_generated_document : nomme dans le cadrage (mention 'register_generated_document') mais AUCUNE RPC de ce nom en base ni dans les migrations. La vraie fonction d'enregistrement existe sous un AUTRE nom : register_ag_document (0050, presente en LIVE). Faux nom dans la cartographie, pas un trou reel.
- DocumentConfidentiality='restricted' (src/lib/documents/api.ts:21,36 + UploadDocumentModal.tsx:18 option 'Restreint') : valeur SANS equivalent en base. L'enum canonique document_visibility n'a que 3 valeurs (gestionnaire_seul/conseil/tous_coproprietaires). La vue v_documents_with_folder ne produit JAMAIS 'restricted' (verifie : viewdef sans 'restricted'). Reference morte cote front.
- DocumentStatus front (api.ts:22) = draft|active|archived|expired ; l'enum DB document_status = active|archived|deleted. 'draft' et 'expired' n'existent pas en base, 'deleted' (soft-delete reel) n'est pas dans le type front. Filtres .neq('status','archived') OK mais le mapping status est partiellement fantome.
- entity_type 'budget_expense' : ajoute a l'enum document_entity_type en 0052 (l.27) ET present en LIVE -- mais api.ts mappe 'depense'/'budget_expense'->'budget' (l.64-65) au lieu de la nouvelle valeur dediee 'budget_expense'. La valeur d'enum existe en base mais le front ne l'emet jamais (dormant cote ecriture).

**Stubs menteurs (succès sans effet)** :
- src/hooks/modules/useDocumentPermissions.ts : updateDocumentConfidentiality (l.290) et grant/revoke/logAccessAction renvoient true (succes) mais n'ecrivent QUE dans accessConfigsStore (l.18) et accessLogsStore (l.19), deux objets module-level en RAM. Aucun appel Supabase d'ecriture. Le reglage de confidentialite/ACL fine et le journal d'acces sont perdus au reload : succes mensonger (ANOM-10 / 'tracabilite fantome').
- src/lib/documents/api.ts:36 VISIBILITY_FROM_CONFIDENTIALITY['restricted']='gestionnaire_seul' : l'utilisateur choisit 'Restreint' dans UploadDocumentModal, l'UI accepte, mais le doc est silencieusement stocke en 'gestionnaire_seul'. Pas un crash, mais le niveau choisi est un mensonge (le 4e niveau a ete abandonne -- D69-bis).

**Capacités dormantes (schéma riche non câblé)** :
- documents.expiration_date / deletion_blocked : colonnes presentes + triggers trg_document_expiration (calculate_document_expiration) et trg_prevent_document_deletion DESORMAIS actifs en LIVE (verifie pg_trigger). La retention legale est donc cablee, MAIS aucune UI ne pilote retention_years ni n'affiche les docs expirants : getExpiringDocuments()/v_documents_expiring existent (api.ts:500) sans ecran consommateur visible -> capacite calculee mais non exposee.
- documents.search_text (tsvector) + trg_document_search_text + idx GIN : la recherche plein-texte FR est alimentee en base ; searchDocuments() (api.ts:402) l'utilise via .textSearch, mais c'est le seul point d'entree -- potentiel sous-exploite ailleurs.
- document_versions : table + vue v_document_versions + immutabilite (trg_version_no_update prevu) = versioning complet en base. Cote front, getDocumentVersions() lit la vue (api.ts:710) mais AUCUN ecrivain : aucune fonction n'insere de nouvelle version (le compat fige is_current_version=true, version=current_version_no). Historique structurellement pret, jamais alimente.
- pv_templates (0052) + set_default_pv_template + increment_template_usage : feature editeur de templates PV (settings/templates) presente en base et RLS-protegee ; a verifier qu'un ecran l'utilise reellement (hors perimetre GED strict mais cree dans la migration GED).
- documents.coproprietaire_id + visibility='conseil'/'tous_coproprietaires' : modele pret pour le portail coproprietaire/CS (3 niveaux reels), mais aucune lecture filtree par role cote portail (portail non implemente). Schema riche, cablage a venir.
- ag_documents.document_id (FK -> documents, 0050 l.39) + retention_until : champ de chainage GED<->AG present, mais register_ag_document peut le laisser NULL (l'edge tolere l'echec d'enregistrement, cf. 0050 l.22-23) -> lien GED/AG dormant si non renseigne.

**Doublons / 2 chemins** :
- TROIS vocabulaires de confidentialite coexistent (migration EN/FR a moitie faite) : (1) DB enum document_visibility = gestionnaire_seul/conseil/tous_coproprietaires (3) ; (2) api.ts DocumentConfidentiality = public/council/manager/restricted (4) ; (3) NiveauConfidentialite (src/types/enums/misc.ts:298) = PUBLIC/CS_ONLY/SYNDIC_ONLY/CONFIDENTIEL (4). Le hook useDocumentPermissions parle la 3e, l'API GED la 2e, la base la 1re. Mapping lossy + risque de copier le mauvais exemple.
- Deux chemins de stockage de la confidentialite : colonne reelle documents.visibility (persistee, lue via vues) VS store RAM accessConfigsStore du hook permissions. Le meme concept a deux sources, dont une volatile.
- Deux registres de documents AG : table ag_documents/v_ag_documents (0050, registre des PDF generes convocation/PV/feuille) ET table documents/document_relations (GED generale, entity_type='ag'). Le PV peut exister dans les deux (ag_documents.document_id pointe vers documents) -> double comptage possible si non synchronise.
- Liens entites : colonnes FK legacy (ag_id, invoice_id, contract_id...) traduites a la volee en lignes document_relations (api.ts toCanonicalDocumentRow/legacyFkRelations + vue qui re-expose ces FK en NULL). L'ancien chemin (FK directes) est mort cote base mais survit dans l'interface Document du front -> deux representations du meme lien.

**Verdict à créer / à purger** :
- A CREER : table document_access_log (+ document_access pour ACL fine) si le journal d'acces nominatif et le 'restreint' par utilisateur sont une exigence produit (cf. besoin cadrage 'journaliser acces nominatifs espace CS art.21'). Sinon, decision D69-bis = 3 niveaux suffisent -> alors PURGER. Tant que rien : ANOM-10 = mock RAM trompeur a corriger en priorite.
- A PURGER (code mort cote front) : l'option 'restricted'/'Restreint' de UploadDocumentModal.tsx:18 et la cle 'restricted' de DocumentConfidentiality (api.ts:21,36) -- aucune valeur DB derriere, mapping silencieux vers gestionnaire_seul. Decision D69-bis acte l'abandon du 4e niveau.
- A PURGER : enum NiveauConfidentialite (misc.ts) a 4 valeurs CS_ONLY/SYNDIC_ONLY/CONFIDENTIEL non alignees sur les 3 valeurs DB -- soit le remplacer par les 3 valeurs canoniques, soit le retirer avec le hook RAM. Migration EN/FR a finir.
- A PURGER : statuts front 'draft' et 'expired' (api.ts:22) inexistants en base ; aligner DocumentStatus sur active/archived/deleted.
- A CONSERVER/CABLER (dormant, pas mort) : v_documents_expiring + retention_years (cabler une UI d'echeances de retention), document_versions/v_document_versions (cabler un ecrivain de versions), pv_templates (verifier l'ecran settings/templates).
- A RENOMMER dans la cartographie : 'register_generated_document' -> register_ag_document (la RPC reelle 0050). Faux nom, pas un manque.
- A METTRE A JOUR : le dump _schema_dump.sql (2026-06-23) doit etre regenere depuis le LIVE -- il ne reflete pas 0050/0052 (GED avancee, AG documents). Auditer la GED sur le dump seul mene a de faux orphelins.

**Notes** : METHODE : croisement front (src/lib/documents/api.ts, src/lib/ag/api/documents.api.ts, useGedPageSupabase.ts, useDocumentPermissions.ts, UploadDocumentModal.tsx) x migrations 0020/0050/0052 x base LIVE (information_schema/pg_enum/pg_trigger/viewdef). PIEGE MAJEUR EVITE : le dump fourni est anterieur a 0050/0052 -> j'ai verifie en LIVE plutot que conclure 'absent'. Resultat : la GED canonique (documents/folders/relations/versions/technical + vues de compat + ag_documents + pv_templates) est SAINE et bien cablee en prod. Les vrais drifts sont : (1) ANOM-10 confirme = confidentialite/ACL/journal d'acces en RAM (useDocumentPermissions) -> perdus au reload, stub mensonger ; (2) document_access_log fantome (ni base ni migration) ; (3) triple vocabulaire de confidentialite (3 DB vs 4+4 front) = migration EN/FR inachevee + option 'Restreint' qui ment ; (4) capacites dormantes (retention/expiration desormais cablees en base mais sans UI, versioning sans ecrivain). DISTINCTION absent!=interdit : aucun objet GED n'est 'interdit par la loi' ici ; les manques sont des choix produit (D69-bis 3 niveaux) ou de la dette, pas des contraintes legales. document_access_log = 'absent de notre code/base', a trancher creer-vs-purger selon l'exigence art.21 (journal d'acces nominatif espace CS).

---

## communication

**Références cassées / fantômes** :
- src/features/communication/messagerie/hooks/useMessagerie.ts:326-338 — sendMessage() insert dans `messages` envoie les colonnes `sender_name` ET `is_edited` qui N'EXISTENT PAS sur la table (colonnes live verifiees : id, copro_id, conversation_id, author_id, content, message_type, attachment_id, reply_to_id, read_by, edited_at, created_at). PostgREST renvoie PGRST204 → le message n'est JAMAIS persiste (seule la copie optimiste locale survit jusqu'au refresh). Chemin d'ecriture casse.
- src/features/communication/messagerie/hooks/useMessagerie.ts:90-105 mapMessage() lit `row.sender_name`, `row.is_edited`, `row.attachments` — aucune de ces 3 colonnes n'existe sur `messages` (attachments = `attachment_id` uuid seul). Lecture silencieusement degradee (tous retombent sur fallback : senderName='Inconnu', isEdited=false, attachments=[]).
- src/features/communication/messagerie/hooks/useMessagerie.ts:52-54 mapConversationPreview() lit `row.last_sender_name` et `row.last_sender_role` — la vue `v_conversations_overview` (verifiee live) NE LES EXPOSE PAS. Fallback silencieux ('Admin CoProFlex' / undefined) : le nom/role du dernier expediteur affiche dans la liste de conversations est faux par construction.
- src/lib/services/convocation-dispatch.service.ts:214-228 saveEnvoiTracking() construit des entrees en camelCase (`coproprietaireId`, `trackingRef`, `documentId`, `error`, `sentAt`) ET avec des `method` en MAJUSCULES FR ('EMAIL','RECOMMANDE','LETTRE_SIMPLE','AVIS_ELECTRONIQUE','REMISE_MAIN_PROPRE'). Le RPC `save_ag_envoi_tracking` (verifie live) lit en snake_case (`coproprietaire_id`, `tracking_ref`, `document_id`, `error_message`, `sent_at`) et caste `method::notification_channel` (valeurs attendues : email/registered_email/postal/registered_postal/hand_delivery). Resultat : cast `'EMAIL'::notification_channel` → 22P02 invalid_input_value, le RPC LEVE une exception et 0 ligne de suivi n'est ecrite (et meme sans l'enum, tous les champs camelCase tomberaient a NULL). Double mismatch (casse + vocabulaire enum).
- src/lib/services/convocation-dispatch.service.ts:146 dispatchEmail() appelle l'edge function `send-convocation-email` via supabase.functions.invoke — mais AUCUNE edge function n'est deployee sur le live (list_edge_functions = []). Tout envoi EMAIL de convocation tombe dans le catch → status 'error' 'Erreur envoi email'. Idem pour ag_send_convocations, ag_send_relance, run_payment_reminders, send_manual_payment_reminder, communication-workflow, email_webhook (presentes en local supabase/functions/, 0 deployees).
- src/app/api/mail/send/route.ts + src/app/api/mail/inbound/route.ts — chaine 'mails' fonctionne au niveau colonnes (status free-text, pas de CHECK/enum verifie live) MAIS depend de Resend (RESEND_API_KEY) et de secrets (RESEND_INBOUND_SECRET, MAIL_INBOUND_COPRO_ID) ; sans cle reelle resend.ts tombe sur 're_placeholder' (envoi echoue 401), inbound refuse en 503 sans secret. Pas un drift schema, mais cable sur une infra non provisionnee.

**Stubs menteurs (succès sans effet)** :
- src/lib/services/convocation-dispatch.service.ts:62-71 dispatchConvocation() branche 'AVIS_ELECTRONIQUE' : retourne status:'queued' sentAt:now SANS aucun envoi reel (pas d'edge, pas de Resend, pas d'enregistrement). Succes mensonger : l'UI affiche 'en file' alors que rien n'est parti.
- src/features/communication/mail/hooks/useMailbox.ts:332-380 addLabel/removeLabel/createLabel/moveToFolder/createFolder — mutations PUREMENT locales (setState), AUCUN appel Supabase. Les libelles/dossiers sont des constantes client (DEFAULT_FOLDERS/DEFAULT_LABELS) ; la colonne `mails.label_ids` n'est jamais ecrite par ces actions. L'utilisateur croit creer/poser un label, rien n'est persiste (table mail_labels deja droppee par le passe).
- src/lib/services/convocation-dispatch.service.ts:73-85 (RECOMMANDE/LETTRE_SIMPLE/REMISE_MAIN_PROPRE) retournent status:'queued' + un postalEntry pour ZIP : honnete pour le courrier (zip a imprimer) MAIS le 'queued' suggere un suivi qui ne sera jamais ecrit en base (cf. save_ag_envoi_tracking casse). A surveiller plutot que stub franc.

**Capacités dormantes (schéma riche non câblé)** :
- Table `email_templates` : 6 lignes seedees live, CRUD complet (listEmailTemplates/getEmailTemplate/updateEmailTemplate dans src/lib/finance/api.ts:1722-1787, appeles par src/hooks/modules/useFinanceData.ts:904/929) MAIS aucun chemin d'envoi (mail send, convocation, relance) ne CHARGE un template pour composer un email — le send route ecrit `body` brut, le subject est hardcode. Capacite riche (variables jsonb, body_html, par-copro vs systeme) totalement deconnectee de l'envoi reel.
- Enum `notification_channel` (email, registered_email, postal, registered_postal, hand_delivery) et `delivery_status` (pending, queued, sent, delivered, opened, clicked, bounced, failed, cancelled) tres riches, mais le tracking AG (ag_envoi_tracking) n'est jamais peuple (RPC casse, 0 ligne) et le suivi delivered/opened/clicked/bounced n'a aucun producteur (pas de webhook Resend cable cote envoi). Etats dormants.
- Table `mails` colonnes `thread_id`, `in_reply_to`, `body_html`, `attachments`, `label_ids` : presentes, mais le front n'exploite ni le threading (thread_id toujours null a l'ecriture), ni les pieces jointes (attachments toujours null), ni body_html (toujours null cote send/saveDraft). Schema riche sous-exploite.
- Table `messages` colonnes `read_by` (uuid[]), `attachment_id`, `reply_to_id`, `edited_at`, `message_type` (text/file/system) : seuls text + read_by sont utilises ; pieces jointes (attachment_id), reponses (reply_to_id), edition (edited_at) et types file/system jamais ecrits par le front.
- Table `wall_posts` colonnes `visibility` (content_visibility: all_members/council_only/managers_only), `is_locked`, `tags` : le front ecrit toujours visibility='all_members' en dur (useMur.ts:250), is_locked=false, tags jamais poses. Visibilite conseil/gestionnaire et verrouillage dormants.
- Edge functions communication presentes en source mais 0 deployees : `communication-workflow`, `email_webhook`, `ag_send_convocations`, `ag_send_relance`, `run_payment_reminders`, `send_manual_payment_reminder`, `send-convocation-email` — toute l'automatisation d'envoi/tracking est ecrite mais inerte.

**Doublons / 2 chemins** :
- DEUX systemes de suivi d'envoi AG coexistent : (1) `ag_envoi_tracking` (method=notification_channel, alimente — en theorie — par save_ag_envoi_tracking depuis le wizard convocation-dispatch) et (2) `ag_notifications` (channel=notification_channel + notification_type convocation/relance, lu par useAgNotifications via v_ag_notification_stats + get_ag_recipients). Deux tables, deux vocabulaires de colonnes (method vs channel), deux ecrans qui ne se parlent pas — risque de divergence / double saisie du meme fait 'convocation envoyee'.
- DEUX vocabulaires de canal d'envoi : l'enum DB `notification_channel` (email/registered_email/postal/registered_postal/hand_delivery, en minuscules EN) vs les constantes front SENDING_METHOD_LABELS (EMAIL/RECOMMANDE/LETTRE_SIMPLE/AVIS_ELECTRONIQUE/REMISE_MAIN_PROPRE, MAJ FR dans convocation-dispatch.service.ts:18-24). Aucune table de correspondance → cause directe du RPC casse. Migration EN/FR a moitie faite.
- DEUX providers possibles evoques (Brevo vs Resend) mais UN SEUL present dans le code : Resend (src/lib/mail/resend.ts, send/inbound routes). Brevo est ABSENT de src/ (pas un drift, mais le 'choix de provider' n'existe pas : c'est Resend ou rien).
- Messagerie : `mark_conversation_read` (RPC, ecriture durable) ET maj optimiste locale du unread_count coexistent ; OK fonctionnellement, mais la source de verite du dernier expediteur diverge (vue n'expose pas last_sender_name → front invente 'Admin CoProFlex').

**Verdict à créer / à purger** :
- A CORRIGER (cablable demain) : useMessagerie.sendMessage — retirer `sender_name` et `is_edited` de l'insert `messages` (colonnes inexistantes) ; sinon AUCUN message ne se sauvegarde. Bug bloquant a fixer avant toute campagne de test messagerie.
- A CORRIGER : saveEnvoiTracking + RPC save_ag_envoi_tracking — aligner les cles (camelCase→snake_case) ET mapper les methodes FR MAJ → enum notification_channel (EMAIL→email, RECOMMANDE→registered_postal, LETTRE_SIMPLE→postal, AVIS_ELECTRONIQUE→registered_email?, REMISE_MAIN_PROPRE→hand_delivery). Sans ce mapping, 0 suivi d'envoi AG n'est jamais enregistre.
- A CREER/DEPLOYER : les edge functions communication (send-convocation-email, ag_send_convocations, ag_send_relance, email_webhook, run_payment_reminders, communication-workflow) — code present, 0 deploye. Tant qu'elles ne sont pas deployees, EMAIL convocation/relance/rappels sont casses. Decision produit : deployer (Resend) ou re-architecturer en server functions v2.
- A CABLER : email_templates (6 templates seedes, CRUD pret) dans le chemin d'envoi reel (send route + convocation) — aujourd'hui les emails partent avec body brut/subject hardcode, les templates ne servent a rien. Capacite a activer.
- A COMPLETER (vue) : v_conversations_overview — ajouter last_sender_name / last_sender_role (additif, fin de vue) pour que la liste de conversations cesse d'afficher 'Admin CoProFlex' en dur. Sinon purger ces champs du mapper front.
- A CONSOLIDER (decision) : trancher ag_envoi_tracking vs ag_notifications comme source unique du suivi d'envoi AG (purger l'un, ou definir clairement leur perimetre). Eviter de figer deux chemins dans la refonte v2.
- A GARDER tel quel (sain) : tout le module MUR (useMur) — v_wall_feed expose author_name/author_role/is_liked_by_me, triggers trg_wall_likes_count/trg_wall_comments_count actifs, ecritures wall_posts/wall_comments/wall_likes correctes. Reference de cablage propre pour la v2.
- A NOTER (non-drift) : module communication v2-tanstack/src/features/communication = vide (.gitkeep) ; toute cette logique est a re-implementer en v2 — l'occasion de corriger les 3 bugs ci-dessus plutot que de recopier v1.

**Notes** : Methode : le _schema_dump.sql fourni est PARTIEL (seulement 8 trigger-functions + 1 vue tiers_directory ; ni les vues v_*, ni la plupart des RPC, ni meme toutes les colonnes — ex. ag_notifications.notification_type absent du dump mais PRESENT live). J'ai donc verifie chaque objet directement sur le live (projet postgres connecte via MCP Supabase = qqfqrcolzmcbsvfaumiq, base quasi vide : 0 ligne mails/conversations/wall_posts/ag_notifications, 6 email_templates). Existent live et corrects : tables mails/messages/conversations/conversation_members/wall_posts/wall_comments/wall_likes/ag_envoi_tracking/ag_notifications/email_templates ; vues v_conversations_overview, v_wall_feed, v_events_overview, v_ag_notification_stats ; RPC mark_conversation_read, get_ag_recipients, check_convocation_delay, save_ag_envoi_tracking, save_ag_envoi_choices, get_ag_envoi_choices, get_ag_milestones, save_ag_milestone, save_ag_wizard_state, rpc_get_ag_convocation_bundle, rpc_get_ag_coproprietaires ; triggers trg_conversation_last_message, trg_wall_likes_count, trg_wall_comments_count. N'existe PAS (mentionne dans le commentaire 0022 mais jamais cree) : vue v_conversation_messages — sans impact, le front lit la table messages directement. Distinction importante : tous les manques listes sont 'absents de notre code/infra' (edge non deployees, RPC mal appele, vue incomplete), AUCUN n'est 'interdit par la loi'. Provider : Resend uniquement, Brevo totalement absent de src/. 'preferences communication' (annuaire copropriétaires) : aucune table/colonne de preferences de canal trouvee cote communication — la notion existe dans modules.md mais pas en base ni en code (gap, pas drift). Bugs bloquants confirmes empiriquement par lecture du corps SQL du RPC + colonnes live : (1) messages insert sender_name/is_edited, (2) save_ag_envoi_tracking casse-cles+enum.

---

## ventes (mutations / état daté / opposition art.20 / impayés-recouvrement)

**Références cassées / fantômes** :
- DEAD ENUM — src/lib/sales/api.ts:28 et :629 : DbMutationStatus déclare 'final_etat_generated' et createEtatDateSnapshot écrit updateMutationStatus(..., 'final_etat_generated'). L'enum réel public.mutation_status NE contient PAS cette valeur (valeurs réelles : draft, pre_etat_generated, etat_generated, sent_to_notary, signed, validated, cancelled — schema_dump L760 + migration 0079). => 22P02 invalid input value au runtime sur l'état daté FINAL du chemin legacy. (cf ANOM-23)
- v_mutation_detail.opposition (migration 0031:697) — la vue lit public.mutation_oppositions et expose 'opposition' en JSON, MAIS aucune RPC ni .from() n'écrit jamais dans mutation_oppositions => la colonne 'opposition' est TOUJOURS NULL en pratique (capacité branchée en lecture, jamais alimentée).
- src/lib/sales/api.ts:605-642 createEtatDateSnapshot : INSERT direct dans etat_date_snapshots qui n'envoie PAS 'lot_id' ni 'effective_date' (NOT NULL dans la table, schema L2370/2372) => violation NOT NULL au runtime, en plus du CHECK ck_etat_date_payload_parts si le payload n'a pas les 3 parties. Le chemin canonique RPC create_etat_date_snapshot (0031:642) les calcule correctement ; le chemin legacy est cassé.
- Divergence UI/SQL non bloquante (pas une casse) : useMutationDetail.availableActions.canValidate exige status==='signed', mais la RPC validate_mutation (0076:217-222) accepte TOUT statut non terminal (rejette seulement validated/cancelled). Le garde-fou 'signé avant validé' n'existe QUE côté front.

**Stubs menteurs (succès sans effet)** :
- generateEtatDate (src/features/ventes/api/mutationsApi.ts:278-304) et validateMutation (:309-342) : en cas d'absence de champ dans la réponse RPC, retournent success: data?.success ?? true et new_status:'validated' / mutation_id codés en dur côté front. Risque de 'faux succès' si la RPC renvoie un jsonb sans 'success' — l'app affichera 'Mutation validée' même sans confirmation serveur explicite. (les RPC réelles renvoient bien success:true, donc latent, pas actif)
- PAS de vrai stub mensonger côté SQL dans ce domaine : validate_mutation pose réellement lot_owners et retourne gl_posted:false en toute transparence (zéro écriture GL = comportement VOULU lot-centric, pas un mensonge). create_etat_date_snapshot fige réellement le payload. À distinguer de l'edge generate_etat_date / validate_mutation qui N'EXISTENT PAS (le front a déjà migré vers les RPC, commentaires mutationsApi.ts:288 et :319 le notent explicitement).

**Capacités dormantes (schéma riche non câblé)** :
- legal_proceedings (table riche : nature litigation/recovery/other, status pending/in_progress/closed/won/lost, lot_id, debtor_owner_id, amount_at_stake, court, lawyer, CHECK ck_legal_recovery_target imposant lot_id si recovery) — AUCUN .from('legal_proceedings') ni RPC dans src/ (présente UNIQUEMENT dans src/types/supabase.ts généré). => tout le module CONTENTIEUX/recouvrement judiciaire est en base mais NON câblé.
- mutation_oppositions (table art.20 complète : avis_mutation_date, opposition_deadline=avis+15 via CHECK, amount_opposed, causes jsonb, status pending/opposed/paid/released/contested, notaire_payment_date, paid_amount, ledger_transaction_id) — lue via v_mutation_detail mais JAMAIS écrite. Opposition art.20 entièrement dormante côté écriture.
- mutation_steps.step_key 'cloture_compte' (jalon 6 clôture compte vendeur) : marqué 'completed' en bloc par validate_mutation (0076:259-261) mais AUCUNE logique de contrôle de solde (get_lot_balance_45x est seulement un AVERTISSEMENT front non bloquant) => le jalon clôture est dormant/cosmétique.
- payment_reminders.delay_level / reminder_rule_id / provider_message_id / delivery_status : colonnes présentes et la table EST câblée (lib/impayes + lib/finance), mais l'envoi réel passe par un provider email non déployé (0 edge, cf ANOM-04/16) => 'sent' posé sans envoi prouvé.
- etat_date_snapshots.document_id (lien vers le PDF figé en GED) : colonne présente, jamais renseignée par create_etat_date_snapshot (0031:664 ne pose pas document_id) => le PDF d'état daté n'est pas rattaché au snapshot.

**Doublons / 2 chemins** :
- DEUX couches API mutations/ventes complètes et concurrentes : (A) CANONIQUE src/features/ventes/api/mutationsApi.ts + hooks useMutations/useMutationDetail (utilisée par les pages ventes-impayes/ventes/page.tsx et /[id]/page.tsx) ; (B) LEGACY src/lib/sales/api.ts + src/providers/VentesProvider.tsx + hooks useSalesData/useSalesMutations (VentesProvider monté sur ventes-impayes/layout.tsx). Migration 0064 les appelle explicitement 'jumeaux'. La couche B contient le bug 'final_etat_generated' et l'insert état daté cassé ; la couche A passe par les RPC correctes.
- Deux chemins d'état daté incompatibles : A appelle la RPC create_etat_date_snapshot (calcule lot_id/effective_date/payload, fait avancer le statut) ; B fait un INSERT direct + updateMutationStatus avec enum mort. Risque de copier le mauvais.
- Deux jeux de types de statut : src/features/ventes/domain/types.ts (MutationStatus correct, 7 valeurs) vs src/lib/sales/api.ts DbMutationStatus (avec 'final_etat_generated' faux). Vocabulaire dédoublé EN base unique.

**Verdict à créer / à purger** :
- A PURGER (code mort / piège à copie) : src/lib/sales/api.ts, src/providers/VentesProvider.tsx, src/hooks/modules/useSalesData.ts + useSalesMutations.ts — couche legacy jumelle, contient l'enum mort 'final_etat_generated' et l'insert état daté cassé. NB mémoire vente_cleanup_chantier : canonique = features/ventes, mais 'PARITÉ AVANT SUPPRESSION' (les relances impayés y sont câblées : garder useImpayesMutations).
- A PURGER : la valeur de type 'final_etat_generated' partout dans le front — n'a jamais existé en base, la bonne valeur est 'etat_generated'.
- A CREER (schéma prêt, à câbler demain) : RPC d'écriture opposition art.20 (record_mutation_opposition : avis de mutation -> deadline +15j ; + settlement notaire posant ledger_transaction_id), conforme à la table mutation_oppositions déjà en base et déjà lue par v_mutation_detail. Cadré C.12.
- A CREER : module Contentieux/recouvrement sur legal_proceedings (table complète, 0 câblage) — la route /contentieux/impayes existe (page.tsx) mais ne lit que les impayés, pas les procédures.
- A CREER : contrôle de solde réel au jalon 'cloture_compte' (jalon 6) — aujourd'hui get_lot_balance_45x n'avertit que côté front sans bloquer ni générer d'issue ; cadrage C.12 demande une issue forcée si solde≠0.
- A CORRIGER (latent) : durcir generateEtatDate/validateMutation côté front pour ne plus inférer success:true par défaut (risque faux-vert).

**Notes** : ATTENTION SOURCE : le schema_dump du 2026-06-23 est PERIME pour ce domaine — il ne reflète PAS migration 0064 (colonne mutations.buyer_draft) ni 0079 (enum value 'sent_to_notary'). Sa table mutations (L2709) n'a pas buyer_draft et son enum mutation_status (L760) n'a pas sent_to_notary. J'ai vérifié l'état RÉEL sur les migrations 0019->0087 (head live = 0087 par mémoire). | RPC RÉELLEMENT PRESENTES et alignées au front canonique : validate_mutation (0031/0076), create_etat_date_snapshot (0031), generate_etat_date_payload (0031/0076/0080 v2 plate), upsert_mutation_notary (0064), upsert_mutation_step (0031), get_lot_balance_45x (0082). | VUES présentes : v_mutations_overview + v_etat_date_latest (0054), v_mutation_detail (0031, lit l'opposition), v_payment_reminders_overview + v_unpaid_with_reminders + v_unpaid_by_lot (0036/0055). Aucune vue fantôme dans ce domaine. | EDGE generate_etat_date / validate_mutation : N'EXISTENT PAS en tant qu'edge (ce n'est PAS un bug : le front a migré vers les RPC SQL, conforme doctrine v2 'edge = cron/webhooks only'). | Confirme ANOM-25 (drift hors-finance) : opposition art.20 et legal_proceedings = schéma riche dormant. Confirme ANOM-23 (final_etat_generated mort). | validate_mutation NE touche PAS le grand livre (gl_posted:false) — VOULU (le 450 suit le lot, lot-centric) : à ne pas confondre avec un manque.

---
